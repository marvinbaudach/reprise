## What changed

<!-- Describe the user-visible or technical outcome. -->

## Verification

- [ ] `CI / Quality gate` is green
- [ ] The pull request targets `dev`, promotes `dev` to `main`, or is an emergency `hotfix/*` from `main`
- [ ] Merged with the method its head branch requires — squash for `feature/*`, `fix/*`, `chore/*`, `hotfix/*`; merge commit for `dev` → `main` and the `main` → `dev` sync (see [docs/agents/branching.md](../docs/agents/branching.md#merge-method))
- [ ] Manual or deferred checks are documented
- [ ] No real user database or music files were used during verification

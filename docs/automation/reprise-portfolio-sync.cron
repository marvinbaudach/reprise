# Reprise portfolio weekly sync
CRON_TZ=Europe/Zurich
30 7 * * 1 /bin/bash -lc 'mkdir -p ~/.local/state/reprise-portfolio-sync && ~/Projects/reprise/scripts/weekly-portfolio-sync.sh >> ~/.local/state/reprise-portfolio-sync/cron.log 2>&1'

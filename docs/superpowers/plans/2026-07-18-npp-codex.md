# Codex Handoff — Now-Playing-Panel (NPP)

You are implementing `docs/superpowers/plans/2026-07-18-npp-taskplan.md` in
this repository. That plan is the single source of truth for the tasks; the
normative decisions behind it live in
`docs/superpowers/plans/2026-07-18-npp-beschluesse.md` (German — binding).

## Context

- Working directory / branch: this worktree, `feat/now-playing-panel`
  (base `main@fec994c`).
- Read `AGENTS.md`, `TESTING.md`, and `.superpowers/sdd/progress.md` (tail)
  before starting. Tasks marked complete in the ledger are done — never redo
  them.
- A sibling branch `feat/theme-surface-hierarchy` works in parallel. The
  file-ownership table in the taskplan is **binding**: never touch
  `ui/style/theme.rs`, `ui/style/cover_accent.rs`,
  `ui/window/library_chrome.rs`, `ui/window/library_shell.rs`, or
  `docs/ux-rules.md`. Do not edit `AGENTS.md`.

## Execution protocol

1. Execute the plan's tasks **strictly in order T1 → T9**. The "Parallel
   Execution Map" is for multi-agent setups; as a single agent, ignore the
   waves and go sequentially. Do not start a task before the previous one is
   committed.
2. TDD per task as written: failing test first (red), then implementation
   (green). Where a task names test functions (`npp_2_…`, `npp_4_…`,
   `npp_7_…`, `npp_8_…`), use exactly those names — they are the rule-named
   acceptance tests.
3. **Gates before EVERY commit** (all must pass):
   - `cargo fmt --check`
   - `cargo clippy --locked --all-targets --workspace -- -D warnings`
   - `env XDG_DATA_HOME=$(mktemp -d) XDG_CACHE_HOME=$(mktemp -d) cargo test --locked --workspace`
   - `scripts/check-ux-traceability.sh`
   - `scripts/check-architecture.sh`
   Display-marked tests (`#[ignore]`) of the touched area: run via
   `xvfb-run -a` **if the sandbox allows it**; if not, do not fake them —
   list them as "pending display verification" in your final ledger entry.
4. Use the commit messages exactly as given in the plan. One commit per task
   (T2 is the mechanical rename commit — no behavior changes inside it).
   **Never push.** No attribution footers of any kind.
5. Status flips `[geplant] → [aktiv]` happen ONLY in the Beschlussdokument
   (`2026-07-18-npp-beschluesse.md`), only in the tasks that say so
   (T1 → NPP-1, T4 → NPP-2+3, T5 → NPP-4, T7 → NPP-5+9, T8 → NPP-6+7+8,
   T9 → NPP-10), inside that task's commit.
6. After T9, append ONE compact entry to `.superpowers/sdd/progress.md`
   (stage summary: tasks, commits, verification counts, skipped display
   tests) as a final `docs(progress): now-playing panel stage` commit.

## Adaptation policy

- The plan was verified against the live code at `fec994c`, but gtk4-rs /
  libadwaita API details may differ (signal names, builder methods, how the
  player exposes "current track" callbacks). Adapt the implementation, keep
  the specified behavior and the tests.
- Scope drift is not allowed: if a task's premise turns out wrong (e.g. an
  API the plan assumes does not exist in any form), STOP, write the blocker
  to `.codex-blocked.md` in the worktree root, and end the run instead of
  improvising a different design.
- UI copy is English (existing `strings` modules); docs and the
  Beschlussdokument are German; commit messages are English. Follow
  `~/.claude`-independent repo conventions only — this repo's AGENTS.md and
  existing code style win.

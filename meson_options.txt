option(
  'profile',
  type: 'combo',
  choices: ['release', 'debug'],
  value: 'release',
  description: 'Cargo build profile used for the installed binary',
)

option(
  'stem_backend',
  type: 'boolean',
  value: true,
  description: 'Build the experimental out-of-process stem worker (reprise-cli worker + onnxruntime via runtime load-dynamic; no build-time download) and enable its GTK client surface.',
)

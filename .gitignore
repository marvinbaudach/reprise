# Rust build output
/target

# Editor directories and files
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
*.log

# Session scratch (not part of the repo)
.superpowers/

# Git worktrees (local development)
.worktrees/

# Live agent work lock — volatile coordination state, never versioned
# (committing it produced ~150 noise commits). See docs/agent-workflow/STATUS.md.
docs/agent-workflow/LOCK

# Claude Code personal settings (machine-specific hooks)
.claude/settings.local.json

# My Stats redesign progress

Base: `32d53957f7`
Plan: `docs/plans/mystats-redesign.md`

Task T1: complete (commit f2ac601bb0, base 32d53957f7, appended STATS-10 through STATS-17 and committed the binding plan; 2026-07-25 gates passed with isolated XDG cache/data, display gate intentionally not run)
Task T2: complete (commit c16354c471, base f2ac601bb0, aligned week buckets to local Mondays, extended week granularity through 730 days, and added timezone-aware best-week data; workspace 1225 core + 845 GNOME non-display tests passed, 231 existing display tests ignored)
Task T3: complete (commit fad7e28c85, base c16354c471, exposed the seasonal previous-ms baseline and current-year pace projection; extracted hero tests to keep the 799-line snapshot test file under the limit; workspace 1227 core + 845 GNOME non-display tests passed)
Task T4: complete (commit ffe4bf4f2e, base fad7e28c85, added the ninth pure read for per-genre artist/path rows and folded them through group_key into tile metadata; workspace 1230 core + 845 GNOME non-display tests passed)
Task T5: complete (commit 85e70ef8fb, base ffe4bf4f2e, split the period header from the listening-time hero, added responsive KPI pairs and the exact zero-baseline badge, and migrated STATS-1/1a coverage to STATS-11/11a/12; workspace 1230 core + 845 GNOME non-display tests passed, 233 display tests compiled but intentionally not run)
Task T6: complete (commit 88d63cd4bb, base 85e70ef8fb, rebuilt the ribbon as a weekly accent-area chart with month ticks, a dashed best-week marker, an open current endpoint, exact hover copy, and a reveal clip seam; workspace 1230 core + 848 GNOME non-display tests passed, 233 display tests compiled but intentionally not run)
Task T7: complete (commit 8a4743c15d, base 88d63cd4bb, replaced the spotlight with a local-cover band hero, initials fallback, relative ranks, artist navigation, and leader/rank spelling-unify affordances; workspace 1230 core + 848 GNOME non-display tests passed, 235 display tests compiled but intentionally not run)
Task T8: complete (commit 7e5112fe1f, base 8a4743c15d, added the five-row Songs card with relative gradient bars, exact-track hover/focus play, artist focus navigation, keyboard/pointer context actions, and the expandable sortable full ranking; workspace 1230 core + 848 GNOME non-display tests passed, 239 display tests compiled but intentionally not run)
Task T9: complete (commit 8ac72990ec, base 7e5112fe1f, replaced the genre legend with a 22-pixel ranked segment bar and four local-cover tiles carrying share, duration, top artist, album-path navigation, and spelling-unify hints; workspace 1230 core + 848 GNOME non-display tests passed, 240 display tests compiled but intentionally not run)
Task T10: complete (commit 57b7b13ae0, base 8ac72990ec, rebuilt My Stats in the fixed header-to-genres story order, added the thin-history hint, wired song and genre actions, and removed Clock, Highlights, Customize, and StatsLayout; workspace 1228 core + 840 GNOME non-display tests passed, 241 display tests compiled but intentionally not run)
Task T11: complete (commit 3177a8b3a4, base 57b7b13ae0, removed the retired Clock/Highlights snapshot types, computations, and discovered-count query while retaining event-only STATS-0 coverage through Hero, Top Tracks, and Best Week; workspace 1225 core + 840 GNOME non-display tests passed, 241 display tests compiled but intentionally not run)
Task T12: complete (commit fc4342b8bf, base 3177a8b3a4, added named Stats motion tokens, one-shot count/reveal/fade/bar choreography, period-value tweening, and reduced-motion end states; workspace 1225 core + 840 GNOME non-display tests passed, 244 display tests compiled but intentionally not run)
Task T13: complete (this close-out commit, base fc4342b8bf, final permitted gate sweep passed; 1225 core + 840 GNOME non-display tests passed; the display gate was run externally and passed 125/125)
Accepted review findings: complete (commits ea69a4cb42 through d346d1d0f5, base 04d7a07bcc, applied Groups A-E and the accepted active-rule text corrections; the permitted gate sweep passed with 1227 core + 849 GNOME non-display tests, 253 display tests compiled but intentionally not run in the sandbox; the pre-fix display gate was run externally and passed 125/125)
Polish G1: complete (this commit, base 41370d7cd9, kept every genre segment at the measured 22-pixel bar height and added an allocation-level STATS-15 display regression; permitted gates passed, display test compiled but intentionally not run in the sandbox)
Polish G2: complete (this commit, base 0b1884501f, tightened genre-card spacing, added one-step-lighter rank hovers, and normalized the segment tooltip to compact hours and minutes; permitted gates passed, display test compiled but intentionally not run in the sandbox)
Polish G3: complete with architecture adjustment (this commit, base ce1e6533d7, added durable Genre library scopes plus the required narrow ViewSource query projection, wired segment and tile navigation while preserving cover-to-album behavior, and extended FIL-1c/STATS-15 in lockstep; permitted gates passed, four affected display tests compiled but intentionally not run in the sandbox)
Polish G4: complete with contract alignment (this commit, base 33f8336b4e, cropped sparse yearly axes at the first play week, switched them to weekly bars with a since label, added 12-percent headroom, and measured the best-week label left of its marker; STATS-12 was aligned in the same commit, permitted gates passed, display test compiled but intentionally not run in the sandbox)
Polish G5: complete (this commit, base 1746ee01b1, moved the expandable Top Tracks ranking into its own full-width section between the two-column story row and genres, preserved the fixed five-row Songs card, and aligned STATS-10/STATS-14; permitted gates passed, two affected display tests compiled but intentionally not run in the sandbox)
Polish G6: complete (this commit, base 817f170674, replaced variable inline rank bodies with fixed 24-pixel rank and 180-pixel ellipsized name Grid columns so all runner-up bars share one start and fill the remaining width; permitted gates passed, the allocation display regression compiled but intentionally was not run in the sandbox)
Polish G7: complete (this commit, base 357f8f1077, compacted the expanded Top Tracks rows and shared their 8-pixel pill geometry with Top Five, made metadata links hover-only while preserving focus, and routed every My Stats duration and KPI through one localized N h M formatter; STATS-11/STATS-13/STATS-14/STATS-15 were aligned, permitted gates passed with 856 GNOME non-display tests and 258 display tests compiled but intentionally not run in the sandbox)
Follow-up F1: complete (this commit, base 2d92010bf7, replaced expanded-row button chrome with accessible focusable label links and the intrinsic LevelBar with an exact 8-pixel box-pair bar; permitted gates passed and the unchanged display regression compiled but was intentionally not run in the sandbox)
Follow-up F2: complete (this commit, base 85cd456f18, formatted full hours without trailing zero minutes and sub-hour My Stats durations as N min across every shared-formatter caller, preserving signed trend deltas; aligned STATS-11/STATS-13/STATS-14/STATS-15 and passed all permitted gates)
Entrance R3 S1: complete (this commit, base 017d68699b, added the reusable snapshot-translated SlideBin with a real-main-loop allocation-invariance display test; all permitted gates passed and the display test compiled but was intentionally not run in the sandbox)
Entrance R3 S2: complete (this commit, base 3df5120cb1, replaced the burst with the binding hero/KPI/chart/marker/card/bar sequence, added fold-aware card skipping and left-to-right genre reveals, updated STATS-17, and preserved 250 ms period tweening plus reduced-motion end states; all permitted gates passed and 261 display tests compiled but were intentionally not run in the sandbox)
Entrance display gate prerequisite: complete (this commit, base b1c046837e, routed the story-row stylesheet through the sanctioned test helper and documented the existing keyboard partners and focus semantics for My Stats interactions; all non-display policy gates passed)
Entrance display review R1: complete (this commit, base b3998022bc, deferred fold classification to the first allocated frame so below-fold cards land immediately while genuinely unmeasurable cards retain the fail-open animation path; all non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
Entrance display review R2: complete (this commit, base ad57115e3a, replaced the wall-clock sequence sample with a bounded main-loop poll that proves the hero completes before the genre card starts; all non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
Entrance display review R3: complete (this commit, base 04f35766c4, routed the exact genre-tooltip assertion through a SlideBin-aware segment-button accessor and audited every stats child-chain traversal for the new wrapper; all non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
My Stats polish P2: complete (this commit, base 2c89fbdb95, made SlideBin preserve child expansion and layout hints and strengthened the band-card allocation regression; all requested non-display gates passed, with workspace tests isolated to writable XDG roots after the read-only default-cache run failed; the operator-owned display regression compiled but was not run in the sandbox)
My Stats polish P3: complete (this commit, base f21e0bd3f4, verified the shared SlideBin repair through a full-width proportional genre allocation regression, clipped square inner segments to rounded outer corners, and kept both semantic Other and the final ranked segment neutral; all requested non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
My Stats polish P1: complete (this commit, base 54e184feaa, made thin weekly history a compact continuous timeline with equal slots, zero-week ticks, weekly labels, a divider baseline, and a lighter best-week highlight without the retired dashed marker; all requested non-display gates passed and the operator-owned display regressions compiled but were not run in the sandbox)
My Stats polish P4: complete (this commit, base c98365b091, moved the shared plays/time sort toggle beside the Songs kicker, applied it to both rankings, and filled the compact card with six fixed-spacing rows; all requested non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
My Stats final polish Q1: complete (this commit, base d8a267c469, widened short-history bars to 40 percent of their equal slots with a 48-pixel cap and restored an 8-pixel best-week-label gap; all requested non-display gates passed and the operator-owned display regressions compiled but were not run in the sandbox)
My Stats final polish Q2: complete (this commit, base dc81a48a49, aligned every runner-up band row through one shared artist/action grid and asserted the four rank bars' common right edge with a single-pencil fixture; all requested non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
My Stats final polish Q3: complete (this commit, base 66674acbfd, separated the first three genre segments into accent-300/500/700 steps and made later ranks plus the semantic remainder neutral-600 while retaining derived theme colours; all requested non-display gates passed and the operator-owned CSS parser regression compiled but was not run in the sandbox)
My Stats final polish Q4: complete (this commit, base 2b574551f1, made the expanded ranking leave section flow only after its collapse animation, normalized all section gaps to 20 pixels, and replaced the two loose sort buttons with an accessible native AdwToggleGroup that still sorts both rankings; all requested non-display gates passed and the operator-owned display regressions compiled but were not run in the sandbox)
My Stats static entrance simplification: complete (this commit, base 4e5c6b54c2, removed every non-bar entrance path, limited sparse-ribbon motion to staggered baseline growth plus its best-week label, retained a reduced horizontal reveal solely for genre segments, and kept period changes bar-only; all requested non-display gates passed, the three replacement STATS-17 display tests compiled, and the operator-owned display gate was not run in the sandbox)
My Stats entrance order follow-up: complete (this commit, base c83e0be239, chained the best-week label fade to its bar's actual animation completion, guarded stale completions by generation, and made the STATS-17 display regression continuously assert reset-growth-finish-label order; all requested non-display gates passed and the operator-owned display gate was not run in the sandbox)

# Idle Play random-start follow-up

Base: `331217f344`

Task P1: complete (this commit, base 331217f344, made stopped Play/Pause seed and start a random present-library snapshot without row activation, kept empty-library and queue navigation sensitivity honest, and activated PLAY-9; full non-display gates passed, while the rule-named display regression compiled but its isolated Xvfb run was blocked by sandbox D-Bus socket permissions)

# Visualizer sync regression

Base: `7d89d9b20c`

Task V1: complete (this commit, base 7d89d9b20c, reserved spectrum headroom and added sustained bass-dominant breakdown pressure so the decoded WAKE UP profile stays near 8–24% before the breakdown and reaches 88–93% on its major hits; workspace 1312 core + 901 GNOME non-display tests passed, 279 display-required GNOME tests were intentionally ignored, and native visual review remains manual)
Task V2: complete (this commit, base 52efec9753, kept Space global Play/Pause while passive collection views hold focus, preserved native Space handling for direct controls and text entry, and extended ACC-4's isolated GTK and CUA coverage; WAKE UP diagnosis found the reported missing 16-second peak came from the legacy nested worktree, while the canonical worktree's exact player-to-scene profile reached 89% during the UI's displayed 16-second window; workspace 1312 core + 901 GNOME non-display tests passed, 280 display tests were intentionally ignored by the workspace run, and the targeted Xvfb test plus real key-event CUA path passed)
Task V3: complete (this commit, base 05b4adad89, moved the Spectrum analyzer ahead of ReplayGain so stored Track/Album gain cannot attenuate visual energy while audible normalization remains unchanged; the exact WAKE UP Track-ReplayGain profile improved from 43% to 93% visible peak at 16 seconds and from 37% to 87% at 36 seconds; workspace 1312 core + 901 GNOME non-display + 81 Linux platform + 45 stems tests passed, 280 display tests were intentionally ignored, and one unrelated device-sync test failed once before passing both isolated and in the full rerun)
Task V4: complete (this commit, base 9aa28f69fb, added a faster-than-realtime song-corpus profiler and calibrated Bars against WAKE UP, To the Hellfire, and Sun//Eater so compressed metal retains headroom while the reported 16- and 36-second WAKE UP windows peak near 80-82% mean and 88% tallest-bar height with 16-32 ms best alignment; the quieter half now produces 0.5% false-full frames in WAKE UP and at most 6.5% across the Lorna Shore corpus, a three-tick pressure hold preserves fluid short-hit attack, and selected passive view tabs again leave Space to global Play/Pause; workspace 1314 core + 901 GNOME non-display + 81 Linux platform + 45 stems tests passed, 281 display tests were intentionally ignored, the targeted Xvfb regression and isolated real-key CUA song-visuals scenario passed, core purity and architecture/UX gates passed, and cargo audit reported only the accepted RUSTSEC-2024-0436 warning)

# Podcasts & Radio

Task Z1: complete with sandbox-limited visual verification (this commit, base 4b9b80627f, implemented podcast RSS/YouTube and radio-browser sources, external playback/MPRIS, source views, reversible removal, preferences, schema v33 future-only baselines, and all AF rules; fmt, clippy, 1398 core tests, 951 GNOME non-display tests, audit with only accepted RUSTSEC-2024-0436, core purity, motion, input, accessibility, and UX traceability passed; the unchanged origin/dev 806-line MCP file keeps the architecture script red, and sandbox D-Bus socket denial blocks display tests plus the isolated application smoke; real feeds/streams, yt-dlp, ICY, GNOME-Shell MPRIS, and theme icon optics remain the documented manual pass)
Podcast & Radio MCP follow-up: complete (commit 277aad8540, base a1eb838bf4, added capability-gated podcast/YouTube and radio add/edit/remove/refresh tools plus URL-safe cached resources; fmt, strict clippy, workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, motion, input, accessibility, and UX traceability passed; the pre-existing 806-line MCP data file remains the sole architecture-lint failure, the 284/285 parallel display run's unrelated My Stats failure passed on isolated rerun, and a throwaway live MCP smoke read DGE RSS, HOLLOW FALLEN via yt-dlp, and RADIO BOB Metal via PLS without exposing source URLs)

# Concerts credential feedback follow-up

Base: `e38791b251`

Gate repair: complete (commit 47d26b1c1c, base e38791b251, split the MCP data unit tests into a sibling module so the architecture file-size gate passes)
Point 1: complete (commit 72e85defff, base 47d26b1c1c, propagated Concerts settings changes live to the view, sidebar and Updates popover, hid credential-ineligible fetch actions, and made 1000 km the default with 100/250/500/1000 km presets; all non-display gates passed, while the rule-named display regression compiled but its isolated Xvfb run was blocked by sandbox D-Bus socket permissions)
Point 2: complete (commit 5786b63f97, base 72e85defff, kept SET-4 immediate persistence while adding the same Apply/Enter affordance as City and an inline saved acknowledgement to both credential rows; all non-display gates passed, while the display regression compiled but its isolated Xvfb run was blocked by sandbox D-Bus socket permissions)
Point 3: complete (commit 5bc3b56b8b, base 5786b63f97, verified provider credentials off-thread through the shared limiter, mapped fixture-only success/rejection/timeout/transport outcomes to inline feedback, kept secrets out of fixture logs, activated CONC-8 and replaced CONC-5 with CONC-5a; all non-display gates passed, while isolated Xvfb display launches were blocked by sandbox D-Bus socket permissions)
Sidebar progress follow-up: complete (this commit, base 5bc3b56b8b, pinned the shared progress activity below Issues at the sidebar bottom and added an FB-2a allocation regression)
MCP catalog follow-up: complete (this commit, base e7a208112e, added complete path-free Concerts cache and Releases history resources while preserving the filtered Concerts contract and never exposing provider credentials)

# Instrumental production readiness

Base: `3fc0b2bf8a`

Task I1: complete (this commit, base 3fc0b2bf8a, removed the test-only fake backend from user builds and gated the context action and worker on a compiled production backend)
Task I2: complete (this commit, base ee14aa8d7a, exposed one verified runtime-readiness contract for the model and native ONNX Runtime and made the production backend and preferences consume it)
Task I3: complete (this commit, base e4bd73aae4, packaged the official x86_64 ONNX Runtime 1.22.0 archive with a pinned SHA-512 and licence notices, embedded the installed library path and build-time SHA-256, and added a release packaging contract; fmt, strict clippy, affected stem tests, QA linters, core purity, and the workspace suite with the two local-TCP MCP fixtures filtered passed; the unfiltered suite reached only those two fixtures, which the sandbox blocks at TcpListener::bind with EPERM; audit reported only accepted RUSTSEC-2024-0436)
Task I4: complete (this commit, base c628b1a4f3, moved all native inference into a separately packaged reprise-worker process, replaced the GTK render thread with a serialized process supervisor, preserved provisioning without linking ort into GTK, and added dependency, lifecycle, and Meson install contracts; fmt, strict workspace clippy, 1401 core + 948 GNOME non-display workspace tests, QA linters, UX traceability, architecture, worker-isolation/runtime-packaging contracts, core purity, an installed-binary scratch smoke, and the full workspace suite passed; audit reported only accepted RUSTSEC-2024-0436 from the cached advisory database because the sandbox cannot lock the global cache for a refresh)
Task I5: complete (this commit, base 81d14c4d11, made the experimental preference gate immediately create or remove one window-owned conversion surface and worker supervisor, cleared wake dispatch on disable, refreshed all sidebar/browse/AI projections live, and added lifecycle regressions; fmt, strict workspace clippy, full workspace tests, all-features instrumental tests, architecture, accessibility, input, motion, UX traceability, worker isolation/runtime packaging, core purity, and audit passed with only accepted RUSTSEC-2024-0436; the rule-named GTK regression compiled but its isolated Xvfb run was blocked by sandbox D-Bus socket EPERM)
Task I6: complete (this commit, base ff27d37c31, made the gated Conversions sidebar row a real multi-track drop target, routed context-menu and drop requests through one missing-safe batch/dedup path, refused to enqueue until the model and packaged runtime verify, and opened the exact Experimental settings page when first-use assets need action; fmt, strict workspace and all-features GNOME clippy, isolated full workspace tests, architecture, accessibility, input, motion, UX traceability, worker isolation/runtime packaging, and audit passed with only accepted RUSTSEC-2024-0436; INST-14 records the new drop contract as a planned rule proposal for stage review)
Task I7: complete (this commit, base 27b21049f0, added a packaged worker end-to-end acceptance that creates an isolated job, renders the fixture through a separate installed process, verifies staging and lifecycle events, and made the release gate execute it; fmt, strict workspace clippy, all non-socket workspace tests serially, all-features instrumental and stems tests, Meson install, architecture, accessibility, input, motion, UX traceability, worker isolation/runtime packaging, core purity, and cached audit passed with only accepted RUSTSEC-2024-0436; the sandbox blocks two unrelated loopback-listener MCP tests with EPERM, and real HTDemucs inference remains the explicit model/runtime-gated manual acceptance)

# Ticketmaster bundled build credential

Base: `3fc0b2bf8a`

Task TM1: complete (commit aefc61d4c5, base 3fc0b2bf8a, resolved Ticketmaster credentials in stored then runtime then bundled-build order, treated blank values as absent, redacted credential debug output, and documented local and GitHub Actions release injection plus binary extractability; required gates passed with isolated XDG state and serial workspace tests; visible affiliate disclosure remains a separate review question before public release)
Task TM2: complete (commit 14f40a4442, base aefc61d4c5, removed the Ticketmaster credential editor, the credential-entry status action and the Updates key hint while retaining the optional Bandsintown row and legacy stored Ticketmaster resolution; all required gates passed after one unrelated one-shot progress flake passed 10 focused reruns and the repeated full workspace gate)
Task TM3: complete (this commit, base 14f40a4442, promoted New Releases and Concerts settings to peer Preferences pages, left only their activation switches on Plugins, preserved the newly merged Podcast and Radio plugin controls, kept page controls live-sensitive to module state, and activated SET-7; fmt, strict clippy, audit, core purity, architecture and all non-socket workspace tests passed after rebase onto current dev, while two new Radio MCP loopback tests were blocked only by the sandbox's TCP bind restriction)

# PR 64 UI follow-up repair

Base: `428c9bbff9`

Autocomplete surface: complete (commit 6f69d0aeff, base 428c9bbff9, preserved the popover surface behind transparent inner autocomplete views and added the manual TAG-9 rule proposal)
Repeat tooltips: complete (commit bbe3028309, base 6f69d0aeff, explained all three repeat modes from the current behavior)
Now Playing transition: complete (commit 2b8302599a, base bbe3028309, replaced the whole-panel crossfade with an interruption-safe outgoing-cover fade that waits for cold-cache resolution and activated NPP-13 with corrected MOT-1/MOT-6 references)
Catalog gate repair: complete (commit 20591fb5cb, base 2b8302599a, synchronized all seven catalogs with the Podcasts/Radio sources inherited from dev, completed the German and Spanish translations, and removed fuzzy entries)
Verification: complete with sandbox exclusions (format, strict clippy, rustdoc, architecture, accessibility, input, UX traceability, motion, gettext, audit with only accepted RUSTSEC-2024-0436, 1404 core tests, 959 GNOME non-display tests, and all other workspace tests passed; two inherited MCP Radio tests requiring local sockets and the isolated NPP-13 Xvfb regression were blocked by sandbox socket permissions and remain required on GitHub CI)

# Android synchronization follow-up

Base: `e83d7303955`

Single-device navigation polish: complete (this commit, base e83d7303955, skipped the chooser for one connected device, restored sidebar visibility with non-homogeneous content sizing, and replaced the split-action device row with a vertically centered neutral full-card navigation target; format, strict clippy, audit, core purity, architecture, accessibility, input, motion and UX traceability gates passed, while two inherited MCP Radio tests and isolated display tests remain blocked by sandbox socket permissions)
Dev compile repair: complete (this commit, base ea1b3dc7c1, removed the stale Concerts preferences callback that the single-device navigation rebase reintroduced after the callback API had intentionally been deleted)

# Tag autocomplete design 4a surface follow-up

Base: `d889ad6073`

Task TAG-AUTOCOMPLETE-SURFACE: complete (this commit, base d889ad6073, made the complete popover contents the single theme-surface owner, kept the menu/scroller/viewport/list transparent, clipped selected rows to the rounded surface, and added an isolated CUA evidence scenario; format, strict clippy, architecture, UX traceability, motion, input, accessibility, audit with only accepted RUSTSEC-2024-0436, 1401 core tests, 962 GNOME non-display tests with 291 display tests ignored, 86 Linux platform tests, 45 stems tests, and all remaining workspace tests passed in isolated host state; the CUA render showed the three library matches, selected accent row, and create row on one continuous surface, while compositor alpha around the detached X11 popup remains a native-desktop manual check)

# Space and sidebar regressions

Space/sidebar follow-up: complete (this commit, base 9f7275b27f, kept Space as global play/pause while the left sidebar toggle has focus, preserved native Space behavior for other controls and the existing passive-collection contract, kept Issues pinned at the sidebar bottom below progress activity, and added six-toggle CUA regression coverage; format, strict clippy, full workspace tests with 1403 core plus 971 GNOME non-display passes, audit with only accepted RUSTSEC-2024-0436, core purity, architecture, accessibility, input, motion, UX traceability, and the CUA helper contract passed; actual desktop interaction remains the manual acceptance check)

# Source-view UX follow-ups

Task R1: complete (this commit, base 3fc0b2bf8a, preserved smart-playlist membership while honoring visible column sorting, emitted narrow queue row removals instead of full replacements, and made hidden content-stack pages unable to push the bottom player bar out of view; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, file-size checks, and the isolated rule-named PLAY-7b display regression passed)
Task R2: complete (this commit, base c2fc5ba5a7, aligned Radio with the Podcast toolbar geometry, vertically centered Concert artist cells under CONC-10, added fixed Cancel plus Search/Preview actions to the Podcast add dialog, and clarified that scan exclusions are files removed from the library; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, UX traceability, file-size checks, and the three affected isolated display regressions passed)
Task R3: complete (this commit, base 5efa49a12c, added ten-second undoable per-episode removal for RSS and YouTube, persisted committed GUID dismissals against feed reimport, retained downloaded files behind an explicit Trash offer, and kept subscription removal cascade-complete; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, UX traceability, and file-size checks passed)
Task R4: complete (this commit, base 9f9eb5cf30, turned Recently Added into a rolling seven-day Library scope without a 50-track cap, sorted it newest-first, exposed a removable scope pill that returns through history to the remembered Library, and migrated the built-in smart playlist to a semantic role; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, UX traceability, file-size checks, and the isolated FIL-8 display regression passed)
Task R5: complete (this commit, base f1c7d07086, added an optional hidden-by-default Added column with ISO timestamp rendering, persisted editor visibility/order/width, legacy-layout normalization, and added_at sorting; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, UX traceability, file-size checks, and the isolated BROWSE-9 display regression passed)
Task R6: complete (this commit, base bc5c6beda5, applied descending direction to every compound column-sort term, kept the active Now Playing tab outside the track-identity crossfade, and constrained the Podcast episode table to a shrinkable scroller so the structural player bar stays visible; fmt, strict clippy, every non-socket workspace test, audit with only accepted RUSTSEC-2024-0436, core purity, UX traceability, and file-size checks passed; the two loopback-socket MCP radio tests and isolated display runs were blocked by sandbox socket permissions)

# Simplified Android playlist mirroring

Base: `ea1b3dc7c1`

Baseline gate repair: complete (this commit, base ea1b3dc7c1, removed the stale Concerts preferences callback reintroduced by the navigation merge; format, strict clippy, core purity, audit and the workspace suite excluding the two inherited sandbox-blocked Radio socket tests passed)
Task 1: complete (this commit, base 5876de477d, added the default MP3 256 transfer profile, bitrate-aware copy/transcode decisions, conservative target-size estimation and duplicate-safe per-playlist plus union projections; format, strict clippy, full workspace tests, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 2: complete (this commit, base e8024a6c74, added schema v34 with non-cascading explicit source/device/profile inventory, durable playlist-path inventory, safe Entire Library retirement and legacy Opus replacement markers; adversarial review also replaced the last implicit transfer-size fingerprint, and format, strict clippy, full workspace tests, architecture, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 3: complete (this commit, base d232eed045, added a deterministic safety-blocking mirror planner with deduplicated playlist projections, explicit copy/replace/remove operations, unavailable-track retention, managed-orphan cleanup and stable root-level M3U snapshots; format, strict clippy, full workspace tests, architecture, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 4: complete (this commit, base f668b11a54, replaced the retired two-worker Opus ring buffer with capability-gated sequential CBR MP3/ID3 transfers, preserved required tags and embedded covers, verified .part sizes before finalization, and documented and gated GStreamer Good Plug-ins; format, strict clippy, full workspace tests, architecture, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 5: complete (this commit, base b3310898a9, removed the global runtime lock, proved channel-gated concurrent sync and cancellation isolation with one file operation per device, added generation-guarded smoothed MTP-only rates, and delayed replacement deletion until the verified new file was inventoried; adversarial inventory-failure coverage plus format, strict clippy, full workspace tests, architecture, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 6: complete (this commit, base a7adfeb6bd, queried capacity on the resolved target storage, replaced the full phone-content surface with Reprise-managed paths plus aggregate Reprise/other-music usage, and added honest complete/unknown/inconsistent after-sync composition projections; adversarial blocked-plan and capacity-inconsistency coverage plus format, strict clippy, full workspace tests, architecture, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 7: complete (this commit, base b6718fcad7, exposed a toolkit-neutral compact page projection with all MP3 qualities, complete manual and smart playlist rows, duplicate-safe physical totals, path-free blockers and warnings, honest storage and change summaries, persistent immediate edits, and per-device start/cancel controls; integrated safe mirror execution, unavailable-track retention, ordered playlist replacement, and failure-safe publication, with format, strict clippy, full workspace tests, architecture, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 8: complete (this commit, base eb3b0f4d11, added a compact live Android playlist-mirroring dialog with all MP3 qualities, editable manual and smart playlist rows, deduplicated target totals, path-free change/storage/blocker/warning copy, in-place progress updates, per-device start/cancel controls, eject, focus restoration and no retired entire-library, pin or ratings controls; format, strict clippy, full workspace tests, architecture, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed, while the compiled isolated display test could not start because the sandbox denied the private D-Bus socket)
Task 9: complete (this commit, base d87527076b, routed each sidebar device card to its compact sync dialog and projected independent playlist-mirroring changes, warnings, finishing state and smoothed MTP-copy rates without consulting the retired entire-library selection; adversarial warning-state coverage plus format, strict clippy, full workspace tests, architecture, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Dev integration: complete (this merge commit, remote dev 9f9fbd809b, imported PRs #72-#75 with all 95 changed Git blobs verified against GitHub, preserved both parallel schema migrations by keeping Podcasts/Radio at v34 and Recently Added at v35 while renumbering device-sync inventory to v36, and added a regression for the parallel v34 upgrade path)
Task 10: complete (this commit, base 00bfb605c9, removed the retired Device browser, Synchronization preferences, ad-hoc queue/action and legacy pin projections; routed the main menu through a compact zero/one/many-device launcher; preserved new Dev sources and generic Preferences navigation; and degraded saved Device places safely to Library; format, strict clippy, 1441 core tests, 969 GNOME non-display tests with 292 display-only ignores, 89 Linux platform tests, 49 stems tests, architecture, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Safety follow-up: complete (this commit, base b27f69ad48, made MP3 target-size projections conservative for copied metadata and unknown durations, deferred managed and replaced-path removals until every playlist snapshot is published, guarded playlist cancellation between writes and removals, and made the local GIO transfer test wait for storage inspection; format, strict clippy, 1441 core tests, 973 GNOME non-display tests with 292 display-only ignores, 89 Linux platform tests, 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed; the full workspace was green with only the two inherited Radio loopback fixtures filtered because this sandbox rejects their local TcpListener bind with EPERM)
Storage review follow-up: complete (this commit, base 1cb94d9971, replaced the single Reprise-only progress fraction with a theme-derived Music, after-sync growth, Other and Free composition bar, kept the complete values in path-free text, hid the visualization for unknown capacity, and added the planned MTP-7 rule proposal for owner review; format, strict clippy, the unfiltered full workspace with 1441 core tests, 974 GNOME non-display tests and 292 display-only ignores, 89 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Ledger correction: the Storage review follow-up workspace run filtered the two inherited Radio loopback fixtures because this sandbox rejects their local TcpListener bind with EPERM; all other workspace tests and the focused storage regressions passed.
Task 11: complete (this commit, base acc744e17b, aligned the path-free agent, local D-Bus and MCP contracts with the compact playlist-mirroring page; exposed manual and smart playlist identity, MP3 quality, deduplicated totals, full change and storage projections, blockers, warnings, controls and progress; replaced ambiguous single-name configuration with validated multi-source identity and explicit MP3 quality while clearing the inert Opus field; format, strict clippy, full workspace tests except the same two inherited sandbox-blocked Radio loopback fixtures, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed; the additional MCP mpris suite passed with its two inherited no-session-player fixtures filtered, and the typed app/client endpoints plus focused runtime tests passed while private D-Bus roundtrips self-skipped because dbus-daemon is unavailable in this sandbox)
Concurrent review follow-up: complete (commit 4b603e3ac7, base acc744e17b, independently corrected the legacy single-playlist agent path to select an explicit MP3 profile with a 256 kbit/s default, cleared the inert Opus field and replaced the obsolete plan; Task 11 supersedes that narrow compatibility surface with stable multi-source identities and the complete path-free page contract)
Task 12: complete (this merge commit, parents 0ec96b3df4 and 4b603e3ac7, reconciled the concurrent review without discarding its history, replaced the retired Android-sync draft with the maintained simplified playlist-mirroring plan, documented the final architecture, safety invariants, path-free agent/D-Bus/MCP contract and explicit manual-device boundary, and closed the stage with only the test-device checks plus planned MTP-7 owner review outstanding; format, strict clippy, the unfiltered full workspace suite, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Dev refresh: complete (this merge commit, live remote dev 066d2abcb6 via exact snapshot import 64763af0cf, all 13 changed file blobs verified byte-for-byte through GitHub after shell transport failed, preserved the R6 compound-sort, Now Playing transition, Podcast scroller and sidebar DnD extraction alongside simplified Android mirroring; format, strict clippy, the unfiltered full workspace suite, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
# CAVA visualizer DSP

Base: `433376ee5c`

Task CAVA-1: complete (commit b9ffc5bc, base 433376ee5c, added the MIT-attributed CAVA logarithmic and FFT-quantized band planner with 1-256 bar support and pinned even/odd sample-rate cutoffs; format, strict clippy, workspace tests, audit with only accepted RUSTSEC-2024-0436, and core purity passed)
Task CAVA-2: complete (commit c8b59893, base b9ffc5bc, added rolling normalized mono PCM ingestion, CAVA Hann windows, dual pure-Rust real FFTs, upstream EQ/bin aggregation, and 200 Hz/2 kHz target-band regressions; format, strict clippy, workspace tests, audit with only accepted RUSTSEC-2024-0436, and core purity passed)
Task CAVA-3: complete (commit 09bb263e, base c8b59893, ported CAVA's frame-rate-adjusted quadratic gravity and additive integral memory with immediate attack and settling release regressions; format, strict clippy, workspace tests, audit with only accepted RUSTSEC-2024-0436, and core purity passed)
Task CAVA-4: complete (commit 1fda8685, base 09bb263e, added CAVA autosensitivity, a configurable historical low-level gate, bounded finite output, normalized-PCM sanitization, silence-safe gain caps, stable maximum smoothing, reset, and 256-bar endurance coverage; format, strict clippy, workspace tests, audit with only accepted RUSTSEC-2024-0436, and core purity passed)
Task CAVA-5: complete (commit f8866bae, base 1fda8685, replaced the legacy dB, beat, dynamics, and display-envelope pipeline with a clock-synchronized pre-ReplayGain CAVA PCM branch and 64 one-to-one neon bars, including filter-replacement and stream-handoff recovery; format, strict clippy, workspace tests, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, motion, input parity, accessibility, diff purity, and file-size gates passed)
Latest-dev gate repair: complete (this commit, base 0218da51, extracted the unchanged sidebar collection-boundary navigation into a focused sibling after current dev exceeded its new 600-line orchestrator gate; format, strict clippy, isolated workspace tests with the two sandbox-blocked loopback tests explicitly skipped, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, motion, input parity, accessibility, diff purity, and file-size gates passed)
Bass glow follow-up: complete (this commit, base 437d0c7f, added an AC-22 presentation-only bass-energy envelope with immediate attack, soft release, two broad neon glows and two nonlinearly stronger breakdown auras while preserving all 64 CAVA bar heights; the final Gallery render was visually reviewed and the 1920x1080 render diagnostic stayed within budget at 8.116 ms p95 and 11.410 ms p99; format, strict clippy, rustdoc, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, motion, input parity, accessibility, diff purity and file-size gates passed; every workspace test was exercised, with all non-socket tests passing and the two inherited Radio MCP loopback fixtures blocked only at sandbox TcpListener bind with EPERM; all 295 isolated display tests were launched but the sandbox blocked their private D-Bus sockets before their test bodies)
Cold-start saturation fix: complete (this commit, base 287454e8, preserved CAVA's autosensitivity state and gain formula while shape-normalizing only its cold calibration overshoot to 85% headroom, preventing song-start and post-seek full-bar flashes; the operator-approved To the Hellfire 5:23.3 fakesink regression improved from 0.880 maximum mean and 33/64 near-full bars to 0.416 and 0/64; format, strict clippy, full workspace tests, core purity, architecture, UX traceability, motion, input parity, accessibility, file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed)
Breakdown glow calibration: complete (this commit, base 9010aea5, recalibrated the existing presentation-only bass-energy glow against the operator-approved Unbreakable 3:18-3:40 passage so recurring 0.446 RMS bass waves reach the aura while preserving restrained lead-in glow; the offline 60 Hz render showed visible glow in 45.0% and aura in 22.7% of frames, while the To the Hellfire 5:15-5:35 cross-check remained selective at 19.6% and 5.6%; no bar heights, CAVA DSP state, audio files, tags, database state, or audio output changed)
Dev refresh 2: complete (this merge commit, live remote dev 73bb12dcd2 via exact snapshot import 770ded0068, all 32 changed file blobs verified byte-for-byte through GitHub after shell transport failed, preserved the CAVA visualizer/playback refactor and sidebar boundary extraction alongside simplified Android mirroring; format, strict clippy, the unfiltered full workspace suite, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)

# Simplified Android transfer-profile follow-up

Task 13: complete (this commit, base 8fe17f1609, replaced selectable MP3 bitrates with Opus 160 kbit/s as the default, MP3 256 kbit/s as the compatibility fallback and byte-preserving Original passthrough; restricted lossy encoding to known lossless sources so lossy, unknown and ambiguous formats stay unchanged; added schema v37 with MP3-preserving upgrade behavior and idempotent repair, actual-output paths and stable copy fingerprints, Opus tags/covers, GTK plus agent/D-Bus/MCP profile contracts, and isolated Opus, MP3 and byte-identical FLAC end-to-end coverage; format, strict workspace and mpris clippy, the unfiltered workspace suite with 1440 core tests, 976 GNOME non-display tests, 92 Linux platform tests and 49 stems tests, the additional mpris suite and private D-Bus roundtrip, rustdoc, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, diff purity, file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed; the isolated MTP-2 GTK test could not start because the sandbox denied its private D-Bus socket, and real-device checks plus the planned MTP-7/MTP-8 owner review remain open)
Task 14: complete (this commit, base 5c64a9e148, projected optional GIO target access as Writable, Read-only or Unknown through platform inspection, core storage/page controls, the compact GTK dialog and the path-free agent state; known read-only targets block start with an explicit reason while incomplete capability evidence stays honest and non-blocking; extracted storage copy to preserve the Rust file-size gate and added planned MTP-9 for owner review; format, strict clippy, the unfiltered workspace suite with 1442 core tests, 977 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, diff purity, file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed; live Pixel storage metadata reported explicit writable access with consistent capacity)
Task 15: complete (this commit, base ce77a076d4, kept successful transfers in non-editable Finishing state until a post-sync device inspection completed, moved the success timestamp and lifecycle toast behind that readback, exposed the actually observed Reprise track count in a Last synchronization row, and kept failed readback from claiming success; closed the direct runtime read-only bypass and added planned MTP-10 for owner review; format, strict clippy, the unfiltered workspace suite with 1442 core tests, 981 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, isolated MTP-2 GTK rendering, architecture, accessibility, input, motion, GStreamer, UX traceability, diff purity, file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 16: complete (this commit, base 07c6368a9d, replaced the idle unconfigured device-card action prompt with known storage access plus free-space information while preserving Needs attention for real scan, sync, warning and invalid-selection states; activated and covered MTP-11; format, strict clippy, the unfiltered workspace suite with 1442 core tests, 982 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 17: complete (this commit, base 2d8cfbaa38, corrected the mirror planner to warn about but never delete physical device files absent from this Reprise database's explicit inventory, preserving pre-existing Music/Reprise content when local sync history is fresh or lost; format, strict clippy, the unfiltered workspace suite with 1443 core tests, 982 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 18: complete (this commit, base d02878e532, added schema v38 and a per-device last verified synchronization time to every available playlist row, persisted it atomically only after successful device readback, preserved previous times across failed or partial runs and migrations, and activated MTP-12; adversarial migration, readback and timestamp-write failures passed alongside the direct one-device launcher plus independent two-device parallel start and cancel coverage; format, strict clippy, the unfiltered workspace suite with 1446 core tests, 984 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 224 active rules, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 19: complete (this commit, base 10634ecbc1, made host-backed smoke instances non-unique and gave them an explicit isolated MPRIS bus identity so they can coexist with a normal Reprise session; formalized the temporary-root SimulatedMtpDeviceBackend as the stable hardware-free E2E boundary and verified Opus 160, MP3 256 and byte-identical Original through it; an explicitly authorized Pixel MTP acceptance run independently confirmed all three profiles, post-sync readback and per-playlist timestamps while preserving 201 pre-existing untracked files; format, strict clippy, the unfiltered workspace suite with 1446 core tests, 986 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, rustdoc, architecture, accessibility, input, motion, GStreamer, UX traceability with 224 active rules, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)

# QA regression fixes

Task QA-80: complete (this commit, base 066d2abcb6, made Podcasts⇄Music atomic so outgoing and incoming dense source tables never overlap while other outer page switches retain the Standard crossfade)
Task QA-78: complete (this commit, base 5a9e8b985b, queued the stable-ID scroll anchor before GTK can paint a reload's transient position-zero state, then retained the exact offset restore for metadata and rating saves)
Task QA-79: complete (this commit, base d757e13293, moved the shared progress activity inside the bottom-pinned Issues section between its heading and the Missing files / Import errors sources)
Live-QA follow-up: complete (this commit, base 179f00f3e6, restored Standard-duration Podcasts⇄Music motion without overlapping readable tables, carried the pre-dialog stable-ID viewport anchor through asynchronous rating and tag saves, and suppressed inherited expansion so running activity sits directly above the issue rows; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, and file-size checks passed; the three visible desktop behaviors remain the explicit manual acceptance check)
Live-QA refinement: complete (this commit, base 73ce1d26d9, made plain-Library rating-only Tag Editor saves update cached and realised rating cells without any model signal or scroll operation, retained query reloads for rating-dependent views, and made active scanner, doctor, and relink cards atomically replace the complete Issues surface while keeping persistent device state independent; fmt, strict clippy, 982 GNOME non-display tests, full workspace tests, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, and file-size checks passed; isolated display execution was blocked before GTK startup by sandbox D-Bus socket EPERM, so both visible behaviors remain the explicit manual acceptance check)
Live-QA bottom placement: complete (this commit, base 581f75da54, explicitly suppressed computed vertical expansion on the Issues/progress replacement stack and pinned it above the player so active scanner, doctor, and relink cards occupy the former bottom-left Issues position; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, and file-size checks passed; native visual placement remains the explicit manual acceptance check)
Live-QA stack-page placement: complete (this commit, base 26778d03ce, placed a flexible spacer before the shared progress cards because GtkStack still allocates its active page at full height, keeping scanner, doctor, and relink cards at the former bottom-left Issues position regardless of page allocation; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, and file-size checks passed; native visual placement remains the explicit manual acceptance check)
Live-QA revealer placement: complete (this commit, base 968b68d991, removed fully inactive scanner, doctor, and relink revealers from layout while preserving their active fades, moved flexible allocation into the shared progress root, and made the sidebar root fill its page so the visible card block sits directly above the player; fmt, strict clippy, the full host-isolated workspace suite, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, file-size checks, and both isolated display regressions passed; isolated CUA directly confirmed the scanner edge above the player, while later unrelated keyboard scenarios outlived the deliberately shortened app timer and are not claimed)
Responsive-window follow-up: complete (this commit, base 802ec513ac, activated STYLE-5, STYLE-6, and MINI-5; kept the complete player bar inside horizontally, vertically, and jointly constrained windows; ellipsized long metadata without shifting centered transport; made tall hidden and Now Playing content surrender height; folded secondary columns transiently with exact live-state restoration; and offered the user-controlled Compact Mode switch once per session; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, architecture, accessibility, input, motion, UX traceability, QA linters, gettext, CUA helper, diff, and file-size checks passed; isolated CUA with generated audio directly confirmed 720x760, 1200x420, 720x420, reopened columns, Compact Mode, long-title playback, and the short open-panel layout)
Task FIL-9: complete (this commit, base 179f00f3e6, centered the loaded track after setting, changing, or clearing search and facet filters whenever it remains visible; fmt, strict clippy, every non-socket workspace test, audit with only accepted RUSTSEC-2024-0436, architecture, QA linters, core purity, UX traceability, and file-size checks passed; the isolated display regression compiled but sandbox socket permissions blocked its Xvfb execution)

# Album-cover consistency follow-up

Base: `066d2abcb6`

Task COV-1: complete (this commit, base 066d2abcb6, activated BROWSE-10, detected divergent embedded artwork by normalized album identity in the opt-in cover worker, and made the resulting shared downloaded cover canonical without writing to music files; format, strict clippy, 1412 core + 978 GNOME non-display + 88 Linux platform + 49 stems tests, architecture, UX traceability, core purity, file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed; two inherited MCP Radio tests requiring local TCP listeners were blocked by sandbox EPERM while the otherwise complete workspace run passed with those exact tests filtered)
Assumption COV-1: album identity remains the existing normalized file-tag pair of Album Artist plus Album; intentional disc-specific artwork under that same identity is unified only when the user-enabled cover-download batch can obtain a shared cover.
Manual COV-1: real Cover Art Archive retrieval and the visible post-restart convergence remain a native-desktop/network check because validation used synthetic FLACs, isolated XDG state, and no real library or database.
Badge/filter follow-up: complete (commit fb16b8bc73, base 492ea733f6, PR #85 merged into remote dev as 348c419351; anchored the compact Updates badge and refreshed its scope through persisted Concerts filter changes; local gates passed, while GitHub Actions contained no steps and returned BlobNotFound for its log archive)
Remote-dev synchronization: complete (this merge commit, base 03bf59decb, merged remote dev 393e2822d4; retained the active STYLE-5, STYLE-6, MINI-5, MOT-8, FB-8, and TAG-1 behavior while integrating FIL-9, CAVA AC-22, BROWSE-10 cover consistency, and the Updates badge follow-up; format, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, architecture, accessibility, input, motion, UX traceability, QA linters, gettext, CUA helper, diff, and file-size checks passed; the isolated responsive-window CUA scenario passed again after the merge and confirmed the long-title, narrow, short, combined, reopened-column, Compact Mode, and short-open-panel layouts)
QA/dev integration: complete (this merge commit, base 393e2822d4, retained Standard-duration non-overlapping Music/Podcasts motion, pre-dialog metadata anchors, rating-only in-place refresh, and the bottom-pinned Issues/progress replacement while integrating current dev; stabilized FIL-9 centering across GTK's delayed post-reload scroll by refining the target through settled frames; format, strict clippy, rustdoc, full host-isolated workspace tests, 161 rule-named display tests, 26 motion display tests, 11 CSS display tests, core purity, architecture, accessibility, input, motion, UX traceability, QA linters, file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed; the operator visually accepted the QA behaviors before publication)
Dev refresh 3: complete (this merge commit, live remote dev 144672eaef, preserved the simplified playlist-only Android flow without a Device browser while integrating the latest cover consistency, source-transition, viewport-anchor, rating refresh, Updates badge and bottom-pinned Issues/progress work; resolved the Sidebar conflict by keeping its extracted root module and delegating the new complete Issues/activity replacement region into it, and routed My Stats through the new safe content-stack transition helper; format, strict clippy, the unfiltered workspace suite with 1447 core tests, 996 GNOME non-display tests and 297 display-only ignores, 93 Linux platform tests and 49 stems tests, rustdoc, architecture, accessibility, input, motion, GStreamer, UX traceability with 226 active rules, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Android sync review follow-up: complete (this commit, base 4cf97aeba1, deduplicated path-free agent warnings by category, made post-sync managed readback include byte-preserved Original formats without mistaking playlists or partials for tracks, generalized the guarded simulator to independent temporary roots, and added GIO-backed E2E coverage for parallel devices plus preservation of untracked managed audio; activated owner-approved MTP-7 through MTP-10 with rule-named storage, profile-policy, access and readback tests; renamed the branch to the required feature namespace; format, strict clippy, the unfiltered workspace suite with 1447 core tests, 1000 GNOME non-display tests and 297 display-only ignores, 94 Linux platform tests and 49 stems tests, rustdoc, architecture, accessibility, input, motion, GStreamer, UX traceability with 230 active rules, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Assumption Android sync review: retained the one-variant Mp3Quality type because it is the narrow compatibility boundary for persisted v37 settings and existing public profile payloads; flattening it would add migration and API churn without simplifying the user-facing three-profile model.

# Responsive-window QA follow-up

Responsive panels and waveform seek: complete (commit 202b14cbca, base 484de08f32, activated STYLE-7 so snapped windows close both side panels in one transient non-persisting transition with exact 10-second Undo restoration and reachable reopen toggles; fixed pre-map waveform peaks so the seek bars build on the first frame instead of leaving only the playhead line; format, strict clippy, full host-isolated workspace tests, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, gettext, CUA helper, diff, and file-size checks passed; isolated CUA directly confirmed paired side-panel closure, Undo restoration, waveform rendering, long-title centering, narrow and short player reachability, column folding, and the Compact Mode suggestion)
Dev refresh 4: complete (this merge commit, live remote dev 461fadb886, preserved the simplified playlist-only Android flow, modern transfer profiles, verified readback, parallel-device simulator and path-free agent state while integrating the responsive side-panel, player-bar, waveform-seek, compact-mode suggestion, track-column and translation follow-ups; the only content conflict was the append-only progress ledger and both histories were retained; format, strict clippy, the unfiltered workspace suite with 1447 core tests, 1006 GNOME non-display tests and 302 display-only ignores, 94 Linux platform tests and 49 stems tests, rustdoc, architecture, accessibility, input, motion, GStreamer, UX traceability with 234 active rules, core purity, diff purity, Rust file-size enforcement, 167 rule-named display tests, 26 motion display tests, 10 CSS display tests and audit with only accepted RUSTSEC-2024-0436 passed)

# Discography gaps

Base: `144672eaef`

Task DG1: complete (this commit, base 144672eaef, retained historical regular Album and EP release groups as durable catalog rows while preserving the 90-day and 20-per-artist Updates contract and transient Single retention; format, strict clippy, the complete host-isolated workspace suite except the two sandbox-blocked Radio MCP loopback tests, core purity, file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed)
Task DG2: complete (this commit, base 2539a6c087, added schema-v36 official release lengths and requires distinct local Album Artist plus Album track identities to cover the full released Album or EP before ownership is complete, so advance singles, duplicate files, unknown remote lengths, and future releases remain visible gaps)
Task DG3: complete (this commit, base 2539a6c087, pages and deduplicates every MusicBrainz release-group browse response, retains undated Album/EP catalog entries, enriches local title matches with the smallest multi-track official edition, and defaults new configurations to the bounded all-artists rotation)
Task DG4: complete (this commit, base 2539a6c087, activated NR-16 through NR-18 so the full Releases view and its sidebar badge contain only Album/EP gaps for current library artists, expose Missing/Incomplete/X-of-Y statuses, retain Type/Hidden filters, and remove the six-month footer)
Task DG5: complete (this commit, base 2539a6c087, documented the approved-desktop-partner, disclosure, allowlist, and privacy boundary in `docs/plans/releases-affiliate-links.md` and planned NR-19; no affiliate tracking was enabled without a qualifying contract)
Task DG6: complete (this commit, base ea907fae0f, activated NR-20 and added a visible provisions-free Bandcamp purchase column for exact MusicBrainz `/album/…` relations on real Bandcamp hosts; lookalike domains, artist homepages, search URLs, tracking parameters added by Reprise, and non-Bandcamp targets never receive a purchase button)
Discography-gaps verification: complete (format, strict clippy, the complete host-isolated workspace suite except the two sandbox-blocked Radio MCP loopback tests, core purity, file-size checks, UX traceability with 219 active rules, and cached audit with only accepted RUSTSEC-2024-0436 passed; the isolated NR-20 display test was blocked before GTK startup by sandbox D-Bus socket EPERM, and no real database, music files, or desktop session were used)
Discography-gaps commit incident: resolved (the sandbox initially rejected the DG2 commit through a read-only worktree metadata path; once Git metadata became writable, DG2-DG5 landed together in this single unpushed recovery commit rather than the planned per-task commits)
Dev refresh 5: complete (this merge commit, live remote dev 6cdb1af33e, preserved the simplified Android playlist mirror, modern transfer profiles, verified per-playlist timestamps, parallel-device runtime and simulator while integrating Discography gaps and Bandcamp links; resolved the parallel schema-number collision by retaining Android Sync v36-v38 and moving the Discography track-count migration to v39 with an upgrade-preservation regression; format, strict clippy, rustdoc, the unfiltered workspace suite with 1460 core tests, 1009 GNOME non-display tests and 302 display-only ignores, 94 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 235 active rules, core purity, diff purity, Rust file-size enforcement, 167 rule-named display tests, 26 motion display tests, 10 CSS display tests and audit with only accepted RUSTSEC-2024-0436 passed)
Task 20: complete (this commit, base f974b47858, replaced the per-device modal with a non-modal full page opened from the sidebar card or device launcher; retained only the three-profile transfer setting plus playlist selection, profile-dependent per-playlist sizes, verified sync times, delta, storage, progress, smoothed MTP rate, Sync/Cancel and Eject; treated dynamic device and playlist names as plain text to eliminate ampersand markup failures; corrected known-lossless size projections and removed a page-subscription retention cycle; added the isolated android-sync-page background CUA scenario with a simulated MTP root and real Opus publication; format, strict clippy, rustdoc, the unfiltered workspace suite with 1461 core tests, 1010 GNOME non-display tests and 302 display-only ignores, 94 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 235 active rules, core purity, diff purity, Rust file-size enforcement, 167 rule-named display tests, 26 motion display tests, 10 CSS display tests, the CUA helper contract and simulated-device acceptance run, and audit with only accepted RUSTSEC-2024-0436 passed)
Task 21: complete (this commit, base 8c2786412f, replaced the settings-shaped device page with a simple playlist-first dashboard whose shared hero contains device identity, MTP status, last device sync, storage composition and Sync/Eject actions; kept only selectable playlist rows with profile-dependent size and verified playlist time plus a compact transfer/delta/progress overview; projected local playlists before asynchronous MTP inspection, repaired the page-controller lifetime, and made each full playlist row one accessible toggle action; the background CUA scenario selected a playlist, observed its 2.4 MiB Opus projection, started synchronization and verified the published Opus file; format, strict clippy, rustdoc, the unfiltered workspace suite with 1461 core tests, 1012 GNOME non-display tests and 303 display-only ignores, 94 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 236 active rules, core purity, diff purity, Rust file-size enforcement, 168 rule-named display tests, 26 motion display tests, 10 CSS display tests, the CUA helper contract and simulated-device acceptance run, and audit with only accepted RUSTSEC-2024-0436 passed)
Task 22: complete (this commit, base 209f3adc9a, activated MTP-15 and stabilized the playlist and overview cards against dynamic sync copy, separated the live MTP transfer rate into its own labeled row, and kept free device space visible first in every sidebar phase; completed the path-free MCP control surface with verified device and playlist sync times, storage write access, capability-gated eject and can_eject state while retaining one shared deduplicated MirrorPlan across overlapping playlists; format, strict clippy, rustdoc, the unfiltered workspace suite with 1461 core tests, 1012 GNOME non-display tests and 305 display-only ignores, 95 Linux platform tests and 49 stems tests, the full MPRIS-enabled MCP suite, architecture, accessibility, input, motion, GStreamer, UX traceability with 237 active rules, core purity, Rust file-size enforcement, 170 rule-named display tests, 26 motion display tests, 10 CSS display tests, the CUA helper contract and simulated-device acceptance run, and audit with only accepted RUSTSEC-2024-0436 passed; the optional gettext catalog check remains red on unchanged base HEAD because twelve previously integrated Arabic Releases/Bandcamp entries are absent)
Task 23: complete (this commit, base 319945794f, activated MTP-16 and MTP-17, locked the per-device transfer-profile choice across runtime recreation, and made `Music/Reprise` an authoritative mirror root that removes every safe non-desired audio, playlist or other file only after desired tracks and playlists are published; desired physical playlists remain protected, unknown same-size destination contents are overwritten instead of trusted by size, and all content outside `Music/Reprise` remains untouched; format, strict clippy, rustdoc, the unfiltered workspace suite, 13 focused MirrorPlan tests, 20 Linux storage tests, 39 GNOME device-runtime tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 239 active rules, core purity, Rust file-size enforcement, rule-named, motion and CSS display tests, the isolated `android-sync-page` CUA simulator run, and audit with only accepted RUSTSEC-2024-0436 passed; no real device or user library data was used)

# Weekly architecture review cleanup

Dead-code cleanup: complete (this commit, base 96fb39d7c5, removed four verified orphan UI paths from the legacy tag-editor helpers, track-model invalidation escape hatch, unused sidebar badge builder and stale PlayerBar mute fields; focused tests, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, motion, GStreamer, UX traceability with 239 active rules, QA linters, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 0.1: complete (this commit, base add6821575, confined automatic podcast cleanup to the canonical configured download root, rejected foreign and symlink-escaped stored paths without deleting files or clearing their database pointers, and retained stale missing-file reconciliation; the focused red-green regression, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, motion, UX traceability with 239 active rules, QA linters, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 0.2: complete (this commit, base b4066239b1, made MTP partial-file cleanup fail closed so a cleanup error blocks every subsequent copy, playlist publication and managed deletion, records no successful sync time, and remains visible as a track-independent synchronization failure; the focused red-green MTP-5 regression, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, motion, UX traceability with 239 active rules, QA linters, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 0.3: complete (this commit, base b51f74f341, enforced active rule NR-20 in the reusable Core by rejecting Bandcamp album targets containing any query parameters, including common source and campaign tracking forms, while preserving unchanged direct album URLs on real Bandcamp hosts; the focused red-green rule test, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, motion, UX traceability with 239 active rules, QA linters, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 0.4: complete (this commit, base 8b6116ff58, released the podcast runtime's subscriber RefCell borrow before invoking callbacks so a callback can safely register another subscriber during notification without a reentrant panic; the focused red-green reentrancy regression, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, motion, UX traceability with 239 active rules, QA linters, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 0.5: complete (this commit, base 32eb1028e4, made the podcast and radio row context menus reachable from the whole row instead of only the cell content box; a new shared ui::source_context_surface takes the cell's Adwaita padding and re-applies it on the surface that carries the secondary-click gesture, so measured pointer coverage of a realized podcasts row rose from 52% to 100% while the wrapped child keeps its previous geometry, the podcast keyboard path moved off recycled unfocusable cells onto the ColumnView, and both tables build their menu through one function; six new rule-named display tests built from the production factories, format, strict workspace clippy, rustdoc, the full unfiltered workspace suite, 176 rule-named plus 26 motion plus 10 CSS display tests, architecture, accessibility, input, motion, UX traceability, QA linters, MSRV, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed; the gettext catalogue check fails identically at HEAD and is untouched by this change; the plan's real-event CUA acceptance was set up against an isolated Xvfb/openbox/D-Bus session with a cua-driver daemon bound to that display and confirmed window discovery and the seeded radio view, but the click sweep itself was stopped on the operator's instruction because the radio, YouTube and podcast surfaces are currently being rebuilt)
Stage 0: complete (tasks 0.1-0.5 landed on fix/thin-core-headless-mcp-stage0 above dev at 96fb39d7c5, closing every review blocker of the 21-28 July weekly review; the full gate set is green except the gettext catalogue check, which fails identically at HEAD and is untouched by this stage. Deferred and genuinely manual: the plan's real-event pointer acceptance for the podcast and radio context menus was prepared against an isolated Xvfb/openbox/D-Bus session with a cua-driver daemon bound to that display - window discovery and the seeded radio view were confirmed, the click sweep was stopped on the operator's instruction because those surfaces are being rebuilt; real devices, GVfs MTP, hardware audio and actual GNOME rendering remain unverified as always. Nothing is pushed.)

# Thin core and headless MCP — Stage 1

Task 1.1: complete (commit 650cdc5536, base 9ed9c731a8, made the runtime a binding design instead of a parked idea: section 9 of multi-frontend-core.md draws the ownership line so Beschluss 1 keeps the database and the runtime gets only the state that never reaches it, and specifies the four lifecycle states with complete transitions, an exclusive lease taken before any effect is opened, D-Bus activation as the single start path, snapshot-then-deltas reconnection with no replay and no buffered commands, an idle shutdown that refuses to interrupt work, four shared error categories and a per-resource capability matrix recording today's state; section 2.1 gained a forward note and the user-visible half became RUN-1..RUN-5 [geplant] in the new UX section AG; UX traceability with 239 active rules passed; the window-close question is deliberately open and marked as a Regelvorschlag because it is a product decision, not an architectural consequence)
Task 1.2: complete (commit 8ef1c2fee9, base 650cdc5536, replaced the sixteen-field positional device-sync tuple — declared once in reprise-platform-linux and again in reprise-mcp, with seven consecutive u64s in one nested row — by named types in a new reprise-runtime-protocol crate that both sides share; snapshots now cross D-Bus as a{sv} dictionaries keyed by field name so a reorder is unobservable and an unset Option is an absent key rather than a (bool, T) pair, PROTOCOL_VERSION plus a ProtocolVersion property give the seam a handshake that refuses a foreign major version with an actionable message, and command enums stay in-process because D-Bus has no sum type; nine schema tests pin round trips including nested dictionaries and sparse optionals, the checked-in wire field names and path freedom, and the existing private-bus integration round trip now runs end to end through the named types; the protocol crate is optional in reprise-mcp behind its mpris feature so check-architecture.sh's rule that the default MCP build depends on reprise-core alone still holds; format, strict workspace clippy with and without all features, rustdoc, the full workspace suite, the all-features MCP suite, architecture, accessibility, input, motion, UX traceability, QA linters, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 1.2 review fix: complete (this commit, base 8ef1c2fee9, from an adversarial review of the protocol change: the field-by-field migration of the sixteen-field tuple and its four nested rows was confirmed correct with no dropped, swapped or polarity-inverted field, but two contradictions were real. `is_compatible_with` refused a peer with a lower minor version, which contradicts the crate's own documented rule that a minor bump only adds optional fields an older peer may omit — it would have hard-failed the most ordinary upgrade sequence, a client updated on disk while the older runtime still runs; only the major version decides now, and the minor stays for a client that wants to skip a feature knowingly. Section 9.8 of the architecture plan asserted that playback and device tools are in the default MCP build, which the shipped `mpris` feature gate contradicts; the rule is now stated as the target it is, with Task 3.4 named as the place it becomes true)
Task 1.3: complete (this commit, base a7f47576ee, gave the thin-frontend goal a number instead of an intention: scripts/check-frontend-thinness.sh counts direct rusqlite, filesystem and thread use plus business-worker files in reprise-gnome and pins each at its measured value as both ceiling and floor, so growth fails and an extraction fails too until it lowers the budget in the same commit; GStreamer and zbus are bans rather than budgets because the frontend already reaches zero on both. Counting strips comments and #[cfg(test)] blocks — without the first, ui/mod.rs's own documentation of the zbus ban would trip the check enforcing it, and without the second, writing a test would be punished; the block is skipped rather than the file truncated, so production code after a test module stays measured, which the first draft missed and which is worth 27 rusqlite and one thread use. The thirteen files carrying #[allow(dead_code)] are pinned per file so a new escape hatch has to justify itself in the commit that adds it. The ratchet was verified against deliberate violations, not just assumed to work. Two plan items are honestly not delivered: cargo-machete is not installed, so the check reports SKIPPED instead of passing quietly, and -D unreachable-pub is deferred with its measurement recorded — it is 1240 sites in reprise-gnome, roughly 60% of them pub const string-table entries in modules already private to crate::ui, so it is a mechanical mass edit for its own quiet moment rather than a lint flip. Wired into check-merge-readiness.sh and the qa-linter policy list; format, strict workspace clippy with and without all features, rustdoc, the full workspace suite, the all-features MCP suite, architecture, UX traceability and QA linters passed)
Stage 1: complete (tasks 1.1-1.3 plus one review fix; merged into dev together with Stage 0 as PRs #110 and #111, dev now at 7014d364e6). Acceptance was local and not GitHub: the account's Actions cannot start jobs, and the repository's branch protection turns out to be unavailable on its plan anyway, so scripts/check-merge-readiness.sh against the current origin/dev is the gate — it passed after merging the newer dev (PR #109) into the stack, with the new thinness ratchets unchanged. Branches deleted, worktree moved to feature/thin-core-state-machines, and the nightly build was triggered and verified against the installed binary rather than its log.

# Visualizer bass glow

Bass glow rework: complete (this commit, base 323859faf3, moved the glow layer off the CAVA bands, which auto-sensitivity re-normalizes, so a quiet sung passage climbed to the same band values as a drop and the two broad glows plus their inner auras were lit almost permanently; PR #82 had made that visible rather than caused it by compressing the trigger window from [0.42, 1.0] to [0.36, 0.49], i.e. from 58% of the scale to 13%, which turned a ramp into a switch. The new reprise-core BassPressureDetector measures the same mono PCM with no gain of its own — 30-150 Hz Butterworth band, block RMS in true dBFS, and a 2 s baseline of the track's own bass — and derives one value a rhythmic kick reaches and a second that only a sustained breakdown lifts; SpectrumFrame carries both, the player feeds the detector alongside CAVA and resets it on stream change and DISCONT, and bars.rs scales the two glow layers straight off them, so the engine holds no bass heuristic at all. Calibrated against decoded audio of The Browning "WAKE UP" and Lorna Shore "To the Hellfire" rather than guessed: their quiet intros measure around -40 dBFS and their walls of sound around -14 dBFS, so absolute level alone separates them by 25 dB, and the checked-in bass_pressure_corpus example replays raw PCM through the real detector to re-check the numbers. Rendering the full chain at three moments of "WAKE UP" confirms the intended grading on screen — intro 0.00, rhythmic passage 0.35 with no aura, breakdown 0.89 with aura 0.95. AC-22 is replaced by AC-23 and its 37 tests re-hung, and the visual now names the analysis it reacts to in a 2x2 readout under the canvas, laid out that way because four columns broke the fixed 300 px panel of NPP-1 — caught by three red display tests, not by inspection. Format, strict workspace clippy, the full workspace suite, 178 rule-named plus 26 motion plus 10 CSS display tests, architecture, accessibility, input, motion, UX traceability with 239 active rules and frontend thinness passed; check-merge-readiness.sh was not run because the worktree carries unrelated untracked plan files)

Bass glow rework, corrections and follow-up: the entry above was written before the work finished and two of its numbers are wrong — 39 tests were re-hung from AC-22 onto AC-23, not 37, and the display run it cites as 178 rule-named tests became 180 once the two new readout guards existed. Three things also happened after it: driving the running app through cua-driver (isolated Xvfb/openbox/D-Bus session, its own cua-driver daemon, a copy of the library) showed the readout's second row silently clipped — caption above value needs 88 px where the strip under the canvas offers about 52 — and its captions truncated to "BA…" once they did fit, so caption and value now share a line and the levels read in whole decibels; both bounds are now pinned by measuring the widget against the real panel width and free strip height with the production stylesheet loaded. The Visual tab traded its speaker icon for rising bars, since Adwaita ships no equalizer glyph and a speaker says where audio comes out rather than what it looks like. A rust-reviewer pass approved the change with no critical or high findings; its two actionable notes were applied — the biquad state is snapped to zero at 1e-30 so a fade-out cannot decay into subnormal floats on the streaming thread, and SpectrumFrame::with_bass_pressure is #[must_use]. Merged as PR #116 (dev 49834b78f2) after the full display set ran green post-merge with origin/dev. One investigation closed negative and is worth recording so nobody repeats it: the AT-SPI tree appeared to show three SongVisualizer instances, but counting canvases in the real widget tree returns exactly one — cua-driver traverses the ScrolledWindow/Viewport subtree more than once and assigns separate element indices, so the duplication is in the snapshot, not the app.

Promotion to main: complete (PR #118, origin/main d1de9e3bfb, promoting dev 49834b78f2 — 108 commits, 261 files, +27400/-13713 since the previous stable revision: the visualizer bass-pressure rework, the thin-core runtime contracts and state machines, Android sync transfer profiles, device-sync step labels, releases with Bandcamp discography gaps, the QA viewport and responsive-player regressions, idle visuals with restored lyrics, and the English UX rulebook. scripts/check-merge-readiness.sh ran complete against origin/main and ended green: architecture, multi-frontend core boundaries, frontend lint, core purity, Rust file-size enforcement, the full workspace suite plus doc-tests, 180 rule-named plus 26 motion plus 10 CSS display tests each in its own process, accessibility, input parity, motion tokens, the GStreamer device-sync runtime, UX traceability with 239 active rules, and the dependency audit with only the accepted RUSTSEC-2024-0436. One honest note: stats_10_section_gaps_stay_equal_when_top_tracks_expand_and_collapse is load-sensitive rather than broken — it waits a fixed 400 ms for a GtkRevealer transition before asserting is_child_revealed(), which fails under a four-way parallel display run and passes serially as the gate runs it; converting it to wait on the transition instead of a deadline is open work and predates this promotion)

# Device sync log

Sync log: complete (PR #123, base dev 75f75a09d8, from a live question the app could not answer — a playlist that synced as 200 tracks showed 104 on the phone, and the only record was one last_synced_at per playlist plus two warnings in the whole sync path. Runs are now recorded in two new tables behind schema v40: sync_runs holds device, transfer profile, planned count and the balance of copied/skipped/removed/failed with the reason a run did not complete, and sync_events itemizes only files that deviated, so a 278-file run leaves a handful of rows rather than hundreds. Three decisions carry the design: the entry opens at the start rather than closing at the end, so a run whose session dies is marked interrupted by the next one instead of vanishing — "it never finished" being the answer someone actually wants; removals are itemized even though the balance counts them, because the mirror owns Music/Reprise under MTP-17 and what it deleted is the question that gets asked; and every log write is best-effort, so recording can never fail a transfer. Retention is the last thirty runs, ageing out with their deviations. The device page grows a "Recent transfers" card of expandable rows, with the wording in the frontend because the core deliberately holds none. Two guards earned their keep on the way: the schema-version pin went red on the v40 migration exactly as intended, and the frontend-thinness ratchet caught the log helpers naming Connection in their signatures — rusqlite use in reprise-gnome had grown 537 to 540 — so they now take the runtime instead and the count is back at its budget, which is also the truer shape since they never needed a database handle. Rule MTP-20 activated. Ten core tests, seven wording tests, one display test against the real page, the full workspace suite, format, strict clippy, architecture, accessibility, frontend thinness and UX traceability with 242 active rules passed. Honestly open: the log proves itself only against a real device on the next sync, and it cannot explain the run that prompted it)

# Instrumental feature removal (GTK frontend only)

GUI removal: complete (this commit, base dc290f7100, deleted the GTK instrumental surface — conversion view/model/wiring, worker-host supervisor, runtime, context-menu trigger, Conversions sidebar row and drop target, and the INST-12 model-download flow — extracted the master gate into ui/experimental.rs, and retired the 12 active plus 2 planned GUI-only INST rules; reprise-core, reprise-cli, reprise-mcp and reprise-stems are untouched, so the CLI can still create instrumentals and the AI badge plus FIL-7 keep marking them; the UX traceability gate passes with 228 active rules covered, but NOTHING in this workspace could be compiled here — GTK 4.22/libadwaita 1.9 are below the Ubuntu 24.04 candidates and GitHub Actions is currently unavailable, so fmt, clippy, tests and the architecture gate must be run locally before this lands)
Warning and gate cleanup: complete (this commit, base 47ba255faf, removed the five reported dead GTK items and their short warning cascade while preserving PlaybackMode::Preview and the AI substrate, removed the Instrumental preview gettext message and restored shared strings accidentally pruned with the instrumental module, synchronized the previously incomplete current catalogs, and updated the worker-isolation gate to require reprise-stems and ORT to remain outside the GTK dependency tree; warning-free GTK build, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, worker isolation, gettext catalogs, UX traceability with 228 active rules, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed; display tests and CUA remain unverified because the sandbox denied the private D-Bus and Xvfb sockets)
Dev integration follow-up: complete (this commit, base d5df760080, lowered the frontend-thinness ratchets to the post-removal measurements — 515 rusqlite sites, 13 thread spawns and 7 worker files — and removed the deleted conversion view from the dead-code allowlist; the focused thinness gate passed)
Remote-dev preservation: complete (this commit, live dev cbb396a1f9, preserved the newly activated RUN-1 and RUN-4 statuses while publishing the GTK instrumental removal on top of the runtime-service work)

# Podcast, YouTube, and Radio channel redesign

Base: `144672eaef`

Task PCR-1: complete (commit a4044619, base 144672eaef, added download byte metadata and an RSS-only per-subscription phone-sync opt-in that is cleared defensively for YouTube sources; finished and reclaimed downloads persist their actual size while paths and sizes clear atomically; format, strict clippy, full workspace tests, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, diff purity, and file-size checks passed)
Task PCR-2: complete (commit 7a7dc06b, base a4044619, split Podcasts and YouTube into source-grouped library places with real bounded artwork, context-aware duplicate-free Podcast/YouTube/Radio search, reversible source and episode removal, atomic manual and automatic download progress states, and RSS-only Android mirroring under Podcasts/Reprise while leaving YouTube and Music/Reprise untouched; two adversarial review rounds were resolved; format, strict clippy, full workspace tests, QA linters, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, diff purity, and file-size checks passed; the isolated grouped-view display tests compiled but their Xvfb run was blocked before GTK startup by sandbox D-Bus socket EPERM)
Task PCR-3: complete (commit 6917b993, base 7a7dc06b, activated POD-10 and completed the YouTube channel detail with an official dated UULF long-form window, worker-backed yt-dlp range expansion to 40, channel-scoped batch actions, shared localized download states, one Opus download policy, and MTP-present per-device RSS sync selection; both independent adversarial reviews were resolved; format, strict clippy, 1435 core tests, 1018 GNOME non-display tests, the otherwise complete workspace suite with only the two inherited local-TCP MCP Radio tests filtered because the sandbox rejects listener creation with EPERM, QA linters, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, diff purity, and file-size checks passed)
Stage-review note PCR: define one app-wide offline presentation contract for cached, empty, queued, interrupted, authentication, rate-limit, and provider-failure states; this remains deliberately outside the Podcast redesign stage instead of introducing a cross-app behavior change without its own specification.
Dev refresh PCR: complete (this merge commit, current dev 96fb39d7c5, preserved the Podcast/YouTube/Radio channel redesign while integrating the authoritative Android playlist mirror and current responsive-window work; resolved the parallel schema history by retaining dev v36-v39 and moving podcast download/device selection to v40-v41 with an upgrade-preservation regression; extended the shared sync plan and isolated `Podcasts/Reprise` storage root for per-device RSS downloads while excluding YouTube, and kept Podcast-only sync actionable in both the execution plan and visible page state; format, strict clippy, rustdoc, the host-isolated workspace suite with 1494 core tests, 1043 GNOME non-display tests and 310 display-only ignores, 97 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 244 active rules, core purity, diff purity, Rust file-size enforcement, release build and audit with only accepted RUSTSEC-2024-0436 passed; the two inherited MCP Radio loopback tests were filtered after the sandbox rejected local TCP listener creation with EPERM, and private-Xvfb CUA could launch the release but could not expose that private display to the driver, so visible channel and device-page behavior remains the explicit manual acceptance check; no real database, music files, device, or desktop session was used)
Task T6-A (Turn 6c, dialogs): complete (commits 1c0a1386, 317d9410, ad813737, base 77b3f54545, implemented the design document's Turn 6c across all three add dialogs — SRC-6 hard provider separation with per-source dialog identity and refused source-foreign URLs, SRC-3a re-pointing the URL path to the dialog's own source, SRC-7 one compact add action with an in-place "Added" acknowledgement plus accessible names that always name the source, SRC-8 vertical-only bounded scrolling with a fixed footer for all three dialogs, and SRC-9 optional compact subscriber counts that are omitted rather than faked; format, strict workspace clippy, 1495 core plus 1049 GNOME non-display tests, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, accessibility, input parity, motion, UX traceability with 248 active rules, and the rule-named Xvfb display gate passed; the add-dialog input projection and result projection were extracted into add_dialog_input.rs and add_dialog_results.rs to stay under the 800-line gate)
Stage-review note T6: the design document's Turn 6 and Turn 7 are captured in docs/plans/podcasts-youtube-radio-turn6.md; Blocks B through H (empty states, remote images, channel detail, MTP redesign with YouTube and podcast sync, offline contract, settings, and MCP parity) remain open, and the owner requires that every GUI capability also be reachable over MCP.
Task T6-B1 (Turn 6f, shared empty-state geometry): complete (commit 3a3c3a43, base c9a07e74, added source_empty_state.rs as the one shared "nothing added yet" geometry for Podcasts, YouTube, and Radio — sidebar glyph in a subdued rounded tile, title, one paragraph of what lands here and where it comes from, exactly one primary Add button with a plus icon, and a quiet secondary URL-path line where the source has one; reused the existing podcasts_empty_state.rs/radio_empty_state.rs classifications rather than replacing them, leaving NoEpisodes/NoResults on their prior status page; fixed radio's toolbar (Add/filter chips/count) staying visible in the true empty state and hid the podcasts footer's now-meaningless refresh row there too; the approved Podcasts secondary copy names OPML import, which has no implementation in this codebase, so it uses the feed-URL wording instead (see plan O-7); added ux-rules.md SRC-10 with rule-named tests; format, strict clippy, the full workspace suite with 1053 GNOME non-display tests and 317 display-only ignores, architecture, accessibility, input parity, motion tokens, UX traceability with 249 active rules, core purity, and cached audit with only accepted RUSTSEC-2024-0436 passed; the rule-named Xvfb display gate passed for every test this commit touched, and confirmed on the unmodified base commit that its two other red tests (podcasts_scroller's play_7b and radio add_dialog's src_5) were pre-existing failures unrelated to this change)
Task T6-B1 (Turn 6f, empty states): complete (commit 50231d3d, base c9a07e74, one shared empty-state geometry for Podcasts, YouTube and Radio behind new rule SRC-10 — sidebar glyph, title, one sentence each for what lands here and where it comes from, exactly one primary Add action, and a quiet secondary URL line; no toolbar, filter row or count in a true empty state; the Radio toolbar previously sat outside the stack and stayed visible with zero stations, which is fixed here; the design's OPML secondary line was deliberately not adopted because no OPML import path exists in this repo and the empty state must not promise one; format, strict clippy, full workspace tests, UX traceability, architecture, accessibility, input parity, motion and the rule-named Xvfb display gate passed)
Task T6-D1 (Turn 6b, download legibility): complete (commit 62e14dd0 merged as a no-ff merge, base 50231d3d, added rule POD-11 with a per-row download column carrying state plus real file size and a channel header summary of window size, downloaded count and combined bytes on disk; totals are computed over the whole channel rather than the visible window so the figure reads as real disk usage, sizes are never invented for undownloaded episodes, and the summary is a pure tested projection; format, strict clippy, full workspace tests, UX traceability with 250 active rules, architecture, accessibility, input parity, motion passed, and the rule-named display gate reported 180 of 182 with only the pre-existing play_7b failure and one test already fixed on the target branch)
Incident T6-verification: an earlier claim in this stage that the rule-named display gate was green was wrong — the gate reports "failed: N of M" rather than "FAILED", so grepping for the latter reads as green while sixteen tests were failing on a mid-refactor tree. Read the block after "== display test summary ==" instead. Two apparent regressions (stats_17, tag_6) turned out to be environment flakes under parallel build load, failing with "Failed to initialize GTK" and passing in isolation on both HEAD and base.
Task T6-HA (Turn 6c/Block H, MCP discovery): complete (commit 9308c9e8 merged as a no-ff merge, base 50231d3d, added the capability-gated music_search_sources tool exposing provider-separated discovery over MCP under the existing sources:manage gate, honouring SRC-6 by querying exactly one provider and SRC-9 by omitting an absent subscriber count entirely; the already-subscribed filtering was moved out of the GNOME crate into reprise-core as podcasts::discovery and radio::search::filter_new_stations so GUI and MCP share one projection instead of drifting, with the existing rule-named tests moved alongside it; no new UX rule was needed because SRC-6 and SRC-9 are already written surface-neutral; format, strict clippy, the full workspace suite including six new MCP integration tests, UX traceability with 250 active rules, architecture and core purity passed after the merge)
Decision T6-HA-url: the MCP discovery response returns each candidate URL with userinfo and fragment stripped and non-http(s) schemes rejected, but deliberately keeps the query string, because SRC-5's own test proves a feed's query string can be part of its identity and stripping it would point a follow-up add at the wrong feed; artwork URLs are omitted entirely. This is a documented deviation from the blanket "no query strings" wording in docs/plans/podcasts-youtube-radio-turn6.md; the owner confirmed it on 2026-07-28, so the plan's wording is the part that is wrong and was corrected.
Task T6-G1 (Turn 7b, Online sources page and global gate): complete (commit d5467ee6 plus follow-up 4803761e, base 12afbde4, added the Online sources preferences page with a global master and independent YouTube, Podcasts and Radio blocks, promoted YouTube to its own peer ModuleDescriptor with id "youtube" and retired podcasts.youtube_enabled outright since there are no installations to migrate, and introduced reprise_core::online_sources as the single authority whose network_allowed ANDs the global switch with each module; NET-1 was replaced by NET-1a and SET-8 was added; the design's Phone sync block was deliberately left out because its rows configure Block E sync work that does not exist yet)
Incident T6-G1-gap: the first G1 commit gated only the YouTube search, so with the switch off podcast search still reached Apple and radio search still reached radio-browser while the page promised "no requests, no downloads, nothing hidden". Both dialogs now decide at one submit choke point before any provider work, state the refusal rather than failing silently, and treat a failed lookup as not allowed; the podcast dialog's SRC-6 and NET-1a guards were merged into one pure, display-free testable function. The lesson is that a privacy claim in copy needs a test per call path, not per feature.
Harness repair T6: the rule-named display gate now retries a test once, on a different Xvfb server number, when and only when it failed with GTK's "Failed to initialize GTK" — an environment fault that produced three false regressions in one day and that always passed on an isolated re-run. Any other failure is still reported unchanged, and a retry logs a visible line. The retry did not trigger in its verifying run, so it is guarded but not yet proven in practice.
Fix T6-play7b: play_7b_podcast_episode_table_scrolls_inside_the_player_bar_boundary had been the gate's one standing failure since before this stage. It compared ScrolledWindow::child() with the table directly, but GtkScrolledWindow hosts a non-scrollable child inside a GtkViewport of its own, so the assertion compared a viewport with a box and could never hold. It now asserts that the table is what the scroller hosts.
Task T6-F1/F3 (6e, offline contract): complete (commit cfc3e331, base eade62d8, wrote NET-3 as the app-wide offline contract covering cached, empty, queued, interrupted, authentication, rate limit and provider failure, kept [geplant] because only part of it is implemented; NET-3a adds a pure display-free projection in reprise_core::connectivity that answers from connectivity plus local availability what a row reads and what an action does, with phone sync falling out of it because MTP transfer is local; NET-3b implements the Radio exception, where a live stream cannot be deferred and Play offers "No connection · Retry" instead of queueing; connectivity is an explicit injectable value rather than inferred from a failed request; LYR-3 was deliberately left out of the migration because it describes a disabled module rather than an offline state; format, strict clippy, full workspace suite, core purity, UX traceability with 253 active rules, architecture, accessibility, input parity and motion passed)
Note T6-F-scope: F2's queue execution and automatic run-on-reconnect, F4's dialog offline mode, and 7f's sync skip notice are modelled but not wired, and remain open. Closes the specification half of issue #107.
Task T6-E1 (7a/7e/7f, sync target and wanted state model): complete (commit 95c65bc4, base dc2597c2, replaced the single managed device folder with three named per-device targets carrying StorageID, path, activation and an optional cap in new table device_sync_targets, added oldest-first cap eviction as a pure function, and introduced wanted_on_device so that marking an episode for the phone before it has a file defers the download instead of demanding the user download first; the global-rules versus per-device-folders split from 7b/7e is expressed structurally — SyncTarget deliberately carries no content-selection field, and its doc comment says why — and the Android media-scanner reasoning behind /Podcasts/Reprise and /Music/Reprise-YouTube is recorded so the defaults are not tidied away later; wanted_on_device composes with connectivity::deferrable_action_outcome from NET-3a rather than duplicating it; new rules MTP-18, MTP-19 and MTP-20 after checking MTP-1 through MTP-17 for collisions; schema moved 41 to 43 with no migration path needed since there are no installations; format, strict clippy, 1536 core tests and the full workspace suite, core purity, cached audit with only accepted RUSTSEC-2024-0436, UX traceability with 256 active rules, architecture, accessibility, input parity, motion, and the display gate at 184 of 184 all passed)
Note T6-E1-followup: POD-8 still hardcodes "downloaded RSS episodes only" and the old single-folder paths. It is not yet contradictory because query_candidates_for_device is untouched and wanted_on_device is inert, but POD-8 and docs/plans/android-sync.md must be updated in E2/E4 when the new model actually replaces the old resolution.
Task T6-E2/E3 (7a/7c, selection and category diff): complete (commit 3ff96687, base 8c1671ce, added MTP-21 for a pure per-category intended-file-set projection and MTP-22 for a per-category plan reading with an aggregated balance; copy and remove stay separate in both files and bytes and has_work counts files rather than bytes, so a deletions-only sync reports work while moving zero bytes — the defect design 7c names, where "3 changes · 0 B" reads as nothing to do while three files are deleted; an item wanted without a file yet is counted apart and never folded into files_to_copy so the balance cannot promise a transfer that will not happen; a category distinguishes switched-off from evaluated-to-zero from could-not-be-evaluated rather than flattening all three into one zero; the existing playlist mirror plan is translated into the same shape instead of being diffed a second time; format, strict clippy, full workspace suite, core purity, UX traceability with 258 active rules, architecture, accessibility, input parity and motion passed)
Incident T6-fil9: fil_9_filter_changes_center_the_visible_playing_track fails with a stable off-by-one-row result (actual 680, expected 614.5, a gap of about one row height). It is NOT a regression from this stage: it fails identically at dc2597c2, the last commit where the display gate reported 184/184, and no commit on this branch touched crates/reprise-gnome/src/ui/track_list/ or scroll_center.rs. Manual runs replicating the gate's full environment still fail, so the difference is not the private XDG roots. Filed as issue #122 rather than chased inside Block E. Note the contrast with the GTK-init flakes: those print "Failed to initialize GTK" and are retried by the gate; fil_9 produces a real assertion and must not be treated as environmental.
Task T6-E4 (7a, transfer): complete (commit 63ba5048, base 4d45a21a, routed the transfer through the three named per-device targets instead of one managed root, switched YouTube phone sync on by making every RSS-only assumption kind-aware rather than special-casing YouTube at one call site, kept music on the transfer profile while copying podcast and YouTube audio 1:1 because they are already Opus or AAC (MTP-24), made the size cap real with oldest-first eviction before an overflowing transfer starts (MTP-25), and added MTP-23 for the write and delete contract; POD-8 asserted both RSS-only selection and a single fixed folder, so it was replaced by POD-12 with its test re-pointed rather than left contradicting the new rules; docs/plans/android-sync.md was updated; all tests drive the recording DeviceBackend double so the gate never needs a phone; format, strict clippy, full workspace suite, core purity, UX traceability with 261 active rules, architecture, accessibility, input parity, motion and the device-sync GStreamer check passed)
Incident T6-commit-sweep: while an agent was working uncommitted in the same worktree, a `git add -A` from the coordinating session swept its entire in-progress E4 change into a commit labelled as a docs-only change. Caught by inspecting the commit's stat, then split apart with a soft reset so the docs commit carries only the doc file and the agent's work was re-verified and committed on its own. The lesson: never `git add -A` in a worktree where a subagent is working; either give the agent its own worktree or stage explicit paths.
Task T6-E5/E7 (7a/7c, device view and sidebar card): complete (commit 9562eda2, base ea22d9ce, gave the device page a content panel with a category-segmented storage bar carrying a hatched "Incoming this sync" segment, a "Device contents never verified" state with a Scan device action reusing the existing inspect call rather than a second scan mechanism, a Content section per named target and a Next synchronization panel reading MTP-22 per category; the sidebar card's blended "N changes · X to transfer" line is gone, replaced by exactly one of four leading sentences projected from the balance, with the full figures moved to the tooltip; the deletions-only case now reads "3 to remove · frees 148.0 MiB" and its test additionally asserts the string never contains "0 B", so the defect design 7c names cannot silently return; the 2026-07-28 addendum is honoured structurally — selection summary and cap are read-only displays of the global Preferences rules and the section's only switch is SyncTarget::enabled, which MTP-28 states as binding text; MTP-11 described an idle card that no longer exists and is marked [ersetzt durch MTP-29]; new rules MTP-26 through MTP-29 after checking MTP-1 through MTP-25 for collisions; verified independently of the agent's report: format, strict clippy, 1566 core and 1095 GNOME tests with none failing, core purity, and all five script gates passed)
Task T6-E-autostart (7a, MTP-30): complete (commit f2567a87, base 9562eda2, the "Sync automatically when this phone connects" switch shipped in 9562eda2 was a dead control — persisted as schema v44 with DEFAULT 1 and rendered into the UI, but no code path ever read it to decide anything, because design 7a and the plan both specify the control under E5 while no block owned the behaviour and F2 covers network reconnect rather than device connect; the owner chose to implement it with the default left on; the decision is the pure should_auto_start over AutoStartFacts in reprise-core, requiring the first refresh after connect, the switch on, a verified scan, error-free planning, an idle connected device and real work per the existing SyncBalance, with the balance reused through a shared category_readings() rather than derived a second time; the GTK side gathers facts in one short borrow, drops it, and only then calls sync_now, mirroring the existing resume_planned hook because sync_now re-enters GTK; a refused or failed automatic start stays silent apart from a tracing warning since the user pressed nothing; verified that the trigger cannot repeat — apply_devices only enqueues a refresh when a device actually connects or reconnects, so a repeated poll of an already-connected device enqueues nothing; format, strict clippy, 2997 workspace tests with none failing, core purity, five script gates, cached audit with only accepted RUSTSEC-2024-0436, and the rule-named display gate at "failed: 0 of 184" all passed)
Note T6-E-autostart-default: turning the switch on by default is a real behavioural change, not a cosmetic default — it broke 33 existing tests that drive sync_now by hand and had begun racing an automatic start. Those fixtures now set sync_automatically: false explicitly with a rule reference. In production the same default means a connected device with pending work begins transferring unprompted; this is the owner's explicit decision, recorded here because it is the kind of default that looks like an accident when read later.
Correction T6-fil9: the incident entry above concluded that fil_9 is not environmental because its failure numbers are constant. That conclusion was wrong, and the reasoning was the trap — a row height is a discrete layout state, so an unsettled layout is off by exactly one row every time, which makes constant numbers evidence of nothing. A controlled re-test (idle twice, then twelve CPU busy-loops applied and released under a shell trap) passed both idle runs and failed one of two loaded runs with the identical 680 versus 614.5. fil_9 is a flaky test sensitive to CPU contention. The "not a regression, fails at dc2597c2 too" half of the original finding stands unchanged. Issue #122 was corrected and retitled rather than left carrying the wrong diagnosis.
Incident T6-cpu-leak: this machine was carrying 48 orphaned CPU busy-loops leaked by an unrelated fil_9 load experiment in another worktree — four batches of twelve, running 3 h 43 min at a load average of about 52 on eight cores, which is very likely why the display gate's GTK-init flakes appeared in the first place and certainly why every fil_9 failure in this stage clustered in that window. The experiment applies hogs and kills them at the end, but its parent shell was reaped first, so the kill never ran and the spinners were reparented to systemd. Removed by verifying each PID individually (command line, orphaned parent, no children) rather than by a broad pattern kill, after an earlier broad pkill in this stage had hit builds belonging to the user. Any future load experiment must arm a shell trap so the hogs die with the script.
Task T6-E6 (7d, device folder browser): complete (commit 0a26675d, base 3b089e56, gave the three named per-device sync targets an editing surface — "Change folder…" in the Content section (MTP-28) opens a browser with storage selection, folder tree navigation, "New folder", a target preview, a warning when the chosen folder sits inside the authoritative Playlists tree (MTP-17), and "Reset to default"; every decision (storage classification, target preview, playlist-conflict warning, reset, and the MTP-32 relocate-vs-recopy choice) is a pure function in reprise_core::device_sync::browser, reusing MTP-18's target_storage_transition rather than re-deriving it; GTK only gathers facts through four new DeviceBackend methods (list_storages/list_folders/create_folder/move_folder, each with an "unavailable" default) and obeys; reprise-platform-linux resolves storage and folder contents fresh from gio::File enumeration every call (no MTP handle is ever persisted) and derives a stable StorageId from each storage's GVfs-reported name since GVfs exposes no raw PTP StorageID; a device that refuses folder creation at a storage's own root surfaces DeviceIoError::CannotCreateAtStorageRoot distinctly; new rules MTP-31 and MTP-32 after checking MTP-1 through MTP-30 for collisions, and MTP-28's closing sentence (which deferred this browser to E6) was updated in place; the FakeBackend test double was extended for browsing/folder creation and split into its own device_sync_fake_backend.rs to keep device_sync_runtime_tests.rs under the file-size gate, so no test needs a real phone; format, strict clippy, the full workspace suite (1596 core, 1108 GNOME non-display plus 327 display-only ignores, 104 Linux platform, 49 stems, full MCP suite), core purity, UX traceability at 268 active rules, architecture, accessibility semantics and input parity all passed). The rule-named Xvfb display gate was intentionally not run by this task, per the coordinating session's instruction that it runs it separately.
Task T6-C1 (Block C, remote image module): complete (commit cdd73a53 plus fixes 1ec22016, 077b32e0 and 5-… see below, base 3b089e56, added reprise_core::remote_image with a pure resolve over an injected fetcher, a bounded on-disk cache and the source_images module, and unblocked the image parts of A2 and B1 which had been shipping on the glyph fallback; the commit also closed a live privacy hole rather than only adding a feature — before it, podcasts_groups, youtube_channel_detail and the radio search passed real remote URLs straight into SourceImage, whose set_url called source_artwork::fetch with no gate check at all, so switched-off online sources still fetched artwork; new rule SRC-11 with a test per call path)
Fix T6-C1-cachehit: SRC-11 states that a cache hit is always shown regardless of the gate and remote_image::resolve implements exactly that, reading the on-disk cache before it consults the flag — but the widget returned before ever reaching it, so a closed gate hid images already on disk that needed no request. The divergence was safe yet the rule is binding, and the cost was invisible: the memory cache is empty at startup, so every restart with sources off dropped artwork it had already downloaded. The five existing src_11 widget tests could not catch it because with an empty cache the fallback looks identical either way; the new test populates the cache through the public core path first and fails without the fix (commit 1ec22016).
Task T6-E6 (7d, device target-folder browser): complete (commits 0a26675d and 33e43649, base 1ec22016, storage selection, a folder tree, New folder, target preview, the playlist-nesting warning and Reset to default, with every decision a pure projection in device_sync::browser and the GTK side only rendering; GVfs exposes no raw PTP StorageID, so derive_storage_id hashes the reported storage name — stable across reconnects for one phone but not bit-identical to the wire value, the one deliberate compromise and documented as such; new rules MTP-31 and MTP-32, MTP-28's "path is read-only text" sentence resolved)
Review T6-codex: an external reviewer (Codex CLI, read-only sandbox, no writes and no builds) audited ea22d9ce..HEAD — 52 files, ~5500 added lines, scoped deliberately to this window rather than the whole 43-commit branch. It returned six P1 defects and three contract gaps; every one was verified against the code before being acted on, and all six P1s are now fixed with a red-then-green proof per fix. What the gates could not see is the point: all of it passed fmt, clippy, 3033 workspace tests, five script gates and a 191-of-191 display gate.
Fix T6-review-storage: E6's browser was largely cosmetic. PlannedWork carried only paths and never the storage_id, every DeviceBackend write went through storage_root() with its hard "prefer internal" rule, and inspection walked hardcoded Music/Reprise, Music/Reprise-YouTube and Podcasts/Reprise. Choosing an SD card and /Music/YT therefore transferred to internal storage anyway, and the next scan did not recognise the chosen folder as that category's inventory, so it reported the category empty and planned the same files again. Storage now threads through the whole transfer chain and inspection resolves each target's own storage and path (commit 667b567d, rules MTP-33 through MTP-35 for this and the two below).
Fix T6-review-deadswitch: remove_deleted was the THIRD persisted-but-never-read control found on this branch, after sync_automatically (MTP-30) and the ungated image fetch. target_podcast_plan passed a hardcoded true, so turning the switch off and unsubscribing still queued the device file for deletion. The pattern is the lesson: a switch that renders and persists reads as finished, and nothing in the gate battery disagrees. Any new setting needs a test that asserts the behaviour differs between its two positions, not that the row exists.
Fix T6-review-race: the folder browser started an uncancelled listing per navigation and appended results unconditionally, so a slow /Music listing could land under /Podcasts and a row click then navigated to a path that does not exist. Now behind a generation token, the same shape cover_loader.rs and source_image.rs already use for recycled rows.
Fix T6-review-savefail: a refused Save logged a warning and closed the folder browser anyway, so a save rejected because the device was busy or gone looked successful and the choice was silently lost. It now reports inline through the same error surface MTP-31 already uses for a refused New folder and keeps the dialog open.
Fix T6-review-gatestale: the artwork workers captured the gate value at enqueue time, so tasks queued while online sources were on kept fetching after the switch went off (commit 077b32e0 moved the decision to a process-wide atomic read immediately before the fetch). That fix was itself incomplete: the atomic was published only from queue_artwork, so it flipped only when somebody rendered another uncached image — and Preferences, the one page where the switch is thrown, shows no source artwork at all, so a draining queue would never learn. It is now republished from set_online_sources_enabled beside the existing SET-4 recomputations and after any successful module toggle.
Incident T6-two-agents-one-worktree: running two subagents concurrently in the same worktree, each instructed to run the full workspace gate, was a coordination error. Their uncommitted work compiled together, so neither could get a meaningful result; one deadlocked waiting on its own backgrounded run. Worse, the two failure sets masked each other — a clippy error introduced in 077b32e0 (needless_pass_by_value) went unnoticed because that run died on the other agent's unrelated build errors, and HEAD briefly failed clippy. Either give each agent its own worktree, or let only one run the workspace gate and have the coordinator run the authoritative battery once both have committed.
Note T6-review-open: three reviewer findings are real but are feature work rather than defects, and are NOT fixed. (1) MTP-21 is [aktiv] and requires "jede ungespielte, bereits heruntergeladene Episode" plus counting a wanted-but-undownloaded episode as waiting; the pure projection honours both and is tested, but the live pipeline calls neither — query_candidates_for_device applies no played filter and files_waiting_for_download is hardcoded 0. The rule-named test asserts only the label, so it stays green even if the selection behaviour were removed entirely. With MTP-30 now live this also means a device can read "Up to date" and skip its automatic sync while work is pending. (2) The device view says "Rules from Preferences" but 7b's Phone sync block does not exist, so the referenced controls cannot be reached; SET-8 requires it once Block E exists, and Block E now does. (3) MCP parity does not cover targets, caps, per-category diffs or wanted_on_device — device_dto still exposes only the legacy aggregate, so an agent can start a sync but cannot see what it would do.
Task T6-HDEFG (Block H, MCP parity for D/E/F/G — closes T6-review-open finding 3): complete (this commit, base 87f9e73a, four new MCP tools plus extensions to two existing surfaces, every one reusing the same reprise-core facade the GUI reads/writes rather than a second path). H-D: moved the YouTube channel detail's window/Shorts-filter/download-summary math out of reprise-gnome into podcasts::channel_window (POD-10/POD-11, now [core][gtk]), added the read-only music_get_channel_detail tool on top of it, extracted podcasts::pipeline::download_episode from refresh's inline auto-download loop so a new music_manage_episodes tool's download/remove/want_on_device batch actions and the refresh pipeline share one download path instead of two. H-E: agent_device_sync::AgentDeviceSyncDevice gained a categories field reusing device_sync::SyncTargetKind/CategoryReading directly (no parallel DTO), threaded through a new CategorySnapshot D-Bus method (kept separate from the already-16-wide Snapshot tuple deliberately) into music_get_device_sync_state, which now reports each named target's folder/cap/diff reading plus an aggregate balance computed via the real aggregate_balance function; wanted_on_device (MTP-20) is exposed as music_manage_episodes' want_on_device action, DB-only, no live device needed. H-F: source_data's PodcastEpisodeDto gained download_state and local_availability (NET-3a's real local_availability() bridge, filesystem-checked) so an agent can tell before acting whether an item is already local — documented honestly as the one connectivity signal Reprise can back today, since NET-3c's real OS network-monitor binding does not exist yet. H-G: new music_manage_online_sources tool reads/writes the exact NET-1a/SET-8 authorities (online_sources::is_enabled/set_enabled, modules::is_enabled/set_enabled). No new ux-rules.md rule was needed — MTP-18/20/22, NET-1a, NET-3a and POD-10/11 were already surface-neutral; only POD-11 gained a [core] tag plus a documentation note now that its projection also lives in core. Found and fixed one response-hygiene gap before it shipped: a failed download's raw provider-error message (which can echo the request URL, and an episode audio_url can carry a private token exactly like a feed URL can, SRC-5) was going straight into the tool response; now sanitized to a fixed string, with a dedicated leak test. format, strict clippy (both without and with --features reprise-mcp/mpris), the full unfiltered workspace suite in both configurations (reprise-core 1607, reprise-gnome 1115 non-display + 328 display-only ignores, reprise-mcp 34 unit + full integration suite, reprise-platform-linux 106, reprise-stems 49, reprise-cli 11+2+4+5+8+7+4), core purity, UX traceability at 271 active rules, architecture, accessibility, input parity, and audit with only accepted RUSTSEC-2024-0436 all passed.
Task T6-E8 (7a/7e, sync rules onto the device page): complete (commit daa8246e, merged as 5b38459d, base 8e86a485, follows owner decisions E-5 and E-6 of 2026-07-29 — exactly one MTP device, so the global-versus-per-device split loses the justification it was built on and the rules move to where the device is; the cap became an editable SpinButton wired to SyncTarget::cap_bytes with a re-plan, the selection summary became a live per-device count from the new podcasts::phone_sync::selection_summary instead of the static "Rules from Preferences" stub, and the "Same on all devices" cross-reference is gone along with the never-built Phone sync block; MTP-28 is [ersetzt durch MTP-37] and SET-8 by SET-9, with every rule-named test re-pointed in the same commit and no orphan mtp_28_/set_8_ names left, verified independently; the cap test proves behaviour rather than existence — lowering the cap evicts the older episode from the next plan, red at "left: 2 right: 1" without the fix; the data model was deliberately NOT refactored, because storing per device stays right even when managing several devices does not)
Note T6-zero-meaning: two adjacent numeric settings now give 0 opposite meanings — a cap of 0 GiB means unlimited (cap_bytes has been Option since MTP-18), while MTP-36's decision made a channel's "latest N" of 0 mean "nothing from this channel", on the argument that unlimited must not be selectable or one number can quietly fill the device. That argument applies to the cap just as well, so MTP-36 is the outlier rather than E8's work. MTP-36 is still [geplant] and unimplemented, so aligning it costs one line of rule text. Raised with the owner, not decided locally.
Incident T6-tmpfs: /tmp (a 16G tmpfs, i.e. RAM) filled to 100% mid-session and failed unrelated commands with ENOSPC — 8450 stale tmp.* directories holding 15G. First diagnosis was wrong: AGENTS.md's isolation recipe looked like the culprit, but check-display-tests.sh does clean up its worker roots. The real defect is narrower: the worker drops the parent's EXIT trap so it never deletes a sibling's log, and was then left with no cleanup at all, so the tidy-up ran only on the normal path. Every interrupted or timed-out gate run therefore abandoned five directories per test, about 955 per run — and this session aborted several. Fixed in d6a5b299 by giving each worker a trap for its own five directories on INT/TERM/HUP as well as EXIT, since bash runs no EXIT trap for an untrapped fatal signal and the aborted run is precisely the case that leaked. The improvement could not be measured cleanly while another agent ran the unfixed script concurrently; it is correct by construction and will show on the next aborted run.
Lesson T6-selfkill: `pgrep -f check-display-tests` matches the agent's own shell, whose command line contains that string. It killed this session's own command once (exit 144) and later reported a gate as running when none was. Match on `comm` plus an explicit self-PID exclusion, or accept both a self-kill and a false positive.
Note T6-mtp-renumber: our MTP-18, MTP-19, MTP-20 and MTP-21 are now MTP-38, MTP-39, MTP-40 and MTP-41. Both this branch and origin/dev appended four rules to a common base of MTP-17 without knowing about each other, so the same four IDs carry two different meanings: dev 's are step naming, failure fallout, the sync run log and proven transfer, ours are the three named sync targets, cap eviction, wanted_on_device and the intended-file-set projection. dev is the pushed trunk and its IDs already appear in merged test names, so this branch moves. Our MTP-22 through MTP-37 keep their numbers because dev never used them, which leaves the block non-contiguous — MTP-17, then dev's 18-21, then ours 22-41 — and that is the honest shape rather than a tidy one. Every rule-named test and every code and plan cross-reference moved in this same commit; the entries above this line deliberately were NOT rewritten, because the ledger records what was true when it was written and a renumbered history would misrepresent it. Read any MTP-18 through MTP-21 above this line as MTP-38 through MTP-41. The renumber had to precede the dev merge: dev's own sync-log files reference MTP-20 meaning dev's rule, so a rewrite afterwards could not have told the two apart.
Merge T6-dev: origin/dev merged into feature/podcast-channel-redesign (this merge commit, 53 commits incoming, 18 conflicting files). Two structural collisions dominated it and neither was a text conflict. First, dev's 7d8a47c1 had translated docs/ux-rules.md into English including its status vocabulary ([aktiv]->[active], [geplant]->[planned], [ersetzt durch X]->[replaced by X]) and moved both check-ux-traceability.sh and check-display-tests.sh onto the English tokens; our 40 new rules were German, so a naive merge would have left them unparsed and the traceability gate would have reported ok while looking at none of them, which is the exact failure dev's own commit message warns about. The rulebook was therefore rebuilt from dev's English version and our 40 additions plus 12 edits to pre-existing rules were re-applied translated, giving 405 unique ids with no duplicates and no German marker left. Second, both lines had appended MTP-18 through MTP-21 to a common base of MTP-17 for different rules; ours were renumbered to MTP-38..41 in the separate preceding commit bd4c1ba3, deliberately before the merge, because dev's sync-log files reference MTP-20 meaning dev's rule and a rewrite afterwards could not have told the two apart. Schema: both sides added a v40 migration, so dev's sync log moved to v45 and SUPPORTED_SCHEMA_VERSION is 45. Engine: dev refactored device_sync_planned.rs onto a DeviceSyncMachine reducer, so our per-target path and storage routing was re-applied on top of it and the additive podcast/YouTube content targets now run as a phase after the machine reports Finished rather than interleaved, because the mirror is authoritative over its folder (MTP-17) and the content targets are not - letting the authoritative step settle first means a content copy cannot be deleted by a mirror cleanup in the same run; our device_sync_music_transfer.rs was deleted since the machine owns that work now. MCP: dev replaced the positional D-Bus tuples with named dict types, which removes the zvariant tuple-arity ceiling that was the stated reason our Block H CategorySnapshot was a separate D-Bus method, so categories moved into DeviceSnapshot as a field and the extra method is gone; PROTOCOL_VERSION is 1.4. Four files were split to stay under the 800-line gate (device_sync_browser_tests.rs, device_sync_inflight_tests.rs, device_sync_run_log.rs, and run_content_phase into device_sync_content_transfer.rs). fmt, strict clippy, the full workspace suite with none failing, core purity, UX traceability at 264 active rules, architecture, accessibility semantics, input parity, motion, and audit with only accepted RUSTSEC-2024-0436 all pass. NOT pushed - the owner tests manually first.
Decision T6-zero: the owner decided on 2026-07-29 that 0 means unlimited in every numeric sync setting. The two adjacent numbers on the device page read 0 in opposite directions - the size cap has meant unlimited since MTP-38 modelled cap_bytes as an Option, while MTP-36's "latest N" was drafted with 0 meaning nothing from this channel. MTP-36 was the outlier and still unimplemented, so alignment cost one line of rule text instead of a rebuild. "Nothing from this channel" is what the 6b channel toggle says; it is not a quantity, so it is not expressed as one. Recorded in MTP-36 and in the plan's section 8a as E-9.
Lesson T6-schema-guard: the runtime protocol's no_snapshot_carries_a_local_filesystem_path test bans any snapshot string starting with "/", and the new target_path field legitimately carries a device path like /Podcasts/Reprise, so the field had to be exempted. The first exemption was written with a positive counter-check that looked right and passed - and would have passed forever, because zvariant serializes each field as {"signature","value"} and the checker only collected plain strings, so it inspected nothing. It was caught by deliberately injecting /home/marvin/Music into target_path and observing the test stay green. The rule this branch already wrote for MTP-21 applies to guards as much as to features: after writing one, break the thing it guards and watch it go red.
Fix T6-split-cohesion: the dev merge pushed device_sync_planned.rs over the 800-line file-size gate, and the script that split out the MTP-20 run log took the whole `impl DeviceSyncRuntime` block with it rather than just reload_sync_history — because it looked for the next line that is exactly "}" at column 0, which is the end of the impl, not the end of the method. sync_now, the sync entry point, therefore came to live in a file whose module documentation says it is about recording runs. It compiled, clippy was clean and every test passed, so no gate disagreed; only reading the file for an unrelated reason surfaced it. sync_now and eject are back in device_sync_planned.rs (792 lines) and device_sync_run_log.rs keeps only the log plus its reader (127 lines). The lesson is narrow and mechanical: a brace-matching split must anchor on the item it is moving, not on the first dedented brace after it.
Incident T6-gate-contention: the post-merge display gate reported "failed: 4 of 196" — three stats_17 entrance tests and stats_10_narrow_window. All four pass in isolation under the gate's own environment recipe. Neither this branch nor its side of the merge ever touched crates/reprise-gnome/src/ui/stats/; dev touched it, and did so specifically to fix display-test flakes (ce633e08, af597def). A second session's display gate was running against a different worktree on the same machine throughout the run, which is the CPU-contention condition the fil_9 correction and the cpu-leak incident already identified for this class of timing- and layout-sensitive test. Treated as contention rather than regression only after the isolation runs, and to be confirmed by a repeat gate on a quiet machine rather than by the reasoning alone.
Lesson T6-selfwatch: a background watcher written as `until ! pgrep -f "check-display-tests.sh --rule-named"; do sleep 30; done` never terminates, because the watcher's own command line contains that string and pgrep matches it. This is the same self-match trap recorded as T6-selfkill, walked into again from the other direction: the first time it killed a shell, this time it produced a watcher that would have waited forever while the gate it watched had already finished. Wait on a specific pid with `kill -0` instead, or exclude the caller's own pid explicitly.
Lesson T6-gate-tree: editing tracked sources while a display gate runs invalidates that gate. Each worker re-invokes `cargo test -p reprise-gnome`, so a mid-run edit means some tests ran against the old tree and some against the new one, and cargo's build lock serialises the workers during the rebuild. The rule that two agents must not share a worktree applies to an agent and a running gate just as much.
Confirmation T6-gate-contention: the repeat display gate on a quiet machine reports "passed: 196, failed: 0 of 196". The four stats failures of the earlier run were CPU contention from a second session's gate running concurrently against another worktree, not a merge regression — now established by a clean repeat rather than by the reasoning alone. Note the count moved from 191 pre-merge to 196: dev brought five further display tests with it.
Task T6-E9-prep (design 7f's preparation phase, MTP-42): complete (this commit, base c111ff72, new dependency-pure `reprise_core::device_sync::preparation` decides, from `MTP-41`'s waiting set, `NET-1a`'s global gate, `NET-3a`'s connectivity and the device's own prepare-before-sync switch, whether a preparation phase exists at all and what it does — `Absent` when online sources are off (wins over every other condition, tested stacked against missing files, offline and switch-off together), `NothingMissing`, `SkippedOffline` while offline (also tested against metered-and-switch-off stacked on top, since offline must win over policy, not just appear first in the code), `Offered` when metered or the switch is off, otherwise `Planned`; `primary_action` projects only `Planned` to "Download & sync" and leaves every other phase, including `Offered`, as a plain "Sync now" — an enum, not a display string, wording stays in the frontend. Added MTP-42 to docs/ux-rules.md [active][core] with its precedence and the offline-beats-metered reasoning as a doc comment. format, strict clippy, the full unfiltered workspace suite (reprise-core 1690 passed incl. 11 new mtp_42_ tests, reprise-gnome 1109 passed + 334 display-only ignores, reprise-platform-linux 117, reprise-stems 49, reprise-cli 11+2+4+5+8+7+4+13+4+2+7+0+0, reprise-mcp 34 unit + full integration suite, reprise-runtime 72 + 4 protocol + 8 schema), core purity (cargo tree -p reprise-core has no gtk4/libadwaita/gstreamer/zbus), UX traceability at 265 active rules, architecture, accessibility semantics, input parity, and audit with only accepted RUSTSEC-2024-0436 all passed. Housekeeping: this worktree's own git branch had been provisioned from a stale, unrelated base (a `dev` merge missing the entire E7/E8/connectivity lineage this task needed); reset in place to feature/podcast-channel-redesign's tip (c111ff72) before starting, losing nothing unique since the sole prior commit was a redundant PR merge already in origin/dev.
Task T6-E9-A/B (7f, preparation projection and download priority): complete (commits d4bf6c04 and 60fb26f4, merged as 2db8ce31, base c111ff72, built by two subagents in separate worktrees and verified here by mutation rather than by their reports). MTP-42 is the pure projection reprise_core::device_sync::preparation: five states (Absent, NothingMissing, SkippedOffline, Offered, Planned) resolved in a fixed order where NET-1a's global gate beats everything, then nothing-missing, then offline, then metered-or-switch-off, then planned. Offline deliberately beats metered because offline is a fact about whether the download can run while metered is policy about whether it should, and offering a download that cannot run either way would be a lie dressed as a choice. MTP-44 gives the existing download manager a priority lane rather than a second path: PodcastsRequest carries a priority, the one worker drains the priority channel first via futures_lite::future::or, and PodcastsOperation::Download and pipeline remain the single operation and single executor; a DownloadForSync variant was considered and rejected as a second path in spirit. Verified by mutation: removing the NET-1a gate turned two mtp_42_ tests red, removing the offline branch turned four red, and reverting the priority lane to FIFO turned two mtp_44_ tests red. fmt, strict clippy, the full workspace suite with none failing, core purity, UX traceability at 266 active rules, architecture, accessibility semantics, input parity, motion and audit with only accepted RUSTSEC-2024-0436 all pass.
Fix T6-E9-hang: the first version of the MTP-44 tests called the blocking recv_prefer_priority directly. That proved the behaviour but failed in the wrong way — reverting the priority lane to FIFO drains the ordinary lane and then waits forever on an empty one, so the mutation run sat for thirteen minutes instead of reporting anything. A gate that hangs reports nothing, which for a rule whose whole purpose is to keep a second download path from drifting in unnoticed is the one failure mode that must not happen. The receive now runs behind a five-second deadline on a detached thread; detached rather than std::thread::scope, because scope joins before returning and would hang on exactly the stuck receive the deadline exists to catch. Confirmed in both directions: unmutated 3 passed in 0.00s, mutated 2 failed in 5.00s naming the cause (commit 17ae47a1). The general lesson is that "would this test go red if I deleted the feature" is not sufficient on its own — it also has to go red rather than silent.
Note T6-two-download-paths: E9's binding constraint is that preparation reuses the existing download manager with no second path. It is satisfied to the letter — no new path was added — but not in spirit, because a second path already existed before E9. reprise_core::podcasts::pipeline::download_episode is used by the refresh auto-download loop and by MCP's music_manage_episodes, while reprise-gnome's own try_download_episode_to_root serves the GUI's manual download with its own episode lookup, its own NET-1a source-kind gate and its own subscription lookup. Block H extracted the shared path for refresh and MCP but left the GTK worker on its copy. E9's preparation now sits on the GTK copy. Converging the two is a refactor with real risk (the GUI path's per-source-kind gating and progress emission must survive) and is deliberately NOT folded into E9; raised with the owner as its own decision rather than decided locally.
Lesson T6-selfkill-again: the pgrep self-match trap recorded as T6-selfkill was hit twice more in this session, from both directions — once as a watcher written `until ! pgrep -f "check-display-tests.sh --rule-named"` that could never terminate because its own command line matched, and once as `pkill -f "verify_e9.sh"` that killed the calling shell with exit 144. Reading the earlier entry was not enough to prevent either. The durable form of the rule: never pass a pattern to pgrep/pkill that appears in the invoking command line — wait on a captured pid with `kill -0`, and kill by pid. Also note that a pid captured by `pgrep | head -1` right after `setsid nohup` can be a short-lived child rather than the script, which made one watcher fire immediately against a gate that was still running.
Lesson T6-codex-stdin: a `codex exec` invocation appeared to run for one hour fifty-five minutes at zero percent CPU. It was not reviewing anything; it sat on "Reading additional input from stdin..." because stdin was left open. Non-interactive tool runs need `< /dev/null` (or an explicit piped prompt). Elapsed time alone is not evidence of work — check CPU and the output stream before assuming progress.
Review T6-codex-merge: an external reviewer (Codex CLI, read-only, no builds) audited 3559f0c8..70890178 — the dev merge plus the rule renumber — and returned eight findings. Six were real and are fixed in b441b400; two were already addressed by later commits the review could not see because it was pinned to the merge commit. Every finding was verified against the code before being acted on, and the two most serious were mine, introduced when the content phase was re-applied on top of dev's DeviceSyncMachine. (1) P1, the compile break: DeviceSnapshot gained a mandatory categories field but two reprise-mcp struct literals omit it, which `cargo check --workspace --all-targets` cannot see because the mpris feature is off by default — AGENTS.md's Block H entry calls for building both configurations and that had been dropped; the mpris build is now part of the battery. (2) P1, data loss: a failed content copy no longer stopped the phases after it, so a failure to copy a new episode could still be followed by a cap-eviction removal (MTP-39/MTP-25) deleting the older resident episode it was meant to replace — the device loses a file and gains nothing. The pre-machine implementation gated every step on failures.is_empty() and that guard was lost in the rewrite. (3) P1, cancellation swallowed: cancelling mid-phase only broke the inner loops and then returned the mirror's original Completed outcome, so finish_sync closed the run log as a success and cleared reconnect resumability. (4) P2, progress restarted at zero although the plan total already includes content bytes, so a mixed run could reach 100% and jump back. (5) P2, every Ok(_) from a removal counted as a deletion although the backend returns Ok(false) for an already-missing file, and content failures never reached the run log at all. (6) P3, a dangling NET-3c cross-reference and two helpers left public beyond their file. All three P1s had passed the entire gate battery: fmt, clippy, the full workspace suite, five script gates and a 196-of-196 display gate. This is the second external review on this branch to find defects the gates let through, and the pattern is worth stating plainly: gates catch what they were written to catch.
Task T6-E9-regression (MTP-23): complete (commit 0f02836d, merged as 1b509ff0, built by a subagent in its own worktree). Two regression tests for the P1 content-phase defects: a failed content copy must stop the later removal phases, and cancelling mid-phase must not be recorded as a completed run. The FakeBackend gained with_replace_track_error in the same style as the existing with_*_error builders so a content copy can be made to fail on demand, and the cancellation test uses a one-second bounded poll rather than an unbounded await, per the lesson in 17ae47a1. Verified independently after the merge by mutating this branch's own implementation: removing the may_continue guards fails exactly the removal test, removing the Cancelled return fails exactly the cancellation test, neither mutation disturbs the other. fmt, strict clippy, the full workspace suite, core purity, 266 active rules, four script gates, the mpris configuration and audit all pass.
Note T6-agent-briefing: the regression-test agent was told its two target defects were already fixed on its base branch. They were not — the fixes were sitting uncommitted in the coordinating session's working tree, so the agent's base legitimately still contained both bugs. It checked the claim against git history rather than trusting the brief, reported the contradiction, and implemented the fixes itself to keep the suite green. That is the right behaviour and cost only duplicated work, which was reconciled by keeping the coordinator's superset and taking the agent's tests. The lesson is for the coordinator: commit before briefing an agent about the state of the branch, because "it is on the branch" is a claim about git, not about a working tree.
Task E9 (design 7f's visible two-phase sync surface, MTP-43): complete (this commit, base 17ae47a1, builds on MTP-42's `preparation::plan_preparation`/`primary_action` and MTP-44's priority lane without re-deriving either decision). Core: `DeviceSettings` gained `prepare_before_sync` (migration v46, `SUPPORTED_SCHEMA_VERSION` bumped to 46, backfilled to `true` for existing rows) — the stored value is only ever what the user chose; offline/metered overrides stay `plan_preparation`'s job, never a mutation of this field. GTK: new `device_sync_preparation.rs` gathers facts in `recompute_delta_silent` (the missing-episode list, real `gio::NetworkMonitor` connectivity/metered, `online_sources::is_enabled`) and orchestrates the download-then-sync run through a new `PreparationDownloader` seam — production adapts `PodcastsRuntime` onto MTP-44's `High` priority lane (the one `PodcastsOperation::Download`, no second path), tests substitute a recording fake so the switch's actual dispatch behaviour is provable without a real worker thread or a channel a regression could hang on. `sync_now` now branches on `primary_action` before either starting a preparation run (`begin_prepared_sync`) or going straight to the renamed `start_transfer_now`; cancelling flips a flag the download loop checks between files, so an already-requested download is never interrupted or rolled back and `wanted_on_device` is never cleared anywhere in this path (verified by grep, not just by argument). The preparation overview (episode titles + "N files to download · SIZE", hidden outright for `Absent`/`NothingMissing`), the switch beside `remove_deleted`/`sync_automatically`, the primary button's "Download & sync" label, the "Step 1 of 2"/"Step 2 of 2" two-phase progress reading, and the `SkippedOffline` "N episodes skipped" note are wired into the page/layout/content-panel/strings modules. Known gap, flagged rather than papered over: no feed or provider in this codebase persists an expected byte size for an undownloaded episode (RSS enclosure `length` and YouTube size are both parsed nowhere), so `MissingFile::size_bytes` is honestly `0` and the overview omits the byte figure instead of showing "0 B" — the file count is still real. Four files were split to stay under the 800-line gate (`device_sync_page_copy.rs` and `device_sync_page_display_tests.rs` out of the page/its tests, `device_sync_effects.rs` out of `device_sync_planned.rs`), plus one line trimmed from `window.rs` to stay under its separate 600-line composition-root cap. Added MTP-43 `[active][gtk]` to docs/ux-rules.md between MTP-42 and MTP-44. format, strict clippy (-D warnings), the full unfiltered workspace suite (reprise-core 1692 passed incl. new `prepare_before_sync`/migration/`preparation_overview` tests, reprise-gnome 1121 passed incl. the two behavioral `mtp_43_` tests in `device_sync_preparation_tests.rs` + 334 display-only ignores, every other crate green), core purity, UX traceability at 267 active rules, architecture, accessibility semantics, input parity, and audit with only accepted RUSTSEC-2024-0436 all passed. Housekeeping: this worktree's own branch had also been provisioned from a stale, unrelated base; reset in place to feature/podcast-channel-redesign's tip (17ae47a1) before starting.
Task T6-E9-C (7f, the visible two-phase sync surface): complete (commit b0f33d6a, merged as fbafe7d4, built by a subagent in its own worktree, verified here rather than taken on report). MTP-43 gives the device page the preparation overview with episode titles, the "Download missing files before syncing" switch (DeviceSettings::prepare_before_sync, schema v46, stored default true), the primary button reading "Download & sync" exactly when MTP-42's projection says so, the two-phase "Step 1 of 2 · Downloading N of M · P%" reading, and the SkippedOffline note. Preparation dispatches through a PreparationDownloader seam that production adapts onto MTP-44's High priority lane, so there is still one download operation and one executor. Cancelling flips a flag checked between files and never touches an in-flight or completed download, and never clears wanted_on_device. Verified by mutation: making plan_preparation ignore prepare_switch_on fails exactly mtp_43_the_switch_actually_gates_whether_a_preparation_download_starts and nothing else — the switch is read, not merely stored, which is the third time this branch has had to prove that explicitly. fmt, strict clippy, the full workspace suite, core purity, 267 active rules, five script gates, the mpris configuration and audit all pass. E9 is now complete across MTP-42, MTP-43 and MTP-44.
Note T6-E9-C-open: two limitations the agent flagged rather than papered over, both real and both deliberately left. (1) Nothing in this codebase knows an episode's size before it is downloaded — no RSS enclosure length is parsed and no YouTube size is fetched — so MissingFile::size_bytes is honestly 0 and the overview reads "N files to download", appending a size only when a real one is known. Design 7f's example line says "2 files to download · 312 MiB"; a fabricated number would have matched the mockup and lied, so the number is omitted instead. Real size estimation needs RSS enclosure parsing plus a schema column and is its own task. (2) Connectivity and metered are read directly from gio::NetworkMonitor in device_sync_compact, following the existing podcast_refresh_scheduler pattern. That contradicts NET-3a, which states connectivity is an explicitly set value and never an inference, and which RadioView already honours through set_connectivity. The behavioural test carries a sanity assertion so a machine without network fails loudly instead of passing for the wrong reason, but the inconsistency stands and should be closed by making connectivity injectable here too.
Incident T6-parallel-field: merging E9-C exposed a break neither subagent could have seen. E9-C added a field to DeviceSettings; the regression-test agent, working in parallel from an earlier base, constructs that struct. Each was green on its own base and the pair does not compile. Worse, the first verification run after the merge printed no test result at all for the step that was supposed to prove the switch behaves — compilation had already failed — and a reader skimming for a FAILED line would have called it green. That is the same shape as the earlier grep-for-FAILED mistake: the absence of an error message is not evidence of success. The coordinator running the full battery after combining parallel work is what caught it, and the check for "did the step actually produce output" belongs in that habit.
Task T6-D4 (6b, download errors classified and sanitized, closes #106): complete (this commit, base 89e3e7e6). Added `PodcastError::classify` in `reprise-core::podcasts` — the single fixed-string classifier (timed out / could not be reached / HTTP error / invalid data / not modified / yt-dlp / disabled) that already existed as MCP's private `source_actions::podcast_source_error` match; that function now delegates to it instead of keeping a second copy. `pipeline::download_episode`'s failure branch and its `tracing::warn!` log line both switched from `error.to_string()`/`%error` to the classified reason, closing the actual leak: a `PodcastError::Transport`/`Body`/`YtDlp` payload can echo the raw provider transport error (which can carry the request URL and, per SRC-5, a private per-subscriber token) or, for yt-dlp's `finalize_download`, a local download path via `Path::display()`. Found and fixed a second, undocumented copy of the whole download executor: `reprise-gnome`'s `podcasts_worker.rs::try_download_episode_to_root` — the code the "Download episode" button and MTP-44's high-priority preparation lane actually call — reimplements `download_episode` rather than calling it, so it had the identical unsanitized `error.to_string()` leak independently; classified it with the same `PodcastError::classify()` rather than merging the two executors (out of scope here, flagged instead — see below). One pre-existing test (`pod_7_failed_worker_download_emits_failed_and_removes_partial`) was asserting the *raw* leaked string as correct behaviour and had to be corrected to the classified one. GTK: `podcasts_groups.rs`'s Failed row used to hide its reason behind `set_tooltip_text` only — a pointer-only affordance this repo's accessibility/input-parity policy forbids — now the classified reason is a second, always-visible label next to the fixed "Download failed" heading; the retry action gets a distinct icon/tooltip (`view-refresh-symbolic` / "Retry download") instead of looking like a first-time download. Retry and fresh-provider-output were already correct (a Failed episode never has `downloaded_path` set, so `toggle_download` re-enters the plain download branch and reruns `queued`→`downloading`→`downloaded` with a real fetcher call, no caching), as was `.part`/postprocessor-leftover cleanup (`download_atomically` and `ytdlp_download::cleanup_artifacts` already swept them on failure) — verified with a new failure-path test for the yt-dlp leftover case, which had no prior coverage. Added `POD-13` `[active][core][gtk]` to `docs/ux-rules.md` after `POD-12`. Split `ytdlp.rs`'s inline test module into `ytdlp_tests.rs` (its own new test pushed the file to 819 lines) to stay under the 800-line architecture gate. New `pod_13_*` tests include a redaction test that feeds a signed URL with a query string, a credential and an absolute local path through both the GUI worker and the shared pipeline and asserts none of it survives into `DownloadState::Failed` or a captured `tracing` log line (checked by temporarily reverting the fix — confirmed it fails with the leaking substrings, then restored). format, strict clippy (`-D warnings`), the full unfiltered workspace suite (reprise-core 1696, reprise-gnome 1135 + 336 display-only ignores, reprise-mcp 121, every other crate green — all 0 failed), core purity, UX traceability at 268 active rules, architecture, accessibility semantics, input parity, the mpris MCP check and audit (only accepted RUSTSEC-2024-0436) all passed.
Note T6-D4-open: `podcasts_worker.rs`'s manual-download executor duplicating `pipeline::download_episode` (not merely sharing its message classifier) predates this task and was not unified here — doing so safely would mean reconciling several existing rule-named tests (`net_1a_download_is_blocked_when_its_source_kind_is_disabled`, `pod_7_download_worker_emits_ordered_monotone_states_and_persists_after_publish`, the `MTP-44` priority tests) against `pipeline::download_episode`'s slightly different contract (it does not itself gate `NET-1a`, its `EpisodeNotFound` path emits no progress states at all). Flagged rather than silently worked around; a genuine merge is a separate, riskier task. Both copies now share the same classifier, so they cannot drift on redaction even though they remain two executors.
Task T6-followups (B2, B3, D2, D3, D4, G2, MTP-36, download convergence): complete (merged into dev as separate squashes 4565720f, e45e997f, df2d8d1f, afe27632, 138992f8, a3bcc7a0, 699ff39e; built by seven subagents in their own worktrees and verified here by mutation rather than by their reports). Each is described in its own pull request; what belongs in the ledger is what the work uncovered rather than what it added. Three of the seven found a live defect nobody had gone looking for. MTP-36: the selection did `take(latest)`, so a channel configured with 0 silently stopped syncing instead of syncing without a limit — the owner's zero decision that morning had been treated as one line of rule text and turned out to describe a real bug. POD-13/D4: a failed download's raw provider message reached both `DownloadState::Failed` and the warn log, and a podcast `audio_url` can carry a private per-subscriber token (SRC-5); the visible reason was also tooltip-only, so it was unreachable by keyboard and screen reader. B2: the podcasts filter row lived inside the swappable stack page, so switching to the no-results state hid the one control a user needs to undo the filter that emptied the list. None of the three was part of its task; each surfaced because the brief required proving the behaviour rather than asserting it — the zero case, the leak case, the visibility difference.
Note T6-convergence-gate: converging the two download executors onto `pipeline::download_episode` revealed that the core function had no NET-1a gate at all. Its two existing callers were each gated before calling in — refresh only reaches it after a same-kind fetch already passed, and MCP checks itself — so the privacy gate was a property of the callers, not of the download. A naive convergence would have deleted the GNOME copy that held the check and left the core path ungated, while the existing `net_1a_` test went green because it covered the deleted code. The gate now lives inside `download_episode` at the same position in the state sequence, so a refusal still emits Queued → Downloading{0,None} → Failed. Proven by forcing the gate open and watching the rule-named test run into an `unreachable!()` double.
Incident T6-my-battery: a private gate battery was used all day instead of `scripts/check-merge-readiness.sh`, the script CI itself runs through `ci-quality.sh`. It had five of that script's checks. `check-frontend-thinness.sh` was among the missing ones, the turn 6/7 merge pushed reprise-gnome's rusqlite budget from 515 to 563, and dev failed that gate from the merge until the owner repaired it in 323c5828. Every "all gates green" claim made before that repair was unsupported for four checks. The lesson is not "remember the gate" but "do not rebuild a gate the repository already defines" — the approximation is what made the omission invisible. Note also that `check-merge-readiness.sh` currently refuses to start for every branch, because `main` is not an ancestor of `dev`; the one-time `-s ours` repair is documented in docs/agents/branching.md and reserved for the owner, so its checks are being run individually until then.
Lesson T6-silent-verification: four times in one session a verification step produced no output and was nearly read as success — a mutation test whose build failed to compile, a `--workspace` test filter that matched nothing, a `tail` that truncated the failing step out of a battery's output, and a gate summary that said HAS FAILURES while ten visible lines said passed. Each time the step had not run at all. The rule that survives: a check that printed nothing did not happen, and the absence of an error message is not evidence. Read the numbers, not the silence — this is the same shape as the earlier grep-for-FAILED mistake and it recurs under time pressure.
Lesson T6-agent-seams: seven subagents working in parallel from a base that moved underneath them produced no defect inside any one contribution, and four at the seams between them: two agents claimed POD-13 for different rules, two independently created the same test file to satisfy the file-size gate, one sanitized a download executor another had already deleted, and one added a struct field another's fixture then failed to construct. All four were invisible to the agents (each was green on its own base) and to their gates, and were caught only by the coordinator running the full battery after combining the work. Parallel agents need disjoint rule ids assigned up front, and the integrator must treat every combination as unverified until it is verified as a whole.
Lesson T6-worktree-base: every agent worktree on this repository is provisioned from `main`, not from the current branch, so each of the seven began with `git reset --hard origin/dev`. That is not incidental — `main` carries a promotion merge commit dev has never seen, which is the same topology fact that stops `check-merge-readiness.sh` from starting. Until the trunk repair lands, every agent brief must say so in its first paragraph rather than as an aside.
Review T6-codex-followups: a second external review (Codex CLI, read-only) audited 3559f0c8..dev after the seven follow-up blocks landed, and was pointed at the seams between parallel contributions rather than at the contributions themselves — because in the first review two of three P1s had come from a conflict resolution, not from an author. It returned two P1 and six P2 findings, every one real, none of which any gate had caught. P1: "keep last N downloaded" ranked all episodes of a subscription and applied the downloaded filter afterwards, so undownloaded episodes consumed rank positions — a show with twenty episodes where the user had downloaded the three oldest gave those ranks 18 to 20 and keep-5 deleted all three, silently, with no error and the files simply gone. It predates the per-channel work, which made it worse by letting the user choose an N that still meant something else. P1: MTP-41 claimed YouTube limits each channel to its latest N "regardless of download state" while the code deliberately admitted only downloaded or explicitly wanted episodes; both positions were defensible so it was raised rather than decided locally, and the owner ruled the code right and the rule wrong (recorded as E-10). P2: a local path in a normal-level log line, raw yt-dlp stderr rendered in the add dialog's preview, and "unlimited" displayed to the user as "latest 0 each" — the device page reporting nothing selected while everything was. P2: two rule-named tests that would not have noticed their own feature being deleted. All fixed; the destructive one verified twice, once by its author and once here by putting the bug back.
Lesson T6-unwatched-tests: the two weak rule-named tests the second review found were both written the same day, under briefs that explicitly demanded a test go red if its behaviour were removed. POD-13's retry test asserted the button's icon and tooltip and never activated it; POD-9's tests covered the computation but never that it reached the surface. The requirement was stated, accepted, and not met, and nobody noticed because nobody asked for the evidence. The durable form of the rule is therefore not "require the test to fail without its feature" but "require the red output to be shown" — an unwatched test is indistinguishable from a test that does nothing, and stating the requirement more firmly does not close that gap. Every rule-named test on this branch is now expected to arrive with its actual failure message from the sabotaged run.
Note T6-final-state: dev carries the complete turn 6/7 plan (blocks A through H, E1 through E9), every follow-up (B2, B3, D2, D3, D4, F2, F4, G2, MTP-36), the download-executor convergence, and both review rounds. Issue #106's fix is on dev (POD-13) but the issue is still open: the `closes #106` keyword only fires when a branch merges into the default branch, which here is main, so nothing on dev can close it and it must be closed by hand or by the trunk repair. Two gaps are deliberate and recorded rather than hidden: neither keep_downloaded nor latest_per_channel has a UI, so both are settable only through the store, and NET-3 point 2's dimmed "Needs network" rows are unwired, with the rule text saying so. The location hoist is likewise half done — storage is app-level, but its settings rows still live on the Concerts preferences page, so a radio user is deep-linked into Concerts to set it. Verification throughout was local only: GitHub Actions could not run for any of it (an account billing condition), and scripts/check-merge-readiness.sh refuses to start for every branch until the one-time `-s ours` trunk repair documented in docs/agents/branching.md, so its fifteen checks were run individually before each merge. The rule-named display gate ends at 214 of 214. No manual acceptance has happened yet; MTP-30 in particular means a connected phone with pending work begins transferring unprompted.

# Runtime client migration preparation — R8

Task R8: complete (commits 4c29059ffa, 3808628077 and 18b35362ef, base 2319b19f53, added bounded revision-stamped pages for both queue sections and an absolute seek command across protocol 4.2, runtime, client and Linux service; whole-queue change detection now invalidates positions beyond the 200-row snapshot; the adversarial review fixed zero-duration episodes being collapsed to the start, pinned the distinct SeekTo wire method and target, corrected the protocol wording and repaired two inherited dev gate drifts; the two unknown-duration regressions and the wire contract were each mutation-proven red then green; format, strict workspace clippy, warning-free workspace rustdoc, the complete workspace suite with only the two sandbox-blocked MCP Radio listener tests excluded, architecture, runtime-service packaging, frontend thinness, UX traceability, motion, accessibility, input parity, QA linters, file-size enforcement and cached audit with only accepted RUSTSEC-2024-0436 passed)
Assumption R8: a non-live item with `duration_ms == 0` has an unknown upper bound, so the runtime preserves the non-negative seek target and lets the backend, which knows the actual stream length, accept or refuse it; live streams remain structurally non-seekable.
Manual R8: the real private-D-Bus queue-page and absolute-seek integration tests could not start because this sandbox rejects Unix-socket binding with `Operation not permitted`; the full merge-readiness run reached the two inherited MCP Radio fixtures and stopped at their same `TcpListener::bind` restriction before display and runtime-bus phases. R8 changes no GTK rendering or pointer behavior, so no CUA claim is made.
Review R8: complete (this commit, base 5cccd286f6, parallel standards/spec review found one P1: whole-queue identities were cloned before and after every 500 ms position report, defeating bounded paging for a large virtual queue; exhaustive PlayerEvent and PlaybackCommand classifications now take the full before-image only for operations that can change queue identity, so position, seek, volume and metadata hot paths stay O(1) in queue length while finish, handoff, error, play, stop, skip and shuffle still publish revision changes; both classifications were mutation-proven red then green; the same review renamed the misleading client wire body `Delta` to `Milliseconds`, unified the page/snapshot limit on `QUEUE_WINDOW`, and caught a stale doc block before commit; format, strict workspace clippy, warning-free rustdoc, the complete workspace suite excluding only the two independently reproduced sandbox-blocked MCP Radio listener tests, architecture, runtime-service packaging, frontend thinness, UX traceability, motion, accessibility, input parity, QA linters, GitHub-flow policy and cached audit with only accepted RUSTSEC-2024-0436 passed)
Deferred R8-review: the standards reviewer noted a non-blocking data-clump/primitive-string smell in the queue-page request carried through the client and D-Bus layers; introducing a new request DTO solely for this already typed three-field wire call would expand the reviewed protocol surface without changing behavior, so it stays deferred rather than being folded into the merge fix.

Verification R8-merge: the isolated private-D-Bus runtime-service suite was rerun after the combined gate and passed 25/25, including queue paging and absolute seek; the rule-named, motion and CSS display scripts remain unverified in this sandbox because their per-test private D-Bus daemons are denied Unix-socket binding with `Operation not permitted`.
Task WTG-1: complete (this commit, base 4c341414, added exact-PR-verified worktree closeout, a conservative report-first scope collector, stale Cargo target reclamation, and a persistent weekly user timer; dirty, locked, active, protected, external, and unmerged worktrees stay untouched, including active-process protection in both immediate and scheduled cleanup; fixed the parallel cover-cache test fixture exposed by the full gate; format, strict clippy, the isolated full workspace suite with 1730 core tests, QA linters, fixture stress tests, systemd verification, audit with only accepted RUSTSEC-2024-0436, core purity, architecture, diff purity, and file-size checks passed; no cleanup was applied to live worktrees, no timer was installed, and no branch was pushed)
Task POD-3 diagnostics: complete (this commit, base 42fd6e9123, classified yt-dlp process, transport, response, media, dependency and finalization failures into actionable privacy-safe user messages and structured operation diagnostics across resolve, search, range, probe, update and download paths; test-first implementation, full workspace tests, strict clippy, formatting, UX traceability, core purity, file-size checks, cached audit with only accepted RUSTSEC-2024-0436, and parallel standards/spec review passed)
Manual POD-3 diagnostics: the provider probe was verified against an isolated public video, but the revised GTK toast rendering remains a manual visual check because the repository forbids using the live desktop and headless execution cannot prove actual rendering.
Task WTG-2: complete (this commit, base 42fd6e91, made timer installation independent of the checkout state by copying the collector to the stable user-local libexec directory and running that copy from the user unit; the regression was mutation-proven red then green, shell syntax, GC fixtures, systemd unit and calendar verification, format, strict workspace clippy, the full workspace suite with 1735 core tests, architecture, diff checks, and audit with only accepted RUSTSEC-2024-0436 passed; the additional QA policy check remains red on the identical base files because an unrelated CUA harness extraction is still uncommitted in another worktree)
Task DBH-W0: complete (current-branch commit 27982a9ee5, original ADR commit 298b8dc7c4, recorded ADR 002 for the private core Db handle; the original decision commit was cherry-picked after the implementation and dev-integration commits rather than rewriting completed history)
Task DBH-W1: complete (commit 1988d7a9f6, base 4c341414, added Db as the non-RefCell owner of the library connection with migrated and in-memory constructors, path reporting, no Deref, and handle tests)
Task DBH-W2: complete (commit 0834c15e08, base 1988d7a9f6, replaced transaction-only &mut Connection boundaries with &Connection plus reviewed unchecked_transaction calls, removing database borrow_mut use without introducing interior mutability)
Task DBH-W3: complete (commit 662e6043b9, base 0834c15e08, migrated the GNOME composition root, feature surfaces, workers and fixtures from Rc<RefCell<Connection>> to Rc<Db>; workers continue to open independent handles)
Task DBH-W4a: complete (commit 2349bafd71, base 662e6043b9, migrated CLI, MCP and runtime ownership and fixtures to Db while retaining independent worker connections)
Task DBH-W4b: complete (commit 559a9d87a6, base 2349bafd71, moved public Core database boundaries to &Db while keeping private query and transaction helpers on &Connection)
Task DBH-W5: complete (commit de7690648c, base 559a9d87a6, sealed Db::conn as pub(crate), removed every external conn access, added narrow Core facades and isolated file-backed frontend fixtures, and ratcheted the exact frontend rusqlite budget from 538 to 112 with an explicit external Db::conn ban)
Integration DBH-dev-sync: complete (merge commit 759611df09, merged origin/dev 42fd6e9123, resolved the three MTP-46 device-sync conflicts while preserving both the new module gating and the sealed Db boundary; targeted Core 19/19 and GNOME 2/2 tests plus the combined workspace battery passed)
Verification DBH: format, strict workspace clippy, warning-free rustdoc, architecture, frontend thinness at exactly 112, core purity, device-sync GStreamer, runtime-service installation, UX traceability, motion, accessibility/input parity, QA linters, diff purity and cached audit with only accepted RUSTSEC-2024-0436 passed; the combined workspace suite passed 3500 tests with 378 ignored and only the two sandbox-blocked MCP Radio listener tests filtered. An unfiltered run independently reproduced Operation not permitted at TcpListener::bind for exactly those two tests and no others.
Manual DBH: rerun adds_a_url_only_radio_favorite_from_icy_metadata and resolves_a_pls_radio_favorite_before_probing_and_storing_it in an environment that permits local TCP listeners; cargo-machete was unavailable and therefore reported SKIPPED by the thinness gate. No visible GTK behavior changed, so no CUA claim is made.
Fix DBH-gates: complete (commits 49bd9a6ae5 and b31c19bb78, base 27982a9ee5, the full closeout battery exposed two narrow post-migration gate drifts: the CUA contract omitted the runner's existing sqlite3 dependency and public rustdoc linked the now-private raw open functions; both failures were reproduced, minimally corrected and verified green)
Stage DBH-closeout: recorded in this commit (base b31c19bb78, synchronizes ADR 002 and the multi-frontend plan with the implemented seam, replaces the stale in-progress Claude Code handover with the final branch state, and records every wave without amending completed commits; the required clean-tree merge-readiness run follows this commit and is reported by the executing agent rather than preclaimed in the ledger; no repository lock or separate coordination board exists in this checkout, no push or PR was performed, and the next roadmap stage remains owner-controlled)
Verification DBH-clean-tree: complete (after closeout commit 53964b0440, MERGE_READINESS_BASE_REF=origin/dev scripts/check-merge-readiness.sh --no-fetch ran from a clean worktree to exit 0: the unfiltered workspace suite including both formerly blocked MCP Radio listener tests passed, as did rule-named display 215/215, motion display 26/26, CSS display 10/10, private runtime D-Bus 25/25, warning-free rustdoc, all static gates and cargo audit with only accepted RUSTSEC-2024-0436; the earlier Manual DBH socket item is therefore resolved, while cargo-machete remains an honest SKIPPED optional thinness subcheck)
Integration DBH-publication-base: complete (merge commit a784b3f389, merged origin/dev 02dec96a02 before publication; the only conflict was the append-only progress ledger, where both upstream POD-3 diagnostics entries and every DBH entry were preserved; the new yt-dlp code had no overlap with the Db-handle diff, its focused 24-test group passed serially and in parallel after one non-reproducing spawn failure in the first parallel run, and the required clean-tree merge-readiness run follows the publication-preparation commit)
Integration DBH-current-dev-tree: complete (current dev 75f2d9cf15 was content-integrated before the publication gate; terminal networking could not fetch the Git object after GitHub merged PR #172, so its five exact WTG-2 file blobs were read through the connected GitHub app and applied locally, while the remote feature commit is parented directly to 75f2d9cf15; no Db-handle path overlaps WTG-2)
Task T6-E2E-podcasts: complete (commit 1970f902, base 42fd6e91, added the ninth CUA acceptance scenario and the first that exercises what a network source actually does rather than only its switch — the real Add Podcast dialog is driven end to end (type a URL, watch the primary button turn from Search into Preview, preview, subscribe) with every request answered from a fixture directory behind `--features test-fixtures`, so the app under test never reaches the network; the assertion is the header count "1 show · 1 episode · 1 new" rather than the show's name, which could have been derived from the URL, and the scenario was mutation-proven by renaming the fixture file until the run failed at 'Systems Weekly'; three costs are recorded in the commit so the next scenario does not re-pay them — the shell auto-closes its side panels at this window size so a source must be opened through `REPRISE_SMOKE_SOURCE` rather than the sidebar, labels are matched exactly and a closed dialog's labels linger in the accessibility tree so absence assertions about them are meaningless, and an open dialog keeps the app alive past the smoke-quit timer, turning a failure into a multi-hour hang instead of a red run)
Task POD-15: complete (commit a2fbb556, base ec1f25c1, the YouTube page reused the podcast page's header verbatim and reported a subscribed channel as "1 show · 2 episodes · 2 new" on a page whose every other control says channel and against a device-sync summary that already distinguishes channels from shows (MTP-45/POD-12); the filter bar already received the page kind for its Add button and dropped it afterwards, so it now keeps it and picks the subject while episode and new counts keep one formatter, which is what stops the two pages drifting apart in the tail; found by the source-youtube acceptance scenario reading the header out of the running app, and mutation-proven — removing the branch turns pod_15_the_youtube_page_header_counts_channels_not_shows red with "1 show · 1 episode · 1 new")
Task T6-E2E-youtube: complete (commit 7c65917c, base a2fbb556, the second half of the source-content acceptance group — three app starts against one profile with the module switched between them: off, where the page is unreachable and neither "No channels yet" nor "Add channel" is anywhere in the tree; on, where the empty state appears, SRC-6 refuses a pasted RSS URL with "That is an RSS feed — add it under Podcasts" on the same dialog that then accepts a channel URL, and Preview plus Subscribe land it on the page; no network is involved because REPRISE_YTDLP_BIN points at a shell script printing two entries and ytdlp.rs reads that variable unconditionally, needing no fixture feature; the proof-carrying assertion is again the count, "1 channel · 2 episodes · 2 new" — the title could have come from the URL but two episodes can only have come from the fake binary's answer, and it is what caught POD-15; the scenario closes the dialog before quitting, since an open AdwDialog turns a failure into a timeout)
Verification T6-display-final: the rule-named Xvfb display gate over the branch tip 7c65917c reports "passed: 216, failed: 0 of 216" (exit 0), which closes the pending item left when several agents added #[ignore]d display tests that had not yet run through it; the count moved 214 to 216 with the two commits above. The evidence was checked rather than trusted: 216 unique "== display test:" blocks, each with exactly one "test result: ok. 1 passed", and no block reporting zero tests — the failure mode of an earlier claim in this stage. It was run on a detached scratch worktree under ~/.cache/reprise-scratch rather than in .worktrees/podcast-channel-redesign, because that worktree carries an unrelated in-progress change (removal of the Experimental preferences page) that does not compile — browse_bar.rs and track_list_columns.rs still call ui::experimental::experimental_enabled while ui/mod.rs no longer declares the module — and which is owned by a separate agent; per Lesson T6-gate-tree the gate must not share a tree with an editing agent in any case. Log kept at ~/.cache/reprise-scratch/display-final-20260730.log.
Task POD-migration-v40: complete (commit 8ba7e635, base c18272ef, fixed the defect behind a live report that both the Podcasts and the YouTube page rendered "no such column: sync_to_phone" instead of a library; the owner's database reports user_version 48, a fully migrated schema, yet lacks exactly the two columns migrate_v40 adds and no others, because that migration alone guarded on the version rather than on the version and its own columns — v34, v43 and v47 all guard on both and therefore repair such a database on the next start, while v40 short-circuited forever. The precondition is this repository's own history: schema numbers were reassigned during development, so a build whose v40 meant something else raised the version past 40 without running this ALTER TABLE. The guard now includes both columns and the version is raised with max() rather than set to 40, which would have rolled a v48 database backwards. Verified three ways: the new test drops both columns from a fully migrated database and is red before the change; the full workspace suite and strict clippy pass; and reprise-cli run against a copy of the affected installation restored both columns, left user_version at 48, and made the failing query return its two subscriptions. The owner's real database was never opened by any tool — every check ran on a copy.)
Task POD-16: complete (commit a03a8505, base 8ba7e635, the library footer rendered DbError's Display on a failed load, which for a rusqlite input error is the message plus the whole failing statement and a byte offset — five lines of SQL where the "last updated" line belongs, on both pages. Both failure paths of that one label now carry a fixed sentence and log the detail: a new "Could not read your subscriptions" for an unreadable library, and POD-11's existing "Refresh failed · showing saved episodes" for a failed refresh, which was already self-contained and merely had the raw error appended. Beyond legibility this closes the same leak class POD-13 closes for provider errors, since a statement can carry a path or an identifier. New rule POD-16; the display test reproduces the reported footer byte for byte, asserts the replacement string and the absence of the statement separately, and is red before the change; format, strict clippy, the full workspace suite, UX traceability at 275 active rules, and the rule-named display gate at "failed: 0 of 217" passed.)
Deferred POD-16-toasts: the same file still has fourteen show_error(&error.to_string()) toast sites and the podcast worker maps its errors with a plain to_string(), so a database or gio error can still reach a toast verbatim. They are a different surface from the reported footer and several already carry classified text (POD-13), so they were left rather than folded into this fix; a sweep should classify them together rather than one call site at a time.
Task POD-ytdlp-part: complete (this commit, base 4745717e, fixed the defect behind a live report that no YouTube episode could be downloaded — every attempt ended in "Download failed · YouTube source could not be read with yt-dlp", logged as failure_kind="other", exit_code=1, with the provider text discarded by POD-13 so the cause was invisible. yt-dlp reserves a trailing `.part` for its own partial downloads: it strips that suffix from the output template, writes the media under the shortened name, and its post-processor then fails to find what it just wrote, ending in exit 1 with "Postprocessing: WARNING: unable to obtain file audio codec with ffprobe". downloads::partial_path appends exactly that suffix because download_atomically needs a temporary name, and the value was handed straight to -o. For RSS this is harmless since Reprise performs that download itself; only the yt-dlp executor passes the name to a foreign tool that reserves it. Isolated against the real binary (2026.07.04): the identical command succeeds with `-o episode.opus` and exits 1 with `-o episode.part`, so the suffix is the whole trigger. The executor now asks for `<temporary>.download`, a name yt-dlp honours literally; it already learns the real output through --print after_move:filepath and finalize_download moves that onto the temporary path, so the atomic-publish contract and every `.part` rule are untouched, and the new name still starts with the temporary path's prefix so cleanup_artifacts keeps sweeping it. Both executors have carried this since 89e3e7e6. Test-first with a fake binary that reproduces the contract rather than the symptom — it refuses any -o ending in `.part` — and verified end to end against the real binary, which returns exit 0 and a genuine Ogg/Opus file for the new path. Format, strict clippy, the full workspace suite at 3515 passing, UX traceability at 275 active rules and the rule-named display gate at "failed: 0 of 217" passed.)
Incident POD-ytdlp-orphans: the failures above left residue that the code cannot see. Because yt-dlp stripped `.part`, it wrote each attempt to the destination path itself, which is the one name cleanup_artifacts does not match — it sweeps by the temporary path's prefix. The owner's library holds eight such files, about 293 MB, against a single row with a downloaded_path. They are not usable: `file` reports WebM, so they are the raw pre-conversion downloads wearing an `.opus` name, and adopting them through reclaim_existing would hand the player and the device sync a mislabelled container. They also block their own episodes after the fix, since download_atomically refuses to publish over an existing destination. The fix makes the situation unreachable going forward — yt-dlp can no longer write to the destination — but the existing files are the user's and were left untouched for them to decide. Worth a separate look: a destination the database does not know about leaves the row permanently on "Download failed" with no way out from the UI, which is a dead end rather than an error.
Task Package-A-sync-settings: complete (commit edc83e8dd6, base 6c757fddd5, made Plugins the single settings home for optional capabilities, re-hosted capability settings as expander children, implemented SET-11's state-preserving Online-content kill switch and read-only collapsed disclosures, retained the complete runtime/sidebar notification fan-out, activated SET-10 and SET-11, and synchronized all gettext catalogs; format, strict workspace clippy, the full workspace suite, UX traceability, architecture, accessibility semantics, input parity, motion tokens, gettext catalogs, audit with only accepted RUSTSEC-2024-0436, core purity, diff purity, and file-size checks passed; isolated display tests did not start because the sandbox denied the private D-Bus daemon's Unix-socket bind with Operation not permitted, so visible GTK behavior remains manually unverified; no push was performed)
Task NET-3d Package D: complete (commits 486fcdad31, 7146f94221 and 573821dddc, base 6c757fddd5, added the five-state SourceErrorKind projection with safe Display and explicit timestamped details, converted Podcasts, Radio, Concerts, covers, portraits and lyrics, classified feed 404/410 and 429 with shared Retry-After parsing and 2/4/8-second capped retry policy, unified Podcast/Radio/Concert feed and search timeouts at 10 seconds while retaining the 15-second podcast download budget, and activated NET-3d with a rule-named test; format, strict workspace clippy, 1783 Core tests, the complete workspace suite excluding only the two independently reproduced sandbox-blocked MCP Radio listener tests, UX traceability at 276 active rules, architecture, core purity and audit with only accepted RUSTSEC-2024-0436 passed; the expected gettext catalog drift remains red and no catalogs were touched)
Deferred NET-3d integration: the owned Core boundary exposes `PodcastError::retry_delay` and classifies the existing yt-dlp bot-check/helper messages, but the excluded podcast refresh schedulers still need to schedule that delay after presenting the first readable state, and the excluded `podcasts/ytdlp.rs` boundary still discards raw stderr before constructing `PodcastError`; both are marked with `TODO(integration)` in `podcasts.rs` because changing either file was outside Package D ownership.
Package B B1: complete (this commit, base 6c757fddd5, schema v49 now stores the global online-source gate off for fresh databases and existing databases without demonstrable use, preserves explicit values, and keeps the gate on for existing subscriptions, radio favourites, downloaded episodes, or populated positive image caches; the in-code fallback is off and affected test fixtures opt in explicitly)
Package B B2: complete (this commit, base 7b1b3138b3, the first off-to-on transition atomically persists its one-shot and leaves every online source module off except Radio; later master toggles preserve all per-module choices, and v49 marks demonstrable prior use or an explicit pre-existing master value as already enabled so upgrades are never reset)
Package B B3: complete (this commit, base 7da8903861, added the one-shot non-modal online discovery banner at the top of the Library; Review and Not now persist dismissal before hiding, Review opens the stable Plugins preferences page, and an already-enabled master gate suppresses the banner)
Package B B4: complete (this commit, base 070c503efb, replaced NET-2 with active NET-2a, added active NET-4 in one contiguous section-T edit, and re-pointed the migration and display tests to their current rule IDs)
Task SYNC-C1: complete (commit 0eb1ec8da0, base b5467e6ae5, projected one active MTP device in Core, kept every later device inert and session-free with the binding amber copy and no sync action, removed the device chooser, and guarded every sync entry point; rule-named Core, fake-backend, runtime and sidebar tests were mutation-proven red then green; format, strict workspace clippy, UX traceability at 275 active rules, audit with only accepted RUSTSEC-2024-0436, core purity, diff purity and file-size checks passed; the workspace suite could not run the two socket-bound MCP Radio tests in the sandbox, and the operator separately verified both tests green outside it)
Task SYNC-C2: complete (this commit, base ef3049fce6, made GVfs UUID then USB serial the only durable device identities, resolved serials through a fail-soft sysfs boundary, kept no-key devices usable without persisting their volatile MTP URI, and re-keyed an unambiguous legacy URI row even when the bus/device address changed on replug while preserving ambiguous or conflicting rows; focused MTP-48 Core, Linux and GNOME tests, format, strict workspace clippy, the revised workspace suite with only the two operator-verified sandbox-blocked MCP Radio listener tests filtered, UX traceability with 276 active rules, architecture and file-size checks, accessibility, input parity, core purity, and cached audit with only accepted RUSTSEC-2024-0436 passed; display tests and real-phone/sysfs behavior were not run)
Task SYNC-C3: complete (this commit, base 615f252b3e, persisted stable-device verification time and on-device size beside the existing local name and target folders, projected the active connection first and remembered devices afterward with no disconnected diff, kept remembered pages readable but unsyncable, and added local rename plus pointer/keyboard Forget device actions that delete only Reprise metadata; rule-named MTP-49 Core, runtime, page-copy and sidebar tests, format, strict workspace clippy, the revised workspace suite with only the two operator-verified sandbox-blocked MCP Radio listener tests filtered, UX traceability with 277 active rules, architecture and file-size checks, accessibility, input parity, core purity, and cached audit with only accepted RUSTSEC-2024-0436 passed; no display test or real phone was run)
Task SYNC-C4: complete (this commit, base e506cd6381, changed the transfer-profile heading to the catalog-declared music-only copy and replaced preparation-time NetworkMonitor reads with explicit injectable connectivity and metered seams, leaving the requested window-level network-changed integration TODO in the owned runtime boundary; focused MTP-24 and NET-3a tests, format, strict workspace clippy, the revised workspace suite with only the two operator-verified sandbox-blocked MCP Radio listener tests filtered, UX traceability with 277 active rules, architecture and file-size checks, accessibility, input parity, and cached audit with only accepted RUSTSEC-2024-0436 passed; no display test was run)
Task SYNC-C5: complete (commits 0eb1ec8da0, 615f252b3e and e506cd6381, base b5467e6ae5, replaced MTP-3 with active MTP-47 and added active MTP-48/MTP-49 strictly inside section E, with rule-named Core, Linux, runtime, page and sidebar tests; UX traceability reports 277 active rules covered)
Fix SYNC-C-review: complete (commits dd7d340a67 and 8fce1bad6c, base 66bb57b683, the final regression and scope audit restored the ComputingDelta projection and selectable playlists while a remembered device is being re-inspected, removed an initial-idle race from the MTP-47 simulated-backend test, corrected MTP-1's obsolete connected-only source assertion, and moved the context-menu helper plus CSS tests back inside the files this package owns)
Verification SYNC-C-final: complete (after 8fce1bad6c, format, strict workspace clippy, the revised workspace suite with exactly the two operator-verified socket-bound MCP Radio tests filtered, UX traceability with 277 active rules, architecture, accessibility semantics, input parity, Core dependency purity, and cached audit with only accepted RUSTSEC-2024-0436 passed; focused regressions covered MTP-22 category diffs, MTP-26 verification and Scan device copy, MTP-27 category storage segments and free-space arrow, MTP-31 target-folder browsing, MTP-37/MTP-38 content rows and persisted target folders, MTP-43 Download & sync, pre-diff inspection, interrupted reconnect/readback, insufficient-space rejection before transfer, failed-copy-before-cap-removal ordering, and oldest-first cap projection; gettext catalogs remain expected-red with 78 fatal catalog mismatches and were not changed; no display test, real phone, real sysfs device, user database, music files or live desktop was used)
Manual SYNC-C: verify UUID/USB-serial identity across a physical replug, the inert amber and remembered dimmed presentation, pointer and keyboard rename/Forget device actions, and actual transfer behavior against a real phone; attach the real network-changed signal at the TODO in device_sync_compact.rs from its integration package. New translatable strings are declared in device_sync_strings.rs for the catalog-owning package.
Task Package-F1: complete (commit a1cfb31553, base 9051319feb, carried the typed yt-dlp failure kind and sanitized raw stderr together, derived SourceErrorKind exhaustively from the enum, kept fixed POD-13 Display copy, and exposed private diagnostics only through details; real fake-process stderr, exhaustive mapping, safe-copy and path/URL/secret redaction tests were mutation-proven red then green)
Task Package-F2: complete (commit 924bc6518a, base a1cfb31553, added a pure timestamp-parameterized retry decision and bounded in-process per-subscription retry state to the existing serial refresh pipeline; failures become readable immediately, provider calls wait for the shared 2/4/8-second policy, success resets the attempt count, and exhaustion returns to the fixed refresh interval; rule-named pure and pipeline tests were mutation-proven red then green)
Task Package-F3: complete (commit dd9ad32bbe, base 924bc6518a, corrected only NET-3d's stale scheduler annotation while preserving the absent Details block, absent banner, and planned NET-3 presentation contract)
Verification Package-F: format, strict workspace clippy, the full isolated-XDG workspace suite with 1814 Core tests and exactly the two operator-designated socket tests filtered, UX traceability with 278 active rules, architecture, Core dependency purity, diff purity, file-size checks and cached audit with only accepted RUSTSEC-2024-0436 passed; no GTK, platform, device-sync, radio, concert, database, gettext, user-data or live-desktop file was changed, no CUA claim is made, and no push was performed.
Assumption Package-F: the retry state deliberately remains process-local and reuses the existing refresh trigger rather than adding persistence or a second scheduler; the 2/4/8-second values are minimum provider deferrals, while an actual retry occurs on the next invocation of the existing loop.
Task Package-E-sync-pickers: complete (commits 273bad0bb2, d3dea95a49, 382370f86d and 13ff1205a5, base 9051319feb, added one shared device-page picker for playlists, YouTube episodes and podcast episodes; reused the existing playlist, subscription-device and wanted-on-device decisions without a second selection store; kept explicit episode pins across rule changes and ageing; activated MTP-50 and replaced MTP-37; format, strict workspace clippy, the complete workspace suite with exactly the two operator-owned loopback tests filtered, UX traceability with 278 active rules, architecture, accessibility semantics, input parity, Core dependency purity and audit with only accepted RUSTSEC-2024-0436 passed; gettext catalogs remain expected-red with 23 missing entries declared in device_sync_strings.rs and po was not touched; display tests compiled but were not run because the sandbox cannot bind the private D-Bus Unix socket)
Task LVF-P1: complete (this commit, base b5467e6ae5, made podcast and YouTube metadata omit unknown values and empty separators, formatted durations as minutes or hours, hid redundant author lines, omitted zero-byte summaries, and split only title tails repeated across at least three episodes; the focused red-green tests, format, strict workspace clippy, isolated workspace suite except the two inherited sandbox-blocked Radio MCP listeners, filtered MCP rerun, file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed)
Task LVF-P3: complete (this commit, base 96c4568861, accumulated split RSS and Atom title events including entities and CDATA, parsed episode artwork, added the guarded v49 image_url migration and query projection, backfilled missing dates and artwork on upsert, and resolved YouTube handles to stable channel identities before preferring Atom with dated yt-dlp metadata as the fallback; the immutable live-database check found both active YouTube subscriptions use @handle URLs and all 53 stored episodes lacked dates on the old flat-playlist path, while v49 was still free on current origin/dev; focused red-green tests, 1766 core tests, format, strict workspace clippy, core purity, file-size checks, and cached audit passed, and the isolated workspace suite passed except the two inherited sandbox-blocked Radio MCP listeners)
Task LVF-P4: complete (this commit, base 057ca4a7c3, derived EpisodeRow.is_new from first_seen_at greater than subscription.added_at, anchored every backlog row to added_at on the first successful fetch even after an earlier failure, marked only later arrivals new, renamed source summaries to new_count, and hid the New pill for old unstarted backlog episodes without conflating discovery with playback; the active POD-1 and POD-9 contracts and their rule-named tests changed together; focused red-green tests, format, strict workspace clippy, UX traceability, core purity, file-size checks, and cached audit passed, and the isolated workspace suite passed except the two inherited sandbox-blocked Radio MCP listeners)
Task LVF-P2: complete (this commit, base d65b3d5e6a, bounded decoded source textures and their cache by requested dimensions, centered fixed-aspect cover surfaces, compacted episode rows to a two-line activation target without a redundant play button or idle status label, added episode artwork with RSS and YouTube aspect ratios plus the show-cover fallback, dimmed repeated YouTube title tails, and made unsubscribe available consistently on full-row hover and keyboard focus; pure and non-display GNOME tests passed 1170 with 358 display tests ignored, strict workspace clippy, UX traceability, file-size checks, the isolated workspace suite except the two inherited sandbox-blocked Radio MCP listeners, and cached audit with only accepted RUSTSEC-2024-0436 passed; the new ignored row-measurement and texture tests could not be executed here because the sandbox denied the private D-Bus socket and GTK/Xvfb initialization, so their visual assertions remain for the final display/manual pass)
Task LVF-P6: complete (this commit, base e27e4c8f2b, shortened the shared YouTube add action to "Add channel", added plural-aware "Show all N episodes" copy, regenerated the complete gettext template from every POTFILES.in source, synchronized all seven locale catalogs without fuzzy entries, and completed the German and Spanish catalogs for the podcast, YouTube, and Radio strings that had accumulated since the stale checked-in template; the string unit test, gettext catalog gate, format, strict workspace clippy, and diff check passed)
Task LVF-P5: complete (this commit, base 7d8c361055, limited each collapsed source group to ten episode rows, appended one plural-aware show-all action carrying the full group count, and kept the expansion as local view state distinct from the source expander state; a pure red-green boundary test covers 0, 9, 10 and 15 episodes, the ignored display test asserts ten episode children plus one action for a 15-row group, the row-interaction extraction keeps every substantially edited code file under 800 lines, and format, strict workspace clippy, non-display GNOME tests at 1172 passed with 359 ignored, and the isolated workspace suite except the two inherited sandbox-blocked Radio MCP listeners passed)
Fix LVF-CUA-contract: complete (this commit, base df2f08737a, updated the isolated Podcasts and YouTube source scenarios to require zero new backlog episodes and to reject the removed unknown-date and zero-byte placeholders; the deterministic CUA helper contract passed, while the real private run was blocked before app launch because Xvfb could not bind any private X11 socket in this sandbox, so no screenshot or observed GTK claim is made)
Stage LVF-closeout: complete (this commit, base 5cc84cbc31, records the final consolidated proof: cargo fmt --all and strict workspace clippy passed; reprise-core passed 1767 tests; reprise-gnome passed 1172 non-display tests with 359 ignored both normally and under xvfb-run, while the new ignored display tests failed only at gtk4::init because this sandbox cannot create an X11 socket; the full isolated workspace suite passed except the same two inherited Radio MCP listener tests blocked at TcpListener::bind with Operation not permitted; gettext, UX traceability at 275 active rules, core purity, diff checks and cached audit with only accepted RUSTSEC-2024-0436 passed; a WAL-aware read-only backup of the real v48 database migrated through reprise-cli to v49 with image_url present and integrity_check ok while the real database remained untouched; the connected GitHub read-back confirmed remote dev is still exactly b5467e6ae5 with SUPPORTED_SCHEMA_VERSION 48 and no v49 migration, so v49 remains collision-free; the supplied untracked plan remains untouched, no repository lock or coordination board exists in this checkout, no push or PR was performed, and visual GTK/CUA acceptance remains explicitly unverified because Xvfb failed before app launch)
Task Package-G-projection: complete (this commit, base 811050c1c6, added the pure source-failure presentation decision that keeps any non-empty cache on a banner, reserves the full-area state for a genuinely empty source, chooses provider-specific actions from the five-state taxonomy, and collapses three or more failures into one notice; four focused Core tests were observed red before the implementation and green afterward)
Task Package-G-surfaces: complete (this commit, base 40984396c2, added the reusable neutral banner and full-area failure state with collapsed copyable monospace Details, retained cached podcast and YouTube lists across refresh failure, consumed RefreshSummary.failed as one collected notice, wired one gio::NetworkMonitor into the existing connectivity seams, rendered Needs network and Unavailable now per row, replayed offline Download and Load more clicks in order on reconnect, surfaced radio reconnect failure with Retry and Find a new URL, removed raw podcast action and add-dialog errors from ordinary UI, added query-specific non-error empty search copy, activated NET-3, and replaced POD-16 with POD-17; focused tests were observed red before each projection and green afterward)
Fix Package-G-presentation: complete (this commit, base eae768d347, moved the classified failed-download reason out of the row and into its tooltip, matched the specified cached-episode and empty-channel support copy, and corrected the Radio connectivity seam documentation; both focused projection tests were observed red before the implementation and green afterward)
Task Package-G-typed-failures: complete (this commit, base 678e8f87dc, extended podcast refresh summaries with per-subscription id, title and SourceErrorKind while retaining the aggregate count for existing non-GTK consumers, kept raw provider payloads out of the richer result, and covered both ordinary source failures and YouTube identity-resolution failures; focused tests were observed red before implementation, format, strict workspace clippy, the complete workspace suite with exactly the two operator-designated socket tests filtered, UX traceability at 279 active rules, architecture, accessibility semantics, input parity, motion tokens, Core dependency purity, file-size checks and cached audit with only accepted RUSTSEC-2024-0436 passed; the live advisory refresh could not acquire its read-only sandbox lock, so cargo audit was rerun successfully with --no-fetch)
Task Package-G-classified-routes: complete (this commit, base 7157907670, routed typed podcast refresh kinds to their provider-specific banner or empty-state copy and actions without exposing raw provider text, retained exact subscription identity for SourceGone unsubscription, kept the explicit connectivity seam authoritative for offline presentation, and made Radio's Find a new URL action re-resolve a stored radio-browser UUID rather than merely repeating playback; focused rule-named projection tests were observed red before implementation and green afterward, format, strict workspace clippy, the complete workspace suite with exactly the two operator-designated socket tests filtered, UX traceability at 279 active rules, architecture, accessibility semantics, input parity, motion tokens, Core dependency purity, diff and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; display tests and the gettext catalog gate were not run)
Task Package-G-collected-notice: complete (this commit, base 25f59374a5, made a batch of three or more failed podcast or YouTube sources one bounded notice that names at most three affected sources plus a remaining count, with truthful distinct support copy for populated and empty views; the rule-named projection test was observed red before implementation and green afterward, format, strict workspace clippy, the complete workspace suite with exactly the two operator-designated socket tests filtered, UX traceability at 279 active rules, architecture, accessibility semantics, input parity, motion tokens, Core dependency purity, diff and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; the newly declared translatable strings intentionally leave the integration-owned gettext catalog gate red, and display tests were not run)
Task Package-G-reconnect: complete (this commit, base 40ae5c37e1, made the NetworkMonitor Offline-to-Online transition remove only its stale offline notice, retained provider-specific failures, replayed transient podcast actions only until the first worker refusal, and restored that action plus everything behind it in original order instead of silently losing clicks; synchronized active NET-3c with the implemented refusal semantics, and observed both rule-named tests red before implementation and green afterward; format, strict workspace clippy, the complete workspace suite with exactly the two operator-designated socket tests filtered, UX traceability at 279 active rules, architecture, accessibility semantics, input parity, motion tokens, Core dependency purity, diff and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; display tests and the gettext catalog gate were not run)
Fix Package-G-POD-13: complete (this commit, base ec9e35811c, corrected the final rule audit's tooltip-only regression by keeping the classified failed-download reason in its tooltip while restoring the binding keyboard- and touch-visible second line beside Download failed; the existing pure rule-named test was changed first and observed failing against the tooltip-only projection, then green after the implementation, while the pre-existing ignored display assertion remains aligned; format, strict workspace clippy, the complete workspace suite with exactly the two operator-designated socket tests filtered, UX traceability at 279 active rules, architecture, accessibility semantics, input parity, motion tokens, Core dependency purity, diff and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; display tests and the gettext catalog gate were not run)
Fix Package-G-Radio-connectivity: complete (this commit, base bad722f019, kept the Radio connectivity seam from dismissing dead-stream and other provider failures when NetworkMonitor reports Online, while still clearing an offline-only notice and clearing tracked failure state after a successful playback snapshot; synchronized active NET-3b, corrected the seam's stale documentation, and extracted the pure projection plus its rule-named red-green test so radio_view.rs remains below 800 lines; format, strict workspace clippy, the complete workspace suite with exactly the two operator-designated socket tests filtered, UX traceability at 279 active rules, architecture, accessibility semantics, input parity, motion tokens, Core dependency purity, diff and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; display tests and the gettext catalog gate were not run)
Stage Package-G-closeout: complete (this commit, base e2e1beb955, Package G is fully implemented across commits 40984396c2, eae768d347, 678e8f87dc, 7157907670, 25f59374a5, 40ae5c37e1, ec9e35811c, bad722f019 and e2e1beb955: cached lists survive fetch failures behind one neutral reusable banner, genuinely empty sources use the shared full-area failure state, both surfaces share collapsed copyable monospace Details, typed per-subscription failures select their safe provider-specific copy and actions, three or more failures become one bounded named notice, rows consume the explicit connectivity projection, NetworkMonitor drives the existing Podcast/YouTube/Radio seams, offline actions replay without losing refused work, Radio retains its live-stream exception and directory URL recovery, and successful results remove their surface silently. The old Deferred POD-16-toasts item is resolved: every remaining podcast show_error call now logs its technical input but renders only the fixed safe action message, while refresh failures use the five-state taxonomy. NET-3, NET-3a, NET-3b, NET-3c, NET-3d and POD-17 are active and synchronized; POD-13 remains binding with its visible classified download reason as well as the requested tooltip. Final format, strict workspace clippy, the complete workspace suite with exactly the two operator-designated socket tests filtered, UX traceability at 279 active rules, architecture, accessibility semantics, input parity, motion tokens, Core dependency purity, diff, owned-path and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed. The integration-owned gettext catalog gate was intentionally not run and is expected red for the declared N_! strings; display tests were not run because the sandbox cannot bind their private display/session sockets, so banner geometry, Details/Copy interaction, real NetworkMonitor transitions and pointer/keyboard/touch presentation remain for the operator. No repository lock or coordination board exists in this checkout, no user data or live desktop was used, and no push was performed.)
Integration sync-and-errors: complete (branch feature/sync-and-errors, base b5467e6ae5 then merged with origin/dev at 85cad3d87f, integrating packages A (Plugins as the single settings home, SET-10/SET-11 replacing SET-6a/SET-7/SET-9), B (fresh-install online gate off with NET-2a grandfathering and the NET-4 discovery banner), C (one active MTP device with a stable identity and remembered devices, MTP-47/48/49 replacing MTP-3), D (the five-state SourceErrorKind taxonomy, NET-3d), E (three shared device-sync pickers, MTP-50 replacing MTP-37), F (podcast refresh backoff and typed yt-dlp diagnostics) and G (the error and offline surface); 279 active rules covered, workspace suite green with reprise-platform-linux run single-threaded, display suite run by the operator because the Codex sandbox cannot bind its private D-Bus socket, gettext catalogs complete for de and es)
Fix integration-grandfather-evidence: complete (base 15e69e830e, review found that the online-gate evidence check counted five data traces and none of them can see Concerts or New Releases, so a user following only those got the gate switched off on update while NET-2 had already left their module enabled — and because the first-enable one-shot was never marked for them, their next master toggle counted as a first ever enable and overwrote every module key; ONLINE_MODULES now names in core which modules reach the network, an explicitly enabled one counts as demonstrable use, and the first enable only seeds a module nobody has decided; first_enable_turns_every_online_source_off_except_radio asserted the destructive behaviour and was rewritten to the fresh-install case it always meant)
Fix integration-mtp-47-takeover: complete (base ee60bc0ece, added the missing test for the transition review named riskiest for the single-session rule: unplugging the active device mid-transfer with a second attached, asserting the waiting device is inspected exactly once, the concurrent-transfer ceiling stays at one, and the takeover starts no copy; verified against a deliberately inverted expectation first so it measures the takeover rather than passing by accident)
Fix integration-radio-classifier: complete (base 288b920e96, gave RadioError the single classify() PodcastError already had, replacing a hand-rolled match on the MCP surface that had already lost information by collapsing source-gone and rate-limited into one message; annotated NET-3d to say the core contract is implemented while its surfaces are not, matching what NET-3b and NET-3c say about themselves)
Fix integration-ytdlp-typing: complete (base 9051319feb, finished a half-done migration whose landed half was worse than either end state — classify_ytdlp_message was already deleted while five sites still built the untyped PodcastError::YtDlp, which mapped unconditionally to unreachable, so a missing yt-dlp binary would never have reached "the helper needs an update"; every construction site now names a kind, the untyped variant is gone so the compiler enforces it, and three kinds were kept apart rather than folded together because each already had its own user-facing sentence)
Fix integration-device-connectivity: complete (base fcd6ccc87e, closed package C's TODO now that package G had built the route: device sync takes the app's connectivity and metered belief from ui::window::source_connectivity instead of having a seam nothing fed, so the preparation phase no longer plans against a value that never changed; the allow(dead_code) on both setters is gone because they have production callers)
Fix integration-offline-clobber: complete (base 1f4b6b21f0, review found the same defect in the podcast and radio views: entering offline posted its notice unconditionally, rewriting a 404 or dead-station notice as offline and discarding what it named, after which reconnect correctly cleared it as transient — the source was still gone and the app never said so again; both guards now refuse to post the offline notice over anything more specific, and the new tests drive the writing path the existing ones never touched)
Task Package-H-core: complete (this commit, base 33bc3866d2, Concerts and New Releases refresh results retain classified source failures instead of discarding them behind a count, raw provider status remains available only through SourceError::details, MusicBrainz 429 maps to the shared rate-limit state, and missing Concerts credentials remain a distinct configuration outcome; focused tests were observed red before implementation and green afterward, format, strict workspace clippy, the complete workspace suite with exactly the two operator-designated socket tests filtered, UX traceability, architecture, accessibility semantics, input parity, motion tokens, Core dependency purity, file-size checks and cached audit with only accepted RUSTSEC-2024-0436 passed)
Task Package-H-surfaces: complete (this commit, base c1a7f4dc08, Concerts and New Releases now reuse the shared neutral banner, full-area failure state and collapsed copyable Details block while retaining cached lists, update ages, filters and fetch affordances; successful fetches clear failures silently, missing Concerts credentials route to its Plugins row rather than retry, and the window's sole NetworkMonitor feeds both new explicit connectivity seams without overwriting provider failures; active NR-21 and CONC-11 record the behavior and their rule-named pure tests drive the offline writing and reconnect paths; format, strict workspace clippy, the complete workspace suite with exactly the two operator-designated socket tests filtered, UX traceability at 281 active rules, architecture, accessibility semantics, input parity, motion tokens, Core dependency purity, diff and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; the integration-owned gettext catalog gate and all display tests were intentionally not run, no repository lock or coordination board exists in this checkout, no user data or live desktop was used, and no push was performed)
Task Package-H-concerts-releases: complete (commits c1a7f4dc08 and 10161141f0, base 33bc3866d2, brought Concerts and New Releases onto the shared failure surface package G had built for Podcasts, YouTube and Radio — the same banner over a surviving cached list, the same collapsed copyable Details block, the same full-area state for a genuinely empty source, and the same explicitly-set connectivity from ui::window::source_connectivity rather than a second NetworkMonitor reader; missing Concerts credentials stay a configuration outcome that opens the Concerts Plugins row instead of being dressed as a network failure; activated CONC-11 and NR-21 with rule-named tests, one of which drives the offline writing path that the podcast and radio views had got wrong; leaving these two views out of package G was a scoping mistake, not a decision)
Fix integration-catalogs-H: complete (this commit, base 10161141f0, translated the nine strings package H declared into German and Spanish so the gate stays complete)
Task POD-17-tags-and-names: complete (commit 1455f335, base 85cad3d8, newly downloaded podcast and YouTube episodes now carry Title, Album/show, Artist, Album Artist/show and one UTC recording day written best-effort to the unpublished temporary before its final size is recorded; YouTube channel listings ask yt-dlp for their approximate upload timestamps, missing dates still import and can be filled later without replacing an exact feed date; phone paths are now `<Show>/<YYYY-MM-DD> - <Title>.<ext>`, byte-capped and collision-disambiguated only when needed by the stable episode id, while Reprise inventories `.audio` files instead of silently re-copying them forever. The owner's VOID PREACHER channel gives all 27 videos the same title, so beyond the date no naming or tagging scheme can tell them apart in a player. Downloads made before this change keep no tags, and the owner will re-download his one legacy file himself; a file named `.audio` remains invisible to Android's media scanner because the `is_audio_file` change fixes only Reprise's own inventory. Anyone who had already synced gets one-off device churn: with removal enabled the old names are deleted and the new names copied, while with removal disabled both remain until manually cleaned up. Format, locked strict workspace clippy, the locked full workspace suite, UX traceability at 278 active rules, architecture including file-size and core-purity checks, and the unchanged yt-dlp reserved-output-path regression all passed; the rule-named display gate was run outside this sandbox on exactly the implementation working tree and reported `passed: 217`, `failed: 0 of 217`)
Merge integration-dev-second: complete (this commit, merged origin/dev at 64291f8149 into feature/sync-and-errors, resolving four real collisions: dev and this branch had both claimed schema v49 — dev's `db_podcasts_radio::migrate_v49` keeps it because it is already in users' databases and ours became v50 with SUPPORTED_SCHEMA_VERSION 50; both had claimed rule ids MTP-47 and POD-17, so ours moved to MTP-48/49/50/51 and POD-19 while dev's episode-naming and tagging rules keep theirs, renamed in the rulebook and in every rule-named test; dev's new `PodcastError::TagWrite` joined the taxonomy in the "try again" bucket because it is transient and offers the same action; dev's new `pipeline_tag_tests.rs` assumed the pre-`NET-2a` default and had to enable the global gate, which is the new contract rather than a workaround — the module flag alone is not consent)
Fix integration-pipeline-size: complete (this commit, base as above, both sides had grown `pipeline.rs` past the 800-line gate, so the in-process refresh backoff state moved to `pipeline_retry.rs`; recorded there rather than silently carried: its key includes the *address* of the Connection, so a dropped connection and a new one allocated at the same address inherit each other's backoff — harmless with one long-lived connection, but in tests it can make a fresh database believe it is already in backoff, and fixing it properly means giving `Db` a process-unique id, which is a change to the database boundary and not to this file)
Task Podcast-playing-marker-1: complete (this commit, base 31d8fa062a, activating the already-loaded podcast or YouTube episode now routes through the existing play/pause transport instead of rebuilding its external-media session; a pure activation decision was observed red before implementation and green afterward, format, strict workspace clippy, the full workspace suite and audit with only accepted RUSTSEC-2024-0436 passed)
Task Podcast-playing-marker-2: complete (this commit, base cde64fb351, grouped podcast rows and YouTube channel-detail rows now reuse the canonical animated playing marker, preserve the play/pause hover affordance above it and carry a visible accent edge plus tint; NAV-10a source and CSS-token tests were observed red before implementation and green afterward, format, strict workspace clippy, the full serial workspace suite and audit with only accepted RUSTSEC-2024-0436 passed)
Task Podcast-playing-marker-3: complete (this commit, base b84f56df57, the external podcast phase now reaches both episode surfaces as an EpisodeMark, Resolving and Playing animate the canonical marker while Paused and Failed freeze it and swap the hover glyph, and an unchanged episode id updates the registered widgets without rebuilding either list; pure phase and render-decision tests were observed red before implementation and green afterward, format, strict workspace clippy, 1219 non-display reprise-gnome tests, the full serial workspace suite, UX traceability at 283 active rules, architecture including file-size and core-purity checks, and audit with only accepted RUSTSEC-2024-0436 passed; the isolated podcast-backlog CUA run could not start Xvfb because the sandbox denied its private X11 socket, so marker animation and hover remain a manual display check, no real library or live desktop was used, no repository lock or coordination board exists in this checkout, and no push was performed)
Task Podcast-playing-marker-4: complete (this commit, base 2df1eabc2b, the rulebook half of the task that the implementation run skipped: POD-20 records both halves of the behaviour — the shared marker on every surface the loaded episode appears in, and activation toggling pause instead of reopening the session — as the podcast counterpart to RAD-1. POD-17 and POD-18 were already taken by the episode-naming work, so this is POD-20. The two pure decision tests were renamed to carry the id, which is what binds the gate to the rule, and the decision helper carries the rule reference. Format, UX traceability at 284 active rules, strict workspace clippy, 1219 non-display reprise-gnome tests, and the 14 affected rule-named display tests run individually under Xvfb outside the implementation sandbox — 14 passed, 0 failed — all passed; no push was performed)
Task POD-21 episode-neighbours: complete (this commit, base dabecd1efd, external podcast and YouTube sessions now own a frozen rendered-order neighbour snapshot; main and compact transport plus MPRIS route through that session, automatic advances skip up to three failed episodes, direct failures retain their existing stop behavior, Radio remains context-free, and Now Playing renders external identity while hiding Lyrics; POD-21 is active and the reversed podcasts-radio MPRIS decision is documented)
Assumption POD-21: the rewritten Podcasts view no longer uses `PodcastsModel::store()` as the plan's verified anchor claimed, so the snapshot is taken from its actual full `SourceGroup` render model, including collapsed and played episodes; YouTube channel detail snapshots its own projected rendered list. No decision in the plan's "Decisions already taken" section was changed.
Verification POD-21: focused red-green tests, locked format and strict workspace clippy, all static merge-readiness scripts, UX traceability at 284 active rules, Core dependency purity, file-size and diff checks, and cargo audit with only accepted RUSTSEC-2024-0436 passed; the complete runnable locked workspace suite passed with exactly the two sandbox-denied MCP Radio `TcpListener::bind` tests skipped, after one unrelated yt-dlp log-capture test passed on isolated rerun.
Manual POD-21: three rule-named GTK display tests were written and compiled but could not be run in this sandbox as directed, so button sensitivity, external header/tile rendering, Lyrics fallback and visible GTK behavior remain unverified; the clean merge-readiness command remains non-green because warning-free rustdoc already fails on the unchanged `source_error.rs` `Display` link, while private D-Bus and the two MCP Radio listener tests cannot bind sandbox sockets. No repository lock or coordination board exists, no user data or live desktop was used, and no push was performed.

# Source-list reveal and unsubscribe de-duplication

Task 1: complete (this commit, base 2a63e13a82, added the shared pure SRC-12 reveal policy and 1.5-second user-scroll grace with four non-display tests; a narrow temporary dead-code allowance keeps the plan's task boundary warning-free until the source views consume the policy in Task 4; format, strict workspace clippy, the complete workspace suite, and audit with only accepted RUSTSEC-2024-0436 passed)
Task 2: complete (this commit, base 5bbd898e39, added the pure podcast/YouTube reveal target that locates the owning group and reports when an episode lies beyond the ten-row preview, with five non-display tests; format and strict workspace clippy passed, the first parallel workspace run exhausted temporary space and cascaded into ENOSPC failures, the unchanged suite then passed serially without deleting any external files, and audit reported only accepted RUSTSEC-2024-0436)
Task 3: complete (this commit, base f6f0cbd129, added variable-height episode centering from the row's real compute_point geometry, bounded post-render layout waiting, reduced-motion jumping, and Adwaita motion-token animation with three pure geometry tests; adversarial review corrected the sample implementation to stop the previous animation through the shared replace_animation helper before starting a replacement; format, strict workspace clippy, the complete serial workspace suite, and audit with only accepted RUSTSEC-2024-0436 passed)
Task 4: complete (this commit, base 00344fc8ca, wired podcast and YouTube episode identity changes through the shared reveal policy, distinguished synchronous in-view activation from external changes, tracked scroll activity, expanded the source and preview window before centering the rendered row, and kept phase-only marker updates viewport-neutral; origin/dev already owned SRC-12 for episode bulk selection, so the reveal contract and its earlier provisional references were corrected to the next append-only ID SRC-13 rather than violating the binding rulebook; podcasts_view.rs remains below the 800-line gate at 795 lines, focused tests were observed red before implementation and six SRC-13 tests passed afterward, format, strict workspace clippy, the complete serial workspace suite, and audit with only accepted RUSTSEC-2024-0436 passed; origin/dev advanced independently to e64375c5e9 after Task 0, without changing any Task 4 implementation symbol)
Task 5: complete (this commit, base cf419e40e0, added the pure visible-row station locator and wired Radio view entry through the shared SRC-13 policy to the existing uniform-row scroll-center calculation, leaving filtered-out and disconnected stations untouched; the reveal mechanics and scroll-activity state live in radio_reveal.rs so radio_view.rs remains below the file-size gate at 798 lines, and the focused two tests were observed red before implementation and green afterward; format, strict workspace clippy, 1255 non-display reprise-gnome tests and audit with only accepted RUSTSEC-2024-0436 passed; the first workspace run exposed the sandbox's read-only home cache and passed after XDG_CACHE_HOME was isolated inside this worktree, after which the complete suite passed until exactly the two inherited MCP Radio listener tests were denied at TcpListener::bind with Operation not permitted)
Task 6: complete (this commit, base 716f7a115e, removed the grouped episode row's hover-only play/pause glyph and marker swap, reduced its thumbnail and activation interfaces accordingly, kept the marker stable through phase restyling, and added the existing reprise-hover utility class to the plain Box row; YouTube channel detail retains its persistent thumbnail play glyph but no longer swaps it with the marker on hover, and source tests pin both surfaces; the two planned tests were observed red before implementation and seven relevant POD-20 tests passed afterward, format, strict workspace clippy, 1257 non-display reprise-gnome tests, the complete serial workspace suite with the two inherited sandbox-denied MCP Radio listeners explicitly skipped, and cached audit with only accepted RUSTSEC-2024-0436 passed)
Task 7: complete (this commit, base 1a79dc0eef, removed the duplicate podcast header unsubscribe star and the inaccessible radio hover remove star while retaining both context-menu actions, simplified group_header to return only its widget, and removed Radio's now-unused removal callback seam; the planned non-display tests were observed red before implementation and four SRC-4 tests passed afterward with the display-gated header test remaining ignored in the ordinary suite, then that test passed alone under an isolated private D-Bus/Xvfb/XDG session; adversarial review corrected the inline radio source test so its own icon-name assertion did not make itself fail, the still-used dead podcast table helper remains untouched as planned, and format, strict workspace clippy, 1260 non-display reprise-gnome tests, the complete serial workspace suite with the two inherited sandbox-denied MCP Radio listeners explicitly skipped, and cached audit with only accepted RUSTSEC-2024-0436 passed)
Task 8: complete (this commit, base c4c031916d, activated SRC-13 as the append-only reveal contract, amended SRC-4 to make the context menu the sole unsubscribe or favorite-removal surface, and amended POD-20 so the shared marker stays stable under the app-wide hover tint; UX traceability passed with 287 active rules covered, and the unchanged code remained green under format, strict workspace clippy, 1260 non-display reprise-gnome tests, and cached audit with only accepted RUSTSEC-2024-0436)
# YouTube verification-required downloads

YouTube browser-session fix: complete (this commit, base 31d8fa062a, current installed yt-dlp 2026.07.04 and ffmpeg 8.1.2 were present while five live download attempts failed as `verification_required`; added the explicit `REPRISE_YTDLP_COOKIES_FROM_BROWSER` opt-in and forwards its validated browser selector to listing, search, resolve and download without exposing cookies, URLs, profile paths or provider stderr, while local version/update maintenance never reads the browser session; four focused behavior tests and an isolated production-discovery child-process regression cover the boundary, with the latter mutation-checked red when the environment wiring was deliberately broken and green after restoration; format, strict workspace clippy, the full unfiltered workspace suite, architecture, UX traceability with 283 active rules, QA linters, Core dependency purity, Rust file-size checks and audit with only accepted RUSTSEC-2024-0436 passed; no real browser profile, YouTube account, user database, music file or live desktop was accessed, and no push was performed)
Task YouTube-browser-recovery: complete (commits fc4e18d75b, ef14552c96 and 733abc50b1, base 31d8fa062a, replaced the diagnostic-only environment seam on every product path with an authoritative privacy-default browser setting, added the Plugins selector plus selected-browser YouTube sign-in action and fixed manual fallback, routed verification-required downloads to a persistent leak-safe and gettext-backed failed-row explanation, and covered listing, range listing, both searches, resolve and download while proving probe/update cookie-free; the opt-out was mutation-observed red before implementation, both adversarial re-reviews cleared, format, strict workspace clippy, full unfiltered workspace tests with 1872 Core tests and 1218 GNOME tests plus 363 expected display ignores, strict Rustdoc, architecture, frontend thinness, accessibility, input parity, motion, gettext including the checked-in POT and all seven catalogs, UX traceability at 284 active rules, Core dependency purity, file sizes and live audit with only accepted RUSTSEC-2024-0436 passed; the two new real-widget assertions compile but could not initialize GTK because this sandbox denies both Unix and TCP listener sockets and this GTK build has no headless backend, while private-Xvfb CUA cannot attach across display namespaces, so visual geometry and clicking remain an explicit manual check; no real browser profile, YouTube account, user database, music file or live desktop was accessed)
Merge YouTube-browser-recovery-latest-dev: complete (this commit, integrated dev at a228907f61, preserved dev's podcast playing-marker rule as POD-20 and moved the YouTube browser-recovery rule plus every rule-named test to POD-21; a parallel full-suite run exposed Linux ExecutableFileBusy while launching fresh yt-dlp test fixtures, so a deterministic red test now guards a four-attempt 10 ms retry limited to that transient kernel error while all other start failures remain immediate; the regression and the nine-test subprocess group passed five consecutive runs, format, strict workspace clippy, the full unfiltered workspace suite with 1879 Core tests and 1602 GNOME tests, strict Rustdoc, architecture, frontend thinness, accessibility, input parity, motion, gettext, device-sync runtime, runtime-service install, UX traceability at 286 active rules, Core dependency purity, file sizes and cached audit with only accepted RUSTSEC-2024-0436 passed; the two POD-21 real-widget tests passed under isolated host Xvfb, the complete rule-named display run passed 221 tests before five GTK-initialization failures and all five passed isolated reruns, so widget behavior is covered but visual appearance and clicking remain a manual check; no real browser profile, YouTube account, user database, music file or live desktop was accessed)
Merge YouTube-browser-recovery-current-dev: complete (this commit, integrated dev at 2a63e13a82, preserved its episode-neighbour rule as POD-21 and moved the YouTube browser-recovery rule plus every rule-named test to POD-22; format, strict workspace clippy, the complete unfiltered workspace suite with 1879 Core tests and 1612 GNOME tests, strict Rustdoc, architecture, frontend thinness, accessibility, input parity, motion, gettext, device-sync runtime, runtime-service install, UX traceability at 287 active rules, Core dependency purity, file sizes and cached audit with only accepted RUSTSEC-2024-0436 passed; running both POD-22 display tests together exposed GTK's same-thread initialization constraint after the first test passed, and isolated private D-Bus/Xvfb reruns then passed one of one for each test, so their widget state is verified while visual appearance and clicking remain a manual check; no real browser profile, YouTube account, user database, music file or live desktop was accessed)

Fix NR-16 featured credits: complete (this commit, base 31d8fa062a, MusicBrainz release groups now count the library artist only before a `feat.`/`ft.` guest-credit boundary while preserving `&` co-headliners, and a successful refresh atomically removes previously cached false positives only when they were attributed to that guest artist; the public refresh-to-query regression was observed red for both a fresh guest row and a seeded cached row before the respective implementation, then green, and the complete artist-news group, format, strict workspace clippy, full workspace suite, audit with only accepted RUSTSEC-2024-0436, UX traceability at 283 active rules, architecture, Core dependency purity, diff and file-size checks passed; the isolated CUA attempt never started Reprise because Xvfb could not create a private display socket under `/tmp/.X11-unix`, so the running GTK list remains visually unverified; no real user data, live desktop, push, or PR was used)
Fix NR-16 merge-readiness docs: complete (this commit, base 5a1619371e, the required clean-tree gate exposed a pre-existing broken intra-doc link on unchanged `origin/dev`; qualifying `Display` as `std::fmt::Display` is the smallest documentation-only repair, and the exact warning-free workspace rustdoc command passed before the complete clean-tree gate rerun)
Task Episodes-as-queue-citizens Package 1: complete (commit cc8b6c1c7c, base 31d8fa062a, made the manual UpNextQueue store typed Track and Episode items, migrated legacy plain-id sessions, resolved mixed metadata with one batched query per item kind, and kept the automatic container Queue track-only)
Task Episodes-as-queue-citizens Package 2: complete (commit cc8b6c1c7c, base 31d8fa062a, separated direct and queued podcast origins, advanced queued episodes without listen credit or gapless pre-feed, silently removed unsubscribed episodes, skipped resolution and playback failures through the bounded queue guard, and collapsed consecutive episode-failure toasts)
Verification Episodes-as-queue-citizens Packages 1-2: format, strict locked workspace clippy, warning-free rustdoc, 1866 Core tests, the complete isolated-XDG workspace suite with exactly the two operator-designated MCP Radio loopback tests filtered, gettext catalogs, UX traceability at 285 active rules, architecture, frontend thinness, runtime installation, accessibility semantics, input parity, motion tokens, device-sync GStreamer, Core dependency purity, diff and file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed; the unfiltered workspace run independently reached only those two inherited tests and reproduced the sandbox's Operation not permitted at TcpListener::bind; display tests and real playback were not run, no real database, music files, or desktop session was used, no repository lock or coordination board exists, and packages 3-5 were not started)
Task Episodes-as-queue-citizens Package 3: complete (commit f53c7eb47b, base 2ae41c9862, rendered mixed typed Track and Episode rows in the Queue and Up Next surfaces with collision-safe metadata, episode title/show/duration projection, source glyphs pending the unmerged artwork branch, blank track-only columns, a hidden and write-rejecting rating boundary, typed queue activation and position-based keyboard reorder, and the planned append-only CTX-11 heterogeneous-selection route; format, strict locked workspace clippy, warning-free rustdoc, locked and unlocked complete workspace suites, architecture, device-sync GStreamer, accessibility semantics, input parity, runtime installation, frontend thinness, UX traceability at 285 active rules, motion tokens, Core dependency purity, diff and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; the live advisory refresh could not take its read-only sandbox lock, and display/runtime-bus readiness checks were blocked before GTK by the sandbox denying private D-Bus socket binding, so the compiled ignored mixed-panel display test remains unclaimed; no real database, music files, or desktop session was used, no push was performed, and packages 4-5 were not started)
Verification correction Episodes-as-queue-citizens Package 3: a subsequent clean-tree `scripts/check-merge-readiness.sh --no-fetch` run reached the rule-named display balance sheet and reported 216 passed with four failures, all in the sibling-owned excluded `ui/podcasts` tree (`SRC-4`, `POD-15`, `SRC-10`, and `POD-12` tests); the wrapper stopped there before its motion-display, CSS-display, runtime-bus, and audit tail, and exact isolated reruns including Package 3's mixed-panel test were blocked before execution by the sandbox denying the private D-Bus socket, so those five tests remain unclaimed here rather than classified; all pre-display readiness gates passed and the implementation-owned non-display workspace tests remain green)
Task Episodes-as-queue-citizens Package 4: complete (commits 5176837205 and 7001bb3f57, base a6ec8267ac, added one collision-safe typed track/episode drag payload, converted the Queue sidebar and Up Next panel drop routes to typed items while playlists reject episode payloads, added grouped podcast/YouTube episode drag sources with whole-selection semantics, and exposed Play Next/Add to Queue through the selection-aware episode menu and its keyboard route with stale/unavailable guards; SRC-4 was split append-only into radio SRC-4a and podcast SRC-4b, CTX-12 was activated, German and Spanish plural feedback was completed, and the two inherited over-limit UI orchestrators were cohesively extracted back under their hard caps)
Verification Episodes-as-queue-citizens Package 4: architecture, device-sync GStreamer, accessibility semantics, input parity, frontend thinness, UX traceability at 288 active rules, motion tokens, gettext catalogs, format, strict locked workspace clippy, both locked and unlocked complete workspace suites, Core dependency purity, diff and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; the live advisory refresh could not acquire its read-only sandbox lock and succeeded with `--no-fetch`; no display test, pointer drag, real database, music files, live desktop, push, or package-5 work was performed)
Verification correction Episodes-as-queue-citizens Package 4: the subsequent clean-tree `scripts/check-merge-readiness.sh --no-fetch` reran and passed every stage through the full workspace suite, then entered the rule-named GTK display batch and produced no result for more than five minutes, so that wrapper was interrupted without classifying or claiming the display tests; during final verification `origin/dev` advanced by two commits (`a228907f61`, `2a63e13a82`) that overlap podcast playback/view files, and this already-tested package was deliberately not silently rebased over that new integration state)
Reconciliation Episodes-as-queue-citizens onto current dev: complete (this commit, base e64375c5e9, rebased Packages 1-4 over POD-20, POD-21 and POD-22; retained independent podcast `origin` and frozen `neighbours`, extended the neighbour snapshot to typed queue items so manual-queue transport follows mixed track/episode order without id collisions, routed every episode surface through POD-20's single external snapshot marker, and made QUE-9 defer marker and neighbour ownership to POD-20/POD-21; format, strict locked workspace clippy, the complete locked unfiltered workspace suite, architecture, accessibility semantics, input parity, frontend thinness, UX traceability at 291 active rules, motion tokens, device-sync GStreamer, gettext with complete German and Spanish catalogs, Core dependency purity, diff and Rust file-size checks, and live audit with only accepted RUSTSEC-2024-0436 passed; display tests were compiled but not run as directed, no real database, music files or desktop session was used, no push was performed, and Package 5 was not started)
Fix NR-16 featured credits: complete (this commit, base 31d8fa062a, MusicBrainz release groups now count the library artist only before a `feat.`/`ft.` guest-credit boundary while preserving `&` co-headliners, and a successful refresh atomically removes previously cached false positives only when they were attributed to that guest artist; the public refresh-to-query regression was observed red for both a fresh guest row and a seeded cached row before the respective implementation, then green, and the complete artist-news group, format, strict workspace clippy, full workspace suite, audit with only accepted RUSTSEC-2024-0436, UX traceability at 283 active rules, architecture, Core dependency purity, diff and file-size checks passed; the isolated CUA attempt never started Reprise because Xvfb could not create a private display socket under `/tmp/.X11-unix`, so the running GTK list remains visually unverified; no real user data, live desktop, push, or PR was used)
Fix NR-16 merge-readiness docs: complete (this commit, base 5a1619371e, the required clean-tree gate exposed a pre-existing broken intra-doc link on unchanged `origin/dev`; qualifying `Display` as `std::fmt::Display` is the smallest documentation-only repair, and the exact warning-free workspace rustdoc command passed before the complete clean-tree gate rerun)
Task Episodes-as-queue-citizens Package 5: complete (commit 2edf0b3627, base a9f87d07b4, added collision-safe typed track/episode lists beside unchanged track-only legacy id fields across runtime protocol, Linux control and MCP queue reads; kept every outward queue write command track-only and its present-track validation intact; gave episode MPRIS metadata the distinct `/org/reprise/Reprise/episode/{id}` identity with title, show-as-artist and length but no album or rating; and carried the typed projection through the GNOME agent mirror)
Verification Episodes-as-queue-citizens Package 5: locked formatting, strict locked workspace clippy, the complete locked workspace suite, architecture, accessibility semantics, input parity, frontend thinness, UX traceability at 288 active rules, motion tokens, runtime-service installation, worker isolation, Core dependency purity, diff and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; one intermediate workspace rerun hit an inherited parallel log-capture flake in a yt-dlp test, whose exact test passed twice before the complete unfiltered suite passed again; the added protocol compatibility test decodes an older dictionary without typed fields, mixed runtime/MCP/MPRIS tests preserve colliding numeric ids without widening legacy fields, and an episode-only database id is still rejected as a track write; GTK display tests were written in earlier packages and compiled but could not be run here because Xvfb is unavailable, so no rendering claim is made; `scripts/check-merge-readiness.sh` was deliberately not run as instructed, no real database, music files, live desktop, push, or unrelated worktree was touched)
Baseline Episodes-as-queue-citizens Package 5: the extra release-only `scripts/check-stem-runtime-packaging.sh` probe remains red on the unchanged base because `build-aux/meson-cargo-build.sh` lacks the two ONNX runtime environment markers the check requires; the package-5 diff does not touch packaging and the merge-readiness static gate set does not invoke this release check, so it was recorded rather than expanded into unrelated work)

# YouTube GUI metadata language

YouTube GUI metadata language: complete (this commit, base e64375c5e9, YouTube listings, extended listings and channel searches ask yt-dlp for metadata in Reprise's active gettext language while retaining the explicit persisted browser choice on every product path; unsupported locales fall back to English source strings, Simplified Chinese maps to YouTube's `zh-CN`, provider originals remain the fallback, and stored titles update through the existing refresh upsert; active POD-23 and its rule-named subprocess test cover the boundary, locale resolution tests cover catalog and LANGUAGE precedence, and the isolated source-youtube CUA fixture exposes its expected title only for `youtube:lang=en`; no product behavior from the signed-in browser recovery or episode-neighbour integrations was replaced)

# Display gate reliability

Task display-gate-worker-tmpdir: complete (this commit, base 66967b66f8, the rule-named display runner now puts each GTK fixture under a worker-owned TMPDIR, derives a 4000-wide X display band from the runner PID so concurrent worktrees stop claiming the same server numbers, and gives only the environment-only `Failed to initialize GTK` signature a second retry; the change is byte-identical to the reviewed `fix/display-gate-worker-tmpdir` implementation with blob fd16952ef99b932a9df8b557f0bf3120ae54c8a9; the pre-fix host run supplied by the owner passed 216 of 220 rule-named tests and all four failures stopped before their assertions at GTK initialization, while shell parsing, format, strict workspace clippy, warning-free Rustdoc, the complete unfiltered workspace suite, architecture, Android sync runtime, runtime-service install, frontend thinness, accessibility, input parity, motion, UX traceability at 288 active rules, Core dependency purity, file sizes and cached audit with only accepted RUSTSEC-2024-0436 passed)
Manual display-gate-worker-tmpdir: rerun the authoritative rule-named display gate on the host before marking PR #184 ready or merging; this sandbox cannot bind the private X11, D-Bus and runtime-service sockets required by that gate.

# Platform crossfade gate determinism

Task crossfade-generation-test-determinism: complete (this commit, base a578e4d745, the stream-generation contract test now starts the real CrossfadeEngine at a deterministic in-window position instead of duplicating the separate backend test's 500 ms position-ticker dependency; delaying the ticker beyond the old 15 s deadline reproduced the owner's exact missing-AdvancedToNext panic before the change, while the corrected test passed in 1.02 s with the same delayed ticker, and five consecutive complete reprise-platform-linux library runs each passed 124 of 124 tests; format, strict workspace clippy, warning-free Rustdoc, architecture, Android sync runtime, runtime-service install, frontend thinness, accessibility, input parity, motion, UX traceability at 288 active rules, file sizes and cached audit with only accepted RUSTSEC-2024-0436 passed; the complete workspace suite passed through Core, GNOME and the affected platform crate, then only the two known MCP Radio listener tests failed because this sandbox rejects TcpListener::bind with Operation not permitted, so the authoritative host merge-readiness rerun remains required)

Owner merge override for PR #184: approved (2026-07-31, after the authoritative host gate encountered a single empty tracing-capture assertion at 1879 of 1880 Core tests; the exact test, all nine yt-dlp process tests, a complete 1880-test Core run, a 128-thread complete Core run, and a 3200-operation concurrent capture stress probe all passed on the same tree, no speculative test weakening was made, and the owner then explicitly instructed Codex to push and merge; the replacement integration branch starts at current dev c565671eb4 because the original PR head had become 19 commits stale, preserving both sides of the append-only ledger and the current podcast module and UX-rule changes)
Task Lyrics-fallback-chain: complete (this commit, base 31d8fa062a, split the lyrics boundary into focused model, LRC, cache, breaker, chain, local, LRCLIB and NetEase modules; added embedded-tag and sidecar lookup, synchronized-over-plain ranking, provider provenance, per-host three-failure five-minute breakers, eight-second provider timeouts, cache v2 with consensus-only negative entries, source-prefixed offline fixtures with the legacy LRCLIB alias, two-phase local-first GTK loading, source-specific footer labels, synchronized LYR-1/2/3/5 rules, and regenerated all gettext catalogs with complete German and Spanish translations; 1898 Core tests and the complete workspace suite passed, strict workspace clippy, format, UX traceability at 285 active rules, gettext catalogs, Core dependency purity, diff checks, bounded fixture reads, and cached audit with only accepted RUSTSEC-2024-0436 passed; display-only GTK tests remained ignored by their established runner contract, no repository lock or coordination board exists, no real library data or live desktop was used, and no push was performed)
Task Lyrics-library-batch: complete (this commit, base db11b05166, added the opt-in serial and cancellable library-wide lyrics cache run after cover completion and completed scans, live-track metadata query, local and cache skip policy, seven-day synchronized-upgrade throttle, all-breakers-open termination, shared ScanControls progress with translated copy, and active LYR-6; adversarial review also bounded both NetEase HTTP steps to one eight-second source budget and kept the composition roots below their architecture limits; 1901 Core tests, cargo build, strict workspace clippy, format, UX traceability at 286 active rules, gettext catalogs, architecture, Core dependency purity, diff checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; the full workspace suite passed except the same two inherited Radio MCP listener tests blocked by sandbox socket EPERM, and the complete MCP suite passed with exactly those two tests filtered while all remaining platform, runtime and stems suites passed; the ignored Lyrics ScanControls display test could not start because the sandbox denied the private D-Bus socket, so no visual GTK claim is made; no repository lock or coordination board exists, no real library data or live desktop was used, and no push was performed)
Task Lyrics-sidecar-writeback: complete (this commit, base 2bbf2c0c70, synchronized network lyrics are atomically published as non-overwriting `.lrc` sidecars beside existing tracks while cache and lookup results survive every write failure; device sync copies and removes the sidecar as an unmetered attachment across music, podcast and YouTube audio paths, ignores it as an independent inventory item, and preserves a newly copied sidecar across same-basename profile replacements; active LYR-7 and the repository safety contract record the behavior; strict workspace clippy, the full workspace suite with 1931 Core and 1261 non-display GNOME tests, format, UX traceability at 291 active rules, gettext catalogs, Core purity and cached audit with only accepted RUSTSEC-2024-0436 passed; no real music file, database, network service or live desktop was used, and no push was performed)
Task Album-cover-writeback: complete (this commit, base b5e0663a6d, downloaded album covers remain in the XDG cache and are additionally published best-effort as `cover.<validated extension>` in every distinct directory derived from matching live track paths; every canonical folder-image name blocks publication, same-directory temporary files use atomic no-replace linking rather than overwrite-capable Unix rename, and write failures leave cache success unchanged while release-group covers remain cache-only; active COVER-1 and the repository safety contract record the behavior; 1940 Core tests, 1261 non-display GNOME tests with 372 expected display ignores, strict workspace clippy, the full workspace suite, format, UX traceability at 292 active rules, gettext catalogs, Core purity and cached audit with only accepted RUSTSEC-2024-0436 passed; no real music file, database, network service or live desktop was used, and no push was performed)

Task wave-0-0.0-green-base: complete (commits da4fed7794 and b3f2154350, base 577765b3b9, restores a green base for the wave-0 parallel agents: `scripts/check-architecture.sh` failed on its very first check because `ui/window/window.rs` sat at exactly 600 lines against a `>= 600` cap, so `build_player_backends` moved byte-identically into the new sibling `ui/window/player_backends.rs` as `pub(super) fn build` — declared in `window/mod.rs` rather than in `window.rs`, whose `#[path = "window.rs"] mod surface;` binding would otherwise have resolved the submodule to `ui/window/window/` — leaving the composition root at 579 lines with the cap untouched and no doc comments trimmed; `scripts/check-frontend-thinness.sh` failed because 65f0b143d6 (#189) spent two budgets without moving them, so `threads` goes 14 to 15 for the new `ui/lyrics/lyrics_batch.rs` worker and `filesystem` goes 17 to 19 for the two `std::fs::metadata` sidecar probes added to `ui/device_sync/device_sync_effects.rs`, with moving that batch worker into `reprise-core` recorded in the commit message as the better follow-up rather than done here; an adversarial Rust review of the diff confirmed the move is behaviour-preserving and the budget attribution correct, and its one finding — the new module's doc comment claiming to be the only window-layer reach for a concrete `reprise_platform_linux` type, which `window.rs:80,141,142,147` and `window_runtime_wiring.rs:149` disprove — was fixed in the same commit; architecture, frontend thinness, UX traceability at 298 active rules, motion tokens, accessibility semantics, input parity, runtime-service install, Android sync runtime, format, strict workspace clippy, warning-free Rustdoc and `cargo audit` with only accepted RUSTSEC-2024-0436 all passed, and the complete workspace suite reported 3881 passed, 0 failed and 401 ignored across 54 suites, identical to the pre-change baseline measured on untouched `origin/dev`; the display-test batch at the tail of `check-merge-readiness.sh` was still running when the branch was pushed and is left for the owner, `scripts/check-stem-runtime-packaging.sh` remains red for missing ONNX markers outside the merge gate, and GitHub Actions fails on every branch in this repository including `main` because the job never reaches a runner; no real music file, database or live desktop was used, and nothing was pushed to `dev` or `main`)
Task stale-stem-runtime-gate: complete (commit db5531bae9, base 577765b3b9, `scripts/check-stem-runtime-packaging.sh` had been red on `dev` since 4745611bed removed the instrumental frontend: it still required `REPRISE_BUNDLED_ORT_DYLIB` and `REPRISE_BUNDLED_ORT_DYLIB_SHA256` in the GTK build script `build-aux/meson-cargo-build.sh`, which that commit correctly dropped because `reprise-gnome` links neither `reprise-stems` nor `ort` and has nothing to point a dylib at, so the check demanded exactly the coupling its sibling `scripts/check-stem-worker-isolation.sh` forbids and the two gates contradicted each other; the stale requirement is removed and a comment records why the GTK build is deliberately unchecked, since `rg --quiet` under `set -e` made the failure silent and it therefore read as an ONNX packaging gap rather than as a gate that had outlived its subject; nothing else is relaxed and the check was mutation-tested — renaming the SHA256 marker in the worker build script still fails it and restoring it passes — with the Flatpak onnxruntime module, its ordering before the app module, the pinned archive URL and SHA512, the build commands, the worker script markers and both `option_env!` reads in `crates/reprise-stems/src/provision.rs` all still asserted; the branch changes no Rust at all, and `scripts/check-merge-readiness.sh` cannot pass on this base because `origin/dev` still fails its first architecture check at `window.rs` with 600 lines, which PR #196 fixes, so the full gate applies once this rebases onto a merged #196; no real music file, database or live desktop was used, and nothing was pushed to `dev` or `main`)

Task lyrics-batch-to-core-handover: complete (commit 1a831fbdad, base e836d125c1, `docs/plans/lyrics-batch-to-core.md` writes up the follow-up that #196 recorded but deliberately did not do — moving the library-wide lyrics batch decision logic out of `ui/lyrics/lyrics_batch.rs` into `reprise-core` beside the providers, cache and breakers it already depends on; the document is grounded against the tree at e836d125c1 with file:line citations verified rather than written from memory, and it corrects two open assumptions from the #196 message: the move does NOT lower the `threads` budget because every worker in this repository keeps thread ownership in `reprise-gnome` and exposes a synchronous callback-driven function from core in the manner of `library::scanner::scan_folder_with_progress`, so `thread::Builder::new()` merely relocates to the surviving adapter, and `filesystem` never moves at all since `lyrics_batch.rs` contributes zero to it while the two matches #189 spent were the `device_sync_effects.rs` sidecar probes; `ui/cover/cover_download_batch.rs` is identified as the structural twin with the same defect rather than as a template, and whether it is in scope is raised as an explicit question for the owner; the hard-won items recorded are that LYR-6's "after completed library scans" trigger is transitive through a process-lifetime always-alive subscription on the cover batch so a one-shot rewrite would break an `[active]` rule with no failing test, that `Db` is deliberately neither `Send` nor `Sync` so background work must re-open by path as `scan_worker.rs` and `concerts_worker.rs` do, that exactly one line — `glib::spawn_future_local` — is load-bearing GTK while every other apparent coupling is dependency-pure, that `reprise-core::concerts::CancellationToken` already supplies the cancellation half of the template while the scanner supplies the progress-callback half, and that core exposes no single skip-policy-aware entry point so the batch's two-stage decision duplicates what `load_or_fetch_with_options` derives internally; LYR-6 keeps its ID and text because this is a pure refactor, but the document asks for the level tag to become `[core] [gtk]` in the same commit and notes that `check-ux-traceability.sh` never verifies tags against test locations so the omission would not be caught; the 11 pure and 4 progress tests are enumerated with the two irreducibly-GTK display tests marked as unable to move; architecture, frontend thinness and UX traceability at 298 active rules passed on the documentation-only change, and nothing was pushed to `dev` or `main`)
Task lyrics-batch-to-core: complete (commits df85a68564 and this commit, base dd004e8465, moved the serial library-wide lyrics batch decision loop and its pure tests into `reprise-core` while GNOME retains thread, channel, permanent-subscription and GLib reconciliation ownership; unified `needs_fetch` and lookup cache policy through one shared `Skip`/`Fetch`/`RetryForSynced` cache decision carried through a private provider-bundled lookup seam, with exact record-and-time snapshot revalidation preserving the prior result when a concurrent lookup publishes a newer cache record or the classification crosses its TTL boundary; retagged LYR-6 `[core] [gtk]` without changing its ID or text and covered all three triggers including the permanent rescan re-arm; frontend thinness budgets remain deliberately unchanged at rusqlite 112, filesystem 19, threads 15 and workers 7; `scripts/check-merge-readiness.sh --no-fetch` is green with 3885 workspace tests passing and 0 failing, against 3879 on the base — the six pure batch tests moved across the crate boundary, taking `reprise-core` from 1958 to 1968 and `reprise-gnome` from 1316 to 1310 (376 ignored, unchanged), and the review also removed `lyrics::needs_fetch`, which the shared classification left without a single caller; follow-up: move the structural twin `ui/cover/cover_download_batch.rs` decision logic into `reprise-core` separately)

Task Releases-fetch-progress: complete (this commit, base 18f9b129dc, the full Releases footer now replaces a stale update age with an immediate fetching state and determinate checked/total artist progress, then measures the refreshed age from the successful whole-run completion rather than the network start; successful empty runs persist a completion timestamp while partial or failed runs retain the previous cache age, and legacy per-artist ledgers are seeded before a failed attempt can rewrite that compatibility fallback; NR-6 is append-only replaced by active NR-22 with core, presentation and real button-to-worker GTK wiring coverage, and the new messages are synchronized across every catalog with complete German and Spanish translations; focused tests were observed red before implementation and green afterward, format, strict workspace clippy, warning-free Rustdoc, the complete workspace suite with exactly the two sandbox-denied MCP Radio listener tests filtered, UX traceability at 300 active rules, architecture, accessibility semantics, input parity, frontend thinness, motion tokens, runtime-service installation, Android sync runtime, Core purity, diff and Rust file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; the unfiltered suite reached those same two inherited TcpListener `Operation not permitted` failures, and the ignored NR-22 display test compiled but could not start because the sandbox denied the private D-Bus socket, while CUA additionally reported no reachable AT-SPI bus or top-level X11 windows, so no visual rendering claim is made; no real database, music files, live desktop, push, or unrelated dirty worktree was touched)

# Advance after deleting the loaded track

Task loaded-track-delete-advance: complete (this commit, base bc0cc3ab88, a successful explicit Remove from library or Move to Trash action now advances a deleted loaded track through the normal automatic queue path, so Play Next wins, unavailable entries are skipped, the frozen snapshot continues and an exhausted queue stops; failed deletions and deletions of other tracks do not advance, while watcher, scanner, startup and maintenance purges retain PLAY-5a/PLAY-5b's non-interrupting open-stream behavior; BROWSE-8 is replaced by active BROWSE-11 and its queue, repeat-one, focus and metadata-navigation tests; the rule-named regression was observed red before implementation and green afterward, format, strict workspace clippy, architecture including accessibility and input parity, UX traceability at 301 active rules, Core dependency purity, diff and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed; the final workspace proof passed with exactly two sandbox-blocked Radio MCP listener tests and one independently proven GStreamer parallel flake filtered, while the flaky test passed five exact reruns and the complete 126-test platform library serially; isolated CUA never started Reprise because the private D-Bus socket bind was denied with Operation not permitted, so visible desktop behavior remains a manual check; the normal automatic advance was chosen as the narrowest existing transport contract, no real music file, database or live desktop was used, and no push was performed)

Task Multi-surface-P0-1: complete (this commit, base 1209983c13, added the empty MIT `reprise-view` workspace crate with its deliberate `reprise-core` edge and recorded the live baseline as 3490 passed with 378 display-gated ignores; the post-change suite differed only by the two baseline MCP Radio socket-bind failures, format and strict workspace clippy passed, and cached audit reported only accepted RUSTSEC-2024-0436 after the sandbox blocked the updating audit from locking the read-only advisory database)
Task Multi-surface-P0-2: complete (this commit, base e44f7a3b9a, added a fail-closed all-features and all-targets dependency-tree gate for `reprise-view`; mutation proof rejected `zbus v5.18.0` with `reprise-view must not depend on GTK, libadwaita, GLib, GStreamer, or zbus`, rejected `reprise-runtime-protocol` with `reprise-view may depend on reprise-core only; found:`, and passed after both temporary edges were removed; architecture, format and strict workspace clippy passed, the workspace suite differed only by the exact two baseline MCP Radio failures, and cached audit reported only accepted RUSTSEC-2024-0436 after the updating audit hit the same read-only sandbox lock)
Task Multi-surface-P0-3: complete (this commit, base 31798ce57c, recorded active ownership for the multi-surface spec, P0 files and parked plans while leaving every `reprise-gnome` path excluded until P1a opens; the completed episodes ownership remains immediately above the new final top-level heading, architecture, format and strict workspace clippy passed, the workspace suite differed only by the exact two baseline MCP Radio failures, cached audit reported only accepted RUSTSEC-2024-0436 after the updating audit hit the read-only sandbox lock, no repository lock or coordination board existed to release, and Part B was not started)

# Tag Editor save no longer throws the library to the top

Task tag-save-viewport-jump: complete (this commit, base b232cee2f5, saving any tag from the Tag Editor threw the track table to the very top of the library and selected whatever row happened to sit there, before a later reload pulled it back — the reported "jumped to the top and then back"; the cause is not the save reload but `TransientFocusGuard::capture`, which remembered the focused widget itself, and opened from the table that widget is a recycled `GtkColumnView` row: after the save's `items_changed(0, old, new)` the same widget is bound to a different row, so `restore()` focused it and GTK scrolled and selected there; `capture` now remembers the enclosing ColumnView/ListView/GridView instead, which is never recycled and keeps its own focus row, and `try_focus` accepts a focus a container handed on to a child so the fallback no longer fires; this fixes every one of the 26 capture call sites, not just the Tag Editor, so the context menu, delete dialog, playlist export and issue views stop jumping too; three new TAG-1 display regressions sample the vertical adjustment once per rendered frame — the existing TAG-1 test only asserts the settled end state and is blind to a transient jump — and the focus-restore one was observed red at 6662 px dropping to 0 px, green after the fix, and red again in a deliberate counter-check with the fix disabled, in both the debug and release profile and with and without a window manager; the earlier hypotheses of the doubled save reload, the sidebar refresh and a plain `column_view.grab_focus()` were each disproved by a green test rather than assumed; format, strict workspace clippy and the complete workspace suite passed with zero failures; the redundant idle reload in `refresh_after_tag_mutation_with_anchor` remains untouched wasted work and is deliberately left to its own change; no real music file, database or live desktop was touched and nothing was pushed)

Task tag-save-viewport-jump-2: complete (this commit, base 4fc648116f, the first attempt above did NOT fix the reported jump and its green test was measuring the wrong thing: sampling the viewport on the frame clock returns no samples at all when nothing damages the widget, so the assertion passed vacuously; sampling on a timer instead shows the table standing at the top of the library for some 500 ms after every tag save, with or without the earlier `stable_focus_target` change and with or without the doubled reload, so neither was the cause; the actual cause is that `items_changed(0, old, new)` resets a `GtkColumnView`'s focus row to the top, and the dialog's focus restore then makes GTK reveal that row — remembering the view rather than the recycled row never addressed it, and a scroll pin around the focus change was measured to be ineffective and removed rather than kept as insurance; `TransientFocusGuard::restore` now focuses the row already visible at the view's top edge via `ListScrollFlags::FOCUS`, which has nothing to reveal and therefore cannot scroll; the save refresh additionally re-queries once instead of twice, since only the deferred reload can be trusted to rebind rows behind a closing dialog, with one test pinning the query count and another proving the written tag still reaches the visible cells; every display regression was observed red before and green after, and the focus fix was re-checked red with it disabled; format, strict workspace clippy and the complete workspace suite passed with zero failures across 40 suites; the genre column is hidden in the default layout, so the tests edit the album instead — a genre edit would have proven nothing about what reached the cells; no real music file, database or live desktop was touched)
# START-1 library-first startup

Task 1: complete (this commit, base 107f9cdd6b, a normal start now routes to the library root with only the remembered sort while stale places, searches, facets, anchors and selection are discarded; the rule-named regression was observed red before implementation and green afterward, format, strict workspace clippy, the complete unfiltered workspace suite with 1323 GNOME non-display tests and 378 expected display ignores, and audit with only accepted RUSTSEC-2024-0436 passed)
Task 2: complete (this commit, base d218ff3cb1, session restore now sets the loaded-track marker and paused equaliser state without claiming the pre-routing viewport; the rule-named policy regression was observed red before implementation and green afterward, the complete unfiltered workspace suite passed twice as required, format, strict workspace clippy and audit with only accepted RUSTSEC-2024-0436 passed, and the edited code file remains below 800 lines)
Task 3: complete (this commit, base 38099d9145, startup now centers a present loaded track through the hardened post-routing scroll scheduler, leaves an absent track at the top, preserves selection and activates START-1; both rule-named display regressions were observed red before implementation and then passed individually under Xvfb with their full exact libtest paths, format, strict workspace clippy, the complete unfiltered workspace suite with 1324 GNOME non-display tests and 380 expected display ignores, audit with only accepted RUSTSEC-2024-0436 and all edited code files below 800 lines passed)
# Direct podcast queue context

Task Podcast-queue-context Packages A-C: complete (this commit, base 2e5aff6e44, typed virtual context tails now carry tracks or episodes; direct podcast and YouTube playback projects the current episode, the unchanged manual line and the frozen show-labelled neighbour context while leaving the music QueueSnapshot untouched; queue playback restores the same music projection, QueuedEpisode remains unchanged, and active QUE-10 plus the QUE-2/QUE-7/QUE-9 cross-references document the boundary)
Verification Podcast-queue-context Packages A-C: format, strict workspace clippy, the complete unfiltered workspace suite, UX traceability with 303 active rules, focused queue-section and queue-transport tests, Rust file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed; display-gated tests remained ignored by their existing contract, no real database, music files, live desktop, push, or unrelated worktree was touched)
Task Podcast-queue-context Package D: complete (this commit, base 8d401d90bf, the shared queue model and both its management ColumnView and compact panel rendering paths now subscribe to external-media changes through the same callback-registration seam as queue changes; episode start, neighbour skip and stop therefore rebuild and render one shared projection, while the compact panel preserves its existing hidden-panel early-out)
Verification Podcast-queue-context Package D: the pure dual-subscription regression, format, strict workspace clippy, the complete unfiltered workspace suite, Rust file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed; display-gated tests remained ignored by their existing contract)
Task Podcast-queue-context Packages E-F: complete (this commit, base 0dd970538a, activating a direct episode context row now advances through the existing frozen-neighbour playback path without starting a new session; episode context rows expose no remove or reorder affordance in either queue surface, stale mutation callbacks are rejected with debug diagnostics, manual Play Next entries remain editable, and the QueuedEpisode projection and interactions stay unchanged)
Verification Podcast-queue-context Packages E-F: the requested queue-sections, queue-transport, podcast, Core up-next, GNOME build and strict GNOME clippy commands passed; format, strict workspace clippy, the complete unfiltered workspace suite, UX traceability with 303 active rules, Core dependency purity, Rust file-size and diff checks, and audit with only accepted RUSTSEC-2024-0436 also passed; display-gated tests remained ignored by their existing contract, no real database, music files, live desktop, push, or unrelated worktree was touched)

Task P1a-W0-queue-seam: complete (commits 739bd5a0bf, e4ed05ebb1 and this commit, base 2e5aff6e44, the permanent QueueViewModel Send+Sync assertion was first observed red with E0277 proving the stored Rc closure was neither Send nor Sync, then green after VirtualContext became data-only and callers supplied the unchanged remaining_window query through ContextWindow; the fully qualified QUE-7 laziness regression passed 1/1 and the QUE-7 group passed 2/2; the unfiltered workspace result remained exactly 3899 passed, 0 failed and 403 ignored against the clean baseline, strict Clippy and format passed, and cached audit reported only accepted RUSTSEC-2024-0436; the cross-wave rule is recorded in the waves plan on feature/multi-surface-frontends, which is where it belongs — this branch carries code and this ledger entry only; no repository lock or coordination board exists, no push was performed, and no real database, music files or live desktop was used)


Task P1a-Welle-1: complete (this commit, base 67165099f6, Task 1 cab3824071 established the shared-view floor, Task 2 b4ad51e1b0 moved playlist import navigation, Task 3 e7b0ef3322 introduced translation-neutral `Message` values, review 61457dc9b9 replaced the two loose plural Options with `plural: Option<Plural>` and removed every unused scan export except `RETRY`, and Task 4 44bd250f2c moved playlist naming behind the unchanged GTK adapter seam; unplanned commit 9b59cbbe11 preceded Task 1 because `scripts/check-architecture.sh` was already red at the branch point — `ui/track_list/track_list.rs` stood at 603 lines against its 600-line cap, so 74 lines moved unchanged into `ui/track_list/track_list_wiring.rs`, and this ledger entry records the rationale missing from that commit message; mutation proofs made the shared-view floor and forbidden dependency edge red before green, the Task 4 tests failed with four missing-function errors before passing, the moved scan catalog retained 7 byte-identical msgids and the playlist catalog retained its 1 byte-identical msgid, the isolated suite rose from the 3903-passed baseline to 3912 passed with 0 failed and 410 ignored across 56 suites after filtering only the same 2 sandbox-blocked Radio socket tests, and `view_floor` rose 0 → 5 → 59 → 65 → 85; format, strict workspace clippy, architecture, frontend thinness and cached audit with only accepted RUSTSEC-2024-0436 passed, no repository lock or coordination board existed to release, no real music file, database or live desktop was used, and nothing was pushed)
Task P1a-Welle-2: complete (this commit, base 6782adb1e7, Task 1 5a09062448 moved `LyricsTrack`, `RequestIntent`, `LyricsState` and their six unchanged behavior tests into `reprise-view` behind the existing GTK adapter path and added the permanent `Send + Sync` assertion; Task 2 2599a54138 moved the 18 argument-free Lyrics labels as public msgids, removed the duplicate `N_!` macro, and folded `RETRY` into one definition because both call sites name the same argument-free user action; this task records the msgid rule and expands the toolkit countercheck to include `Send + Sync` and thread ownership after `lyrics_worker.rs` proved the second false-positive heuristic case; the new state seam first exited 101 with missing types before all six moved tests passed, the new catalog seam first exited 101 with exactly 18 missing labels before its public-value test passed, all 18 before/after msgids were byte-identical including U+00B7 and remained present in de, es, fr, ar, bn and hi, and `view_floor` rose 85 → 205 → 224; the isolated suite rose from the 3912-passed baseline to 3913 passed with 0 failed and 410 ignored across 56 suites after filtering only the same 2 sandbox-blocked Radio socket tests; format, strict workspace clippy, architecture, frontend thinness and cached audit with only accepted RUSTSEC-2024-0436 passed, while the optional whole-catalog script remains red on the inherited Arabic Stats mismatch for `Play this track`, outside the Lyrics catalog; no repository lock or coordination board existed to release, no real music file, database or live desktop was used, and nothing was pushed)

# Escape clears the episode selection

Task podcast-escape-clears-selection: complete (this commit, base 2e5aff6e44, an unmodified bubble-phase Escape on the Podcasts root now clears the grouped-library selection or every remembered YouTube channel selection according to the visible stack page, rerenders only after a real change, consumes only that successful clear, and leaves modified, unrelated, and empty second key presses to existing owners; SRC-12 records the behavior, both pure selection owners and the propagation boundary were observed red before implementation and green afterward, and the ignored real-widget regression compiles against the same `podcasts.set-selected` action and controller signal without being run or claimed in this display-less sandbox; the plan-focused tests and GNOME build, format, strict workspace clippy, the complete unfiltered workspace suite, UX traceability at 302 active rules, input parity, accessibility semantics, diff and Rust file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed; the broader architecture lint remains non-green solely because unchanged base file `track_list.rs` is already 603 lines against its 600-line orchestrator limit, no real database, music files, or live desktop was used, the test-generated local cache was removed, no repository lock or coordination board exists, and no push was performed)

# Podcast pointer selection chrome removal

Task podcast-pointer-selection: complete (this commit, base 7fec2ea1db, removed the redundant episode checkboxes and selection toolbars from both podcast surfaces, moved the episode-specific accessible name and selected state onto each row, kept the SRC-14 pointer and keyboard selection path as the sole mechanism, moved the live selection count into both existing summary lines, retained all context-menu batch actions, synchronized SRC-12 and gettext catalogs, and left every display-only test ignored and unclaimed)

Task P1a-Welle-3: complete (this commit, base 82c6cf79d4, Task 1 d1c2223521 moved the six FIL visibility and place-label decisions plus their 12 unchanged behavior tests into `reprise-view` behind the unchanged GTK module path, with a permanent `Send + Sync` assertion over the actual boundary data and a temporary `Rc<()>` field rejected as neither trait; Task 2 2513109c09 moved 16 argument-free Browse labels as public msgids, moved five parameterized labels and the data-dependent result-count selection as `Message` values, split the shared restriction bool from GTK-only Pango markup, preserved the existing saturating integer conversions, and deleted the now-unused GTK `result_count` wrapper instead of allowing its warning; this task records the data-dependent-msgid and surface-rendering rules and corrects Welle 3 from three parallel areas and ~3,200 LOC to the measured 345 LOC in two Browse files; the new restriction seam first exited 101 with 31 missing-function errors before all 12 moved tests passed, the catalog seam first exited 101 with 27 missing values/functions before its two shared tests and all four existing GTK renderer tests passed, all 25 before/after msgid literals including both plural pairs were byte-identical and remained present in de, es, fr, ar, bn and hi, and `view_floor` rose 224 → 274 → 366; the live baseline was 3912: the Welle-2 ledger itself records 3913 rather than the plan's 3914, and 589992f9eb then removed one self-referential View test; the two new catalog tests brought the isolated suite to 3914 passed with 0 failed and 410 ignored across 56 suites after filtering only the same two sandbox-blocked Radio socket tests; format, strict workspace clippy, architecture, frontend thinness and cached audit with only accepted RUSTSEC-2024-0436 passed before all three commits, while updating audit could not lock the read-only global advisory cache and the optional whole-catalog script remained red on exactly the inherited missing Arabic `Play this track` entry; no repository lock or coordination board existed to release, no real music file, database or live desktop was used, and nothing was pushed)
Task Podcast-queue-context-rebase: complete (this merge commit, merged origin/dev at 5fdcf6ad80, QUE-10 was rebuilt on the data-only `VirtualContext`: `ContextWindow` now returns typed `QueueItem` rows, the GTK-owned `QueueContextWindow` selects frozen direct-podcast neighbours or the ordinary music window, and the permanent `QueueViewModel: Send + Sync` assertion remains intact without a stored closure; dev's prefix/window boundary logic, this branch's interaction guards and unchanged-model render guard, and both sides of the append-only ledger and QUE-2/QUE-7/QUE-9/QUE-10 rules were preserved; the typed-window regression was observed failing at compile time before implementation, then focused queue, projection, model, podcast and Core tests passed; the complete unfiltered workspace suite, strict workspace clippy, format, file-size checks and audit with only accepted RUSTSEC-2024-0436 passed; the requested gettext gate remains red only on dev's already-recorded Arabic Stats mismatch for `Play this track`, with no branch delta in `po/ar.po`, `po/reprise.pot` or `po/POTFILES.in`; display-gated tests remained ignored, no real database, music files, live desktop, push, or unrelated worktree was touched)

# Source context-menu parity

Task source-context-menu-parity: complete (commits 2ef7449207 and this commit, base d6ac0a9abc, grouped Podcast/YouTube rows and YouTube channel-detail rows now share one selection-aware menu opener across secondary click, Menu/Shift+F10 and the row's discovery button; grouped source headers answer secondary click through the source menu, channel detail owns one shared `PodcastSelection` per channel, SRC-14 records the parity, and Radio remained unchanged because its capture-phase row surfaces, table keyboard controller and ACC-1 coverage were already complete)
# Retire the podcast/YouTube Show filter

Task drop-podcast-show-filter: complete (this commit, base d6ac0a9abc, removed the redundant Show section and chip from the podcast and YouTube filter bar, stopped computing and threading show names, reduced active-filter handling to the unchanged Unplayed and Downloaded dimensions while leaving `PodcastFilter::source` intact, removed the persisted show field, and added schema v51 to delete only the retired `podcasts.filter.show` setting; no earlier retired-setting helper existed, so the cleanup intentionally lives in the versioned database migration; SRC-2 now records that expandable show/channel groups provide per-source narrowing, the filter-only Show string was retired while the unrelated episode-table Show label remains, and all gettext catalogs were regenerated)
Verification drop-podcast-show-filter: the schema-cleanup and remaining-filter regressions were observed red before implementation and green afterward; the requested Core podcast tests, GNOME podcast tests, GNOME build, strict workspace clippy and gettext catalog gate passed; format, architecture, UX traceability, Core dependency purity, file-size and diff checks also passed; the workspace suite passed after excluding only the two unchanged MCP Radio tests whose local `TcpListener::bind` calls are denied by this sandbox, and cached audit reported only accepted RUSTSEC-2024-0436 after the updating audit could not lock the read-only global advisory database; display-gated tests remained ignored, the existing private CUA podcast scenario does not inspect the detached X11 filter-popover window so the removal remains visually unclaimed, no real database, music files, live desktop, push, or unrelated worktree was touched, and the untracked user plan was preserved)

# Episode open in browser

Task episode-open-in-browser: complete (this commit, base a5faa4e5bc, added a row-targeted `podcasts.open-in-browser` action through the shared external-link gateway, exposed it beside Copy URL exactly when a validated YouTube watch URL or RSS episode page exists, retained the row target during multi-selection, synchronized SRC-4b and all gettext catalogs, and extracted the focused pure regressions to keep every edited Rust file below 800 lines; the URL policy and single- and multi-selection menu seams were each observed red before implementation and green afterward; focused podcast tests passed 179 with 48 display-gated ignores, the GNOME build, format, strict workspace Clippy, complete workspace suite, gettext catalogs, UX traceability at 303 active rules, diff and Rust file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed; display-gated tests remained ignored by contract, no real database, music files, live desktop, browser, or external URL was used, no repository lock or coordination board exists, and nothing was pushed)

Task P1a-Welle-4: complete (commits 67799efb62 and this commit, base f7079c9380, moved the unchanged 125-line waveform-shaping implementation plus its seven pure behavior tests into `reprise-view` behind the existing `player_bar::waveform_shape` adapter path, widened only the five cross-crate exports to `pub`, removed the now-unused test-only aggregate/smoothing import rather than allowing its warning, and raised `view_floor` 366 → 459; the shared seam first exited 101 with 15 missing-symbol errors before its 7 tests passed, while the remaining waveform-seek group passed 14 tests with 11 display-gated ignores; no `Send + Sync` assertion was added because `DisplayBar` is a `Copy` enum containing only a unit variant and `f32`, so such an assertion would guard no owned or thread-affine state; format, strict workspace clippy, architecture, frontend thinness, the now-green gettext catalog check, and live audit with only accepted RUSTSEC-2024-0436 passed; after rebasing onto the live `origin/dev`, the isolated workspace suite passed 3934 tests with 0 failed and 410 ignored across 56 suites after filtering only the same two sandbox-denied Radio listener fixtures, whose unfiltered run failed exactly at `TcpListener` with `Operation not permitted`, reconciling to the live 3936-test baseline; Wave 4 closes the free-file relocation phase at floor 459 and Wave 5 begins extraction work, no repository lock or coordination board existed to release, no real database, music files or live desktop were used, and nothing was pushed)
# YouTube channel names as subscription titles

Task Package A: complete (this commit, base 782e064d3d, YouTube refresh metadata now uses a non-empty channel identity and never the uploads-playlist title; an authorless refresh preserves the stored title, yt-dlp projects the channel already present in flat-playlist JSON without another process call, and RSS titles remain feed-owned; the focused regressions were observed red before implementation and green afterward; format, strict workspace clippy, the complete workspace suite, Core podcast tests, architecture, UX traceability, Core dependency purity, file-size and diff checks passed, and audit reported only accepted RUSTSEC-2024-0436; no real database, music files, live desktop, push, or unrelated worktree was touched, and the untracked user plan was preserved)
Task Package B: complete (this commit, base 47480fddef, schema v52 renames only YouTube subscriptions with a present non-empty channel author while preserving already-correct rows, authorless placeholders and RSS titles; the migration round-trip was observed red before implementation and green afterward; format, strict workspace clippy, the complete workspace suite, Core podcast tests, architecture, UX traceability, Core dependency purity, file-size and diff checks passed, and cached audit reported only accepted RUSTSEC-2024-0436 after the updating audit could not lock its read-only advisory cache; no real database, music files, live desktop, push, or unrelated worktree was touched, and the untracked user plan was preserved)
Task Packages C-D: complete (this commit, base d3ca49b714, the shared pure source-header projection emits the channel title with no subtitle for YouTube and retains the distinct author subtitle for RSS across the grouped list and channel detail title path; SRC-5 records the corrected identity contract while the existing facts line remains untouched; the pure presentation regression was observed red before implementation and green afterward; the focused GNOME podcast suite passed 180 with 48 display-gated ignores and the GNOME build passed; format, strict workspace clippy, the complete workspace suite, architecture, UX traceability, Core dependency purity, file-size and diff checks passed, and cached audit reported only accepted RUSTSEC-2024-0436; no real database, music files, live desktop, push, or unrelated worktree was touched, and the untracked user plan was preserved)

Task P1a-Welle-5: complete (this commit, base fbcc34c38a, Task 1 b610bf8190 extracted `QueueViewModel`, `QueueSection`, `VirtualContext` and `ContextWindow` inside `reprise-gnome` behind the unchanged `queue_sections` path while leaving `reprise-view` and `view_floor` 459 untouched; its permanent `Send + Sync` assertion moved with the model and a temporary `Rc<()>` field failed with both E0277 Send and Sync errors, proving that the Welle-0 no-stored-closure guard survives the extraction; Task 2 88b8a4a50a moved the three data-dependent queue-header choices to byte-identical `Message` values, kept rendering in the GTK adapter, and passed the already-rendered Music fallback into `compose_virtual` after inspecting its projection call site; Task 3 b216933f97 moved the finished model to `reprise-view::queue`, widened its cross-crate API, retained all 71 callers through the adapter, rejected a temporary valid GTK dependency with the architecture gate, and raised `view_floor` 459 → 743; Task 4 9b27fb3926 found no widget-bound behavior on the eleven-variant `ColumnId`, moved it and its three pure mappings to `reprise-view::columns` behind the unchanged `column_layout` path without editing its callers, and raised `view_floor` 743 → 806; Task 5 records the proven extract-translation-move rule; the isolated baseline was 3939 passed, 0 failed and 413 ignored across 56 suites after filtering only the same two sandbox-denied MCP Radio listener tests, the three translation tests raised it to 3942 and the two column tests to 3944, reconciling to 3946 including the two filtered tests; one full Task-4 run hit the unrelated timing-sensitive gapless handoff test, which passed immediately in isolation and the unchanged full suite then passed; format, strict workspace clippy, architecture, frontend thinness, the now-green gettext catalog gate, and cached audit with only accepted RUSTSEC-2024-0436 passed, while live audit could not lock the sandbox-read-only global cache; every edited code file remains below 800 lines, no repository lock or coordination board existed to release, no real database, music files or live desktop were used, and nothing was pushed)

# Identity versus size

Fix identity-vs-size: complete (commits 50d5a7a4ec, ec37967a8f and this commit, base 87986cdb9a, separated real file size from optional filesystem identity without a schema migration; the regression first failed with left `Some(MoveCandidate { id: 1, path: "/gone/unrelated.flac" })` and right `None`, then passed unignored alongside the positive no-identity fingerprint test and the identity-first ordering proof; the existing Linux rename, copy-delete and ambiguity tests stayed green; format, strict workspace Clippy, architecture, frontend thinness, Core dependency purity, diff and file-size checks passed; the isolated runnable workspace suite passed 3948 tests with 0 failed and 413 ignored across 56 suites after filtering only the same two sandbox-denied Radio listener fixtures, whose exact unfiltered run failed only at `TcpListener::bind` with `Operation not permitted`; cached audit reported only accepted RUSTSEC-2024-0436 after the updating audit could not lock its read-only global advisory database; no real database, music files, live desktop, push, schema or migration was touched, and no repository lock or coordination board existed to release)

Task P1a-Welle-6: complete (commits 513b8ba99c, 3757a6e641 and this commit, base cb7b559189, moved the whole 244-line queue-row coordinate and reorder mapping into `reprise-view::queue::rows` behind the unchanged GNOME adapter path, retained all 51 call sites and 11 behavior tests, and raised `view_floor` 806 → 870; the shared seam was observed red with the missing module before it passed, while `column_widths.rs` deliberately remains in GNOME because its two callers persist GTK desktop table-column widths and the phone Compose surface has no corresponding resizable-column contract; the adversarially checked relocation inventory is now exhausted, and whether each surface owns its remaining presentation logic or that logic is rewritten for a shared layer is recorded as an open owner product question without a recommendation; the isolated baseline and final suite each reconciled to 3955 tests with 0 product failures and 413 ignored across 56 suites, with 3953 passing after filtering only the same two sandbox-denied MCP Radio listener fixtures whose unfiltered run fails at `TcpListener::bind` with `Operation not permitted`; format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, and live audit with only accepted RUSTSEC-2024-0436 passed; every edited code file remains below 800 lines, no repository lock or coordination board existed to release, no real database, music files, live desktop, or push was used)
Task library-source-residence: complete (commits 61f92ff8af, 39073c1b28 and this commit, base 25ebc838c7, introduced the `LibrarySource: Send + Sync` residence contract with honest `None` degradation, kept Unix `lstat`/`st_dev` behavior identical, parameter-injected it into the scanner root guard, vanish classification and playback-fault maintenance query, retained the shared classifier as the Linux parity seam, and proved the same Deleted/Unmounted/Unknown triad with a provider-tree token unrelated to POSIX; the parameter choice over a closure or Effect and the remaining Package-2/Package-3 inventory are recorded in the Android spike; both focused tests were observed red before implementation and green afterward, format, strict workspace Clippy, the 56-suite workspace run with 3955 runnable tests passing, 0 failed and 413 ignored after filtering only the two sandbox-denied MCP Radio listener fixtures, architecture, frontend thinness, gettext catalogs, Core dependency purity, diff and file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed; the exact unfiltered test command reached those same two fixtures and failed only because this sandbox denies `TcpListener::bind` with `Operation not permitted`, no real database, music files or live desktop were used, no repository lock or coordination board existed to release, and nothing was pushed)

Task source-empty-state-icon-alignment: complete (this commit, base 78c729257f, the shared source empty-state and failure-state glyphs now expand through their fixed-height tiles so GTK centers them on both axes, and the SRC-10 allocation regression checks the glyph and tile centers; the display test compiled but its red/green allocation run and CUA observation were blocked because this sandbox denied the private D-Bus/Xvfb sockets and exposed neither an accessible X11 window nor AT-SPI, while the focused non-display SRC-10 test, format, strict workspace Clippy, and the isolated runnable workspace suite passed after filtering only the same two sandbox-denied MCP Radio listener fixtures whose unfiltered run failed solely at `TcpListener::bind` with `Operation not permitted`; live audit reported only accepted RUSTSEC-2024-0436, the edited code file remains below 800 lines, no real database, music files, live desktop, or `main` was touched, and the repository lock remains held until publication into `dev` finishes)
Task library-source-traversal: complete (commits db5dd58660, 986c526d0b and this commit, base 78c729257f, replaced the scanner's exact tree pre-count with a previous-catalog estimate and documented its visible first-scan/rescan behavior, added an object-safe streaming `LibrarySource::walk` visitor with source-neutral entries and inline errors, preserved native scanner order, filename-sorted relink, cancellation, traversal-error handling and Unix `follow_links(false)`, and proved the same ordered audio-file projection with an in-memory DocumentsProvider-style tree unrelated to `walkdir`; the Android spike now records the measured 53 raw presence/metadata sites in 22 files, the app-private `cover.rs` counterexample, and the mandatory per-site classification before Package 3; both behavior seams were observed red before implementation and green afterward, format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, Core dependency purity, diff and file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed before each code commit; the final 56-suite runnable workspace gate passed 3956 tests with 0 failed and 413 ignored after excluding two sandbox-denied MCP Radio listener fixtures plus one timing-sensitive CAVA test that passed separately in 0.05 seconds, while live audit could not lock the read-only global advisory cache; no real database, music files or live desktop were used, no repository lock or coordination board existed to release, and nothing was pushed)

# Compact dismissible cached-source failures

Task podcast-refresh-banner-dismiss: complete (this commit, base 78c729257f, replaced the tall stacked cached-source failure card with one compact horizontal summary row whose wrapped safe copy, provider actions, collapsed Details toggle and labelled close button stay together while technical Details alone expand below; dismissal clears the caller-owned current failure state for Podcasts/YouTube, Radio, Concerts and Releases rather than merely hiding a widget that the next render would reopen, while a later real failure can still create a new notice; POD-19 and the isolated `podcast-backlog` CUA scenario now pin the compact and dismissible contract, including expanding Details before closing; the display-free regression was observed red as a missing `banner_chrome` production projection before implementation and green afterward, the display-gated interaction regression compiles, focused source-error and all four caller failure suites, gettext catalogs, CUA shell lint, format, strict locked workspace Clippy, warning-free Rustdoc, architecture, accessibility, input parity, frontend thinness, UX traceability at 303 active rules, motion tokens, runtime-service installation, Android sync runtime, diff and Rust file-size checks, and live audit with only accepted RUSTSEC-2024-0436 passed; the complete unfiltered workspace run passed Core and 1307 GNOME non-display tests with 389 expected ignores before failing only the two unchanged MCP Radio fixtures whose `TcpListener::bind` is denied by the sandbox, and a later socket-filtered run hit one unrelated parallel yt-dlp log-capture flake which passed immediately in isolation; Xvfb and the private CUA display could not bind a socket in this sandbox, so no visual rendering claim is made and the isolated CUA evidence attempt remains at `/home/marvin/.cache/reprise-cua-evidence/podcast-banner/run-20260802T163117Z-2`; no real database, music files, live desktop, push or unrelated dirty worktree was touched, and no repository lock or coordination board existed to release)
Task library-source-probe: complete (commits df99504734, bc8f252a0f and this commit, base f6afaeb434, first combined the scanner's two metadata syscalls without involving the trait and preserved its asymmetric failed-stat result of mtime zero plus absent stat, then routed all 21 and only the classified Class-A presence and metadata sites through object-safe `LibrarySource::probe` and shallow `read_directory`; walk and directory entries carry optional source-owned facts, Unix deliberately leaves them absent so each audio file pays one deferred stat and each non-audio entry none, while a future SAF cursor can avoid a second Binder round trip; a counting source proves one query per audio path and zero for non-audio or metadata-carrying entries, including the vanish reconciliation that now reuses paths already observed by the walk; Classes B, C and E remain unchanged, every edited Rust file is below 800 lines, format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, Core purity, diff checks and live audit with only accepted RUSTSEC-2024-0436 passed; the 56-suite runnable workspace proof passed 3963 tests with 0 failed and 414 ignored after separating only the same two sandbox-denied MCP Radio listener fixtures, and those fixtures plus their complete 10-test binary passed independently, reconciling to 3965 passing tests; no real database, music files or live desktop were used, no repository lock or coordination board existed to release, and nothing was pushed)

Task library-source-handles: complete (commits a46de19994, 6f9504bfe8 and this commit, base b032e927d7, measured the Class-A consumers before choosing a concrete named `LibraryReadHandle` with the required `Read + Seek` capabilities, added explicit non-default `open_read` implementations, routed the separable lyrics, cover and provenance reads through `LibrarySource`, and proved a `content:/` sidecar whose bytes exist only in a `Vec<u8>`; `tag_mutation`, `scanner_repair`, `writeback_publish` and the reader inside `write_ai_tags` remain wholly unchanged because their reads are coupled to writes, while the Android spike records all 84 raw I/O sites, 55 app-private sites, 18 Class-A sites, the four completed pure reads, and Package 5's unresolved `create_new`/`rename` atomicity guarantees; format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, Core purity, diff and Rust file-size checks, and live audit with only accepted RUSTSEC-2024-0436 passed; the isolated 56-suite runnable workspace proof passed 3966 tests with 0 failed and 415 ignored after separating only the same two sandbox-denied MCP Radio listener fixtures, reconciling to 3968 total tests; the active multi-surface branch's ownership of the append-only spike was re-audited as divergent and unchanged since 01:48, and this later explicit plan's narrow additive Task 4 was applied without stacking or modifying that branch; no real database, music files, live desktop, repository lock, coordination board or push was used)
# Episode multi-selection menu and grouped source alignment

Task podcast-selection-menu-ux: complete (this commit, base 78c729257f, removed the row-targeted Open in browser action from episode multi-selection menus while retaining every genuine batch action, vertically centered the grouped RSS/YouTube source identity beside its artwork, and aligned SRC-5, SRC-12 and SRC-4b; the menu regression was observed red before implementation and the focused menu and 181-test podcast suites passed afterward; the new rule-named layout regression passed in the clean merge gate's private D-Bus/Xvfb display, while CUA remained unavailable because its daemon could neither connect to AT-SPI nor bind its sandbox-inaccessible socket; format, strict workspace Clippy, GNOME build, Rust documentation, architecture, accessibility, input parity, device-sync runtime, runtime-service install, frontend thinness, UX traceability, motion tokens, Core dependency purity, diff and file-size checks passed, the clean merge gate's complete unfiltered workspace suite passed including the two Radio listener fixtures that had failed one earlier transient sandbox run, and audit reported only accepted RUSTSEC-2024-0436; the rule-named display balance was 244 passed and three unrelated TAG-1 track-list refresh tests failed together after AT-SPI registration errors, with an isolated rerun stopped before the test by a denied private D-Bus socket; no real database, music files, browser, live desktop, push, or unrelated worktree was touched)

# CI quality-gate dependency repair

Task ci-meson-dependency: complete (this commit, base 0f80a8112d, installed Meson in the Arch Linux CI container because the required runtime-service install check invokes `meson setup` before any later gate can run, and added a workflow policy regression that was observed red before the package was declared and green afterward; the focused GitHub-flow policy test, complete QA linter policy suite, diff check, format, strict workspace Clippy, the complete unfiltered workspace suite and audit with only accepted RUSTSEC-2024-0436 passed; the local sandbox still denied a private D-Bus socket and could not initialize GTK through Xvfb, so the already-published PR's isolated GitHub runner is the next required display-gate proof; no product code, real database, music file, live desktop or unrelated worktree was touched)

# CI bounded-runner artifact repair

Task ci-bounded-runner-artifacts: complete (this commit, base da8f1b80b7, disabled incremental compilation and debug information for Cargo dev/test profiles only in the bounded GitHub Actions job after two independent hosted runners exhausted their disks while compiling unchanged workspace tests; the second clean log proved the failure at 74 MB free with `rustc-LLVM ERROR: IO failure on output stream: No space left on device`, before any product test verdict; the policy regression was observed red before the three runner constraints were added and green afterward, and the complete QA linter policy suite and diff check passed; the required gate commands and local developer profiles remain unchanged, no product code, real database, music files or live desktop was touched)

# CI deterministic failure probes

Task ci-deterministic-failure-probes: complete (this commit, base 5fbd3dfe24, replaced five chmod-based write-failure fixtures that become writable under the Arch job's root user with deterministic missing-parent failures and a source-neutral existing-path test double, while retaining the cache, no-overwrite, no-temporary and best-effort lookup assertions; yt-dlp descendant checks now classify a Linux zombie as exited instead of mistaking its unreaped `/proc` entry for a running process, with a direct process-state regression; the seven failures were observed in the first disk-complete CI run, all affected tests and the complete 10-test yt-dlp process suite passed afterward, and format, strict workspace Clippy, all 1993 Core tests, cached audit with only accepted RUSTSEC-2024-0436, architecture, frontend thinness, QA linters, Core purity, diff and file-size checks passed; no real database, music files or live desktop was touched)

# CI deterministic device-root refusal probe

Task ci-deterministic-device-root-refusal: complete (commit e16dbe04dd, base 29d5c04382, replaced the last permission-bit device write-refusal fixture with a deterministic GIO PermissionDenied result because the Arch quality-gate container runs as root and could create the supposedly locked folder; the CI failure was observed with an unexpected Ok result, the replacement test was observed red as a missing result-mapping seam and green after the production match was extracted unchanged, and the complete 126-test Linux platform suite, format, strict platform Clippy, diff and file-size checks passed; no product behavior, real database, music files or live desktop was touched)

# PR 237 dev reintegration

Task podcast-selection-menu-dev-reintegration: complete (this commit, branch head b135981390, dev 1850384607, integrated the concurrently merged LibrarySource read-handle work without dropping either side, implemented the new explicit open_read requirement for the metadata-only ExistingPathSource test double, and extracted that double into a 47-line sibling module after the combined source.rs first failed the architecture limit at 809 lines; the combined 1996-test Core suite, 126-test Linux platform suite, two focused SRC-12 menu tests, six deterministic write-failure tests, format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, GitHub-flow policy, Core purity, diff and file-size checks passed, and audit reported only accepted RUSTSEC-2024-0436; no real database, music files, live desktop or main was touched)
Task library-source-tags: complete (commits 55ed7678e3, 5d7aa0a769, 3c2c439d04 and this commit, base 2f52231cd6, reproduced Lofty 0.24's extension-seeded path-open and unknown-extension error behavior over `LibrarySource::open_read`, left the deliberately content-sniffed repair temp-file reader unchanged, moved all five pure library tag reads through the active source, and proved a complete source-only tagged scan plus path/source parity for broken-tag and unsupported-format verdicts; the 13 hidden Lofty path sites and all eight deliberate exclusions are recorded in the Android spike, while the counting source measures one probe plus one open without carried metadata and zero probes plus one open when the walk carries it, because a SAF descriptor cannot supply the provider metadata cursor and therefore cannot genuinely combine those two remote operations; format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, Core purity, diff and Rust file-size checks, and live audit with only accepted RUSTSEC-2024-0436 passed; the isolated 56-suite runnable workspace proof passed 3971 tests with 0 failed and 415 ignored after separating only the same two MCP Radio listener fixtures whose final unfiltered and exact reruns were denied at `TcpListener::bind` with `Operation not permitted`, while their earlier Task 3 controls had passed independently and reconciled the product baseline to 3973 tests; no real database, music files, live desktop, repository lock, coordination board or push was used)
Task Android MVP Phase 0: complete (this commit, base c4c2e626f5, transplanted the UniFFI 0.32 Rust crate and Android Gradle scaffold from spike/android-core-feasibility commit 68cbed1923 under the production android path without any prebuilt shared libraries or build outputs, added and twice ran the reproducible x86_64 NDK build script, and enforced the FFI-to-Core-only dependency boundary with a deliberate Linux-platform mutation that failed on GLib, GStreamer and zbus before the restored graph passed; the final dependency tree contains only reprise-android-ffi and reprise-core among workspace crates, format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, diff and Rust file-size checks, and live audit with only accepted RUSTSEC-2024-0436 passed; the isolated 59-suite runnable workspace proof passed 3971 tests with 0 failed and 415 ignored after excluding only the same two sandbox-denied MCP Radio listener fixtures, whose exact unfiltered run again failed solely at TcpListener::bind with Operation not permitted after 3499 tests had passed; no emulator, real database, music files, live desktop, push, repository lock or coordination board was used)
Task Android MVP Phase 1 Steps 1 and 3: complete (this commit, base a6a959086b, proved with the exact realistic Content-URI that Path::starts_with returns true for its tree URI and extension returns flac, and pinned scanner_vanish::candidates_under_root against a migrated in-memory database so the escaped LIKE prefilter returns the URI-shaped child but not a lookalike tree; A3 independently pins the literal percent escape so its authoritative Path post-filter cannot mask a widened SQL pattern, and mutations removing either the descendant slash-wildcard suffix or the percent escape were observed red before both focused tests passed after restoration; A1, its FFI descriptor probe and every emulator action were deliberately not attempted, so no seekability verdict is claimed, while the research report records A2 and A3 and leaves A1 open; format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, Core purity, diff and Rust file-size checks, and live audit with only accepted RUSTSEC-2024-0436 passed; the isolated 59-suite runnable workspace proof passed 3973 tests with 0 failed and 415 ignored after excluding only the same two sandbox-denied MCP Radio listener fixtures, whose exact unfiltered run again failed solely at TcpListener::bind with Operation not permitted after 3501 tests had passed; no Android FFI behavior, emulator, real database, music files, live desktop, push, repository lock or coordination board was used)
Task Android MVP Phase 1 Step 2 probe implementation: complete (this commit, base f7cc621097, added an inspectable UniFFI descriptor report for initial read, absolute seek, second read and byte comparison with per-operation errors, explicitly adopts ownership transferred by `ParcelFileDescriptor.detachFd()` so `File` closes the descriptor on every return path, and replaced the Android scaffold screen with the minimal `ACTION_OPEN_DOCUMENT_TREE` picker that opens the first direct file and logs the complete result under `RepriseA1`; the public seam was observed red before implementation and green for both a regular audio file and a non-seekable pipe, the x86_64 Android native library and generated Kotlin bindings built successfully, while Gradle could not start Kotlin compilation because this sandbox denies the wildcard socket required by its file-lock listener; no emulator or real SAF document was used and no A1 verdict is claimed or recorded; format, strict workspace Clippy, architecture, frontend thinness and gettext catalogs passed, cached audit reported only accepted RUSTSEC-2024-0436 after live audit could not lock the read-only global advisory cache, and the reconciled workspace proof passed 3974 tests with 0 failures and 415 ignored after the same two sandbox-denied MCP Radio listener fixtures were separated and two parallel-hung GStreamer controls passed exact isolated reruns; every edited code file remains below 800 lines, no desktop behavior, Core contract, schema, plan phase, research verdict, real database, music files, live desktop, push, repository lock or coordination board was touched)
Task Android MVP Phase 2: complete (this commit, base b2dd70db33, added the flat four-method UniFFI `SafSource` callback and the Rust `BridgedSource` adapter, directly adopted the now device-proven readable and seekable SAF descriptor without a fallback, carried provider size, modification time and stable document identity on directory entries, and derived a streaming Rust walk with native or filename order, inline errors, immediate Stop propagation and no whole-tree materialization; the in-memory provider proof was observed red before implementation and then matched the Unix source's ordered audio-file projection, and focused regressions additionally cover zero per-file probes for carried metadata, direct seek behavior, stable provider-scoped identity, error continuation and Stop; Kotlin and UniFFI can distinguish confirmed provider absence from callback failure, but `LibrarySource::probe` has no failure arm, so only confirmed absence maps to `None` while a failed callback maps to conservative fact-free present metadata, preventing transient Binder failure from licensing either `missing_since` write and recording the Core contract limitation for the real source; the x86_64 Android library and generated Kotlin bindings built successfully, format, strict workspace Clippy, architecture, frontend thinness and gettext catalogs passed, cached audit reported only accepted RUSTSEC-2024-0436 after live audit could not lock the read-only global advisory cache, and the isolated workspace suite reached only the same two sandbox-denied MCP Radio listener fixtures after all preceding suites passed; every edited code file remains below 800 lines, Phase 3 and all desktop behavior stayed untouched, no real database, music files, live desktop, push, repository lock or coordination board was used)
Task library-source-presence: complete (this commit, base 1056f11bfc, replaced `LibrarySource::probe`'s two-state `Option` with explicit `Present`, confirmed `Absent` and indeterminate `Unknown`, migrated every caller by its existing conservative meaning, and made only confirmed absence reach the scanner-vanish and playback-fault `missing_since` writes; the two database-observable regressions each exited 101 red because the contract could not express `Unknown` before both passed green, the SAF bridge now maps callback failure directly to `Unknown` without fact-free metadata, and Unix now maps only `NotFound` to absence while permission, I/O and every other stat error become `Unknown` instead of a false absence — the sole desktop behavior change; the clean 59-summary workspace run passed 3986 tests by keyword with 0 failed and 415 ignored, after an earlier overlapping run exposed the known yt-dlp tracing-capture flake and its exact isolated control passed; format, strict workspace Clippy, live audit with only accepted RUSTSEC-2024-0436, architecture, frontend thinness, gettext catalogs, Core dependency purity, diff and Rust file-size checks passed, every edited code file remains below 800 lines, no real database, music files, live desktop, push, repository lock or coordination board was used)
Task Android MVP Phase 3 Steps 1 and 2: complete (this commit, base a396394e31, replaced the A1 probe with the deliberately small app-private database, tree-configuration, scan and track-list FFI surface; scan progress preserves the first scan's `total: None`, a fake one-track SAF provider proves the complete public Rust seam and app-private `reprise.db`, and opaque provider scans bypass only Unix mount-point lookup while every desktop entry point remains pinned to the unchanged native path; the Kotlin `AndroidSafSource` implements exactly the four bridge methods, returns document id, size and modification time in the same child cursor, distinguishes an empty missing-document result from provider failure, adopts read descriptors, and the picker persists read permission before scanning; the public seam was observed red at compile time before implementation, its first green run exposed an invalid Unix mount lookup for a Content URI, and the corrected focused source and desktop mount/restore regressions passed; `scripts/android-build.sh` built the x86_64 native library and generated bindings, and a direct Kotlin 2.4.10 K2 compile with Android API 37 and the generated binding passed because Gradle itself cannot start its sandbox-denied wildcard-IP file-lock listener; format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, Core purity, diff and code-file size checks passed, and live audit reported only accepted RUSTSEC-2024-0436; the first exact unfiltered workspace run exited 0, while the isolated reproducible balance passed 3984 tests with 0 failed and 415 ignored across 59 summaries after separating only the two unchanged MCP Radio listener fixtures whose `TcpListener` is denied with `Operation not permitted`, reconciling to 3986 tests; Phase 3 Step 3 and every emulator/device action remain deliberately unrun, so no device scan verdict is claimed, no real database, music files, live desktop, schema, push, repository lock or coordination board was used)
Fix Android MVP source mount boundaries: complete (this commit, base 1ea4de02ba, replaced the four-layer `resolve_native_mount_points` platform flag with the mandatory source-owned `LibrarySource::mount_point` question, retained per-parent memoization, kept the Unix answer and all desktop behavior unchanged, made SAF and source-only doubles decline instead of inventing a grouping boundary, and proved two vanished source-backed rows remain missing with `mount_point` NULL while the source's absence answer is queried once for their shared parent)
Fix Android MVP source display names: complete (this commit, base a5b4615375, added mandatory source-owned title fallback naming, kept Unix file-stem behavior unchanged, and carried SAF cursor display names through the existing child facts without a second Binder query per file)
Task Android MVP Phase 4 Steps 1 and 2: complete (this commit, base 45c84be6be, added the deliberately plain three-state Compose folder, scanning and track-list surface, kept unknown first-scan totals indeterminate, and plays selected content URIs directly through activity-owned Kotlin MediaPlayer lifecycle without adding a PlaybackBackend bridge; the device check remains separate and unrun here)
Fix Android MVP source container names: complete (this commit, base 7b7b16b2b6, added mandatory source-owned container naming, preserved Unix parent-directory album fallback, carried already-known SAF parent display names without another Binder query, left unknown containers empty, and moved the two remaining relink title fallbacks from identifier stems to source display names; the SAF album and relink title seams were each observed red before green, and the remaining Core parent/name/stem/extension uses were classified as storage classification, traversal, cache/temp safety, sidecar, or destination-path questions rather than library-item labels; format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, Core purity, diff and code-file size checks passed, cached audit reported only accepted RUSTSEC-2024-0436 after live audit could not lock the read-only global cache, and the isolated runnable workspace balance passed 3987 tests with 0 failed and 415 ignored after excluding only the same two sandbox-denied MCP Radio listener fixtures whose unfiltered run failed solely at `TcpListener::bind` with `Operation not permitted`; no real library data was used and the device-run build outputs were preserved unstaged)
Task Android MVP Phase 5: complete (this commit, base e862328c5f, recorded the device evidence as a mixed abstraction verdict rather than a success claim: storage packages 1, 2, 4 and 5 held for the measured local SAF provider, package 3's two-state probe signature did not and became a three-state contract, and the misplaced `is_absolute`, platform mount boolean, display-name and container-name assumptions show why self-authored doubles answer but do not discover missing source questions; future write and watcher packages now explicitly start from the second concrete source before extracting a shared contract; format, strict workspace Clippy, architecture, frontend thinness and gettext catalogs passed, cached audit reported only accepted RUSTSEC-2024-0436, and the isolated runnable workspace rerun passed 3987 tests with 0 failed and 415 ignored after excluding only the two sandbox-denied MCP Radio listener fixtures; the first balance hit one timing-sensitive crossfade assertion that passed immediately in exact isolation and in the complete rerun, no real library data or device was used, and the existing device-run build outputs remain preserved unstaged)
Fix Android MVP library restart: complete (this commit, base cdd85d6aeb, the selected SAF tree is now saved synchronously in app-private SharedPreferences after its read grant is made persistable; launch validates both the persisted grant and a real tree probe, configures the source, and lists the existing database catalog without scanning, while revoked access or a removed tree gets a dedicated choose-again state that never enters scanner reconciliation; an explicit Rescan action reuses the remembered tree and the four controller seams passed their red/green Kotlin executable, the complete Android source passed a direct Kotlin 2.4.10 Compose compile, and the architecture gate passed; all 890 tracked Gradle/build artifacts under `android/.gradle`, `android/build`, and `android/app/build` were removed from the index and ignored while remaining on disk, no Rust changed so Rust gates were not run, and the separately owned device check was deliberately not run)
Fix Android MVP SAF file identity: complete (this commit, base 043a394787, the SAF bridge no longer hashes provider document IDs into a claimed file identity because the measured external-storage ID is path-derived and changes on rename; probe and walk metadata now honestly return `identity: None` so scanner move detection uses its existing fingerprint fallback, while the unchanged `residence_token` remains valid because it identifies the configured persisted tree grant rather than an individual file)
Task podcast-selection-menu-dev-reintegration-2: complete (this commit, branch head d0efde2e76, dev 1aec27a71c, integrated the concurrently merged source-backed Lofty tag reads without dropping either side; the source trait merge was conflict-free and the append-only ledger retained both histories, while the extracted metadata-only test source remained the sole feature delta in source.rs and every edited Rust file stayed below 800 lines; format, strict workspace Clippy, all 2000 Core tests, all 126 Linux platform tests, 13 focused SRC-12 GNOME tests with two expected display ignores, architecture, frontend thinness, gettext catalogs, GitHub-flow policy, accessibility, input parity, UX traceability, motion tokens, runtime-service installation, Android sync factories and Core dependency purity passed, and cached audit reported only accepted RUSTSEC-2024-0436 after live audit could not lock the read-only advisory cache; CUA remains unavailable at the sandbox AT-SPI, X11 and socket boundaries, so no new visual observation is claimed; no real database, music files, live desktop or main was touched)
Task ci-serialized-display-runner: complete (this commit, base 7485484a38, serialized the hosted display gate after two otherwise complete CI attempts reproduced the same three unrelated timing-sensitive GTK failures only with four concurrent Xvfb workers; the runner now uses the display harness's existing one-worker default, and both GitHub-flow and aggregate QA policy pin that bounded choice; the focused policy regression was observed red before the workflow change, the aggregate policy then exposed and removed its stale four-worker expectation, and both suites passed afterward; format, strict workspace Clippy and the complete unfiltered workspace suite passed from an isolated writable XDG data/cache/config root in a dedicated target directory, while the deliberately unisolated control failed only 18 cache writes against the sandbox-read-only default path; cached audit reported only accepted RUSTSEC-2024-0436; no product code, real database, music files, live desktop or main was touched)
Task ci-tag-refresh-display-repair: complete (this commit, base b7c9f8fdee, kept the tag-save viewport stable across the synchronous model replacement by applying its stable-ID scroll anchor immediately when rows are allocated and retaining the existing idle scroll refinement, and made the save-refresh display fixture sort by title so changing its deliberately secondary album field no longer moves the edited row out of view; the hosted serial display run supplied the red evidence with 246 of 249 tests passing and all three failures isolated to TAG-1 viewport observations, while the focused nine non-display TAG-1 tests, format, strict workspace Clippy, architecture, frontend thinness, the complete workspace suite excluding only the two sandbox-denied MCP listener fixtures, cached audit with only accepted RUSTSEC-2024-0436, GitHub-flow policy, QA linters, accessibility, input parity, UX traceability, motion tokens, runtime-service installation, Android sync factories, Core dependency purity and Rust file-size checks passed; CUA remains unavailable at the sandbox AT-SPI, X11 and socket boundaries, so hosted Xvfb must supply the rendering proof and no live desktop or user data was touched)
Task ci-tag-refresh-display-repair-2: complete (this commit, base caca279907, used the next hosted serial run's 247-of-249 result to separate the fixed save-reload top frame from two remaining failures: focus restoration's vertical-disabled `scroll_to(...FOCUS...)` still reset the adjustment to zero synchronously before returning to the restored viewport, so every supported list view now reapplies the captured value in that same turn; the refresh rendering test now writes Artist under a title sort because its 900-pixel fixture visibly realises Artist row cells while GTK only realised the horizontally clipped Album header, making the previous album-label search incapable of observing a successful rebind; the red hosted failures reported reference 6662 versus sampled zero and a visible title/artist row set without album cells, while the focused nine non-display TAG-1 tests, focus shortcut contract, format, strict workspace Clippy, the complete workspace suite excluding only the two sandbox-denied MCP listener fixtures, cached audit with only accepted RUSTSEC-2024-0436, architecture, frontend thinness, gettext catalogs, GitHub-flow policy, QA linters, accessibility, input parity, UX traceability, motion tokens, runtime-service installation, Android sync factories, Core dependency purity and Rust file-size checks passed; CUA remains blocked at the sandbox display and accessibility boundaries, no live desktop or user data was touched, and the hosted display rerun remains the rendering proof)
Task ci-tag-refresh-display-repair-3: complete (this commit, base 5646d8fbc7, used hosted serial run 30772773669's 247-of-249 result to prove that GTK can overwrite both the save-reload anchor and focus-restored viewport after their initiating API calls have returned, then introduced one short-lived adjustment hold that synchronously restores the requested value across both value and bounds changes during those GTK handovers; the hold starts before a preserved-anchor model replacement, adopts the stable-ID target when rebuilt geometry becomes usable, also protects every supported list view around `scroll_to(...FOCUS...)`, disconnects both signal handlers after 250 ms, and has a display-independent range-clamping regression that was first observed failing only because constructing a GTK adjustment requires initialization before the calculation was extracted into a pure seam; the focused TAG-1 and hold tests, format, strict workspace Clippy, the complete isolated workspace suite excluding only the two sandbox-denied MCP listener fixtures, cached audit with only accepted RUSTSEC-2024-0436, architecture, frontend thinness, gettext catalogs, GitHub-flow policy, QA linters, accessibility, input parity, UX traceability, motion tokens, runtime-service installation, Android sync factories, Core dependency purity and Rust file-size checks passed; CUA remains blocked at the sandbox display and accessibility boundaries, no live desktop or user data was touched, and the new exact-head hosted display run remains the rendering proof)

# Restore the last playback context and browser destination

Task session-playback-restore: complete (this commit, base 1aec27a71c, a normal start now restores the last valid browser destination and the complete typed queue, reconstructs the last loaded podcast or YouTube episode from its stable catalog identity as paused metadata, resolves a fresh source on first Play, and selects, expands and centers the restored track or episode only when it is present in the rendered destination; external source artwork now reaches the full player bar through the same SRC-11 gate, cache and decode path as the Now Playing panel; START-1 and BROWSE-5 are replaced by active START-3 and BROWSE-12, with active QUE-11 and PLAY-10 recording the persistence and artwork contracts; every new regression was observed red before its minimal implementation and green afterward, format, strict workspace Clippy, the complete workspace suite, architecture including accessibility and input parity, UX traceability at 305 active rules, Core dependency purity, diff and Rust file-size checks, and live audit with only accepted RUSTSEC-2024-0436 passed; isolated display and CUA observation could not start because the sandbox denied the private D-Bus socket bind with Operation not permitted and cua-driver reported no reachable AT-SPI bus, so visible cover, expansion, selection and centering remain host/manual checks rather than claimed observations; no real database, music files, live desktop or push was used)

# P1a wave 7 — device sync page projection

Task P1a-Welle-7: complete (commits 6e43d318da, 310846de63 and this commit, base 28f5007155, rewrote the device-page copy inside GNOME before moving its toolkit-free seam to `reprise-view`, replaced boundary-rendered strings with catalogued `Message`/`Plural` values, named the four progress fields as `ProgressCopy`, narrowed three broad `DeviceView` reads to core state and plain values, and raised `view_floor` from 904 to 1352; local-time/gettext rendering, conversion from the GTK-owned preparation run, and the compound blocker wrapper remain in the 193-line GNOME adapter because the flat `Message` contract cannot express two independently pluralized nested reasons without contortion; the unchanged device-page expectations passed 15 tests with 6 established display ignores, the new view-only projection regression runs without GTK, format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, dependency purity, diff and Rust file-size checks passed, and the isolated full suite reconciled to 3986 passed, 0 failed and 417 ignored after filtering only the same two sandbox-denied MCP Radio listener fixtures whose exact run failed solely at `TcpListener::bind` with `Operation not permitted`; live audit could not lock the read-only advisory cache while cached audit reported only accepted RUSTSEC-2024-0436, all 6712 newly measured candidate lines remain an upper bound rather than a forecast, the four next clusters are recorded in the wave plan, no repository lock or coordination board existed to release, and no real database, music files, live desktop or push was used)

# Android playback behind the Core contract

Task Android playback Tasks 1-2: complete (this commit, base 2e26d1fefa, measured the Android library player's required `PlaybackBackend` commands and Kotlin-originated event subset before introducing one named Rust-owned UniFFI event object; the bridge accepts production-time stream generations and only StateChanged, Position, TrackFinished, AdvancedToNext and Error, preserving call order without exposing a closure across FFI, while StreamTags and Spectrum remain outside this local-library slice; the focused public-seam test exited 101 red on the missing bridge before passing green, and the x86_64 Android release build plus generated Kotlin bindings proved the object and enums cross UniFFI; format, strict workspace Clippy, live audit with only accepted RUSTSEC-2024-0436, architecture, frontend thinness, gettext catalogs, diff and Rust file-size checks passed; the isolated socket-filtered workspace suite passed 4002 tests with 0 failed and 417 ignored across 59 summaries, while the exact unfiltered run reached only the same two unchanged MCP Radio listener fixtures whose `TcpListener::bind` calls are denied by this sandbox; the Linux backend and Core contract remained byte-identical, no real database, music files, emulator, live desktop, push, repository lock or coordination board was used, and Task 3 was not started)
Task Android playback Task 3: complete (this commit, base 91c8197a6d, implemented the Android `PlaybackBackend` adapter and flat UniFFI Media3 command port, with every trait method entering one Kotlin application-Looper dispatch that runs inline on its owner and otherwise posts and waits; the production listener stamps StateChanged, Position, TrackFinished, AdvancedToNext and Error with the generation captured on that same Looper, `content://` reaches `MediaItem.fromUri` unchanged, pre-fed successors use Media3 gapless playlists, Crossfade is explicitly mapped to Gapless, and audio effects plus spectrum return unsupported-capability errors; `ReprisePlaybackService` now owns ExoPlayer and MediaSession with the required foreground-service permissions and declaration for background playback, notification, lock-screen and media-button integration, while Task 4 app wiring and the separate device check remain untouched; both public seams were observed red before green, the x86_64 native library and generated bindings built, the complete Kotlin production source compiled against Android API 37 and Media3 1.10.1, and the pure Kotlin Looper tests passed; format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, diff and code-file size checks passed, cached audit reported only accepted RUSTSEC-2024-0436 after live audit could not lock the read-only advisory cache, and the isolated socket-filtered workspace suite passed 4003 tests with 0 failed and 417 ignored across 59 summaries while the exact unfiltered run again failed only the two unchanged MCP Radio fixtures whose `TcpListener::bind` calls are denied by this sandbox; Core, GStreamer and `reprise-platform-linux` remained byte-identical, no real database, music files, emulator, device, live desktop, push, repository lock or coordination board was used)
Task Android playback Task 4: complete (this commit, base 2ead2a3687, tapping a track freezes the current Compose list as a Core `Queue` and starts at its tapped cursor; natural end, gapless handoff, Next and Previous all use the queue's `advance_auto`, `next_manual` and `previous` decisions in Rust, while the service-owned Media3 port remains the only audio path and MediaSession commands route back through that Core owner; the deleted activity `MediaPlayer` path has no successor duplicate, and the surface renders play/pause, Previous, Next and the position/duration delivered by bridge snapshots without polling; the public Core-session seam exited 101 red before its focused queue/event tests passed, the Kotlin snapshot projection was observed red before the production compile and pure surface tests passed, and the x86_64 native library plus UniFFI bindings rebuilt; the separate device check remained deliberately unrun, all desktop crates and the Core contract stayed byte-identical, and no real database, music files, emulator, device, live desktop, push, repository lock or coordination board was used)
Verification Android playback Task 4: format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, diff purity, code-file sizes, x86_64 Android build and binding generation, Kotlin production compilation and pure Kotlin surface tests passed; the socket-filtered workspace balance passed 4006 tests with 0 failed and 417 ignored by keyword across 59 summaries, while the required unfiltered command exited 101 only at the same two unchanged MCP Radio fixtures whose `TcpListener::bind` calls are denied by this sandbox after 3533 tests had passed; live audit exited 1 because the managed session cannot lock the read-only advisory cache, and cached audit passed with only accepted RUSTSEC-2024-0436)
Task Android playback Task 5: complete (this commit, base b13e1eb875, recorded the second-backend balance in the Android research report: `PlaybackBackend` was materially better prepared than the five-package storage seam because its commands, events, duration, documented degradation and stream generations came from a real asynchronous GStreamer/GTK path rather than only self-written doubles; Media3 required no Core signature or desktop change, while `set_next`'s path-only meaning and toggle-only play/pause strained, and the missing reusable Queue/backend/event playback session became the main cut to change next time; the report explicitly contrasts those findings with all five storage assumptions and limits its evidence to seams, Android cross-compilation and bindings because the separate device check remained unrun; format, strict workspace Clippy, the socket-filtered 4006-passed/0-failed/417-ignored workspace balance by keyword, architecture, frontend thinness, gettext catalogs, diff purity and code-file sizes passed, cached audit reported only accepted RUSTSEC-2024-0436 after live audit again could not lock the read-only advisory cache, no desktop code, real data, emulator, device, live desktop, push, repository lock or coordination board was touched)

# Android browse and search

Task Android browse Task 1: complete (this commit, base 71369379da, exposed Core-owned album and artist summaries, canonical multi-disc album membership/order and literal text search through UniFFI records without adding SQL, grouping, filtering or sort policy to the bridge; the 13-test Android FFI baseline grew to 17 focused passes and one Core regression pins disc-before-track order, the x86_64 native library plus generated Kotlin bindings built at the final tree, format, strict workspace Clippy, live audit with only accepted RUSTSEC-2024-0436, architecture, frontend thinness, gettext catalogs and Core dependency purity passed, and the isolated workspace balance passed 4011 tests with 0 failed and 417 ignored across 59 summaries after separating only the same two sandbox-denied MCP Radio listener fixtures whose exact unfiltered run failed at `TcpListener::bind` with `Operation not permitted`; the first unisolated workspace control had additionally proved the sandbox's default XDG cache read-only, the non-GTK friction is recorded in the commit message, no Kotlin or desktop frontend source file changed, and Task 2 was not started)
Task Android browse Task 2: complete (this commit, base 4f024f195b, added readable Titles, Albums and Artists Compose tabs, routed every title query including the empty query through Core, opened albums through their Core identity and played from the exact album snapshot rather than the library snapshot; the pure Kotlin browse seam was observed red before its four behaviors passed, the complete Android production source compiled with Kotlin 2.4.10 and its Compose plugin, both Kotlin surface executables passed, format, strict workspace Clippy, architecture, frontend thinness and gettext catalogs passed, and the isolated socket-filtered workspace balance remained 4011 passed, 0 failed and 417 ignored across 59 summaries while the exact unfiltered run exited 101 only at the same two sandbox-denied MCP Radio listener fixtures; live audit could not lock the read-only advisory cache while cached audit reported only accepted RUSTSEC-2024-0436, the Gradle wrapper likewise could not lock its read-only distribution cache so the established direct compiler proof was used, no Rust, desktop frontend, schema, real data, emulator, device, live desktop or push was touched, and the separate device check remains deliberately unrun)
Task Android browse Task 3: complete (this commit, base 11559a418a, compared every browse friction against the current GNOME and Compose seams and separated Core-owned album membership, ordering, empty-search and tapped-context decisions from FFI ownership, widget timing, navigation chrome and formatting; only `session_restore` plus `view_session` of the four mechanically selected P1a clusters gained direct support, while `tag_edit_flow`, `column_layout` plus `keyboard_reorder`, and `missing_view` plus `import_errors_view` gained none from this read-only mobile slice; the report records the more urgent evidence outside those clusters: Android silently materializes at most 500 title or album-track rows without a total or continuation while GNOME traverses a counted model in lazy 200-row windows, and complete album/artist summaries have the opposite unbounded-materialization risk; the documentation acceptance check was observed red before the record and passed afterward, format, strict workspace Clippy, architecture, frontend thinness and gettext catalogs passed, and the isolated socket-filtered workspace balance remained 4011 passed, 0 failed and 417 ignored across 59 summaries while the exact unfiltered run exited 101 only at the same two sandbox-denied MCP Radio listener fixtures; live audit could not lock the read-only advisory cache while cached audit reported only accepted RUSTSEC-2024-0436, no product or desktop code, real data, emulator, device, live desktop or push was touched, and the separate device check remains deliberately unrun)

# Filter-clear playback continuation

Task filter-clear-random-continuation: complete (this commit, base 2684ed4d52, kept search- or facet-filtered Music playback immutable until exhaustion and then, only when the visible Music list is the complete live library, handed off to a fresh random full-library snapshot whose first title differs from the one just finished; active PLAY-11 records the exception while ordinary unfiltered snapshots, still-filtered lists, playlists, one-title libraries, explicit Play Next and Repeat semantics remain unchanged; the focused public decision seam was observed red before both PLAY-11 regressions passed green, format, strict workspace Clippy, live audit with only accepted RUSTSEC-2024-0436, architecture, frontend thinness, UX traceability, Core dependency purity and the CUA helper contract passed, and the isolated socket-filtered workspace balance passed 4013 tests with 0 failed and 417 ignored across 59 summaries while the exact unfiltered run exited 101 only at the same two sandbox-denied MCP Radio listener fixtures; the deterministic CUA scenario was added but its isolated visual run stopped before app launch because Xvfb could not bind its private `/tmp/.X11-unix` socket in the sandbox, no real database, music files, live desktop or push was used)
# Counted library windows

Task library-window: complete (commits 194c2080b7, 885ca3c0ac, 9856bda6ec, 6fea7e3b1c and this commit, base 2684ed4d52, named one owned Core request and counted window response, moved all four browse facades plus UniFFI to bounded continuations, and made Compose page title, album, artist and album-track lists in 200-row windows without changing the desktop; the greater-than-one-window regressions prove exact totals and gapless continuations while the repaired Gradle task now executes 18 real JUnit tests rather than discovering zero, with a deliberate visible-count mutation observed as exactly 1 of 18 failures before all 18 passed; the research record keeps the load boundary honest: an emulator Storage scan counted 3.520 provider round trips for 1.824 files in 562 directories, or 1,93 per file, with the picker's 42 calls separate, but listing and tag reads remain unseparated, the observed 3–5/s progress rate showed no downward trend without becoming a timing benchmark, and `scan_folder_inner` still holds one transaction across the entire scan; format, strict workspace Clippy, live audit with only accepted RUSTSEC-2024-0436, architecture, frontend thinness and gettext catalogs passed, and the isolated unfiltered workspace suite reconciled by keyword to 4017 passed, 0 failed and 417 ignored across 59 summaries after the unisolated control failed only 18 cache-writing Core tests against the sandbox's read-only default XDG path; a large real library, physical-device timing, long-scroll behavior and transaction impact remain unmeasured, the plan is fully checked off, no product fix was made for those recorded findings, no real library data, music files or live desktop was touched, no repository lock or coordination board existed to release, and nothing was pushed)
Task track-change-ui-stall P0: complete (this commit, base 5fff82d152, loaded-track markers now update in place before an idle-deferred pixel-adjustment reveal, and a monotonic generation guard cancels superseded retries without changing NAV-10a automatic-scroll suppression, selection, focus, or the FIL-9 centring math; the display-free generation-and-centre regression failed at compile time before implementation and passed afterward, format, strict workspace Clippy, the complete isolated workspace suite, UX traceability with 306 active rules, architecture, diff and Rust file-size checks passed, and audit reported only accepted RUSTSEC-2024-0436; no display-backed test, real database, music file, live desktop, push, repository lock, coordination board, or unrelated worktree was used)
Task track-change-ui-stall P1: complete (this commit, base e77be7a929, empty search binds now return before resolving the accent and borrow rather than clone the live filter, unchanged ratings skip the five-star refresh, and same-track rating-cell rebinds retain their refresh and now-playing registry callbacks while unbind still clears both; the three display-free hot-path regressions were observed red at compile time before passing with all FIL-5 tests, format, strict workspace Clippy, the complete isolated workspace suite, UX traceability with 306 active rules, architecture, diff and Rust file-size checks passed, and audit reported only accepted RUSTSEC-2024-0436; the rating factory was extracted cohesively to keep substantially edited Rust files below 800 lines, and no display-backed probe, real database, music file, live desktop, push, repository lock, coordination board, or unrelated worktree was used)
Task track-change-ui-stall P2: complete (this commit, base 7e95a1068b, the cover accent now changes the display-wide provider once per track and lets a temporary all-toplevel CSS class interpolate accent-derived properties over the existing 400 ms Ambient token, while the no-op Adwaita target only owns replacement-safe cleanup and reduced-motion timing; the display-free provider/end-point regression was observed red at compile time before all 12 cover-accent tests passed with one existing display test ignored, format, strict workspace Clippy, UX traceability with 306 active rules, architecture, diff and Rust file-size checks passed, the requested optimized reprise-gnome build completed, the complete touched-crate suite passed 1323 tests with 392 display tests ignored, and the refreshed cached audit reported only accepted RUSTSEC-2024-0436 after the default fetch could not lock the sandbox's read-only advisory cache; one complete workspace run passed before the final multi-toplevel review adjustment, while subsequent final-tree workspace attempts exposed only moving pre-existing yt-dlp helper/log-capture and CAVA spectrum timeouts in untouched Core/Linux crates, every exact failing regression passed alone, and the serialized run cleared all 2014 Core tests before the independent Linux CAVA timeout; no display-backed probe, real database, music file, live desktop, push, repository lock, coordination board, or unrelated worktree was used)

# Updates popover delta

Task updates-popover-delta: complete (commits 76ab32d0fb, 53d0893610, d6f140023b, 1e85c46cb9, 60eea74ac3 and this commit, base 912456dc65, replaced the Updates overview pile with a capped pre-stamp visit batch, unified release candidates with badge eligibility, preserved the whole unseen scope for stamping, made empty sections disappear while retaining active-module jump rows, moved refresh and age into the fixed 380-pixel header, and kept release status chips allocated while hover or focus reveals actions; the six DeltaBatch rules were observed red before implementation, the required final Core suite passed 2021 tests and the GNOME build passed, and format, strict workspace Clippy, architecture, frontend thinness, UX traceability at 307 active rules, motion tokens, Core dependency purity, diff and Rust file-size checks passed; cached audit reported only accepted RUSTSEC-2024-0436 after live audit could not lock the sandbox's read-only global advisory cache, while gettext freshness reports the deliberately deferred catalog regeneration required after the plan's prohibition on editing `po/*.po`; display-gated NR-9b, NR-10a and NR-23 regressions compile but remain deliberately unrun in this displayless sandbox, no release or concert overview, schema, translation catalog, real database, music file, live desktop, push, external file, repository lock or coordination board was touched)

Task updates-popover-delta follow-up: complete (this commit, base 5585525a87, two design defects found by screenshotting the finished popover against a copy of the real library rather than by any test: the count chip announced a held-over batch as "N new" while the badge had already cleared, and moving "Fetch now" into the header had reduced it to an unlabelled symbolic glyph; DeltaBatch now carries `unseen` so a held-over batch renders without a count, the update age moved inside the fetch button to restore one labelled target, and NR-23 plus the spec record both causes; 27 Core updates tests and all 14 isolated display tests passed, UX traceability passed at 307 active rules, and the gettext catalogs were regenerated with --no-fuzzy-matching after the string changes)
Task filter-clear-stop-case-matrix: complete (this commit, base ed31a77849, verified PLAY-11 on a live desktop-class host after the sandbox run could not reach app launch: the hand-off was observed visually in two independent runs — filtered title playing, filter cleared mid-playback with the running title and snapshot unchanged, then a different library title taking over with the full list visible and a log free of GTK/GLib criticals, panics and RefCell failures — and the adversarial review of the diff found no defect, so no product code was changed; the six documented stop cases were then covered end-to-end in a new `scripts/cua-e2e/filter_clear_matrix.sh`, one app instance, library and profile per case, asserting the continuation log line, the seeded-snapshot count that catches a mis-aimed click, and a clean log, all seven cases passing; the runner is deliberately AT-SPI-free because cua-driver 0.17.0 returns `atspi_tree_empty` for window scope on this host even though the a11y bus is healthy and the registry reports children, which a stock gnome-calculator reproduces, so the existing CUA suite degrades rather than fails here; `REPRISE_SMOKE_REPEAT=all` was found to be overwritten by the session restore ~60ms later and is filed as #250, so the Repeat case uses the transport button, and the cua-driver walk defect is filed upstream as trycua/cua#2823 after enumerating the registry to prove the app is present with the right PID and a non-empty tree while an empty `xdg-desktop-portal-gtk` object sorts first; format, strict workspace Clippy, the isolated workspace suite at 4015 passed/0 failed/417 ignored across 59 summaries including both MCP Radio listener fixtures that the sandbox had denied, audit with only accepted RUSTSEC-2024-0436, architecture, frontend thinness, UX traceability, the CUA helper contract, Core dependency purity and diff checks all passed against a `crates/` tree byte-identical to ed31a77849; no real database, music files, live desktop session or push was used, and the published PLAY-11 commit was left untouched)
# Optional Rhythmbox source capability

Task Multi-surface-P3: complete (commits b27d8642a9, 32f98e2d50, aceb4cc33d and this commit, base 5fff82d152, named a required `RhythmboxImportCapability` with explicit Unix `Supported` and DocumentsProvider `Unsupported` answers, kept the candidate path in GNOME, routed rhythmdb and playlist content, metadata and presence through `LibrarySource`, and made the first-run offer distinguish unsupported, absent, non-file and unknown without a compile-time platform erasure; the verified inventory corrected three `File::open` plus one `metadata` with the omitted `playlists_path.is_file`, and all five direct calls are gone; the Core seam first failed to compile for the missing capability, source-backed parser and unsupported error before its provider-only XML and reject-before-read tests passed, while the GNOME policy test first failed for the missing decision function before passing beside the byte-unchanged desktop file/missing/directory test; adversarial review extracted the new capability type when `source.rs` reached 803 lines, leaving it at 793 and every edited Rust file below 800; format, strict workspace Clippy, architecture, frontend thinness, gettext catalogs, Core dependency purity and the isolated full workspace balance of 4022 passed, 0 failed across 59 summaries passed, while the deliberately unisolated first control failed only 18 cache-writing tests against the sandbox-read-only default XDG path; live audit could not lock the read-only global advisory cache and cached audit reported only accepted RUSTSEC-2024-0436; no real database, music files, live desktop, push or remote branch was touched, the dedicated worktree lock is released after the final record commit, and no manual rendering check is required because the desktop present/absent/non-file behavior and widgets are unchanged)

# Rhythmbox source contract correction

Task Multi-surface-P3-capability-withdrawal: complete (this commit, base d4c44052f3, withdrew `RhythmboxImportCapability` from `LibrarySource` after the owner measurement found twelve required answers serving only the concrete GNOME call and the self-created Core guard; removed the enum, all trait/adapter answers, `require_rhythmbox_import`, `UnsupportedSource` and its test while retaining `&dyn LibrarySource` as the already-real `open_read`/`probe` seam, because a Rhythmbox-only trait would duplicate those primitives and a concrete Unix parameter would discard the provider-only XML proof; no guard prevents a future intentional non-GNOME caller because no such caller or requirement exists, and CLI, MCP and Android remain unwired; all five direct Core filesystem reaches remain absent, only confirmed `Absent` or a confirmed non-file suppresses the GNOME offer, and the renamed policy regression failed first against the old two-argument capability seam, passed after the correction, failed with the exact Unknown-to-false mutation and passed after restoration; all 15 remaining Rhythmbox Core tests passed, format, strict workspace Clippy, architecture, frontend thinness apart from its explicit optional cargo-machete skip, gettext catalogs, Core dependency purity, diff and Rust file-size checks passed, and the isolated workspace suite summed by keyword to 4065 passed, 0 failed and 431 ignored across 59 summaries — exactly one fewer pass for the deleted speculative guard; sandbox `cargo audit` scanned 479 locked dependencies and reported only accepted RUSTSEC-2024-0436, but its global advisory database cannot be treated as live-refreshed here, so the owner live audit remains the authoritative security gate; no real database, music files, live desktop, push or remote branch was touched, and no repository lock or coordination board exists to release)

# Reprise Mobile — option 2a M1 ground

Task mobile-m3-M1: complete (this commit, base fee534edeb, placed every existing Android screen on one dark-only Nocturne Material 3 theme with the measured teal primary, blurple tertiary, ground and surface roles, the 4/8/12/16/28 shape scale and Roboto Flex typography; bundled Roboto Flex and Material Symbols Rounded with their upstream licences for offline use; the architecture gate now fails closed on raw Compose colour constructors or named literals outside `NocturneTheme.kt`, its missing-checker regression exited 127 red before green, and a deliberate `Color(0xFF4FDBD4)` in the real browse source exited 1 before removal; the browse implementation and both existing test files remained unedited, their freshly executed XML recorded 18 tests with 0 failures/errors/skips including the 500-of-1824 visible-count and request-once continuations, and both XML mtimes were newer than the recorded Gradle start rather than merely reported up to date; format, strict workspace Clippy, the isolated 59-summary workspace balance at 4065 passed/0 failed/431 ignored, live audit with only accepted RUSTSEC-2024-0436, architecture, QA linters, Core dependency purity, x86_64 Android native build plus regenerated UniFFI bindings, forced-fresh Gradle unit tests and debug APK assembly passed; no `reprise-gnome` source, browse behaviour, real data, emulator, device, live desktop or push was touched, and M2 was not started)

# Reprise Mobile — option 2a M2 library screen

Task mobile-m3-M2: complete (this commit, base de211e85d5, carried Core `Track.play_count` and `Track.rating` through the existing paged `TrackWindow` and Kotlin projection without changing its total, `has_more`, offset or limit contract; replaced the plain browse surface with the Nocturne 2a Library frame at the specified 64/32/72/56/72/80 dp metrics, real play-count badges and ratings, an 8-percent teal current-row tint with on-primary-container title and four animated playing bars, a progress-bearing mini player, and the one honest navigation destination Library; Playlists, Queue and Sync destinations plus the sync-assist chip remain absent because the Android FFI has no calls or pairing/transport backend for them, and cover slots use an explicit no-artwork symbol because artwork also does not cross this boundary; the redesigned list-end regression was observed red at Kotlin compile time before proving one continuation after row 500 and none for a repeated offset, while the FFI regression was observed red for both missing fields before preserving the exact paged result; adversarial review retained playback errors and the full progress track, format, strict workspace Clippy, architecture and theme lint, live audit with only accepted RUSTSEC-2024-0436, x86_64 Android native build and regenerated UniFFI bindings passed, and the isolated workspace balance summed by keyword to 4071 passed, 0 failed and 430 ignored across 59 summaries; deleting `android/app/build/test-results/testDebugUnitTest` before the exact Gradle test/APK command forced one executed test task, and both resulting XML mtimes were newer than the recorded start with 21 tests, 0 failures, 0 errors and 0 skipped; every edited code file remains below 800 lines, zero paths under `crates/reprise-gnome` changed, no real data, emulator, device, live desktop, push, repository lock or coordination board was used, and M3 was not started)

# Reprise Mobile — option 2a M3a artwork boundary

Task mobile-m3-M3a: complete (this commit, base ebbc05d904, made the cover-cache root an immutable platform input while keeping the desktop XDG wrappers and every `reprise-gnome` path byte-identical; added one lazy per-track UniFFI artwork call outside all paged queries, source-backed embedded and sidecar resolution with no network path, an exact 168 px thumbnail for the measured 56 dp slots at 3x density, bounded Kotlin path caching, off-caller decode work and generation-guarded delivery for recycled rows; edge-to-edge is explicit, the frame consumes only the status-bar inset, and Material 3 NavigationBar retains its own bottom inset without changing the 64 dp app bar or 80 dp navigation bar; the list-query regression failed under an intentional eager-resolution mutation before passing, and the delivery regression failed when its final generation check was removed before passing; format, strict workspace Clippy, the complete isolated workspace balance of 4081 passed, 0 failed and 433 ignored across 59 summaries, architecture, frontend thinness, Core dependency purity, Android theme lint, both Android native targets plus regenerated UniFFI bindings, forced-fresh Gradle unit tests and debug APK assembly passed; both fresh result XML files were newer than the recorded Gradle start and summed to 27 tests with 0 failures, errors or skips; the one already-committed `updates_tests.rs` expression current rustfmt rejected was mechanically normalized so the full format gate is green, live audit could not lock the sandbox-read-only global advisory database while the cached scan reported only accepted RUSTSEC-2024-0436, every edited code file remains below 800 lines, no real data, emulator, device, live desktop, push, repository lock or coordination board was used, artwork rendering and system-bar geometry remain the explicit owner device check, and M3b was not started)
Task mobile-m3-M3b: complete (this commit, base 2dc0cc2227, added the predictive-back Now Playing sheet growing over the preserved Library scaffold at the measured 364/28 dp and 28/36 plus 16/24 type metrics; the 1092 px MobileFull cover stays one per-size lazy off-main-thread request with the M3a generation guard, list covers remain 168 px and paged queries remain artwork-free; exported seek, shuffle and typed repeat setters plus honest snapshot readbacks, with the seek-head ownership regression observed red before snapshot ticks stayed suppressed through drag release and Repeat One verified on Media3's production AdvancedToNext path rather than a fake-only completion path; TrackRow now carries Core id, the rating boundary returns a visible error when no live row matched, and playback records exactly one play through should_count_play using a per-track high-water position; only backed playback, mode and rating actions ship, with no Lyrics, playlist, queue or sync affordance; adversarial review corrected the Repeat One event path and per-size/per-track Compose state keys, and cohesive test/type extractions leave every substantially edited code file below 800 lines; format, strict workspace Clippy, the final isolated workspace balance of 4089 passed, 0 failed and 433 ignored across 59 summaries, live audit with only accepted RUSTSEC-2024-0436, architecture, frontend thinness, Core dependency purity, Android theme lint, both Android native targets plus regenerated UniFFI bindings, forced-fresh Gradle unit tests and debug APK assembly passed; the test-result directory was deleted before Gradle, both TEST XML mtimes were 1785841810 after the recorded 1785841786 start and summed to 32 tests with 0 failures, errors or skips; zero `crates/reprise-gnome` paths, real data, emulator, device, live desktop or push were touched, no repository lock or coordination board existed to release, and the owner still needs to verify sheet growth/collapse, predictive-back motion, 364 dp cover rendering, wave drag smoothness, mode/rating feedback and list continuity on the target emulator; M4, M5 and M6 were not started)
Task mobile-m3-M3b-refactor: complete (this commit, base 1bbddd9775, applied the accepted review of M3b without starting M4, M5 or M6; the Repeat One regression now runs on a two-track fixture because a one-track queue wraps to index 0 under both Repeat::All and Repeat::One and therefore passed the reviewer's `AndroidRepeatMode::One => Repeat::All` mutation — that same mutation was re-applied and observed red at `Repeat::One must stay on the track Media3 just advanced past, left Some(1), right Some(0)` before being reverted green; play counting moved off Media3's application thread into one named session-owned writer thread holding a single long-lived handle over the same database file, so the half-duration position tick and the `TrackFinished` handler in front of `play_uri` no longer carry a `Db::open_ready` plus `UPDATE`, the timestamp is taken where the play happened, a play that cannot be queued or written leaves a `tracing` warning instead of vanishing, and `Drop` drops the sender then joins so teardown drains what is queued — which is also what makes the play-count test deterministic through an explicit session drop; the shared `stats::set_rating` was returned to its unconditional id-addressed `UPDATE` after the Android task had silently narrowed it with `AND removed_at IS NULL`, it now delegates to the identical `set_rating_in` statement so only three rating writers remain and each answers a different question (unconditional, live-row-and-report, live-row-and-still-this-file), pinned by a seeded removed track asserting the desktop writes it while the reporting writer refuses it; the rating failure left the playback snapshot for its own transient message with an occurrence counter so a repeat restarts its four-second dismissal instead of being wiped by the next 500 ms tick or standing forever while paused; `nowPlayingExpanded` became `rememberSaveable`; and the frame deviations were corrected — the wave now runs only left of the head with a flat line right of it, the readout is elapsed in teal with the remainder as `−m:ss` instead of `1:53 / 2:20`, and the play button is 80 dp on the shape scale's 28 dp rounded square with a 40 sp glyph, all three pinned by unit tests; `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test -p reprise-core --lib` at 2036 passed 0 failed 1 ignored, `cargo test -p reprise-android-ffi` at 26 passed 0 failed, and `scripts/check-android-theme.sh` all exited 0; the Gradle test-result directory was deleted before the run, both TEST XML mtimes were 1785844224 after the recorded 1785844211 start and summed to 35 tests with 0 failures, errors or skips alongside a green debug APK; zero `crates/reprise-gnome` paths changed, no emulator, device or `adb` was touched, and the shared artwork executor, the sheet's auto-reopen and truncated exit animation, bitmap recycling and a Compose `androidTest` source set were left alone as explicitly out of scope)

# Android Compose behavior in the host gate

Task mobile-compose-ui-tests: complete (this commit, base ca71c2b325, measured the two available routes and chose host-side Compose testing because `compose-ui-test-junit4` 1.11.4 runs green with Robolectric 4.16.1 on simulated API 36 inside the existing device-free `:app:testDebugUnitTest` task, whereas an `androidTest` suite could not join or execute in that gate without a device or forbidden `adb`; three real composable tests now drive the seek drag across a snapshot tick and release, keep failed ratings visible and visually unchanged, and open the sheet from the mini player, retain the Library composition, then close through activity back; selected-star and mini-player accessible semantics were the only production changes needed; the seek mutation failed at expected 72666 versus 10000 before restoration, and the optimistic-rating mutation failed with Rate 3 reporting Selected true before restoration; the untouched baseline ran 36 tests, while the final fresh result directory ran 39 with zero failures, errors or skips, all three XML mtimes 1785852352 after start 1785852332; Android theme lint and the exact Gradle test plus debug-APK gate exited 0, every edited code file remains below 800 lines, zero paths under `crates/` changed, no emulator, device, `adb`, app, browser, dialog, window or owner desktop was touched, nothing was pushed, and no repository lock or coordination board existed to release)
# Reprise Mobile — option 2a M5 themes

Task mobile-m3-M5: complete (this commit, base ca71c2b325, carried appearance settings through UniFFI as typed stored-theme, writable-theme and color-scheme values rather than a raw key/value pair: unsupported ids retain their payload for fallback while Android can persist only `nocturne` or `dynamic`; startup applies known ids, keeps Nocturne as the default and API fallback without writing back, and exposes wallpaper-seeded dynamic light/dark palettes only on API 31+ through the existing overflow menu; the unknown-id no-write-back surface regression was observed red at Kotlin compile time before passing, with separate red-green slices for typed explicit writes, pre-31 option suppression and the rule that only dynamic colour consults `ui.color_scheme`; the widened directory theme lint first rejected a second theme file, then passed while its deliberate raw colour outside that directory still failed as required; adversarial review kept the Android FFI root below the hard file-size gate by moving its existing lock helper to the owned library type, and confirmed GTK's existing unknown-id startup fallback does not persist; format, strict workspace Clippy, architecture including Android theme lint, the isolated workspace balance of 4098 passed, 0 failed and 433 ignored across 59 summaries, cached audit over 1189 advisories with only accepted RUSTSEC-2024-0436, and the x86_64 Android native build with regenerated UniFFI bindings passed; the live audit refresh alone could not lock the sandbox-read-only advisory cache; the Gradle result directory was removed from its build path before `:app:testDebugUnitTest :app:assembleDebug`, all three TEST XML files had mtime 1785852477 after start 1785852461 and summed to 41 tests with 0 failures, errors or skips; zero `crates/reprise-gnome` paths changed, no app, preview, browser, emulator, device or `adb` was touched, dynamic wallpaper colours, overflow interaction and light/dark system-bar presentation remain the owner's visual check, no repository lock or coordination board existed to release, no push was performed, and M6 was not started)
Task mobile-m3-M6-core-curve: complete (this commit, base 3206eaa7d3, replaced the anonymous ten-value setting with a validated ordered frequency/gain curve and schema-v53 migration at GStreamer's real centres; the migration regression was observed red at schema 52 before proving that all ten unchanged GNOME-facing levels project exactly, the old key is removed rather than retained as a second truth, projection is deterministic in log-frequency space and clamps only its ephemeral picture, and malformed, non-finite, unordered or corruption-range payloads fall back safely; the existing ten-band Core facade and `AudioEffects` shape remain unchanged so zero `crates/reprise-gnome` lines changed; format, strict workspace Clippy, architecture and file-size gates, Core purity, the isolated workspace balance of 4101 passed, 0 failed and 433 ignored, cached audit over 1189 advisories with only accepted RUSTSEC-2024-0436, Android theme lint and the two-ABI Android native build passed; live audit refresh was blocked only by the sandbox-read-only advisory-cache lock; after redirecting Android and JVM user state into the worktree, the fresh Gradle result directory produced four TEST XML files at mtime 1785854738 after start 1785854699 and 44 tests with 0 failures, errors or skips beside a green debug APK; no app, window, dialog, browser, preview, emulator, device or `adb` was used, and no repository lock or coordination board existed)
Task mobile-m3-M6-live-equalizer: complete (this commit, base 3e5cbc1f96, attached `android.media.audiofx.Equalizer` to Media3's live audio-session id, queried the effect's band count, centre frequencies and level range without a topology constant, projected the authored curve in log-frequency space and clamped only the ephemeral device picture, then released and rebuilt the effect on every session-id change; the controller regression failed at Kotlin compile time for the absent capability seam before proving two different runtime topologies, old-effect release and reprojection, while the FFI regression failed at Rust compile time before proving that viewing and applying pass the exact authored curve without rewriting its stored bytes; unavailable effect construction now leaves an honest absent snapshot instead of crashing playback, and the snapshot crosses UniFFI for the settings surface without exposing the curve to GNOME; format, strict workspace Clippy, the complete workspace suite at 4102 passed, 0 failed and 433 ignored, cached audit over 1189 advisories with only accepted RUSTSEC-2024-0436, architecture, Android theme lint, native Android build, fresh JVM tests at 45 passed with 0 failures, errors or skips and debug APK assembly passed; zero `crates/reprise-gnome` lines changed, every edited code file remains below 800 lines, no app, window, dialog, browser, preview, emulator, device or `adb` was used, nothing was pushed, and the physical device's reported topology and audible effect remain unverified)
Task mobile-m3-M6-settings: complete (this commit, base 55bf33cdf7, replaced M5's overflow palette choices with one Settings route and a Nocturne settings surface carrying the live-band equalizer, gapless and every renderable theme while Crossfade and ReplayGain remain deliberately absent; the Robolectric behavior first failed to compile for the absent surface, then proved that viewing and entering edit mode write nothing, the exact plain warning appears before edit mode, the first slider change replaces the whole curve at the device's centres, the overflow contains Settings rather than theme ids, and the two deferred controls do not exist; the curve setter delegates numeric validation to Core and rejects non-finite, unordered or otherwise invalid payloads before locking or writing, while equalizer-enabled and gapless remain direct typed booleans with one setter per key, so no identifier-preservation fiction or combined read-modify-write guard was introduced; the saved Core `TrackTransition` now drives Android at startup and after every explicit settings write instead of a hardcoded Gapless value, with the regression observed red for the absent reload before Off and Gapless both reached Media3, and Linux's already-real GStreamer transition path stayed untouched; live effect/session changes increment a service-owned revision so an open surface re-reads the new ephemeral topology without persisting it; format, strict workspace Clippy, live audit with only accepted RUSTSEC-2024-0436, architecture, Android theme lint, Core purity, the isolated workspace balance of 4104 passed, 0 failed and 433 ignored across 59 summaries, Android native build and debug APK assembly passed; the exact Gradle result directory was deleted before the final run, all five TEST XML files had mtime 1785857199 after start 1785857156 and summed to 47 tests with 0 failures, errors or skips; zero `crates/reprise-gnome` lines changed, every edited code file remains below 800 lines, no app, window, dialog, browser, preview, emulator, device or `adb` was used, nothing was pushed, and settings rendering, the physical device's band topology and audible equalizer and gapless transitions remain unverified)

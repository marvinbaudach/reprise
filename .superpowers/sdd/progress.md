# My Stats redesign progress

Base: `32d53957f7`
Plan: `docs/plans/mystats-redesign.md`

Task T1: complete (commit f2ac601bb0, base 32d53957f7, appended STATS-10 through STATS-17 and committed the binding plan; 2026-07-25 gates passed with isolated XDG cache/data, display gate intentionally not run)
Task T2: complete (commit c16354c471, base f2ac601bb0, aligned week buckets to local Mondays, extended week granularity through 730 days, and added timezone-aware best-week data; workspace 1225 core + 845 GNOME non-display tests passed, 231 existing display tests ignored)
Task T3: complete (commit fad7e28c85, base c16354c471, exposed the seasonal previous-ms baseline and current-year pace projection; extracted hero tests to keep the 799-line snapshot test file under the limit; workspace 1227 core + 845 GNOME non-display tests passed)
Task T4: complete (commit ffe4bf4f2e, base fad7e28c85, added the ninth pure read for per-genre artist/path rows and folded them through group_key into tile metadata; workspace 1230 core + 845 GNOME non-display tests passed)
Task T5: complete (commit 85e70ef8fb, base ffe4bf4f2e, split the period header from the listening-time hero, added responsive KPI pairs and the exact zero-baseline badge, and migrated STATS-1/1a coverage to STATS-11/11a/12; workspace 1230 core + 845 GNOME non-display tests passed, 233 display tests compiled but intentionally not run)
Task T6: complete (commit 88d63cd4bb, base 85e70ef8fb, rebuilt the ribbon as a weekly accent-area chart with month ticks, a dashed best-week marker, an open current endpoint, exact hover copy, and a reveal clip seam; workspace 1230 core + 848 GNOME non-display tests passed, 233 display tests compiled but intentionally not run)
Task T7: complete (commit 8a4743c15d, base 88d63cd4bb, replaced the spotlight with a local-cover band hero, initials fallback, relative ranks, artist navigation, and leader/rank spelling-unify affordances; workspace 1230 core + 848 GNOME non-display tests passed, 235 display tests compiled but intentionally not run)
Task T8: complete (commit 7e5112fe1f, base 8a4743c15d, added the five-row Songs card with relative gradient bars, exact-track hover/focus play, artist focus navigation, keyboard/pointer context actions, and the expandable sortable full ranking; workspace 1230 core + 848 GNOME non-display tests passed, 239 display tests compiled but intentionally not run)
Task T9: complete (commit 8ac72990ec, base 7e5112fe1f, replaced the genre legend with a 22-pixel ranked segment bar and four local-cover tiles carrying share, duration, top artist, album-path navigation, and spelling-unify hints; workspace 1230 core + 848 GNOME non-display tests passed, 240 display tests compiled but intentionally not run)
Task T10: complete (commit 57b7b13ae0, base 8ac72990ec, rebuilt My Stats in the fixed header-to-genres story order, added the thin-history hint, wired song and genre actions, and removed Clock, Highlights, Customize, and StatsLayout; workspace 1228 core + 840 GNOME non-display tests passed, 241 display tests compiled but intentionally not run)
Task T11: complete (commit 3177a8b3a4, base 57b7b13ae0, removed the retired Clock/Highlights snapshot types, computations, and discovered-count query while retaining event-only STATS-0 coverage through Hero, Top Tracks, and Best Week; workspace 1225 core + 840 GNOME non-display tests passed, 241 display tests compiled but intentionally not run)
Task T12: complete (commit fc4342b8bf, base 3177a8b3a4, added named Stats motion tokens, one-shot count/reveal/fade/bar choreography, period-value tweening, and reduced-motion end states; workspace 1225 core + 840 GNOME non-display tests passed, 244 display tests compiled but intentionally not run)
Task T13: complete (this close-out commit, base fc4342b8bf, final permitted gate sweep passed; 1225 core + 840 GNOME non-display tests passed; the display gate was run externally and passed 125/125)
Accepted review findings: complete (commits ea69a4cb42 through d346d1d0f5, base 04d7a07bcc, applied Groups A-E and the accepted active-rule text corrections; the permitted gate sweep passed with 1227 core + 849 GNOME non-display tests, 253 display tests compiled but intentionally not run in the sandbox; the pre-fix display gate was run externally and passed 125/125)
Polish G1: complete (this commit, base 41370d7cd9, kept every genre segment at the measured 22-pixel bar height and added an allocation-level STATS-15 display regression; permitted gates passed, display test compiled but intentionally not run in the sandbox)
Polish G2: complete (this commit, base 0b1884501f, tightened genre-card spacing, added one-step-lighter rank hovers, and normalized the segment tooltip to compact hours and minutes; permitted gates passed, display test compiled but intentionally not run in the sandbox)
Polish G3: complete with architecture adjustment (this commit, base ce1e6533d7, added durable Genre library scopes plus the required narrow ViewSource query projection, wired segment and tile navigation while preserving cover-to-album behavior, and extended FIL-1c/STATS-15 in lockstep; permitted gates passed, four affected display tests compiled but intentionally not run in the sandbox)
Polish G4: complete with contract alignment (this commit, base 33f8336b4e, cropped sparse yearly axes at the first play week, switched them to weekly bars with a since label, added 12-percent headroom, and measured the best-week label left of its marker; STATS-12 was aligned in the same commit, permitted gates passed, display test compiled but intentionally not run in the sandbox)
Polish G5: complete (this commit, base 1746ee01b1, moved the expandable Top Tracks ranking into its own full-width section between the two-column story row and genres, preserved the fixed five-row Songs card, and aligned STATS-10/STATS-14; permitted gates passed, two affected display tests compiled but intentionally not run in the sandbox)
Polish G6: complete (this commit, base 817f170674, replaced variable inline rank bodies with fixed 24-pixel rank and 180-pixel ellipsized name Grid columns so all runner-up bars share one start and fill the remaining width; permitted gates passed, the allocation display regression compiled but intentionally was not run in the sandbox)
Polish G7: complete (this commit, base 357f8f1077, compacted the expanded Top Tracks rows and shared their 8-pixel pill geometry with Top Five, made metadata links hover-only while preserving focus, and routed every My Stats duration and KPI through one localized N h M formatter; STATS-11/STATS-13/STATS-14/STATS-15 were aligned, permitted gates passed with 856 GNOME non-display tests and 258 display tests compiled but intentionally not run in the sandbox)
Follow-up F1: complete (this commit, base 2d92010bf7, replaced expanded-row button chrome with accessible focusable label links and the intrinsic LevelBar with an exact 8-pixel box-pair bar; permitted gates passed and the unchanged display regression compiled but was intentionally not run in the sandbox)
Follow-up F2: complete (this commit, base 85cd456f18, formatted full hours without trailing zero minutes and sub-hour My Stats durations as N min across every shared-formatter caller, preserving signed trend deltas; aligned STATS-11/STATS-13/STATS-14/STATS-15 and passed all permitted gates)
Entrance R3 S1: complete (this commit, base 017d68699b, added the reusable snapshot-translated SlideBin with a real-main-loop allocation-invariance display test; all permitted gates passed and the display test compiled but was intentionally not run in the sandbox)
Entrance R3 S2: complete (this commit, base 3df5120cb1, replaced the burst with the binding hero/KPI/chart/marker/card/bar sequence, added fold-aware card skipping and left-to-right genre reveals, updated STATS-17, and preserved 250 ms period tweening plus reduced-motion end states; all permitted gates passed and 261 display tests compiled but were intentionally not run in the sandbox)
Entrance display gate prerequisite: complete (this commit, base b1c046837e, routed the story-row stylesheet through the sanctioned test helper and documented the existing keyboard partners and focus semantics for My Stats interactions; all non-display policy gates passed)
Entrance display review R1: complete (this commit, base b3998022bc, deferred fold classification to the first allocated frame so below-fold cards land immediately while genuinely unmeasurable cards retain the fail-open animation path; all non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
Entrance display review R2: complete (this commit, base ad57115e3a, replaced the wall-clock sequence sample with a bounded main-loop poll that proves the hero completes before the genre card starts; all non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
Entrance display review R3: complete (this commit, base 04f35766c4, routed the exact genre-tooltip assertion through a SlideBin-aware segment-button accessor and audited every stats child-chain traversal for the new wrapper; all non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
My Stats polish P2: complete (this commit, base 2c89fbdb95, made SlideBin preserve child expansion and layout hints and strengthened the band-card allocation regression; all requested non-display gates passed, with workspace tests isolated to writable XDG roots after the read-only default-cache run failed; the operator-owned display regression compiled but was not run in the sandbox)
My Stats polish P3: complete (this commit, base f21e0bd3f4, verified the shared SlideBin repair through a full-width proportional genre allocation regression, clipped square inner segments to rounded outer corners, and kept both semantic Other and the final ranked segment neutral; all requested non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
My Stats polish P1: complete (this commit, base 54e184feaa, made thin weekly history a compact continuous timeline with equal slots, zero-week ticks, weekly labels, a divider baseline, and a lighter best-week highlight without the retired dashed marker; all requested non-display gates passed and the operator-owned display regressions compiled but were not run in the sandbox)
My Stats polish P4: complete (this commit, base c98365b091, moved the shared plays/time sort toggle beside the Songs kicker, applied it to both rankings, and filled the compact card with six fixed-spacing rows; all requested non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
My Stats final polish Q1: complete (this commit, base d8a267c469, widened short-history bars to 40 percent of their equal slots with a 48-pixel cap and restored an 8-pixel best-week-label gap; all requested non-display gates passed and the operator-owned display regressions compiled but were not run in the sandbox)
My Stats final polish Q2: complete (this commit, base dc81a48a49, aligned every runner-up band row through one shared artist/action grid and asserted the four rank bars' common right edge with a single-pencil fixture; all requested non-display gates passed and the operator-owned display regression compiled but was not run in the sandbox)
My Stats final polish Q3: complete (this commit, base 66674acbfd, separated the first three genre segments into accent-300/500/700 steps and made later ranks plus the semantic remainder neutral-600 while retaining derived theme colours; all requested non-display gates passed and the operator-owned CSS parser regression compiled but was not run in the sandbox)
My Stats final polish Q4: complete (this commit, base 2b574551f1, made the expanded ranking leave section flow only after its collapse animation, normalized all section gaps to 20 pixels, and replaced the two loose sort buttons with an accessible native AdwToggleGroup that still sorts both rankings; all requested non-display gates passed and the operator-owned display regressions compiled but were not run in the sandbox)
My Stats static entrance simplification: complete (this commit, base 4e5c6b54c2, removed every non-bar entrance path, limited sparse-ribbon motion to staggered baseline growth plus its best-week label, retained a reduced horizontal reveal solely for genre segments, and kept period changes bar-only; all requested non-display gates passed, the three replacement STATS-17 display tests compiled, and the operator-owned display gate was not run in the sandbox)
My Stats entrance order follow-up: complete (this commit, base c83e0be239, chained the best-week label fade to its bar's actual animation completion, guarded stale completions by generation, and made the STATS-17 display regression continuously assert reset-growth-finish-label order; all requested non-display gates passed and the operator-owned display gate was not run in the sandbox)

# Idle Play random-start follow-up

Base: `331217f344`

Task P1: complete (this commit, base 331217f344, made stopped Play/Pause seed and start a random present-library snapshot without row activation, kept empty-library and queue navigation sensitivity honest, and activated PLAY-9; full non-display gates passed, while the rule-named display regression compiled but its isolated Xvfb run was blocked by sandbox D-Bus socket permissions)

# Visualizer sync regression

Base: `7d89d9b20c`

Task V1: complete (this commit, base 7d89d9b20c, reserved spectrum headroom and added sustained bass-dominant breakdown pressure so the decoded WAKE UP profile stays near 8–24% before the breakdown and reaches 88–93% on its major hits; workspace 1312 core + 901 GNOME non-display tests passed, 279 display-required GNOME tests were intentionally ignored, and native visual review remains manual)
Task V2: complete (this commit, base 52efec9753, kept Space global Play/Pause while passive collection views hold focus, preserved native Space handling for direct controls and text entry, and extended ACC-4's isolated GTK and CUA coverage; WAKE UP diagnosis found the reported missing 16-second peak came from the legacy nested worktree, while the canonical worktree's exact player-to-scene profile reached 89% during the UI's displayed 16-second window; workspace 1312 core + 901 GNOME non-display tests passed, 280 display tests were intentionally ignored by the workspace run, and the targeted Xvfb test plus real key-event CUA path passed)
Task V3: complete (this commit, base 05b4adad89, moved the Spectrum analyzer ahead of ReplayGain so stored Track/Album gain cannot attenuate visual energy while audible normalization remains unchanged; the exact WAKE UP Track-ReplayGain profile improved from 43% to 93% visible peak at 16 seconds and from 37% to 87% at 36 seconds; workspace 1312 core + 901 GNOME non-display + 81 Linux platform + 45 stems tests passed, 280 display tests were intentionally ignored, and one unrelated device-sync test failed once before passing both isolated and in the full rerun)
Task V4: complete (this commit, base 9aa28f69fb, added a faster-than-realtime song-corpus profiler and calibrated Bars against WAKE UP, To the Hellfire, and Sun//Eater so compressed metal retains headroom while the reported 16- and 36-second WAKE UP windows peak near 80-82% mean and 88% tallest-bar height with 16-32 ms best alignment; the quieter half now produces 0.5% false-full frames in WAKE UP and at most 6.5% across the Lorna Shore corpus, a three-tick pressure hold preserves fluid short-hit attack, and selected passive view tabs again leave Space to global Play/Pause; workspace 1314 core + 901 GNOME non-display + 81 Linux platform + 45 stems tests passed, 281 display tests were intentionally ignored, the targeted Xvfb regression and isolated real-key CUA song-visuals scenario passed, core purity and architecture/UX gates passed, and cargo audit reported only the accepted RUSTSEC-2024-0436 warning)

# Podcasts & Radio

Task Z1: complete with sandbox-limited visual verification (this commit, base 4b9b80627f, implemented podcast RSS/YouTube and radio-browser sources, external playback/MPRIS, source views, reversible removal, preferences, schema v33 future-only baselines, and all AF rules; fmt, clippy, 1398 core tests, 951 GNOME non-display tests, audit with only accepted RUSTSEC-2024-0436, core purity, motion, input, accessibility, and UX traceability passed; the unchanged origin/dev 806-line MCP file keeps the architecture script red, and sandbox D-Bus socket denial blocks display tests plus the isolated application smoke; real feeds/streams, yt-dlp, ICY, GNOME-Shell MPRIS, and theme icon optics remain the documented manual pass)
Podcast & Radio MCP follow-up: complete (commit 277aad8540, base a1eb838bf4, added capability-gated podcast/YouTube and radio add/edit/remove/refresh tools plus URL-safe cached resources; fmt, strict clippy, workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, motion, input, accessibility, and UX traceability passed; the pre-existing 806-line MCP data file remains the sole architecture-lint failure, the 284/285 parallel display run's unrelated My Stats failure passed on isolated rerun, and a throwaway live MCP smoke read DGE RSS, HOLLOW FALLEN via yt-dlp, and RADIO BOB Metal via PLS without exposing source URLs)

# Concerts credential feedback follow-up

Base: `e38791b251`

Gate repair: complete (commit 47d26b1c1c, base e38791b251, split the MCP data unit tests into a sibling module so the architecture file-size gate passes)
Point 1: complete (commit 72e85defff, base 47d26b1c1c, propagated Concerts settings changes live to the view, sidebar and Updates popover, hid credential-ineligible fetch actions, and made 1000 km the default with 100/250/500/1000 km presets; all non-display gates passed, while the rule-named display regression compiled but its isolated Xvfb run was blocked by sandbox D-Bus socket permissions)
Point 2: complete (commit 5786b63f97, base 72e85defff, kept SET-4 immediate persistence while adding the same Apply/Enter affordance as City and an inline saved acknowledgement to both credential rows; all non-display gates passed, while the display regression compiled but its isolated Xvfb run was blocked by sandbox D-Bus socket permissions)
Point 3: complete (commit 5bc3b56b8b, base 5786b63f97, verified provider credentials off-thread through the shared limiter, mapped fixture-only success/rejection/timeout/transport outcomes to inline feedback, kept secrets out of fixture logs, activated CONC-8 and replaced CONC-5 with CONC-5a; all non-display gates passed, while isolated Xvfb display launches were blocked by sandbox D-Bus socket permissions)
Sidebar progress follow-up: complete (this commit, base 5bc3b56b8b, pinned the shared progress activity below Issues at the sidebar bottom and added an FB-2a allocation regression)
MCP catalog follow-up: complete (this commit, base e7a208112e, added complete path-free Concerts cache and Releases history resources while preserving the filtered Concerts contract and never exposing provider credentials)

# Instrumental production readiness

Base: `3fc0b2bf8a`

Task I1: complete (this commit, base 3fc0b2bf8a, removed the test-only fake backend from user builds and gated the context action and worker on a compiled production backend)
Task I2: complete (this commit, base ee14aa8d7a, exposed one verified runtime-readiness contract for the model and native ONNX Runtime and made the production backend and preferences consume it)
Task I3: complete (this commit, base e4bd73aae4, packaged the official x86_64 ONNX Runtime 1.22.0 archive with a pinned SHA-512 and licence notices, embedded the installed library path and build-time SHA-256, and added a release packaging contract; fmt, strict clippy, affected stem tests, QA linters, core purity, and the workspace suite with the two local-TCP MCP fixtures filtered passed; the unfiltered suite reached only those two fixtures, which the sandbox blocks at TcpListener::bind with EPERM; audit reported only accepted RUSTSEC-2024-0436)
Task I4: complete (this commit, base c628b1a4f3, moved all native inference into a separately packaged reprise-worker process, replaced the GTK render thread with a serialized process supervisor, preserved provisioning without linking ort into GTK, and added dependency, lifecycle, and Meson install contracts; fmt, strict workspace clippy, 1401 core + 948 GNOME non-display workspace tests, QA linters, UX traceability, architecture, worker-isolation/runtime-packaging contracts, core purity, an installed-binary scratch smoke, and the full workspace suite passed; audit reported only accepted RUSTSEC-2024-0436 from the cached advisory database because the sandbox cannot lock the global cache for a refresh)
Task I5: complete (this commit, base 81d14c4d11, made the experimental preference gate immediately create or remove one window-owned conversion surface and worker supervisor, cleared wake dispatch on disable, refreshed all sidebar/browse/AI projections live, and added lifecycle regressions; fmt, strict workspace clippy, full workspace tests, all-features instrumental tests, architecture, accessibility, input, motion, UX traceability, worker isolation/runtime packaging, core purity, and audit passed with only accepted RUSTSEC-2024-0436; the rule-named GTK regression compiled but its isolated Xvfb run was blocked by sandbox D-Bus socket EPERM)
Task I6: complete (this commit, base ff27d37c31, made the gated Conversions sidebar row a real multi-track drop target, routed context-menu and drop requests through one missing-safe batch/dedup path, refused to enqueue until the model and packaged runtime verify, and opened the exact Experimental settings page when first-use assets need action; fmt, strict workspace and all-features GNOME clippy, isolated full workspace tests, architecture, accessibility, input, motion, UX traceability, worker isolation/runtime packaging, and audit passed with only accepted RUSTSEC-2024-0436; INST-14 records the new drop contract as a planned rule proposal for stage review)
Task I7: complete (this commit, base 27b21049f0, added a packaged worker end-to-end acceptance that creates an isolated job, renders the fixture through a separate installed process, verifies staging and lifecycle events, and made the release gate execute it; fmt, strict workspace clippy, all non-socket workspace tests serially, all-features instrumental and stems tests, Meson install, architecture, accessibility, input, motion, UX traceability, worker isolation/runtime packaging, core purity, and cached audit passed with only accepted RUSTSEC-2024-0436; the sandbox blocks two unrelated loopback-listener MCP tests with EPERM, and real HTDemucs inference remains the explicit model/runtime-gated manual acceptance)

# Ticketmaster bundled build credential

Base: `3fc0b2bf8a`

Task TM1: complete (commit aefc61d4c5, base 3fc0b2bf8a, resolved Ticketmaster credentials in stored then runtime then bundled-build order, treated blank values as absent, redacted credential debug output, and documented local and GitHub Actions release injection plus binary extractability; required gates passed with isolated XDG state and serial workspace tests; visible affiliate disclosure remains a separate review question before public release)
Task TM2: complete (commit 14f40a4442, base aefc61d4c5, removed the Ticketmaster credential editor, the credential-entry status action and the Updates key hint while retaining the optional Bandsintown row and legacy stored Ticketmaster resolution; all required gates passed after one unrelated one-shot progress flake passed 10 focused reruns and the repeated full workspace gate)
Task TM3: complete (this commit, base 14f40a4442, promoted New Releases and Concerts settings to peer Preferences pages, left only their activation switches on Plugins, preserved the newly merged Podcast and Radio plugin controls, kept page controls live-sensitive to module state, and activated SET-7; fmt, strict clippy, audit, core purity, architecture and all non-socket workspace tests passed after rebase onto current dev, while two new Radio MCP loopback tests were blocked only by the sandbox's TCP bind restriction)

# PR 64 UI follow-up repair

Base: `428c9bbff9`

Autocomplete surface: complete (commit 6f69d0aeff, base 428c9bbff9, preserved the popover surface behind transparent inner autocomplete views and added the manual TAG-9 rule proposal)
Repeat tooltips: complete (commit bbe3028309, base 6f69d0aeff, explained all three repeat modes from the current behavior)
Now Playing transition: complete (commit 2b8302599a, base bbe3028309, replaced the whole-panel crossfade with an interruption-safe outgoing-cover fade that waits for cold-cache resolution and activated NPP-13 with corrected MOT-1/MOT-6 references)
Catalog gate repair: complete (commit 20591fb5cb, base 2b8302599a, synchronized all seven catalogs with the Podcasts/Radio sources inherited from dev, completed the German and Spanish translations, and removed fuzzy entries)
Verification: complete with sandbox exclusions (format, strict clippy, rustdoc, architecture, accessibility, input, UX traceability, motion, gettext, audit with only accepted RUSTSEC-2024-0436, 1404 core tests, 959 GNOME non-display tests, and all other workspace tests passed; two inherited MCP Radio tests requiring local sockets and the isolated NPP-13 Xvfb regression were blocked by sandbox socket permissions and remain required on GitHub CI)

# Android synchronization follow-up

Base: `e83d7303955`

Single-device navigation polish: complete (this commit, base e83d7303955, skipped the chooser for one connected device, restored sidebar visibility with non-homogeneous content sizing, and replaced the split-action device row with a vertically centered neutral full-card navigation target; format, strict clippy, audit, core purity, architecture, accessibility, input, motion and UX traceability gates passed, while two inherited MCP Radio tests and isolated display tests remain blocked by sandbox socket permissions)
Dev compile repair: complete (this commit, base ea1b3dc7c1, removed the stale Concerts preferences callback that the single-device navigation rebase reintroduced after the callback API had intentionally been deleted)

# Tag autocomplete design 4a surface follow-up

Base: `d889ad6073`

Task TAG-AUTOCOMPLETE-SURFACE: complete (this commit, base d889ad6073, made the complete popover contents the single theme-surface owner, kept the menu/scroller/viewport/list transparent, clipped selected rows to the rounded surface, and added an isolated CUA evidence scenario; format, strict clippy, architecture, UX traceability, motion, input, accessibility, audit with only accepted RUSTSEC-2024-0436, 1401 core tests, 962 GNOME non-display tests with 291 display tests ignored, 86 Linux platform tests, 45 stems tests, and all remaining workspace tests passed in isolated host state; the CUA render showed the three library matches, selected accent row, and create row on one continuous surface, while compositor alpha around the detached X11 popup remains a native-desktop manual check)

# Space and sidebar regressions

Space/sidebar follow-up: complete (this commit, base 9f7275b27f, kept Space as global play/pause while the left sidebar toggle has focus, preserved native Space behavior for other controls and the existing passive-collection contract, kept Issues pinned at the sidebar bottom below progress activity, and added six-toggle CUA regression coverage; format, strict clippy, full workspace tests with 1403 core plus 971 GNOME non-display passes, audit with only accepted RUSTSEC-2024-0436, core purity, architecture, accessibility, input, motion, UX traceability, and the CUA helper contract passed; actual desktop interaction remains the manual acceptance check)

# Source-view UX follow-ups

Task R1: complete (this commit, base 3fc0b2bf8a, preserved smart-playlist membership while honoring visible column sorting, emitted narrow queue row removals instead of full replacements, and made hidden content-stack pages unable to push the bottom player bar out of view; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, file-size checks, and the isolated rule-named PLAY-7b display regression passed)
Task R2: complete (this commit, base c2fc5ba5a7, aligned Radio with the Podcast toolbar geometry, vertically centered Concert artist cells under CONC-10, added fixed Cancel plus Search/Preview actions to the Podcast add dialog, and clarified that scan exclusions are files removed from the library; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, UX traceability, file-size checks, and the three affected isolated display regressions passed)
Task R3: complete (this commit, base 5efa49a12c, added ten-second undoable per-episode removal for RSS and YouTube, persisted committed GUID dismissals against feed reimport, retained downloaded files behind an explicit Trash offer, and kept subscription removal cascade-complete; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, UX traceability, and file-size checks passed)
Task R4: complete (this commit, base 9f9eb5cf30, turned Recently Added into a rolling seven-day Library scope without a 50-track cap, sorted it newest-first, exposed a removable scope pill that returns through history to the remembered Library, and migrated the built-in smart playlist to a semantic role; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, UX traceability, file-size checks, and the isolated FIL-8 display regression passed)
Task R5: complete (this commit, base f1c7d07086, added an optional hidden-by-default Added column with ISO timestamp rendering, persisted editor visibility/order/width, legacy-layout normalization, and added_at sorting; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, UX traceability, file-size checks, and the isolated BROWSE-9 display regression passed)
Task R6: complete (this commit, base bc5c6beda5, applied descending direction to every compound column-sort term, kept the active Now Playing tab outside the track-identity crossfade, and constrained the Podcast episode table to a shrinkable scroller so the structural player bar stays visible; fmt, strict clippy, every non-socket workspace test, audit with only accepted RUSTSEC-2024-0436, core purity, UX traceability, and file-size checks passed; the two loopback-socket MCP radio tests and isolated display runs were blocked by sandbox socket permissions)

# Simplified Android playlist mirroring

Base: `ea1b3dc7c1`

Baseline gate repair: complete (this commit, base ea1b3dc7c1, removed the stale Concerts preferences callback reintroduced by the navigation merge; format, strict clippy, core purity, audit and the workspace suite excluding the two inherited sandbox-blocked Radio socket tests passed)
Task 1: complete (this commit, base 5876de477d, added the default MP3 256 transfer profile, bitrate-aware copy/transcode decisions, conservative target-size estimation and duplicate-safe per-playlist plus union projections; format, strict clippy, full workspace tests, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 2: complete (this commit, base e8024a6c74, added schema v34 with non-cascading explicit source/device/profile inventory, durable playlist-path inventory, safe Entire Library retirement and legacy Opus replacement markers; adversarial review also replaced the last implicit transfer-size fingerprint, and format, strict clippy, full workspace tests, architecture, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 3: complete (this commit, base d232eed045, added a deterministic safety-blocking mirror planner with deduplicated playlist projections, explicit copy/replace/remove operations, unavailable-track retention, managed-orphan cleanup and stable root-level M3U snapshots; format, strict clippy, full workspace tests, architecture, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 4: complete (this commit, base f668b11a54, replaced the retired two-worker Opus ring buffer with capability-gated sequential CBR MP3/ID3 transfers, preserved required tags and embedded covers, verified .part sizes before finalization, and documented and gated GStreamer Good Plug-ins; format, strict clippy, full workspace tests, architecture, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 5: complete (this commit, base b3310898a9, removed the global runtime lock, proved channel-gated concurrent sync and cancellation isolation with one file operation per device, added generation-guarded smoothed MTP-only rates, and delayed replacement deletion until the verified new file was inventoried; adversarial inventory-failure coverage plus format, strict clippy, full workspace tests, architecture, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 6: complete (this commit, base a7adfeb6bd, queried capacity on the resolved target storage, replaced the full phone-content surface with Reprise-managed paths plus aggregate Reprise/other-music usage, and added honest complete/unknown/inconsistent after-sync composition projections; adversarial blocked-plan and capacity-inconsistency coverage plus format, strict clippy, full workspace tests, architecture, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 7: complete (this commit, base b6718fcad7, exposed a toolkit-neutral compact page projection with all MP3 qualities, complete manual and smart playlist rows, duplicate-safe physical totals, path-free blockers and warnings, honest storage and change summaries, persistent immediate edits, and per-device start/cancel controls; integrated safe mirror execution, unavailable-track retention, ordered playlist replacement, and failure-safe publication, with format, strict clippy, full workspace tests, architecture, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Task 8: complete (this commit, base eb3b0f4d11, added a compact live Android playlist-mirroring dialog with all MP3 qualities, editable manual and smart playlist rows, deduplicated target totals, path-free change/storage/blocker/warning copy, in-place progress updates, per-device start/cancel controls, eject, focus restoration and no retired entire-library, pin or ratings controls; format, strict clippy, full workspace tests, architecture, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed, while the compiled isolated display test could not start because the sandbox denied the private D-Bus socket)
Task 9: complete (this commit, base d87527076b, routed each sidebar device card to its compact sync dialog and projected independent playlist-mirroring changes, warnings, finishing state and smoothed MTP-copy rates without consulting the retired entire-library selection; adversarial warning-state coverage plus format, strict clippy, full workspace tests, architecture, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Dev integration: complete (this merge commit, remote dev 9f9fbd809b, imported PRs #72-#75 with all 95 changed Git blobs verified against GitHub, preserved both parallel schema migrations by keeping Podcasts/Radio at v34 and Recently Added at v35 while renumbering device-sync inventory to v36, and added a regression for the parallel v34 upgrade path)
Task 10: complete (this commit, base 00bfb605c9, removed the retired Device browser, Synchronization preferences, ad-hoc queue/action and legacy pin projections; routed the main menu through a compact zero/one/many-device launcher; preserved new Dev sources and generic Preferences navigation; and degraded saved Device places safely to Library; format, strict clippy, 1441 core tests, 969 GNOME non-display tests with 292 display-only ignores, 89 Linux platform tests, 49 stems tests, architecture, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Safety follow-up: complete (this commit, base b27f69ad48, made MP3 target-size projections conservative for copied metadata and unknown durations, deferred managed and replaced-path removals until every playlist snapshot is published, guarded playlist cancellation between writes and removals, and made the local GIO transfer test wait for storage inspection; format, strict clippy, 1441 core tests, 973 GNOME non-display tests with 292 display-only ignores, 89 Linux platform tests, 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed; the full workspace was green with only the two inherited Radio loopback fixtures filtered because this sandbox rejects their local TcpListener bind with EPERM)
Storage review follow-up: complete (this commit, base 1cb94d9971, replaced the single Reprise-only progress fraction with a theme-derived Music, after-sync growth, Other and Free composition bar, kept the complete values in path-free text, hid the visualization for unknown capacity, and added the planned MTP-7 rule proposal for owner review; format, strict clippy, the unfiltered full workspace with 1441 core tests, 974 GNOME non-display tests and 292 display-only ignores, 89 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed)
Ledger correction: the Storage review follow-up workspace run filtered the two inherited Radio loopback fixtures because this sandbox rejects their local TcpListener bind with EPERM; all other workspace tests and the focused storage regressions passed.
Task 11: complete (this commit, base acc744e17b, aligned the path-free agent, local D-Bus and MCP contracts with the compact playlist-mirroring page; exposed manual and smart playlist identity, MP3 quality, deduplicated totals, full change and storage projections, blockers, warnings, controls and progress; replaced ambiguous single-name configuration with validated multi-source identity and explicit MP3 quality while clearing the inert Opus field; format, strict clippy, full workspace tests except the same two inherited sandbox-blocked Radio loopback fixtures, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity and audit with only accepted RUSTSEC-2024-0436 passed; the additional MCP mpris suite passed with its two inherited no-session-player fixtures filtered, and the typed app/client endpoints plus focused runtime tests passed while private D-Bus roundtrips self-skipped because dbus-daemon is unavailable in this sandbox)
Concurrent review follow-up: complete (commit 4b603e3ac7, base acc744e17b, independently corrected the legacy single-playlist agent path to select an explicit MP3 profile with a 256 kbit/s default, cleared the inert Opus field and replaced the obsolete plan; Task 11 supersedes that narrow compatibility surface with stable multi-source identities and the complete path-free page contract)
Task 12: complete (this merge commit, parents 0ec96b3df4 and 4b603e3ac7, reconciled the concurrent review without discarding its history, replaced the retired Android-sync draft with the maintained simplified playlist-mirroring plan, documented the final architecture, safety invariants, path-free agent/D-Bus/MCP contract and explicit manual-device boundary, and closed the stage with only the test-device checks plus planned MTP-7 owner review outstanding; format, strict clippy, the unfiltered full workspace suite, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Dev refresh: complete (this merge commit, live remote dev 066d2abcb6 via exact snapshot import 64763af0cf, all 13 changed file blobs verified byte-for-byte through GitHub after shell transport failed, preserved the R6 compound-sort, Now Playing transition, Podcast scroller and sidebar DnD extraction alongside simplified Android mirroring; format, strict clippy, the unfiltered full workspace suite, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
# CAVA visualizer DSP

Base: `433376ee5c`

Task CAVA-1: complete (commit b9ffc5bc, base 433376ee5c, added the MIT-attributed CAVA logarithmic and FFT-quantized band planner with 1-256 bar support and pinned even/odd sample-rate cutoffs; format, strict clippy, workspace tests, audit with only accepted RUSTSEC-2024-0436, and core purity passed)
Task CAVA-2: complete (commit c8b59893, base b9ffc5bc, added rolling normalized mono PCM ingestion, CAVA Hann windows, dual pure-Rust real FFTs, upstream EQ/bin aggregation, and 200 Hz/2 kHz target-band regressions; format, strict clippy, workspace tests, audit with only accepted RUSTSEC-2024-0436, and core purity passed)
Task CAVA-3: complete (commit 09bb263e, base c8b59893, ported CAVA's frame-rate-adjusted quadratic gravity and additive integral memory with immediate attack and settling release regressions; format, strict clippy, workspace tests, audit with only accepted RUSTSEC-2024-0436, and core purity passed)
Task CAVA-4: complete (commit 1fda8685, base 09bb263e, added CAVA autosensitivity, a configurable historical low-level gate, bounded finite output, normalized-PCM sanitization, silence-safe gain caps, stable maximum smoothing, reset, and 256-bar endurance coverage; format, strict clippy, workspace tests, audit with only accepted RUSTSEC-2024-0436, and core purity passed)
Task CAVA-5: complete (commit f8866bae, base 1fda8685, replaced the legacy dB, beat, dynamics, and display-envelope pipeline with a clock-synchronized pre-ReplayGain CAVA PCM branch and 64 one-to-one neon bars, including filter-replacement and stream-handoff recovery; format, strict clippy, workspace tests, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, motion, input parity, accessibility, diff purity, and file-size gates passed)
Latest-dev gate repair: complete (this commit, base 0218da51, extracted the unchanged sidebar collection-boundary navigation into a focused sibling after current dev exceeded its new 600-line orchestrator gate; format, strict clippy, isolated workspace tests with the two sandbox-blocked loopback tests explicitly skipped, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, motion, input parity, accessibility, diff purity, and file-size gates passed)
Bass glow follow-up: complete (this commit, base 437d0c7f, added an AC-22 presentation-only bass-energy envelope with immediate attack, soft release, two broad neon glows and two nonlinearly stronger breakdown auras while preserving all 64 CAVA bar heights; the final Gallery render was visually reviewed and the 1920x1080 render diagnostic stayed within budget at 8.116 ms p95 and 11.410 ms p99; format, strict clippy, rustdoc, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, motion, input parity, accessibility, diff purity and file-size gates passed; every workspace test was exercised, with all non-socket tests passing and the two inherited Radio MCP loopback fixtures blocked only at sandbox TcpListener bind with EPERM; all 295 isolated display tests were launched but the sandbox blocked their private D-Bus sockets before their test bodies)
Cold-start saturation fix: complete (this commit, base 287454e8, preserved CAVA's autosensitivity state and gain formula while shape-normalizing only its cold calibration overshoot to 85% headroom, preventing song-start and post-seek full-bar flashes; the operator-approved To the Hellfire 5:23.3 fakesink regression improved from 0.880 maximum mean and 33/64 near-full bars to 0.416 and 0/64; format, strict clippy, full workspace tests, core purity, architecture, UX traceability, motion, input parity, accessibility, file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed)
Breakdown glow calibration: complete (this commit, base 9010aea5, recalibrated the existing presentation-only bass-energy glow against the operator-approved Unbreakable 3:18-3:40 passage so recurring 0.446 RMS bass waves reach the aura while preserving restrained lead-in glow; the offline 60 Hz render showed visible glow in 45.0% and aura in 22.7% of frames, while the To the Hellfire 5:15-5:35 cross-check remained selective at 19.6% and 5.6%; no bar heights, CAVA DSP state, audio files, tags, database state, or audio output changed)
Dev refresh 2: complete (this merge commit, live remote dev 73bb12dcd2 via exact snapshot import 770ded0068, all 32 changed file blobs verified byte-for-byte through GitHub after shell transport failed, preserved the CAVA visualizer/playback refactor and sidebar boundary extraction alongside simplified Android mirroring; format, strict clippy, the unfiltered full workspace suite, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)

# Simplified Android transfer-profile follow-up

Task 13: complete (this commit, base 8fe17f1609, replaced selectable MP3 bitrates with Opus 160 kbit/s as the default, MP3 256 kbit/s as the compatibility fallback and byte-preserving Original passthrough; restricted lossy encoding to known lossless sources so lossy, unknown and ambiguous formats stay unchanged; added schema v37 with MP3-preserving upgrade behavior and idempotent repair, actual-output paths and stable copy fingerprints, Opus tags/covers, GTK plus agent/D-Bus/MCP profile contracts, and isolated Opus, MP3 and byte-identical FLAC end-to-end coverage; format, strict workspace and mpris clippy, the unfiltered workspace suite with 1440 core tests, 976 GNOME non-display tests, 92 Linux platform tests and 49 stems tests, the additional mpris suite and private D-Bus roundtrip, rustdoc, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, diff purity, file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed; the isolated MTP-2 GTK test could not start because the sandbox denied its private D-Bus socket, and real-device checks plus the planned MTP-7/MTP-8 owner review remain open)
Task 14: complete (this commit, base 5c64a9e148, projected optional GIO target access as Writable, Read-only or Unknown through platform inspection, core storage/page controls, the compact GTK dialog and the path-free agent state; known read-only targets block start with an explicit reason while incomplete capability evidence stays honest and non-blocking; extracted storage copy to preserve the Rust file-size gate and added planned MTP-9 for owner review; format, strict clippy, the unfiltered workspace suite with 1442 core tests, 977 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, diff purity, file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed; live Pixel storage metadata reported explicit writable access with consistent capacity)
Task 15: complete (this commit, base ce77a076d4, kept successful transfers in non-editable Finishing state until a post-sync device inspection completed, moved the success timestamp and lifecycle toast behind that readback, exposed the actually observed Reprise track count in a Last synchronization row, and kept failed readback from claiming success; closed the direct runtime read-only bypass and added planned MTP-10 for owner review; format, strict clippy, the unfiltered workspace suite with 1442 core tests, 981 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, isolated MTP-2 GTK rendering, architecture, accessibility, input, motion, GStreamer, UX traceability, diff purity, file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 16: complete (this commit, base 07c6368a9d, replaced the idle unconfigured device-card action prompt with known storage access plus free-space information while preserving Needs attention for real scan, sync, warning and invalid-selection states; activated and covered MTP-11; format, strict clippy, the unfiltered workspace suite with 1442 core tests, 982 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 17: complete (this commit, base 2d8cfbaa38, corrected the mirror planner to warn about but never delete physical device files absent from this Reprise database's explicit inventory, preserving pre-existing Music/Reprise content when local sync history is fresh or lost; format, strict clippy, the unfiltered workspace suite with 1443 core tests, 982 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 18: complete (this commit, base d02878e532, added schema v38 and a per-device last verified synchronization time to every available playlist row, persisted it atomically only after successful device readback, preserved previous times across failed or partial runs and migrations, and activated MTP-12; adversarial migration, readback and timestamp-write failures passed alongside the direct one-device launcher plus independent two-device parallel start and cancel coverage; format, strict clippy, the unfiltered workspace suite with 1446 core tests, 984 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 224 active rules, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 19: complete (this commit, base 10634ecbc1, made host-backed smoke instances non-unique and gave them an explicit isolated MPRIS bus identity so they can coexist with a normal Reprise session; formalized the temporary-root SimulatedMtpDeviceBackend as the stable hardware-free E2E boundary and verified Opus 160, MP3 256 and byte-identical Original through it; an explicitly authorized Pixel MTP acceptance run independently confirmed all three profiles, post-sync readback and per-playlist timestamps while preserving 201 pre-existing untracked files; format, strict clippy, the unfiltered workspace suite with 1446 core tests, 986 GNOME non-display tests and 293 display-only ignores, 93 Linux platform tests and 49 stems tests, rustdoc, architecture, accessibility, input, motion, GStreamer, UX traceability with 224 active rules, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)

# QA regression fixes

Task QA-80: complete (this commit, base 066d2abcb6, made Podcasts⇄Music atomic so outgoing and incoming dense source tables never overlap while other outer page switches retain the Standard crossfade)
Task QA-78: complete (this commit, base 5a9e8b985b, queued the stable-ID scroll anchor before GTK can paint a reload's transient position-zero state, then retained the exact offset restore for metadata and rating saves)
Task QA-79: complete (this commit, base d757e13293, moved the shared progress activity inside the bottom-pinned Issues section between its heading and the Missing files / Import errors sources)
Live-QA follow-up: complete (this commit, base 179f00f3e6, restored Standard-duration Podcasts⇄Music motion without overlapping readable tables, carried the pre-dialog stable-ID viewport anchor through asynchronous rating and tag saves, and suppressed inherited expansion so running activity sits directly above the issue rows; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, and file-size checks passed; the three visible desktop behaviors remain the explicit manual acceptance check)
Live-QA refinement: complete (this commit, base 73ce1d26d9, made plain-Library rating-only Tag Editor saves update cached and realised rating cells without any model signal or scroll operation, retained query reloads for rating-dependent views, and made active scanner, doctor, and relink cards atomically replace the complete Issues surface while keeping persistent device state independent; fmt, strict clippy, 982 GNOME non-display tests, full workspace tests, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, and file-size checks passed; isolated display execution was blocked before GTK startup by sandbox D-Bus socket EPERM, so both visible behaviors remain the explicit manual acceptance check)
Live-QA bottom placement: complete (this commit, base 581f75da54, explicitly suppressed computed vertical expansion on the Issues/progress replacement stack and pinned it above the player so active scanner, doctor, and relink cards occupy the former bottom-left Issues position; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, and file-size checks passed; native visual placement remains the explicit manual acceptance check)
Live-QA stack-page placement: complete (this commit, base 26778d03ce, placed a flexible spacer before the shared progress cards because GtkStack still allocates its active page at full height, keeping scanner, doctor, and relink cards at the former bottom-left Issues position regardless of page allocation; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, and file-size checks passed; native visual placement remains the explicit manual acceptance check)
Live-QA revealer placement: complete (this commit, base 968b68d991, removed fully inactive scanner, doctor, and relink revealers from layout while preserving their active fades, moved flexible allocation into the shared progress root, and made the sidebar root fill its page so the visible card block sits directly above the player; fmt, strict clippy, the full host-isolated workspace suite, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, file-size checks, and both isolated display regressions passed; isolated CUA directly confirmed the scanner edge above the player, while later unrelated keyboard scenarios outlived the deliberately shortened app timer and are not claimed)
Responsive-window follow-up: complete (this commit, base 802ec513ac, activated STYLE-5, STYLE-6, and MINI-5; kept the complete player bar inside horizontally, vertically, and jointly constrained windows; ellipsized long metadata without shifting centered transport; made tall hidden and Now Playing content surrender height; folded secondary columns transiently with exact live-state restoration; and offered the user-controlled Compact Mode switch once per session; fmt, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, architecture, accessibility, input, motion, UX traceability, QA linters, gettext, CUA helper, diff, and file-size checks passed; isolated CUA with generated audio directly confirmed 720x760, 1200x420, 720x420, reopened columns, Compact Mode, long-title playback, and the short open-panel layout)
Task FIL-9: complete (this commit, base 179f00f3e6, centered the loaded track after setting, changing, or clearing search and facet filters whenever it remains visible; fmt, strict clippy, every non-socket workspace test, audit with only accepted RUSTSEC-2024-0436, architecture, QA linters, core purity, UX traceability, and file-size checks passed; the isolated display regression compiled but sandbox socket permissions blocked its Xvfb execution)

# Album-cover consistency follow-up

Base: `066d2abcb6`

Task COV-1: complete (this commit, base 066d2abcb6, activated BROWSE-10, detected divergent embedded artwork by normalized album identity in the opt-in cover worker, and made the resulting shared downloaded cover canonical without writing to music files; format, strict clippy, 1412 core + 978 GNOME non-display + 88 Linux platform + 49 stems tests, architecture, UX traceability, core purity, file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed; two inherited MCP Radio tests requiring local TCP listeners were blocked by sandbox EPERM while the otherwise complete workspace run passed with those exact tests filtered)
Assumption COV-1: album identity remains the existing normalized file-tag pair of Album Artist plus Album; intentional disc-specific artwork under that same identity is unified only when the user-enabled cover-download batch can obtain a shared cover.
Manual COV-1: real Cover Art Archive retrieval and the visible post-restart convergence remain a native-desktop/network check because validation used synthetic FLACs, isolated XDG state, and no real library or database.
Badge/filter follow-up: complete (commit fb16b8bc73, base 492ea733f6, PR #85 merged into remote dev as 348c419351; anchored the compact Updates badge and refreshed its scope through persisted Concerts filter changes; local gates passed, while GitHub Actions contained no steps and returned BlobNotFound for its log archive)
Remote-dev synchronization: complete (this merge commit, base 03bf59decb, merged remote dev 393e2822d4; retained the active STYLE-5, STYLE-6, MINI-5, MOT-8, FB-8, and TAG-1 behavior while integrating FIL-9, CAVA AC-22, BROWSE-10 cover consistency, and the Updates badge follow-up; format, strict clippy, full workspace tests, audit with only accepted RUSTSEC-2024-0436, core purity, architecture, accessibility, input, motion, UX traceability, QA linters, gettext, CUA helper, diff, and file-size checks passed; the isolated responsive-window CUA scenario passed again after the merge and confirmed the long-title, narrow, short, combined, reopened-column, Compact Mode, and short-open-panel layouts)
QA/dev integration: complete (this merge commit, base 393e2822d4, retained Standard-duration non-overlapping Music/Podcasts motion, pre-dialog metadata anchors, rating-only in-place refresh, and the bottom-pinned Issues/progress replacement while integrating current dev; stabilized FIL-9 centering across GTK's delayed post-reload scroll by refining the target through settled frames; format, strict clippy, rustdoc, full host-isolated workspace tests, 161 rule-named display tests, 26 motion display tests, 11 CSS display tests, core purity, architecture, accessibility, input, motion, UX traceability, QA linters, file-size checks, and audit with only accepted RUSTSEC-2024-0436 passed; the operator visually accepted the QA behaviors before publication)
Dev refresh 3: complete (this merge commit, live remote dev 144672eaef, preserved the simplified playlist-only Android flow without a Device browser while integrating the latest cover consistency, source-transition, viewport-anchor, rating refresh, Updates badge and bottom-pinned Issues/progress work; resolved the Sidebar conflict by keeping its extracted root module and delegating the new complete Issues/activity replacement region into it, and routed My Stats through the new safe content-stack transition helper; format, strict clippy, the unfiltered workspace suite with 1447 core tests, 996 GNOME non-display tests and 297 display-only ignores, 93 Linux platform tests and 49 stems tests, rustdoc, architecture, accessibility, input, motion, GStreamer, UX traceability with 226 active rules, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Android sync review follow-up: complete (this commit, base 4cf97aeba1, deduplicated path-free agent warnings by category, made post-sync managed readback include byte-preserved Original formats without mistaking playlists or partials for tracks, generalized the guarded simulator to independent temporary roots, and added GIO-backed E2E coverage for parallel devices plus preservation of untracked managed audio; activated owner-approved MTP-7 through MTP-10 with rule-named storage, profile-policy, access and readback tests; renamed the branch to the required feature namespace; format, strict clippy, the unfiltered workspace suite with 1447 core tests, 1000 GNOME non-display tests and 297 display-only ignores, 94 Linux platform tests and 49 stems tests, rustdoc, architecture, accessibility, input, motion, GStreamer, UX traceability with 230 active rules, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Assumption Android sync review: retained the one-variant Mp3Quality type because it is the narrow compatibility boundary for persisted v37 settings and existing public profile payloads; flattening it would add migration and API churn without simplifying the user-facing three-profile model.

# Responsive-window QA follow-up

Responsive panels and waveform seek: complete (commit 202b14cbca, base 484de08f32, activated STYLE-7 so snapped windows close both side panels in one transient non-persisting transition with exact 10-second Undo restoration and reachable reopen toggles; fixed pre-map waveform peaks so the seek bars build on the first frame instead of leaving only the playhead line; format, strict clippy, full host-isolated workspace tests, audit with only accepted RUSTSEC-2024-0436, architecture, accessibility, input, motion, UX traceability, QA linters, gettext, CUA helper, diff, and file-size checks passed; isolated CUA directly confirmed paired side-panel closure, Undo restoration, waveform rendering, long-title centering, narrow and short player reachability, column folding, and the Compact Mode suggestion)
Dev refresh 4: complete (this merge commit, live remote dev 461fadb886, preserved the simplified playlist-only Android flow, modern transfer profiles, verified readback, parallel-device simulator and path-free agent state while integrating the responsive side-panel, player-bar, waveform-seek, compact-mode suggestion, track-column and translation follow-ups; the only content conflict was the append-only progress ledger and both histories were retained; format, strict clippy, the unfiltered workspace suite with 1447 core tests, 1006 GNOME non-display tests and 302 display-only ignores, 94 Linux platform tests and 49 stems tests, rustdoc, architecture, accessibility, input, motion, GStreamer, UX traceability with 234 active rules, core purity, diff purity, Rust file-size enforcement, 167 rule-named display tests, 26 motion display tests, 10 CSS display tests and audit with only accepted RUSTSEC-2024-0436 passed)

# Discography gaps

Base: `144672eaef`

Task DG1: complete (this commit, base 144672eaef, retained historical regular Album and EP release groups as durable catalog rows while preserving the 90-day and 20-per-artist Updates contract and transient Single retention; format, strict clippy, the complete host-isolated workspace suite except the two sandbox-blocked Radio MCP loopback tests, core purity, file-size checks, and cached audit with only accepted RUSTSEC-2024-0436 passed)
Task DG2: complete (this commit, base 2539a6c087, added schema-v36 official release lengths and requires distinct local Album Artist plus Album track identities to cover the full released Album or EP before ownership is complete, so advance singles, duplicate files, unknown remote lengths, and future releases remain visible gaps)
Task DG3: complete (this commit, base 2539a6c087, pages and deduplicates every MusicBrainz release-group browse response, retains undated Album/EP catalog entries, enriches local title matches with the smallest multi-track official edition, and defaults new configurations to the bounded all-artists rotation)
Task DG4: complete (this commit, base 2539a6c087, activated NR-16 through NR-18 so the full Releases view and its sidebar badge contain only Album/EP gaps for current library artists, expose Missing/Incomplete/X-of-Y statuses, retain Type/Hidden filters, and remove the six-month footer)
Task DG5: complete (this commit, base 2539a6c087, documented the approved-desktop-partner, disclosure, allowlist, and privacy boundary in `docs/plans/releases-affiliate-links.md` and planned NR-19; no affiliate tracking was enabled without a qualifying contract)
Task DG6: complete (this commit, base ea907fae0f, activated NR-20 and added a visible provisions-free Bandcamp purchase column for exact MusicBrainz `/album/…` relations on real Bandcamp hosts; lookalike domains, artist homepages, search URLs, tracking parameters added by Reprise, and non-Bandcamp targets never receive a purchase button)
Discography-gaps verification: complete (format, strict clippy, the complete host-isolated workspace suite except the two sandbox-blocked Radio MCP loopback tests, core purity, file-size checks, UX traceability with 219 active rules, and cached audit with only accepted RUSTSEC-2024-0436 passed; the isolated NR-20 display test was blocked before GTK startup by sandbox D-Bus socket EPERM, and no real database, music files, or desktop session were used)
Discography-gaps commit incident: resolved (the sandbox initially rejected the DG2 commit through a read-only worktree metadata path; once Git metadata became writable, DG2-DG5 landed together in this single unpushed recovery commit rather than the planned per-task commits)
Dev refresh 5: complete (this merge commit, live remote dev 6cdb1af33e, preserved the simplified Android playlist mirror, modern transfer profiles, verified per-playlist timestamps, parallel-device runtime and simulator while integrating Discography gaps and Bandcamp links; resolved the parallel schema-number collision by retaining Android Sync v36-v38 and moving the Discography track-count migration to v39 with an upgrade-preservation regression; format, strict clippy, rustdoc, the unfiltered workspace suite with 1460 core tests, 1009 GNOME non-display tests and 302 display-only ignores, 94 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 235 active rules, core purity, diff purity, Rust file-size enforcement, 167 rule-named display tests, 26 motion display tests, 10 CSS display tests and audit with only accepted RUSTSEC-2024-0436 passed)
Task 20: complete (this commit, base f974b47858, replaced the per-device modal with a non-modal full page opened from the sidebar card or device launcher; retained only the three-profile transfer setting plus playlist selection, profile-dependent per-playlist sizes, verified sync times, delta, storage, progress, smoothed MTP rate, Sync/Cancel and Eject; treated dynamic device and playlist names as plain text to eliminate ampersand markup failures; corrected known-lossless size projections and removed a page-subscription retention cycle; added the isolated android-sync-page background CUA scenario with a simulated MTP root and real Opus publication; format, strict clippy, rustdoc, the unfiltered workspace suite with 1461 core tests, 1010 GNOME non-display tests and 302 display-only ignores, 94 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 235 active rules, core purity, diff purity, Rust file-size enforcement, 167 rule-named display tests, 26 motion display tests, 10 CSS display tests, the CUA helper contract and simulated-device acceptance run, and audit with only accepted RUSTSEC-2024-0436 passed)
Task 21: complete (this commit, base 8c2786412f, replaced the settings-shaped device page with a simple playlist-first dashboard whose shared hero contains device identity, MTP status, last device sync, storage composition and Sync/Eject actions; kept only selectable playlist rows with profile-dependent size and verified playlist time plus a compact transfer/delta/progress overview; projected local playlists before asynchronous MTP inspection, repaired the page-controller lifetime, and made each full playlist row one accessible toggle action; the background CUA scenario selected a playlist, observed its 2.4 MiB Opus projection, started synchronization and verified the published Opus file; format, strict clippy, rustdoc, the unfiltered workspace suite with 1461 core tests, 1012 GNOME non-display tests and 303 display-only ignores, 94 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 236 active rules, core purity, diff purity, Rust file-size enforcement, 168 rule-named display tests, 26 motion display tests, 10 CSS display tests, the CUA helper contract and simulated-device acceptance run, and audit with only accepted RUSTSEC-2024-0436 passed)
Task 22: complete (this commit, base 209f3adc9a, activated MTP-15 and stabilized the playlist and overview cards against dynamic sync copy, separated the live MTP transfer rate into its own labeled row, and kept free device space visible first in every sidebar phase; completed the path-free MCP control surface with verified device and playlist sync times, storage write access, capability-gated eject and can_eject state while retaining one shared deduplicated MirrorPlan across overlapping playlists; format, strict clippy, rustdoc, the unfiltered workspace suite with 1461 core tests, 1012 GNOME non-display tests and 305 display-only ignores, 95 Linux platform tests and 49 stems tests, the full MPRIS-enabled MCP suite, architecture, accessibility, input, motion, GStreamer, UX traceability with 237 active rules, core purity, Rust file-size enforcement, 170 rule-named display tests, 26 motion display tests, 10 CSS display tests, the CUA helper contract and simulated-device acceptance run, and audit with only accepted RUSTSEC-2024-0436 passed; the optional gettext catalog check remains red on unchanged base HEAD because twelve previously integrated Arabic Releases/Bandcamp entries are absent)
Task 23: complete (this commit, base 319945794f, activated MTP-16 and MTP-17, locked the per-device transfer-profile choice across runtime recreation, and made `Music/Reprise` an authoritative mirror root that removes every safe non-desired audio, playlist or other file only after desired tracks and playlists are published; desired physical playlists remain protected, unknown same-size destination contents are overwritten instead of trusted by size, and all content outside `Music/Reprise` remains untouched; format, strict clippy, rustdoc, the unfiltered workspace suite, 13 focused MirrorPlan tests, 20 Linux storage tests, 39 GNOME device-runtime tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 239 active rules, core purity, Rust file-size enforcement, rule-named, motion and CSS display tests, the isolated `android-sync-page` CUA simulator run, and audit with only accepted RUSTSEC-2024-0436 passed; no real device or user library data was used)

# Weekly architecture review cleanup

Dead-code cleanup: complete (this commit, base 96fb39d7c5, removed four verified orphan UI paths from the legacy tag-editor helpers, track-model invalidation escape hatch, unused sidebar badge builder and stale PlayerBar mute fields; focused tests, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, motion, GStreamer, UX traceability with 239 active rules, QA linters, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 0.1: complete (this commit, base add6821575, confined automatic podcast cleanup to the canonical configured download root, rejected foreign and symlink-escaped stored paths without deleting files or clearing their database pointers, and retained stale missing-file reconciliation; the focused red-green regression, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, motion, UX traceability with 239 active rules, QA linters, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 0.2: complete (this commit, base b4066239b1, made MTP partial-file cleanup fail closed so a cleanup error blocks every subsequent copy, playlist publication and managed deletion, records no successful sync time, and remains visible as a track-independent synchronization failure; the focused red-green MTP-5 regression, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, motion, UX traceability with 239 active rules, QA linters, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 0.3: complete (this commit, base b51f74f341, enforced active rule NR-20 in the reusable Core by rejecting Bandcamp album targets containing any query parameters, including common source and campaign tracking forms, while preserving unchanged direct album URLs on real Bandcamp hosts; the focused red-green rule test, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, motion, UX traceability with 239 active rules, QA linters, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 0.4: complete (this commit, base 8b6116ff58, released the podcast runtime's subscriber RefCell borrow before invoking callbacks so a callback can safely register another subscriber during notification without a reentrant panic; the focused red-green reentrancy regression, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, motion, UX traceability with 239 active rules, QA linters, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 0.5: complete (this commit, base 32eb1028e4, made the podcast and radio row context menus reachable from the whole row instead of only the cell content box; a new shared ui::source_context_surface takes the cell's Adwaita padding and re-applies it on the surface that carries the secondary-click gesture, so measured pointer coverage of a realized podcasts row rose from 52% to 100% while the wrapped child keeps its previous geometry, the podcast keyboard path moved off recycled unfocusable cells onto the ColumnView, and both tables build their menu through one function; six new rule-named display tests built from the production factories, format, strict workspace clippy, rustdoc, the full unfiltered workspace suite, 176 rule-named plus 26 motion plus 10 CSS display tests, architecture, accessibility, input, motion, UX traceability, QA linters, MSRV, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed; the gettext catalogue check fails identically at HEAD and is untouched by this change; the plan's real-event CUA acceptance was set up against an isolated Xvfb/openbox/D-Bus session with a cua-driver daemon bound to that display and confirmed window discovery and the seeded radio view, but the click sweep itself was stopped on the operator's instruction because the radio, YouTube and podcast surfaces are currently being rebuilt)
Stage 0: complete (tasks 0.1-0.5 landed on fix/thin-core-headless-mcp-stage0 above dev at 96fb39d7c5, closing every review blocker of the 21-28 July weekly review; the full gate set is green except the gettext catalogue check, which fails identically at HEAD and is untouched by this stage. Deferred and genuinely manual: the plan's real-event pointer acceptance for the podcast and radio context menus was prepared against an isolated Xvfb/openbox/D-Bus session with a cua-driver daemon bound to that display - window discovery and the seeded radio view were confirmed, the click sweep was stopped on the operator's instruction because those surfaces are being rebuilt; real devices, GVfs MTP, hardware audio and actual GNOME rendering remain unverified as always. Nothing is pushed.)

# Thin core and headless MCP — Stage 1

Task 1.1: complete (commit 650cdc5536, base 9ed9c731a8, made the runtime a binding design instead of a parked idea: section 9 of multi-frontend-core.md draws the ownership line so Beschluss 1 keeps the database and the runtime gets only the state that never reaches it, and specifies the four lifecycle states with complete transitions, an exclusive lease taken before any effect is opened, D-Bus activation as the single start path, snapshot-then-deltas reconnection with no replay and no buffered commands, an idle shutdown that refuses to interrupt work, four shared error categories and a per-resource capability matrix recording today's state; section 2.1 gained a forward note and the user-visible half became RUN-1..RUN-5 [geplant] in the new UX section AG; UX traceability with 239 active rules passed; the window-close question is deliberately open and marked as a Regelvorschlag because it is a product decision, not an architectural consequence)
Task 1.2: complete (commit 8ef1c2fee9, base 650cdc5536, replaced the sixteen-field positional device-sync tuple — declared once in reprise-platform-linux and again in reprise-mcp, with seven consecutive u64s in one nested row — by named types in a new reprise-runtime-protocol crate that both sides share; snapshots now cross D-Bus as a{sv} dictionaries keyed by field name so a reorder is unobservable and an unset Option is an absent key rather than a (bool, T) pair, PROTOCOL_VERSION plus a ProtocolVersion property give the seam a handshake that refuses a foreign major version with an actionable message, and command enums stay in-process because D-Bus has no sum type; nine schema tests pin round trips including nested dictionaries and sparse optionals, the checked-in wire field names and path freedom, and the existing private-bus integration round trip now runs end to end through the named types; the protocol crate is optional in reprise-mcp behind its mpris feature so check-architecture.sh's rule that the default MCP build depends on reprise-core alone still holds; format, strict workspace clippy with and without all features, rustdoc, the full workspace suite, the all-features MCP suite, architecture, accessibility, input, motion, UX traceability, QA linters, core purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed)
Task 1.2 review fix: complete (this commit, base 8ef1c2fee9, from an adversarial review of the protocol change: the field-by-field migration of the sixteen-field tuple and its four nested rows was confirmed correct with no dropped, swapped or polarity-inverted field, but two contradictions were real. `is_compatible_with` refused a peer with a lower minor version, which contradicts the crate's own documented rule that a minor bump only adds optional fields an older peer may omit — it would have hard-failed the most ordinary upgrade sequence, a client updated on disk while the older runtime still runs; only the major version decides now, and the minor stays for a client that wants to skip a feature knowingly. Section 9.8 of the architecture plan asserted that playback and device tools are in the default MCP build, which the shipped `mpris` feature gate contradicts; the rule is now stated as the target it is, with Task 3.4 named as the place it becomes true)
Task 1.3: complete (this commit, base a7f47576ee, gave the thin-frontend goal a number instead of an intention: scripts/check-frontend-thinness.sh counts direct rusqlite, filesystem and thread use plus business-worker files in reprise-gnome and pins each at its measured value as both ceiling and floor, so growth fails and an extraction fails too until it lowers the budget in the same commit; GStreamer and zbus are bans rather than budgets because the frontend already reaches zero on both. Counting strips comments and #[cfg(test)] blocks — without the first, ui/mod.rs's own documentation of the zbus ban would trip the check enforcing it, and without the second, writing a test would be punished; the block is skipped rather than the file truncated, so production code after a test module stays measured, which the first draft missed and which is worth 27 rusqlite and one thread use. The thirteen files carrying #[allow(dead_code)] are pinned per file so a new escape hatch has to justify itself in the commit that adds it. The ratchet was verified against deliberate violations, not just assumed to work. Two plan items are honestly not delivered: cargo-machete is not installed, so the check reports SKIPPED instead of passing quietly, and -D unreachable-pub is deferred with its measurement recorded — it is 1240 sites in reprise-gnome, roughly 60% of them pub const string-table entries in modules already private to crate::ui, so it is a mechanical mass edit for its own quiet moment rather than a lint flip. Wired into check-merge-readiness.sh and the qa-linter policy list; format, strict workspace clippy with and without all features, rustdoc, the full workspace suite, the all-features MCP suite, architecture, UX traceability and QA linters passed)
Stage 1: complete (tasks 1.1-1.3 plus one review fix; merged into dev together with Stage 0 as PRs #110 and #111, dev now at 7014d364e6). Acceptance was local and not GitHub: the account's Actions cannot start jobs, and the repository's branch protection turns out to be unavailable on its plan anyway, so scripts/check-merge-readiness.sh against the current origin/dev is the gate — it passed after merging the newer dev (PR #109) into the stack, with the new thinness ratchets unchanged. Branches deleted, worktree moved to feature/thin-core-state-machines, and the nightly build was triggered and verified against the installed binary rather than its log.

# Visualizer bass glow

Bass glow rework: complete (this commit, base 323859faf3, moved the glow layer off the CAVA bands, which auto-sensitivity re-normalizes, so a quiet sung passage climbed to the same band values as a drop and the two broad glows plus their inner auras were lit almost permanently; PR #82 had made that visible rather than caused it by compressing the trigger window from [0.42, 1.0] to [0.36, 0.49], i.e. from 58% of the scale to 13%, which turned a ramp into a switch. The new reprise-core BassPressureDetector measures the same mono PCM with no gain of its own — 30-150 Hz Butterworth band, block RMS in true dBFS, and a 2 s baseline of the track's own bass — and derives one value a rhythmic kick reaches and a second that only a sustained breakdown lifts; SpectrumFrame carries both, the player feeds the detector alongside CAVA and resets it on stream change and DISCONT, and bars.rs scales the two glow layers straight off them, so the engine holds no bass heuristic at all. Calibrated against decoded audio of The Browning "WAKE UP" and Lorna Shore "To the Hellfire" rather than guessed: their quiet intros measure around -40 dBFS and their walls of sound around -14 dBFS, so absolute level alone separates them by 25 dB, and the checked-in bass_pressure_corpus example replays raw PCM through the real detector to re-check the numbers. Rendering the full chain at three moments of "WAKE UP" confirms the intended grading on screen — intro 0.00, rhythmic passage 0.35 with no aura, breakdown 0.89 with aura 0.95. AC-22 is replaced by AC-23 and its 37 tests re-hung, and the visual now names the analysis it reacts to in a 2x2 readout under the canvas, laid out that way because four columns broke the fixed 300 px panel of NPP-1 — caught by three red display tests, not by inspection. Format, strict workspace clippy, the full workspace suite, 178 rule-named plus 26 motion plus 10 CSS display tests, architecture, accessibility, input, motion, UX traceability with 239 active rules and frontend thinness passed; check-merge-readiness.sh was not run because the worktree carries unrelated untracked plan files)

Bass glow rework, corrections and follow-up: the entry above was written before the work finished and two of its numbers are wrong — 39 tests were re-hung from AC-22 onto AC-23, not 37, and the display run it cites as 178 rule-named tests became 180 once the two new readout guards existed. Three things also happened after it: driving the running app through cua-driver (isolated Xvfb/openbox/D-Bus session, its own cua-driver daemon, a copy of the library) showed the readout's second row silently clipped — caption above value needs 88 px where the strip under the canvas offers about 52 — and its captions truncated to "BA…" once they did fit, so caption and value now share a line and the levels read in whole decibels; both bounds are now pinned by measuring the widget against the real panel width and free strip height with the production stylesheet loaded. The Visual tab traded its speaker icon for rising bars, since Adwaita ships no equalizer glyph and a speaker says where audio comes out rather than what it looks like. A rust-reviewer pass approved the change with no critical or high findings; its two actionable notes were applied — the biquad state is snapped to zero at 1e-30 so a fade-out cannot decay into subnormal floats on the streaming thread, and SpectrumFrame::with_bass_pressure is #[must_use]. Merged as PR #116 (dev 49834b78f2) after the full display set ran green post-merge with origin/dev. One investigation closed negative and is worth recording so nobody repeats it: the AT-SPI tree appeared to show three SongVisualizer instances, but counting canvases in the real widget tree returns exactly one — cua-driver traverses the ScrolledWindow/Viewport subtree more than once and assigns separate element indices, so the duplication is in the snapshot, not the app.

Promotion to main: complete (PR #118, origin/main d1de9e3bfb, promoting dev 49834b78f2 — 108 commits, 261 files, +27400/-13713 since the previous stable revision: the visualizer bass-pressure rework, the thin-core runtime contracts and state machines, Android sync transfer profiles, device-sync step labels, releases with Bandcamp discography gaps, the QA viewport and responsive-player regressions, idle visuals with restored lyrics, and the English UX rulebook. scripts/check-merge-readiness.sh ran complete against origin/main and ended green: architecture, multi-frontend core boundaries, frontend lint, core purity, Rust file-size enforcement, the full workspace suite plus doc-tests, 180 rule-named plus 26 motion plus 10 CSS display tests each in its own process, accessibility, input parity, motion tokens, the GStreamer device-sync runtime, UX traceability with 239 active rules, and the dependency audit with only the accepted RUSTSEC-2024-0436. One honest note: stats_10_section_gaps_stay_equal_when_top_tracks_expand_and_collapse is load-sensitive rather than broken — it waits a fixed 400 ms for a GtkRevealer transition before asserting is_child_revealed(), which fails under a four-way parallel display run and passes serially as the gate runs it; converting it to wait on the transition instead of a deadline is open work and predates this promotion)

# Device sync log

Sync log: complete (PR #123, base dev 75f75a09d8, from a live question the app could not answer — a playlist that synced as 200 tracks showed 104 on the phone, and the only record was one last_synced_at per playlist plus two warnings in the whole sync path. Runs are now recorded in two new tables behind schema v40: sync_runs holds device, transfer profile, planned count and the balance of copied/skipped/removed/failed with the reason a run did not complete, and sync_events itemizes only files that deviated, so a 278-file run leaves a handful of rows rather than hundreds. Three decisions carry the design: the entry opens at the start rather than closing at the end, so a run whose session dies is marked interrupted by the next one instead of vanishing — "it never finished" being the answer someone actually wants; removals are itemized even though the balance counts them, because the mirror owns Music/Reprise under MTP-17 and what it deleted is the question that gets asked; and every log write is best-effort, so recording can never fail a transfer. Retention is the last thirty runs, ageing out with their deviations. The device page grows a "Recent transfers" card of expandable rows, with the wording in the frontend because the core deliberately holds none. Two guards earned their keep on the way: the schema-version pin went red on the v40 migration exactly as intended, and the frontend-thinness ratchet caught the log helpers naming Connection in their signatures — rusqlite use in reprise-gnome had grown 537 to 540 — so they now take the runtime instead and the count is back at its budget, which is also the truer shape since they never needed a database handle. Rule MTP-20 activated. Ten core tests, seven wording tests, one display test against the real page, the full workspace suite, format, strict clippy, architecture, accessibility, frontend thinness and UX traceability with 242 active rules passed. Honestly open: the log proves itself only against a real device on the next sync, and it cannot explain the run that prompted it)

# Instrumental feature removal (GTK frontend only)

GUI removal: complete (this commit, base dc290f7100, deleted the GTK instrumental surface — conversion view/model/wiring, worker-host supervisor, runtime, context-menu trigger, Conversions sidebar row and drop target, and the INST-12 model-download flow — extracted the master gate into ui/experimental.rs, and retired the 12 active plus 2 planned GUI-only INST rules; reprise-core, reprise-cli, reprise-mcp and reprise-stems are untouched, so the CLI can still create instrumentals and the AI badge plus FIL-7 keep marking them; the UX traceability gate passes with 228 active rules covered, but NOTHING in this workspace could be compiled here — GTK 4.22/libadwaita 1.9 are below the Ubuntu 24.04 candidates and GitHub Actions is currently unavailable, so fmt, clippy, tests and the architecture gate must be run locally before this lands)
Warning and gate cleanup: complete (this commit, base 47ba255faf, removed the five reported dead GTK items and their short warning cascade while preserving PlaybackMode::Preview and the AI substrate, removed the Instrumental preview gettext message and restored shared strings accidentally pruned with the instrumental module, synchronized the previously incomplete current catalogs, and updated the worker-isolation gate to require reprise-stems and ORT to remain outside the GTK dependency tree; warning-free GTK build, format, strict workspace clippy, the workspace suite with only the two sandbox-blocked Radio MCP loopback fixtures filtered, architecture, accessibility, input, worker isolation, gettext catalogs, UX traceability with 228 active rules, core purity, diff purity, Rust file-size enforcement and audit with only accepted RUSTSEC-2024-0436 passed; display tests and CUA remain unverified because the sandbox denied the private D-Bus and Xvfb sockets)
Dev integration follow-up: complete (this commit, base d5df760080, lowered the frontend-thinness ratchets to the post-removal measurements — 515 rusqlite sites, 13 thread spawns and 7 worker files — and removed the deleted conversion view from the dead-code allowlist; the focused thinness gate passed)
Remote-dev preservation: complete (this commit, live dev cbb396a1f9, preserved the newly activated RUN-1 and RUN-4 statuses while publishing the GTK instrumental removal on top of the runtime-service work)

# Podcast, YouTube, and Radio channel redesign

Base: `144672eaef`

Task PCR-1: complete (commit a4044619, base 144672eaef, added download byte metadata and an RSS-only per-subscription phone-sync opt-in that is cleared defensively for YouTube sources; finished and reclaimed downloads persist their actual size while paths and sizes clear atomically; format, strict clippy, full workspace tests, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, diff purity, and file-size checks passed)
Task PCR-2: complete (commit 7a7dc06b, base a4044619, split Podcasts and YouTube into source-grouped library places with real bounded artwork, context-aware duplicate-free Podcast/YouTube/Radio search, reversible source and episode removal, atomic manual and automatic download progress states, and RSS-only Android mirroring under Podcasts/Reprise while leaving YouTube and Music/Reprise untouched; two adversarial review rounds were resolved; format, strict clippy, full workspace tests, QA linters, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, diff purity, and file-size checks passed; the isolated grouped-view display tests compiled but their Xvfb run was blocked before GTK startup by sandbox D-Bus socket EPERM)
Task PCR-3: complete (commit 6917b993, base 7a7dc06b, activated POD-10 and completed the YouTube channel detail with an official dated UULF long-form window, worker-backed yt-dlp range expansion to 40, channel-scoped batch actions, shared localized download states, one Opus download policy, and MTP-present per-device RSS sync selection; both independent adversarial reviews were resolved; format, strict clippy, 1435 core tests, 1018 GNOME non-display tests, the otherwise complete workspace suite with only the two inherited local-TCP MCP Radio tests filtered because the sandbox rejects listener creation with EPERM, QA linters, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, UX traceability, diff purity, and file-size checks passed)
Stage-review note PCR: define one app-wide offline presentation contract for cached, empty, queued, interrupted, authentication, rate-limit, and provider-failure states; this remains deliberately outside the Podcast redesign stage instead of introducing a cross-app behavior change without its own specification.
Dev refresh PCR: complete (this merge commit, current dev 96fb39d7c5, preserved the Podcast/YouTube/Radio channel redesign while integrating the authoritative Android playlist mirror and current responsive-window work; resolved the parallel schema history by retaining dev v36-v39 and moving podcast download/device selection to v40-v41 with an upgrade-preservation regression; extended the shared sync plan and isolated `Podcasts/Reprise` storage root for per-device RSS downloads while excluding YouTube, and kept Podcast-only sync actionable in both the execution plan and visible page state; format, strict clippy, rustdoc, the host-isolated workspace suite with 1494 core tests, 1043 GNOME non-display tests and 310 display-only ignores, 97 Linux platform tests and 49 stems tests, architecture, accessibility, input, motion, GStreamer, UX traceability with 244 active rules, core purity, diff purity, Rust file-size enforcement, release build and audit with only accepted RUSTSEC-2024-0436 passed; the two inherited MCP Radio loopback tests were filtered after the sandbox rejected local TCP listener creation with EPERM, and private-Xvfb CUA could launch the release but could not expose that private display to the driver, so visible channel and device-page behavior remains the explicit manual acceptance check; no real database, music files, device, or desktop session was used)
Task T6-A (Turn 6c, dialogs): complete (commits 1c0a1386, 317d9410, ad813737, base 77b3f54545, implemented the design document's Turn 6c across all three add dialogs — SRC-6 hard provider separation with per-source dialog identity and refused source-foreign URLs, SRC-3a re-pointing the URL path to the dialog's own source, SRC-7 one compact add action with an in-place "Added" acknowledgement plus accessible names that always name the source, SRC-8 vertical-only bounded scrolling with a fixed footer for all three dialogs, and SRC-9 optional compact subscriber counts that are omitted rather than faked; format, strict workspace clippy, 1495 core plus 1049 GNOME non-display tests, cached audit with only accepted RUSTSEC-2024-0436, core purity, architecture, accessibility, input parity, motion, UX traceability with 248 active rules, and the rule-named Xvfb display gate passed; the add-dialog input projection and result projection were extracted into add_dialog_input.rs and add_dialog_results.rs to stay under the 800-line gate)
Stage-review note T6: the design document's Turn 6 and Turn 7 are captured in docs/plans/podcasts-youtube-radio-turn6.md; Blocks B through H (empty states, remote images, channel detail, MTP redesign with YouTube and podcast sync, offline contract, settings, and MCP parity) remain open, and the owner requires that every GUI capability also be reachable over MCP.
Task T6-B1 (Turn 6f, shared empty-state geometry): complete (commit 3a3c3a43, base c9a07e74, added source_empty_state.rs as the one shared "nothing added yet" geometry for Podcasts, YouTube, and Radio — sidebar glyph in a subdued rounded tile, title, one paragraph of what lands here and where it comes from, exactly one primary Add button with a plus icon, and a quiet secondary URL-path line where the source has one; reused the existing podcasts_empty_state.rs/radio_empty_state.rs classifications rather than replacing them, leaving NoEpisodes/NoResults on their prior status page; fixed radio's toolbar (Add/filter chips/count) staying visible in the true empty state and hid the podcasts footer's now-meaningless refresh row there too; the approved Podcasts secondary copy names OPML import, which has no implementation in this codebase, so it uses the feed-URL wording instead (see plan O-7); added ux-rules.md SRC-10 with rule-named tests; format, strict clippy, the full workspace suite with 1053 GNOME non-display tests and 317 display-only ignores, architecture, accessibility, input parity, motion tokens, UX traceability with 249 active rules, core purity, and cached audit with only accepted RUSTSEC-2024-0436 passed; the rule-named Xvfb display gate passed for every test this commit touched, and confirmed on the unmodified base commit that its two other red tests (podcasts_scroller's play_7b and radio add_dialog's src_5) were pre-existing failures unrelated to this change)
Task T6-B1 (Turn 6f, empty states): complete (commit 50231d3d, base c9a07e74, one shared empty-state geometry for Podcasts, YouTube and Radio behind new rule SRC-10 — sidebar glyph, title, one sentence each for what lands here and where it comes from, exactly one primary Add action, and a quiet secondary URL line; no toolbar, filter row or count in a true empty state; the Radio toolbar previously sat outside the stack and stayed visible with zero stations, which is fixed here; the design's OPML secondary line was deliberately not adopted because no OPML import path exists in this repo and the empty state must not promise one; format, strict clippy, full workspace tests, UX traceability, architecture, accessibility, input parity, motion and the rule-named Xvfb display gate passed)
Task T6-D1 (Turn 6b, download legibility): complete (commit 62e14dd0 merged as a no-ff merge, base 50231d3d, added rule POD-11 with a per-row download column carrying state plus real file size and a channel header summary of window size, downloaded count and combined bytes on disk; totals are computed over the whole channel rather than the visible window so the figure reads as real disk usage, sizes are never invented for undownloaded episodes, and the summary is a pure tested projection; format, strict clippy, full workspace tests, UX traceability with 250 active rules, architecture, accessibility, input parity, motion passed, and the rule-named display gate reported 180 of 182 with only the pre-existing play_7b failure and one test already fixed on the target branch)
Incident T6-verification: an earlier claim in this stage that the rule-named display gate was green was wrong — the gate reports "failed: N of M" rather than "FAILED", so grepping for the latter reads as green while sixteen tests were failing on a mid-refactor tree. Read the block after "== display test summary ==" instead. Two apparent regressions (stats_17, tag_6) turned out to be environment flakes under parallel build load, failing with "Failed to initialize GTK" and passing in isolation on both HEAD and base.
Task T6-HA (Turn 6c/Block H, MCP discovery): complete (commit 9308c9e8 merged as a no-ff merge, base 50231d3d, added the capability-gated music_search_sources tool exposing provider-separated discovery over MCP under the existing sources:manage gate, honouring SRC-6 by querying exactly one provider and SRC-9 by omitting an absent subscriber count entirely; the already-subscribed filtering was moved out of the GNOME crate into reprise-core as podcasts::discovery and radio::search::filter_new_stations so GUI and MCP share one projection instead of drifting, with the existing rule-named tests moved alongside it; no new UX rule was needed because SRC-6 and SRC-9 are already written surface-neutral; format, strict clippy, the full workspace suite including six new MCP integration tests, UX traceability with 250 active rules, architecture and core purity passed after the merge)
Decision T6-HA-url: the MCP discovery response returns each candidate URL with userinfo and fragment stripped and non-http(s) schemes rejected, but deliberately keeps the query string, because SRC-5's own test proves a feed's query string can be part of its identity and stripping it would point a follow-up add at the wrong feed; artwork URLs are omitted entirely. This is a documented deviation from the blanket "no query strings" wording in docs/plans/podcasts-youtube-radio-turn6.md; the owner confirmed it on 2026-07-28, so the plan's wording is the part that is wrong and was corrected.
Task T6-G1 (Turn 7b, Online sources page and global gate): complete (commit d5467ee6 plus follow-up 4803761e, base 12afbde4, added the Online sources preferences page with a global master and independent YouTube, Podcasts and Radio blocks, promoted YouTube to its own peer ModuleDescriptor with id "youtube" and retired podcasts.youtube_enabled outright since there are no installations to migrate, and introduced reprise_core::online_sources as the single authority whose network_allowed ANDs the global switch with each module; NET-1 was replaced by NET-1a and SET-8 was added; the design's Phone sync block was deliberately left out because its rows configure Block E sync work that does not exist yet)
Incident T6-G1-gap: the first G1 commit gated only the YouTube search, so with the switch off podcast search still reached Apple and radio search still reached radio-browser while the page promised "no requests, no downloads, nothing hidden". Both dialogs now decide at one submit choke point before any provider work, state the refusal rather than failing silently, and treat a failed lookup as not allowed; the podcast dialog's SRC-6 and NET-1a guards were merged into one pure, display-free testable function. The lesson is that a privacy claim in copy needs a test per call path, not per feature.
Harness repair T6: the rule-named display gate now retries a test once, on a different Xvfb server number, when and only when it failed with GTK's "Failed to initialize GTK" — an environment fault that produced three false regressions in one day and that always passed on an isolated re-run. Any other failure is still reported unchanged, and a retry logs a visible line. The retry did not trigger in its verifying run, so it is guarded but not yet proven in practice.
Fix T6-play7b: play_7b_podcast_episode_table_scrolls_inside_the_player_bar_boundary had been the gate's one standing failure since before this stage. It compared ScrolledWindow::child() with the table directly, but GtkScrolledWindow hosts a non-scrollable child inside a GtkViewport of its own, so the assertion compared a viewport with a box and could never hold. It now asserts that the table is what the scroller hosts.
Task T6-F1/F3 (6e, offline contract): complete (commit cfc3e331, base eade62d8, wrote NET-3 as the app-wide offline contract covering cached, empty, queued, interrupted, authentication, rate limit and provider failure, kept [geplant] because only part of it is implemented; NET-3a adds a pure display-free projection in reprise_core::connectivity that answers from connectivity plus local availability what a row reads and what an action does, with phone sync falling out of it because MTP transfer is local; NET-3b implements the Radio exception, where a live stream cannot be deferred and Play offers "No connection · Retry" instead of queueing; connectivity is an explicit injectable value rather than inferred from a failed request; LYR-3 was deliberately left out of the migration because it describes a disabled module rather than an offline state; format, strict clippy, full workspace suite, core purity, UX traceability with 253 active rules, architecture, accessibility, input parity and motion passed)
Note T6-F-scope: F2's queue execution and automatic run-on-reconnect, F4's dialog offline mode, and 7f's sync skip notice are modelled but not wired, and remain open. Closes the specification half of issue #107.
Task T6-E1 (7a/7e/7f, sync target and wanted state model): complete (commit 95c65bc4, base dc2597c2, replaced the single managed device folder with three named per-device targets carrying StorageID, path, activation and an optional cap in new table device_sync_targets, added oldest-first cap eviction as a pure function, and introduced wanted_on_device so that marking an episode for the phone before it has a file defers the download instead of demanding the user download first; the global-rules versus per-device-folders split from 7b/7e is expressed structurally — SyncTarget deliberately carries no content-selection field, and its doc comment says why — and the Android media-scanner reasoning behind /Podcasts/Reprise and /Music/Reprise-YouTube is recorded so the defaults are not tidied away later; wanted_on_device composes with connectivity::deferrable_action_outcome from NET-3a rather than duplicating it; new rules MTP-18, MTP-19 and MTP-20 after checking MTP-1 through MTP-17 for collisions; schema moved 41 to 43 with no migration path needed since there are no installations; format, strict clippy, 1536 core tests and the full workspace suite, core purity, cached audit with only accepted RUSTSEC-2024-0436, UX traceability with 256 active rules, architecture, accessibility, input parity, motion, and the display gate at 184 of 184 all passed)
Note T6-E1-followup: POD-8 still hardcodes "downloaded RSS episodes only" and the old single-folder paths. It is not yet contradictory because query_candidates_for_device is untouched and wanted_on_device is inert, but POD-8 and docs/plans/android-sync.md must be updated in E2/E4 when the new model actually replaces the old resolution.
Task T6-E2/E3 (7a/7c, selection and category diff): complete (commit 3ff96687, base 8c1671ce, added MTP-21 for a pure per-category intended-file-set projection and MTP-22 for a per-category plan reading with an aggregated balance; copy and remove stay separate in both files and bytes and has_work counts files rather than bytes, so a deletions-only sync reports work while moving zero bytes — the defect design 7c names, where "3 changes · 0 B" reads as nothing to do while three files are deleted; an item wanted without a file yet is counted apart and never folded into files_to_copy so the balance cannot promise a transfer that will not happen; a category distinguishes switched-off from evaluated-to-zero from could-not-be-evaluated rather than flattening all three into one zero; the existing playlist mirror plan is translated into the same shape instead of being diffed a second time; format, strict clippy, full workspace suite, core purity, UX traceability with 258 active rules, architecture, accessibility, input parity and motion passed)
Incident T6-fil9: fil_9_filter_changes_center_the_visible_playing_track fails with a stable off-by-one-row result (actual 680, expected 614.5, a gap of about one row height). It is NOT a regression from this stage: it fails identically at dc2597c2, the last commit where the display gate reported 184/184, and no commit on this branch touched crates/reprise-gnome/src/ui/track_list/ or scroll_center.rs. Manual runs replicating the gate's full environment still fail, so the difference is not the private XDG roots. Filed as issue #122 rather than chased inside Block E. Note the contrast with the GTK-init flakes: those print "Failed to initialize GTK" and are retried by the gate; fil_9 produces a real assertion and must not be treated as environmental.
Task T6-E4 (7a, transfer): complete (commit 63ba5048, base 4d45a21a, routed the transfer through the three named per-device targets instead of one managed root, switched YouTube phone sync on by making every RSS-only assumption kind-aware rather than special-casing YouTube at one call site, kept music on the transfer profile while copying podcast and YouTube audio 1:1 because they are already Opus or AAC (MTP-24), made the size cap real with oldest-first eviction before an overflowing transfer starts (MTP-25), and added MTP-23 for the write and delete contract; POD-8 asserted both RSS-only selection and a single fixed folder, so it was replaced by POD-12 with its test re-pointed rather than left contradicting the new rules; docs/plans/android-sync.md was updated; all tests drive the recording DeviceBackend double so the gate never needs a phone; format, strict clippy, full workspace suite, core purity, UX traceability with 261 active rules, architecture, accessibility, input parity, motion and the device-sync GStreamer check passed)
Incident T6-commit-sweep: while an agent was working uncommitted in the same worktree, a `git add -A` from the coordinating session swept its entire in-progress E4 change into a commit labelled as a docs-only change. Caught by inspecting the commit's stat, then split apart with a soft reset so the docs commit carries only the doc file and the agent's work was re-verified and committed on its own. The lesson: never `git add -A` in a worktree where a subagent is working; either give the agent its own worktree or stage explicit paths.
Task T6-E5/E7 (7a/7c, device view and sidebar card): complete (commit 9562eda2, base ea22d9ce, gave the device page a content panel with a category-segmented storage bar carrying a hatched "Incoming this sync" segment, a "Device contents never verified" state with a Scan device action reusing the existing inspect call rather than a second scan mechanism, a Content section per named target and a Next synchronization panel reading MTP-22 per category; the sidebar card's blended "N changes · X to transfer" line is gone, replaced by exactly one of four leading sentences projected from the balance, with the full figures moved to the tooltip; the deletions-only case now reads "3 to remove · frees 148.0 MiB" and its test additionally asserts the string never contains "0 B", so the defect design 7c names cannot silently return; the 2026-07-28 addendum is honoured structurally — selection summary and cap are read-only displays of the global Preferences rules and the section's only switch is SyncTarget::enabled, which MTP-28 states as binding text; MTP-11 described an idle card that no longer exists and is marked [ersetzt durch MTP-29]; new rules MTP-26 through MTP-29 after checking MTP-1 through MTP-25 for collisions; verified independently of the agent's report: format, strict clippy, 1566 core and 1095 GNOME tests with none failing, core purity, and all five script gates passed)
Task T6-E-autostart (7a, MTP-30): complete (commit f2567a87, base 9562eda2, the "Sync automatically when this phone connects" switch shipped in 9562eda2 was a dead control — persisted as schema v44 with DEFAULT 1 and rendered into the UI, but no code path ever read it to decide anything, because design 7a and the plan both specify the control under E5 while no block owned the behaviour and F2 covers network reconnect rather than device connect; the owner chose to implement it with the default left on; the decision is the pure should_auto_start over AutoStartFacts in reprise-core, requiring the first refresh after connect, the switch on, a verified scan, error-free planning, an idle connected device and real work per the existing SyncBalance, with the balance reused through a shared category_readings() rather than derived a second time; the GTK side gathers facts in one short borrow, drops it, and only then calls sync_now, mirroring the existing resume_planned hook because sync_now re-enters GTK; a refused or failed automatic start stays silent apart from a tracing warning since the user pressed nothing; verified that the trigger cannot repeat — apply_devices only enqueues a refresh when a device actually connects or reconnects, so a repeated poll of an already-connected device enqueues nothing; format, strict clippy, 2997 workspace tests with none failing, core purity, five script gates, cached audit with only accepted RUSTSEC-2024-0436, and the rule-named display gate at "failed: 0 of 184" all passed)
Note T6-E-autostart-default: turning the switch on by default is a real behavioural change, not a cosmetic default — it broke 33 existing tests that drive sync_now by hand and had begun racing an automatic start. Those fixtures now set sync_automatically: false explicitly with a rule reference. In production the same default means a connected device with pending work begins transferring unprompted; this is the owner's explicit decision, recorded here because it is the kind of default that looks like an accident when read later.
Correction T6-fil9: the incident entry above concluded that fil_9 is not environmental because its failure numbers are constant. That conclusion was wrong, and the reasoning was the trap — a row height is a discrete layout state, so an unsettled layout is off by exactly one row every time, which makes constant numbers evidence of nothing. A controlled re-test (idle twice, then twelve CPU busy-loops applied and released under a shell trap) passed both idle runs and failed one of two loaded runs with the identical 680 versus 614.5. fil_9 is a flaky test sensitive to CPU contention. The "not a regression, fails at dc2597c2 too" half of the original finding stands unchanged. Issue #122 was corrected and retitled rather than left carrying the wrong diagnosis.
Incident T6-cpu-leak: this machine was carrying 48 orphaned CPU busy-loops leaked by an unrelated fil_9 load experiment in another worktree — four batches of twelve, running 3 h 43 min at a load average of about 52 on eight cores, which is very likely why the display gate's GTK-init flakes appeared in the first place and certainly why every fil_9 failure in this stage clustered in that window. The experiment applies hogs and kills them at the end, but its parent shell was reaped first, so the kill never ran and the spinners were reparented to systemd. Removed by verifying each PID individually (command line, orphaned parent, no children) rather than by a broad pattern kill, after an earlier broad pkill in this stage had hit builds belonging to the user. Any future load experiment must arm a shell trap so the hogs die with the script.
Task T6-E6 (7d, device folder browser): complete (commit 0a26675d, base 3b089e56, gave the three named per-device sync targets an editing surface — "Change folder…" in the Content section (MTP-28) opens a browser with storage selection, folder tree navigation, "New folder", a target preview, a warning when the chosen folder sits inside the authoritative Playlists tree (MTP-17), and "Reset to default"; every decision (storage classification, target preview, playlist-conflict warning, reset, and the MTP-32 relocate-vs-recopy choice) is a pure function in reprise_core::device_sync::browser, reusing MTP-18's target_storage_transition rather than re-deriving it; GTK only gathers facts through four new DeviceBackend methods (list_storages/list_folders/create_folder/move_folder, each with an "unavailable" default) and obeys; reprise-platform-linux resolves storage and folder contents fresh from gio::File enumeration every call (no MTP handle is ever persisted) and derives a stable StorageId from each storage's GVfs-reported name since GVfs exposes no raw PTP StorageID; a device that refuses folder creation at a storage's own root surfaces DeviceIoError::CannotCreateAtStorageRoot distinctly; new rules MTP-31 and MTP-32 after checking MTP-1 through MTP-30 for collisions, and MTP-28's closing sentence (which deferred this browser to E6) was updated in place; the FakeBackend test double was extended for browsing/folder creation and split into its own device_sync_fake_backend.rs to keep device_sync_runtime_tests.rs under the file-size gate, so no test needs a real phone; format, strict clippy, the full workspace suite (1596 core, 1108 GNOME non-display plus 327 display-only ignores, 104 Linux platform, 49 stems, full MCP suite), core purity, UX traceability at 268 active rules, architecture, accessibility semantics and input parity all passed). The rule-named Xvfb display gate was intentionally not run by this task, per the coordinating session's instruction that it runs it separately.
Task T6-C1 (Block C, remote image module): complete (commit cdd73a53 plus fixes 1ec22016, 077b32e0 and 5-… see below, base 3b089e56, added reprise_core::remote_image with a pure resolve over an injected fetcher, a bounded on-disk cache and the source_images module, and unblocked the image parts of A2 and B1 which had been shipping on the glyph fallback; the commit also closed a live privacy hole rather than only adding a feature — before it, podcasts_groups, youtube_channel_detail and the radio search passed real remote URLs straight into SourceImage, whose set_url called source_artwork::fetch with no gate check at all, so switched-off online sources still fetched artwork; new rule SRC-11 with a test per call path)
Fix T6-C1-cachehit: SRC-11 states that a cache hit is always shown regardless of the gate and remote_image::resolve implements exactly that, reading the on-disk cache before it consults the flag — but the widget returned before ever reaching it, so a closed gate hid images already on disk that needed no request. The divergence was safe yet the rule is binding, and the cost was invisible: the memory cache is empty at startup, so every restart with sources off dropped artwork it had already downloaded. The five existing src_11 widget tests could not catch it because with an empty cache the fallback looks identical either way; the new test populates the cache through the public core path first and fails without the fix (commit 1ec22016).
Task T6-E6 (7d, device target-folder browser): complete (commits 0a26675d and 33e43649, base 1ec22016, storage selection, a folder tree, New folder, target preview, the playlist-nesting warning and Reset to default, with every decision a pure projection in device_sync::browser and the GTK side only rendering; GVfs exposes no raw PTP StorageID, so derive_storage_id hashes the reported storage name — stable across reconnects for one phone but not bit-identical to the wire value, the one deliberate compromise and documented as such; new rules MTP-31 and MTP-32, MTP-28's "path is read-only text" sentence resolved)
Review T6-codex: an external reviewer (Codex CLI, read-only sandbox, no writes and no builds) audited ea22d9ce..HEAD — 52 files, ~5500 added lines, scoped deliberately to this window rather than the whole 43-commit branch. It returned six P1 defects and three contract gaps; every one was verified against the code before being acted on, and all six P1s are now fixed with a red-then-green proof per fix. What the gates could not see is the point: all of it passed fmt, clippy, 3033 workspace tests, five script gates and a 191-of-191 display gate.
Fix T6-review-storage: E6's browser was largely cosmetic. PlannedWork carried only paths and never the storage_id, every DeviceBackend write went through storage_root() with its hard "prefer internal" rule, and inspection walked hardcoded Music/Reprise, Music/Reprise-YouTube and Podcasts/Reprise. Choosing an SD card and /Music/YT therefore transferred to internal storage anyway, and the next scan did not recognise the chosen folder as that category's inventory, so it reported the category empty and planned the same files again. Storage now threads through the whole transfer chain and inspection resolves each target's own storage and path (commit 667b567d, rules MTP-33 through MTP-35 for this and the two below).
Fix T6-review-deadswitch: remove_deleted was the THIRD persisted-but-never-read control found on this branch, after sync_automatically (MTP-30) and the ungated image fetch. target_podcast_plan passed a hardcoded true, so turning the switch off and unsubscribing still queued the device file for deletion. The pattern is the lesson: a switch that renders and persists reads as finished, and nothing in the gate battery disagrees. Any new setting needs a test that asserts the behaviour differs between its two positions, not that the row exists.
Fix T6-review-race: the folder browser started an uncancelled listing per navigation and appended results unconditionally, so a slow /Music listing could land under /Podcasts and a row click then navigated to a path that does not exist. Now behind a generation token, the same shape cover_loader.rs and source_image.rs already use for recycled rows.
Fix T6-review-savefail: a refused Save logged a warning and closed the folder browser anyway, so a save rejected because the device was busy or gone looked successful and the choice was silently lost. It now reports inline through the same error surface MTP-31 already uses for a refused New folder and keeps the dialog open.
Fix T6-review-gatestale: the artwork workers captured the gate value at enqueue time, so tasks queued while online sources were on kept fetching after the switch went off (commit 077b32e0 moved the decision to a process-wide atomic read immediately before the fetch). That fix was itself incomplete: the atomic was published only from queue_artwork, so it flipped only when somebody rendered another uncached image — and Preferences, the one page where the switch is thrown, shows no source artwork at all, so a draining queue would never learn. It is now republished from set_online_sources_enabled beside the existing SET-4 recomputations and after any successful module toggle.
Incident T6-two-agents-one-worktree: running two subagents concurrently in the same worktree, each instructed to run the full workspace gate, was a coordination error. Their uncommitted work compiled together, so neither could get a meaningful result; one deadlocked waiting on its own backgrounded run. Worse, the two failure sets masked each other — a clippy error introduced in 077b32e0 (needless_pass_by_value) went unnoticed because that run died on the other agent's unrelated build errors, and HEAD briefly failed clippy. Either give each agent its own worktree, or let only one run the workspace gate and have the coordinator run the authoritative battery once both have committed.
Note T6-review-open: three reviewer findings are real but are feature work rather than defects, and are NOT fixed. (1) MTP-21 is [aktiv] and requires "jede ungespielte, bereits heruntergeladene Episode" plus counting a wanted-but-undownloaded episode as waiting; the pure projection honours both and is tested, but the live pipeline calls neither — query_candidates_for_device applies no played filter and files_waiting_for_download is hardcoded 0. The rule-named test asserts only the label, so it stays green even if the selection behaviour were removed entirely. With MTP-30 now live this also means a device can read "Up to date" and skip its automatic sync while work is pending. (2) The device view says "Rules from Preferences" but 7b's Phone sync block does not exist, so the referenced controls cannot be reached; SET-8 requires it once Block E exists, and Block E now does. (3) MCP parity does not cover targets, caps, per-category diffs or wanted_on_device — device_dto still exposes only the legacy aggregate, so an agent can start a sync but cannot see what it would do.
Task T6-HDEFG (Block H, MCP parity for D/E/F/G — closes T6-review-open finding 3): complete (this commit, base 87f9e73a, four new MCP tools plus extensions to two existing surfaces, every one reusing the same reprise-core facade the GUI reads/writes rather than a second path). H-D: moved the YouTube channel detail's window/Shorts-filter/download-summary math out of reprise-gnome into podcasts::channel_window (POD-10/POD-11, now [core][gtk]), added the read-only music_get_channel_detail tool on top of it, extracted podcasts::pipeline::download_episode from refresh's inline auto-download loop so a new music_manage_episodes tool's download/remove/want_on_device batch actions and the refresh pipeline share one download path instead of two. H-E: agent_device_sync::AgentDeviceSyncDevice gained a categories field reusing device_sync::SyncTargetKind/CategoryReading directly (no parallel DTO), threaded through a new CategorySnapshot D-Bus method (kept separate from the already-16-wide Snapshot tuple deliberately) into music_get_device_sync_state, which now reports each named target's folder/cap/diff reading plus an aggregate balance computed via the real aggregate_balance function; wanted_on_device (MTP-20) is exposed as music_manage_episodes' want_on_device action, DB-only, no live device needed. H-F: source_data's PodcastEpisodeDto gained download_state and local_availability (NET-3a's real local_availability() bridge, filesystem-checked) so an agent can tell before acting whether an item is already local — documented honestly as the one connectivity signal Reprise can back today, since NET-3c's real OS network-monitor binding does not exist yet. H-G: new music_manage_online_sources tool reads/writes the exact NET-1a/SET-8 authorities (online_sources::is_enabled/set_enabled, modules::is_enabled/set_enabled). No new ux-rules.md rule was needed — MTP-18/20/22, NET-1a, NET-3a and POD-10/11 were already surface-neutral; only POD-11 gained a [core] tag plus a documentation note now that its projection also lives in core. Found and fixed one response-hygiene gap before it shipped: a failed download's raw provider-error message (which can echo the request URL, and an episode audio_url can carry a private token exactly like a feed URL can, SRC-5) was going straight into the tool response; now sanitized to a fixed string, with a dedicated leak test. format, strict clippy (both without and with --features reprise-mcp/mpris), the full unfiltered workspace suite in both configurations (reprise-core 1607, reprise-gnome 1115 non-display + 328 display-only ignores, reprise-mcp 34 unit + full integration suite, reprise-platform-linux 106, reprise-stems 49, reprise-cli 11+2+4+5+8+7+4), core purity, UX traceability at 271 active rules, architecture, accessibility, input parity, and audit with only accepted RUSTSEC-2024-0436 all passed.
Task T6-E8 (7a/7e, sync rules onto the device page): complete (commit daa8246e, merged as 5b38459d, base 8e86a485, follows owner decisions E-5 and E-6 of 2026-07-29 — exactly one MTP device, so the global-versus-per-device split loses the justification it was built on and the rules move to where the device is; the cap became an editable SpinButton wired to SyncTarget::cap_bytes with a re-plan, the selection summary became a live per-device count from the new podcasts::phone_sync::selection_summary instead of the static "Rules from Preferences" stub, and the "Same on all devices" cross-reference is gone along with the never-built Phone sync block; MTP-28 is [ersetzt durch MTP-37] and SET-8 by SET-9, with every rule-named test re-pointed in the same commit and no orphan mtp_28_/set_8_ names left, verified independently; the cap test proves behaviour rather than existence — lowering the cap evicts the older episode from the next plan, red at "left: 2 right: 1" without the fix; the data model was deliberately NOT refactored, because storing per device stays right even when managing several devices does not)
Note T6-zero-meaning: two adjacent numeric settings now give 0 opposite meanings — a cap of 0 GiB means unlimited (cap_bytes has been Option since MTP-18), while MTP-36's decision made a channel's "latest N" of 0 mean "nothing from this channel", on the argument that unlimited must not be selectable or one number can quietly fill the device. That argument applies to the cap just as well, so MTP-36 is the outlier rather than E8's work. MTP-36 is still [geplant] and unimplemented, so aligning it costs one line of rule text. Raised with the owner, not decided locally.
Incident T6-tmpfs: /tmp (a 16G tmpfs, i.e. RAM) filled to 100% mid-session and failed unrelated commands with ENOSPC — 8450 stale tmp.* directories holding 15G. First diagnosis was wrong: AGENTS.md's isolation recipe looked like the culprit, but check-display-tests.sh does clean up its worker roots. The real defect is narrower: the worker drops the parent's EXIT trap so it never deletes a sibling's log, and was then left with no cleanup at all, so the tidy-up ran only on the normal path. Every interrupted or timed-out gate run therefore abandoned five directories per test, about 955 per run — and this session aborted several. Fixed in d6a5b299 by giving each worker a trap for its own five directories on INT/TERM/HUP as well as EXIT, since bash runs no EXIT trap for an untrapped fatal signal and the aborted run is precisely the case that leaked. The improvement could not be measured cleanly while another agent ran the unfixed script concurrently; it is correct by construction and will show on the next aborted run.
Lesson T6-selfkill: `pgrep -f check-display-tests` matches the agent's own shell, whose command line contains that string. It killed this session's own command once (exit 144) and later reported a gate as running when none was. Match on `comm` plus an explicit self-PID exclusion, or accept both a self-kill and a false positive.
Note T6-mtp-renumber: our MTP-18, MTP-19, MTP-20 and MTP-21 are now MTP-38, MTP-39, MTP-40 and MTP-41. Both this branch and origin/dev appended four rules to a common base of MTP-17 without knowing about each other, so the same four IDs carry two different meanings: dev 's are step naming, failure fallout, the sync run log and proven transfer, ours are the three named sync targets, cap eviction, wanted_on_device and the intended-file-set projection. dev is the pushed trunk and its IDs already appear in merged test names, so this branch moves. Our MTP-22 through MTP-37 keep their numbers because dev never used them, which leaves the block non-contiguous — MTP-17, then dev's 18-21, then ours 22-41 — and that is the honest shape rather than a tidy one. Every rule-named test and every code and plan cross-reference moved in this same commit; the entries above this line deliberately were NOT rewritten, because the ledger records what was true when it was written and a renumbered history would misrepresent it. Read any MTP-18 through MTP-21 above this line as MTP-38 through MTP-41. The renumber had to precede the dev merge: dev's own sync-log files reference MTP-20 meaning dev's rule, so a rewrite afterwards could not have told the two apart.
Merge T6-dev: origin/dev merged into feature/podcast-channel-redesign (this merge commit, 53 commits incoming, 18 conflicting files). Two structural collisions dominated it and neither was a text conflict. First, dev's 7d8a47c1 had translated docs/ux-rules.md into English including its status vocabulary ([aktiv]->[active], [geplant]->[planned], [ersetzt durch X]->[replaced by X]) and moved both check-ux-traceability.sh and check-display-tests.sh onto the English tokens; our 40 new rules were German, so a naive merge would have left them unparsed and the traceability gate would have reported ok while looking at none of them, which is the exact failure dev's own commit message warns about. The rulebook was therefore rebuilt from dev's English version and our 40 additions plus 12 edits to pre-existing rules were re-applied translated, giving 405 unique ids with no duplicates and no German marker left. Second, both lines had appended MTP-18 through MTP-21 to a common base of MTP-17 for different rules; ours were renumbered to MTP-38..41 in the separate preceding commit bd4c1ba3, deliberately before the merge, because dev's sync-log files reference MTP-20 meaning dev's rule and a rewrite afterwards could not have told the two apart. Schema: both sides added a v40 migration, so dev's sync log moved to v45 and SUPPORTED_SCHEMA_VERSION is 45. Engine: dev refactored device_sync_planned.rs onto a DeviceSyncMachine reducer, so our per-target path and storage routing was re-applied on top of it and the additive podcast/YouTube content targets now run as a phase after the machine reports Finished rather than interleaved, because the mirror is authoritative over its folder (MTP-17) and the content targets are not - letting the authoritative step settle first means a content copy cannot be deleted by a mirror cleanup in the same run; our device_sync_music_transfer.rs was deleted since the machine owns that work now. MCP: dev replaced the positional D-Bus tuples with named dict types, which removes the zvariant tuple-arity ceiling that was the stated reason our Block H CategorySnapshot was a separate D-Bus method, so categories moved into DeviceSnapshot as a field and the extra method is gone; PROTOCOL_VERSION is 1.4. Four files were split to stay under the 800-line gate (device_sync_browser_tests.rs, device_sync_inflight_tests.rs, device_sync_run_log.rs, and run_content_phase into device_sync_content_transfer.rs). fmt, strict clippy, the full workspace suite with none failing, core purity, UX traceability at 264 active rules, architecture, accessibility semantics, input parity, motion, and audit with only accepted RUSTSEC-2024-0436 all pass. NOT pushed - the owner tests manually first.
Decision T6-zero: the owner decided on 2026-07-29 that 0 means unlimited in every numeric sync setting. The two adjacent numbers on the device page read 0 in opposite directions - the size cap has meant unlimited since MTP-38 modelled cap_bytes as an Option, while MTP-36's "latest N" was drafted with 0 meaning nothing from this channel. MTP-36 was the outlier and still unimplemented, so alignment cost one line of rule text instead of a rebuild. "Nothing from this channel" is what the 6b channel toggle says; it is not a quantity, so it is not expressed as one. Recorded in MTP-36 and in the plan's section 8a as E-9.
Lesson T6-schema-guard: the runtime protocol's no_snapshot_carries_a_local_filesystem_path test bans any snapshot string starting with "/", and the new target_path field legitimately carries a device path like /Podcasts/Reprise, so the field had to be exempted. The first exemption was written with a positive counter-check that looked right and passed - and would have passed forever, because zvariant serializes each field as {"signature","value"} and the checker only collected plain strings, so it inspected nothing. It was caught by deliberately injecting /home/marvin/Music into target_path and observing the test stay green. The rule this branch already wrote for MTP-21 applies to guards as much as to features: after writing one, break the thing it guards and watch it go red.
Fix T6-split-cohesion: the dev merge pushed device_sync_planned.rs over the 800-line file-size gate, and the script that split out the MTP-20 run log took the whole `impl DeviceSyncRuntime` block with it rather than just reload_sync_history — because it looked for the next line that is exactly "}" at column 0, which is the end of the impl, not the end of the method. sync_now, the sync entry point, therefore came to live in a file whose module documentation says it is about recording runs. It compiled, clippy was clean and every test passed, so no gate disagreed; only reading the file for an unrelated reason surfaced it. sync_now and eject are back in device_sync_planned.rs (792 lines) and device_sync_run_log.rs keeps only the log plus its reader (127 lines). The lesson is narrow and mechanical: a brace-matching split must anchor on the item it is moving, not on the first dedented brace after it.
Incident T6-gate-contention: the post-merge display gate reported "failed: 4 of 196" — three stats_17 entrance tests and stats_10_narrow_window. All four pass in isolation under the gate's own environment recipe. Neither this branch nor its side of the merge ever touched crates/reprise-gnome/src/ui/stats/; dev touched it, and did so specifically to fix display-test flakes (ce633e08, af597def). A second session's display gate was running against a different worktree on the same machine throughout the run, which is the CPU-contention condition the fil_9 correction and the cpu-leak incident already identified for this class of timing- and layout-sensitive test. Treated as contention rather than regression only after the isolation runs, and to be confirmed by a repeat gate on a quiet machine rather than by the reasoning alone.
Lesson T6-selfwatch: a background watcher written as `until ! pgrep -f "check-display-tests.sh --rule-named"; do sleep 30; done` never terminates, because the watcher's own command line contains that string and pgrep matches it. This is the same self-match trap recorded as T6-selfkill, walked into again from the other direction: the first time it killed a shell, this time it produced a watcher that would have waited forever while the gate it watched had already finished. Wait on a specific pid with `kill -0` instead, or exclude the caller's own pid explicitly.
Lesson T6-gate-tree: editing tracked sources while a display gate runs invalidates that gate. Each worker re-invokes `cargo test -p reprise-gnome`, so a mid-run edit means some tests ran against the old tree and some against the new one, and cargo's build lock serialises the workers during the rebuild. The rule that two agents must not share a worktree applies to an agent and a running gate just as much.
Confirmation T6-gate-contention: the repeat display gate on a quiet machine reports "passed: 196, failed: 0 of 196". The four stats failures of the earlier run were CPU contention from a second session's gate running concurrently against another worktree, not a merge regression — now established by a clean repeat rather than by the reasoning alone. Note the count moved from 191 pre-merge to 196: dev brought five further display tests with it.
Task T6-E9-prep (design 7f's preparation phase, MTP-42): complete (this commit, base c111ff72, new dependency-pure `reprise_core::device_sync::preparation` decides, from `MTP-41`'s waiting set, `NET-1a`'s global gate, `NET-3a`'s connectivity and the device's own prepare-before-sync switch, whether a preparation phase exists at all and what it does — `Absent` when online sources are off (wins over every other condition, tested stacked against missing files, offline and switch-off together), `NothingMissing`, `SkippedOffline` while offline (also tested against metered-and-switch-off stacked on top, since offline must win over policy, not just appear first in the code), `Offered` when metered or the switch is off, otherwise `Planned`; `primary_action` projects only `Planned` to "Download & sync" and leaves every other phase, including `Offered`, as a plain "Sync now" — an enum, not a display string, wording stays in the frontend. Added MTP-42 to docs/ux-rules.md [active][core] with its precedence and the offline-beats-metered reasoning as a doc comment. format, strict clippy, the full unfiltered workspace suite (reprise-core 1690 passed incl. 11 new mtp_42_ tests, reprise-gnome 1109 passed + 334 display-only ignores, reprise-platform-linux 117, reprise-stems 49, reprise-cli 11+2+4+5+8+7+4+13+4+2+7+0+0, reprise-mcp 34 unit + full integration suite, reprise-runtime 72 + 4 protocol + 8 schema), core purity (cargo tree -p reprise-core has no gtk4/libadwaita/gstreamer/zbus), UX traceability at 265 active rules, architecture, accessibility semantics, input parity, and audit with only accepted RUSTSEC-2024-0436 all passed. Housekeeping: this worktree's own git branch had been provisioned from a stale, unrelated base (a `dev` merge missing the entire E7/E8/connectivity lineage this task needed); reset in place to feature/podcast-channel-redesign's tip (c111ff72) before starting, losing nothing unique since the sole prior commit was a redundant PR merge already in origin/dev.
Task T6-E9-A/B (7f, preparation projection and download priority): complete (commits d4bf6c04 and 60fb26f4, merged as 2db8ce31, base c111ff72, built by two subagents in separate worktrees and verified here by mutation rather than by their reports). MTP-42 is the pure projection reprise_core::device_sync::preparation: five states (Absent, NothingMissing, SkippedOffline, Offered, Planned) resolved in a fixed order where NET-1a's global gate beats everything, then nothing-missing, then offline, then metered-or-switch-off, then planned. Offline deliberately beats metered because offline is a fact about whether the download can run while metered is policy about whether it should, and offering a download that cannot run either way would be a lie dressed as a choice. MTP-44 gives the existing download manager a priority lane rather than a second path: PodcastsRequest carries a priority, the one worker drains the priority channel first via futures_lite::future::or, and PodcastsOperation::Download and pipeline remain the single operation and single executor; a DownloadForSync variant was considered and rejected as a second path in spirit. Verified by mutation: removing the NET-1a gate turned two mtp_42_ tests red, removing the offline branch turned four red, and reverting the priority lane to FIFO turned two mtp_44_ tests red. fmt, strict clippy, the full workspace suite with none failing, core purity, UX traceability at 266 active rules, architecture, accessibility semantics, input parity, motion and audit with only accepted RUSTSEC-2024-0436 all pass.
Fix T6-E9-hang: the first version of the MTP-44 tests called the blocking recv_prefer_priority directly. That proved the behaviour but failed in the wrong way — reverting the priority lane to FIFO drains the ordinary lane and then waits forever on an empty one, so the mutation run sat for thirteen minutes instead of reporting anything. A gate that hangs reports nothing, which for a rule whose whole purpose is to keep a second download path from drifting in unnoticed is the one failure mode that must not happen. The receive now runs behind a five-second deadline on a detached thread; detached rather than std::thread::scope, because scope joins before returning and would hang on exactly the stuck receive the deadline exists to catch. Confirmed in both directions: unmutated 3 passed in 0.00s, mutated 2 failed in 5.00s naming the cause (commit 17ae47a1). The general lesson is that "would this test go red if I deleted the feature" is not sufficient on its own — it also has to go red rather than silent.
Note T6-two-download-paths: E9's binding constraint is that preparation reuses the existing download manager with no second path. It is satisfied to the letter — no new path was added — but not in spirit, because a second path already existed before E9. reprise_core::podcasts::pipeline::download_episode is used by the refresh auto-download loop and by MCP's music_manage_episodes, while reprise-gnome's own try_download_episode_to_root serves the GUI's manual download with its own episode lookup, its own NET-1a source-kind gate and its own subscription lookup. Block H extracted the shared path for refresh and MCP but left the GTK worker on its copy. E9's preparation now sits on the GTK copy. Converging the two is a refactor with real risk (the GUI path's per-source-kind gating and progress emission must survive) and is deliberately NOT folded into E9; raised with the owner as its own decision rather than decided locally.
Lesson T6-selfkill-again: the pgrep self-match trap recorded as T6-selfkill was hit twice more in this session, from both directions — once as a watcher written `until ! pgrep -f "check-display-tests.sh --rule-named"` that could never terminate because its own command line matched, and once as `pkill -f "verify_e9.sh"` that killed the calling shell with exit 144. Reading the earlier entry was not enough to prevent either. The durable form of the rule: never pass a pattern to pgrep/pkill that appears in the invoking command line — wait on a captured pid with `kill -0`, and kill by pid. Also note that a pid captured by `pgrep | head -1` right after `setsid nohup` can be a short-lived child rather than the script, which made one watcher fire immediately against a gate that was still running.
Lesson T6-codex-stdin: a `codex exec` invocation appeared to run for one hour fifty-five minutes at zero percent CPU. It was not reviewing anything; it sat on "Reading additional input from stdin..." because stdin was left open. Non-interactive tool runs need `< /dev/null` (or an explicit piped prompt). Elapsed time alone is not evidence of work — check CPU and the output stream before assuming progress.
Review T6-codex-merge: an external reviewer (Codex CLI, read-only, no builds) audited 3559f0c8..70890178 — the dev merge plus the rule renumber — and returned eight findings. Six were real and are fixed in b441b400; two were already addressed by later commits the review could not see because it was pinned to the merge commit. Every finding was verified against the code before being acted on, and the two most serious were mine, introduced when the content phase was re-applied on top of dev's DeviceSyncMachine. (1) P1, the compile break: DeviceSnapshot gained a mandatory categories field but two reprise-mcp struct literals omit it, which `cargo check --workspace --all-targets` cannot see because the mpris feature is off by default — AGENTS.md's Block H entry calls for building both configurations and that had been dropped; the mpris build is now part of the battery. (2) P1, data loss: a failed content copy no longer stopped the phases after it, so a failure to copy a new episode could still be followed by a cap-eviction removal (MTP-39/MTP-25) deleting the older resident episode it was meant to replace — the device loses a file and gains nothing. The pre-machine implementation gated every step on failures.is_empty() and that guard was lost in the rewrite. (3) P1, cancellation swallowed: cancelling mid-phase only broke the inner loops and then returned the mirror's original Completed outcome, so finish_sync closed the run log as a success and cleared reconnect resumability. (4) P2, progress restarted at zero although the plan total already includes content bytes, so a mixed run could reach 100% and jump back. (5) P2, every Ok(_) from a removal counted as a deletion although the backend returns Ok(false) for an already-missing file, and content failures never reached the run log at all. (6) P3, a dangling NET-3c cross-reference and two helpers left public beyond their file. All three P1s had passed the entire gate battery: fmt, clippy, the full workspace suite, five script gates and a 196-of-196 display gate. This is the second external review on this branch to find defects the gates let through, and the pattern is worth stating plainly: gates catch what they were written to catch.
Task T6-E9-regression (MTP-23): complete (commit 0f02836d, merged as 1b509ff0, built by a subagent in its own worktree). Two regression tests for the P1 content-phase defects: a failed content copy must stop the later removal phases, and cancelling mid-phase must not be recorded as a completed run. The FakeBackend gained with_replace_track_error in the same style as the existing with_*_error builders so a content copy can be made to fail on demand, and the cancellation test uses a one-second bounded poll rather than an unbounded await, per the lesson in 17ae47a1. Verified independently after the merge by mutating this branch's own implementation: removing the may_continue guards fails exactly the removal test, removing the Cancelled return fails exactly the cancellation test, neither mutation disturbs the other. fmt, strict clippy, the full workspace suite, core purity, 266 active rules, four script gates, the mpris configuration and audit all pass.
Note T6-agent-briefing: the regression-test agent was told its two target defects were already fixed on its base branch. They were not — the fixes were sitting uncommitted in the coordinating session's working tree, so the agent's base legitimately still contained both bugs. It checked the claim against git history rather than trusting the brief, reported the contradiction, and implemented the fixes itself to keep the suite green. That is the right behaviour and cost only duplicated work, which was reconciled by keeping the coordinator's superset and taking the agent's tests. The lesson is for the coordinator: commit before briefing an agent about the state of the branch, because "it is on the branch" is a claim about git, not about a working tree.
Task E9 (design 7f's visible two-phase sync surface, MTP-43): complete (this commit, base 17ae47a1, builds on MTP-42's `preparation::plan_preparation`/`primary_action` and MTP-44's priority lane without re-deriving either decision). Core: `DeviceSettings` gained `prepare_before_sync` (migration v46, `SUPPORTED_SCHEMA_VERSION` bumped to 46, backfilled to `true` for existing rows) — the stored value is only ever what the user chose; offline/metered overrides stay `plan_preparation`'s job, never a mutation of this field. GTK: new `device_sync_preparation.rs` gathers facts in `recompute_delta_silent` (the missing-episode list, real `gio::NetworkMonitor` connectivity/metered, `online_sources::is_enabled`) and orchestrates the download-then-sync run through a new `PreparationDownloader` seam — production adapts `PodcastsRuntime` onto MTP-44's `High` priority lane (the one `PodcastsOperation::Download`, no second path), tests substitute a recording fake so the switch's actual dispatch behaviour is provable without a real worker thread or a channel a regression could hang on. `sync_now` now branches on `primary_action` before either starting a preparation run (`begin_prepared_sync`) or going straight to the renamed `start_transfer_now`; cancelling flips a flag the download loop checks between files, so an already-requested download is never interrupted or rolled back and `wanted_on_device` is never cleared anywhere in this path (verified by grep, not just by argument). The preparation overview (episode titles + "N files to download · SIZE", hidden outright for `Absent`/`NothingMissing`), the switch beside `remove_deleted`/`sync_automatically`, the primary button's "Download & sync" label, the "Step 1 of 2"/"Step 2 of 2" two-phase progress reading, and the `SkippedOffline` "N episodes skipped" note are wired into the page/layout/content-panel/strings modules. Known gap, flagged rather than papered over: no feed or provider in this codebase persists an expected byte size for an undownloaded episode (RSS enclosure `length` and YouTube size are both parsed nowhere), so `MissingFile::size_bytes` is honestly `0` and the overview omits the byte figure instead of showing "0 B" — the file count is still real. Four files were split to stay under the 800-line gate (`device_sync_page_copy.rs` and `device_sync_page_display_tests.rs` out of the page/its tests, `device_sync_effects.rs` out of `device_sync_planned.rs`), plus one line trimmed from `window.rs` to stay under its separate 600-line composition-root cap. Added MTP-43 `[active][gtk]` to docs/ux-rules.md between MTP-42 and MTP-44. format, strict clippy (-D warnings), the full unfiltered workspace suite (reprise-core 1692 passed incl. new `prepare_before_sync`/migration/`preparation_overview` tests, reprise-gnome 1121 passed incl. the two behavioral `mtp_43_` tests in `device_sync_preparation_tests.rs` + 334 display-only ignores, every other crate green), core purity, UX traceability at 267 active rules, architecture, accessibility semantics, input parity, and audit with only accepted RUSTSEC-2024-0436 all passed. Housekeeping: this worktree's own branch had also been provisioned from a stale, unrelated base; reset in place to feature/podcast-channel-redesign's tip (17ae47a1) before starting.
Task T6-E9-C (7f, the visible two-phase sync surface): complete (commit b0f33d6a, merged as fbafe7d4, built by a subagent in its own worktree, verified here rather than taken on report). MTP-43 gives the device page the preparation overview with episode titles, the "Download missing files before syncing" switch (DeviceSettings::prepare_before_sync, schema v46, stored default true), the primary button reading "Download & sync" exactly when MTP-42's projection says so, the two-phase "Step 1 of 2 · Downloading N of M · P%" reading, and the SkippedOffline note. Preparation dispatches through a PreparationDownloader seam that production adapts onto MTP-44's High priority lane, so there is still one download operation and one executor. Cancelling flips a flag checked between files and never touches an in-flight or completed download, and never clears wanted_on_device. Verified by mutation: making plan_preparation ignore prepare_switch_on fails exactly mtp_43_the_switch_actually_gates_whether_a_preparation_download_starts and nothing else — the switch is read, not merely stored, which is the third time this branch has had to prove that explicitly. fmt, strict clippy, the full workspace suite, core purity, 267 active rules, five script gates, the mpris configuration and audit all pass. E9 is now complete across MTP-42, MTP-43 and MTP-44.
Note T6-E9-C-open: two limitations the agent flagged rather than papered over, both real and both deliberately left. (1) Nothing in this codebase knows an episode's size before it is downloaded — no RSS enclosure length is parsed and no YouTube size is fetched — so MissingFile::size_bytes is honestly 0 and the overview reads "N files to download", appending a size only when a real one is known. Design 7f's example line says "2 files to download · 312 MiB"; a fabricated number would have matched the mockup and lied, so the number is omitted instead. Real size estimation needs RSS enclosure parsing plus a schema column and is its own task. (2) Connectivity and metered are read directly from gio::NetworkMonitor in device_sync_compact, following the existing podcast_refresh_scheduler pattern. That contradicts NET-3a, which states connectivity is an explicitly set value and never an inference, and which RadioView already honours through set_connectivity. The behavioural test carries a sanity assertion so a machine without network fails loudly instead of passing for the wrong reason, but the inconsistency stands and should be closed by making connectivity injectable here too.
Incident T6-parallel-field: merging E9-C exposed a break neither subagent could have seen. E9-C added a field to DeviceSettings; the regression-test agent, working in parallel from an earlier base, constructs that struct. Each was green on its own base and the pair does not compile. Worse, the first verification run after the merge printed no test result at all for the step that was supposed to prove the switch behaves — compilation had already failed — and a reader skimming for a FAILED line would have called it green. That is the same shape as the earlier grep-for-FAILED mistake: the absence of an error message is not evidence of success. The coordinator running the full battery after combining parallel work is what caught it, and the check for "did the step actually produce output" belongs in that habit.
Task T6-D4 (6b, download errors classified and sanitized, closes #106): complete (this commit, base 89e3e7e6). Added `PodcastError::classify` in `reprise-core::podcasts` — the single fixed-string classifier (timed out / could not be reached / HTTP error / invalid data / not modified / yt-dlp / disabled) that already existed as MCP's private `source_actions::podcast_source_error` match; that function now delegates to it instead of keeping a second copy. `pipeline::download_episode`'s failure branch and its `tracing::warn!` log line both switched from `error.to_string()`/`%error` to the classified reason, closing the actual leak: a `PodcastError::Transport`/`Body`/`YtDlp` payload can echo the raw provider transport error (which can carry the request URL and, per SRC-5, a private per-subscriber token) or, for yt-dlp's `finalize_download`, a local download path via `Path::display()`. Found and fixed a second, undocumented copy of the whole download executor: `reprise-gnome`'s `podcasts_worker.rs::try_download_episode_to_root` — the code the "Download episode" button and MTP-44's high-priority preparation lane actually call — reimplements `download_episode` rather than calling it, so it had the identical unsanitized `error.to_string()` leak independently; classified it with the same `PodcastError::classify()` rather than merging the two executors (out of scope here, flagged instead — see below). One pre-existing test (`pod_7_failed_worker_download_emits_failed_and_removes_partial`) was asserting the *raw* leaked string as correct behaviour and had to be corrected to the classified one. GTK: `podcasts_groups.rs`'s Failed row used to hide its reason behind `set_tooltip_text` only — a pointer-only affordance this repo's accessibility/input-parity policy forbids — now the classified reason is a second, always-visible label next to the fixed "Download failed" heading; the retry action gets a distinct icon/tooltip (`view-refresh-symbolic` / "Retry download") instead of looking like a first-time download. Retry and fresh-provider-output were already correct (a Failed episode never has `downloaded_path` set, so `toggle_download` re-enters the plain download branch and reruns `queued`→`downloading`→`downloaded` with a real fetcher call, no caching), as was `.part`/postprocessor-leftover cleanup (`download_atomically` and `ytdlp_download::cleanup_artifacts` already swept them on failure) — verified with a new failure-path test for the yt-dlp leftover case, which had no prior coverage. Added `POD-13` `[active][core][gtk]` to `docs/ux-rules.md` after `POD-12`. Split `ytdlp.rs`'s inline test module into `ytdlp_tests.rs` (its own new test pushed the file to 819 lines) to stay under the 800-line architecture gate. New `pod_13_*` tests include a redaction test that feeds a signed URL with a query string, a credential and an absolute local path through both the GUI worker and the shared pipeline and asserts none of it survives into `DownloadState::Failed` or a captured `tracing` log line (checked by temporarily reverting the fix — confirmed it fails with the leaking substrings, then restored). format, strict clippy (`-D warnings`), the full unfiltered workspace suite (reprise-core 1696, reprise-gnome 1135 + 336 display-only ignores, reprise-mcp 121, every other crate green — all 0 failed), core purity, UX traceability at 268 active rules, architecture, accessibility semantics, input parity, the mpris MCP check and audit (only accepted RUSTSEC-2024-0436) all passed.
Note T6-D4-open: `podcasts_worker.rs`'s manual-download executor duplicating `pipeline::download_episode` (not merely sharing its message classifier) predates this task and was not unified here — doing so safely would mean reconciling several existing rule-named tests (`net_1a_download_is_blocked_when_its_source_kind_is_disabled`, `pod_7_download_worker_emits_ordered_monotone_states_and_persists_after_publish`, the `MTP-44` priority tests) against `pipeline::download_episode`'s slightly different contract (it does not itself gate `NET-1a`, its `EpisodeNotFound` path emits no progress states at all). Flagged rather than silently worked around; a genuine merge is a separate, riskier task. Both copies now share the same classifier, so they cannot drift on redaction even though they remain two executors.
Task T6-followups (B2, B3, D2, D3, D4, G2, MTP-36, download convergence): complete (merged into dev as separate squashes 4565720f, e45e997f, df2d8d1f, afe27632, 138992f8, a3bcc7a0, 699ff39e; built by seven subagents in their own worktrees and verified here by mutation rather than by their reports). Each is described in its own pull request; what belongs in the ledger is what the work uncovered rather than what it added. Three of the seven found a live defect nobody had gone looking for. MTP-36: the selection did `take(latest)`, so a channel configured with 0 silently stopped syncing instead of syncing without a limit — the owner's zero decision that morning had been treated as one line of rule text and turned out to describe a real bug. POD-13/D4: a failed download's raw provider message reached both `DownloadState::Failed` and the warn log, and a podcast `audio_url` can carry a private per-subscriber token (SRC-5); the visible reason was also tooltip-only, so it was unreachable by keyboard and screen reader. B2: the podcasts filter row lived inside the swappable stack page, so switching to the no-results state hid the one control a user needs to undo the filter that emptied the list. None of the three was part of its task; each surfaced because the brief required proving the behaviour rather than asserting it — the zero case, the leak case, the visibility difference.
Note T6-convergence-gate: converging the two download executors onto `pipeline::download_episode` revealed that the core function had no NET-1a gate at all. Its two existing callers were each gated before calling in — refresh only reaches it after a same-kind fetch already passed, and MCP checks itself — so the privacy gate was a property of the callers, not of the download. A naive convergence would have deleted the GNOME copy that held the check and left the core path ungated, while the existing `net_1a_` test went green because it covered the deleted code. The gate now lives inside `download_episode` at the same position in the state sequence, so a refusal still emits Queued → Downloading{0,None} → Failed. Proven by forcing the gate open and watching the rule-named test run into an `unreachable!()` double.
Incident T6-my-battery: a private gate battery was used all day instead of `scripts/check-merge-readiness.sh`, the script CI itself runs through `ci-quality.sh`. It had five of that script's checks. `check-frontend-thinness.sh` was among the missing ones, the turn 6/7 merge pushed reprise-gnome's rusqlite budget from 515 to 563, and dev failed that gate from the merge until the owner repaired it in 323c5828. Every "all gates green" claim made before that repair was unsupported for four checks. The lesson is not "remember the gate" but "do not rebuild a gate the repository already defines" — the approximation is what made the omission invisible. Note also that `check-merge-readiness.sh` currently refuses to start for every branch, because `main` is not an ancestor of `dev`; the one-time `-s ours` repair is documented in docs/agents/branching.md and reserved for the owner, so its checks are being run individually until then.
Lesson T6-silent-verification: four times in one session a verification step produced no output and was nearly read as success — a mutation test whose build failed to compile, a `--workspace` test filter that matched nothing, a `tail` that truncated the failing step out of a battery's output, and a gate summary that said HAS FAILURES while ten visible lines said passed. Each time the step had not run at all. The rule that survives: a check that printed nothing did not happen, and the absence of an error message is not evidence. Read the numbers, not the silence — this is the same shape as the earlier grep-for-FAILED mistake and it recurs under time pressure.
Lesson T6-agent-seams: seven subagents working in parallel from a base that moved underneath them produced no defect inside any one contribution, and four at the seams between them: two agents claimed POD-13 for different rules, two independently created the same test file to satisfy the file-size gate, one sanitized a download executor another had already deleted, and one added a struct field another's fixture then failed to construct. All four were invisible to the agents (each was green on its own base) and to their gates, and were caught only by the coordinator running the full battery after combining the work. Parallel agents need disjoint rule ids assigned up front, and the integrator must treat every combination as unverified until it is verified as a whole.
Lesson T6-worktree-base: every agent worktree on this repository is provisioned from `main`, not from the current branch, so each of the seven began with `git reset --hard origin/dev`. That is not incidental — `main` carries a promotion merge commit dev has never seen, which is the same topology fact that stops `check-merge-readiness.sh` from starting. Until the trunk repair lands, every agent brief must say so in its first paragraph rather than as an aside.
Review T6-codex-followups: a second external review (Codex CLI, read-only) audited 3559f0c8..dev after the seven follow-up blocks landed, and was pointed at the seams between parallel contributions rather than at the contributions themselves — because in the first review two of three P1s had come from a conflict resolution, not from an author. It returned two P1 and six P2 findings, every one real, none of which any gate had caught. P1: "keep last N downloaded" ranked all episodes of a subscription and applied the downloaded filter afterwards, so undownloaded episodes consumed rank positions — a show with twenty episodes where the user had downloaded the three oldest gave those ranks 18 to 20 and keep-5 deleted all three, silently, with no error and the files simply gone. It predates the per-channel work, which made it worse by letting the user choose an N that still meant something else. P1: MTP-41 claimed YouTube limits each channel to its latest N "regardless of download state" while the code deliberately admitted only downloaded or explicitly wanted episodes; both positions were defensible so it was raised rather than decided locally, and the owner ruled the code right and the rule wrong (recorded as E-10). P2: a local path in a normal-level log line, raw yt-dlp stderr rendered in the add dialog's preview, and "unlimited" displayed to the user as "latest 0 each" — the device page reporting nothing selected while everything was. P2: two rule-named tests that would not have noticed their own feature being deleted. All fixed; the destructive one verified twice, once by its author and once here by putting the bug back.
Lesson T6-unwatched-tests: the two weak rule-named tests the second review found were both written the same day, under briefs that explicitly demanded a test go red if its behaviour were removed. POD-13's retry test asserted the button's icon and tooltip and never activated it; POD-9's tests covered the computation but never that it reached the surface. The requirement was stated, accepted, and not met, and nobody noticed because nobody asked for the evidence. The durable form of the rule is therefore not "require the test to fail without its feature" but "require the red output to be shown" — an unwatched test is indistinguishable from a test that does nothing, and stating the requirement more firmly does not close that gap. Every rule-named test on this branch is now expected to arrive with its actual failure message from the sabotaged run.
Note T6-final-state: dev carries the complete turn 6/7 plan (blocks A through H, E1 through E9), every follow-up (B2, B3, D2, D3, D4, F2, F4, G2, MTP-36), the download-executor convergence, and both review rounds. Issue #106's fix is on dev (POD-13) but the issue is still open: the `closes #106` keyword only fires when a branch merges into the default branch, which here is main, so nothing on dev can close it and it must be closed by hand or by the trunk repair. Two gaps are deliberate and recorded rather than hidden: neither keep_downloaded nor latest_per_channel has a UI, so both are settable only through the store, and NET-3 point 2's dimmed "Needs network" rows are unwired, with the rule text saying so. The location hoist is likewise half done — storage is app-level, but its settings rows still live on the Concerts preferences page, so a radio user is deep-linked into Concerts to set it. Verification throughout was local only: GitHub Actions could not run for any of it (an account billing condition), and scripts/check-merge-readiness.sh refuses to start for every branch until the one-time `-s ours` trunk repair documented in docs/agents/branching.md, so its fifteen checks were run individually before each merge. The rule-named display gate ends at 214 of 214. No manual acceptance has happened yet; MTP-30 in particular means a connected phone with pending work begins transferring unprompted.

# Runtime client migration preparation — R8

Task R8: complete (commits 4c29059ffa, 3808628077 and 18b35362ef, base 2319b19f53, added bounded revision-stamped pages for both queue sections and an absolute seek command across protocol 4.2, runtime, client and Linux service; whole-queue change detection now invalidates positions beyond the 200-row snapshot; the adversarial review fixed zero-duration episodes being collapsed to the start, pinned the distinct SeekTo wire method and target, corrected the protocol wording and repaired two inherited dev gate drifts; the two unknown-duration regressions and the wire contract were each mutation-proven red then green; format, strict workspace clippy, warning-free workspace rustdoc, the complete workspace suite with only the two sandbox-blocked MCP Radio listener tests excluded, architecture, runtime-service packaging, frontend thinness, UX traceability, motion, accessibility, input parity, QA linters, file-size enforcement and cached audit with only accepted RUSTSEC-2024-0436 passed)
Assumption R8: a non-live item with `duration_ms == 0` has an unknown upper bound, so the runtime preserves the non-negative seek target and lets the backend, which knows the actual stream length, accept or refuse it; live streams remain structurally non-seekable.
Manual R8: the real private-D-Bus queue-page and absolute-seek integration tests could not start because this sandbox rejects Unix-socket binding with `Operation not permitted`; the full merge-readiness run reached the two inherited MCP Radio fixtures and stopped at their same `TcpListener::bind` restriction before display and runtime-bus phases. R8 changes no GTK rendering or pointer behavior, so no CUA claim is made.
Review R8: complete (this commit, base 5cccd286f6, parallel standards/spec review found one P1: whole-queue identities were cloned before and after every 500 ms position report, defeating bounded paging for a large virtual queue; exhaustive PlayerEvent and PlaybackCommand classifications now take the full before-image only for operations that can change queue identity, so position, seek, volume and metadata hot paths stay O(1) in queue length while finish, handoff, error, play, stop, skip and shuffle still publish revision changes; both classifications were mutation-proven red then green; the same review renamed the misleading client wire body `Delta` to `Milliseconds`, unified the page/snapshot limit on `QUEUE_WINDOW`, and caught a stale doc block before commit; format, strict workspace clippy, warning-free rustdoc, the complete workspace suite excluding only the two independently reproduced sandbox-blocked MCP Radio listener tests, architecture, runtime-service packaging, frontend thinness, UX traceability, motion, accessibility, input parity, QA linters, GitHub-flow policy and cached audit with only accepted RUSTSEC-2024-0436 passed)
Deferred R8-review: the standards reviewer noted a non-blocking data-clump/primitive-string smell in the queue-page request carried through the client and D-Bus layers; introducing a new request DTO solely for this already typed three-field wire call would expand the reviewed protocol surface without changing behavior, so it stays deferred rather than being folded into the merge fix.

Verification R8-merge: the isolated private-D-Bus runtime-service suite was rerun after the combined gate and passed 25/25, including queue paging and absolute seek; the rule-named, motion and CSS display scripts remain unverified in this sandbox because their per-test private D-Bus daemons are denied Unix-socket binding with `Operation not permitted`.

Portrait placeholder fingerprint waves 2 and 3 are complete on `feature/portrait-placeholder-fingerprint`.

- The shipped threshold is `0.0025`.
- The Rust corpus gate rejects all 18 placeholders and no photographs, with margins of `10.200x` and `23.707x`.
- Rejected downloads preserve and refresh an existing cached portrait; otherwise they create one negative marker.
- Rejection and near-threshold acceptance both emit the specified WARN event.
- Oceano performs one download, rejects the known silhouette, and does not fall back to the pictured namesake.
- The acceptance oracle expects initials at rank 10 and verifies a negative marker with no cached Oceano image.
- Format, strict workspace Clippy, the isolated workspace suite, architecture, Core purity, harness self-tests, file-size checks, and live audit passed.
- After a conflict-free rebase onto `origin/dev` at `a7febd7d92`, the clean-tree merge-readiness wrapper passed again, including all 672 isolated display tests and all 25 private-session-bus runtime tests.

The exact live acceptance remains unrun because it requires an explicitly supplied real source database. Nothing was pushed and no real Reprise data or live desktop session was touched.

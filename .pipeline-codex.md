Implemented all three blocks as separate commits, preserving the original plan commit:

- `78cc9d3c2a` — `fix(android): keep album covers out of artist surfaces`
- `c1a0d21b24` — `feat(android): expose artists missing portraits`
- `c2612bb535` — `feat(android): prefetch artist portraits`

TDD red evidence:

- PR 1: 7 of 16 focused tests failed under the former album-cover behavior.
- PR 2.1: compilation failed with missing freshness API (`E0425`).
- PR 2.2: all four FFI tests failed with missing method errors (`E0599`).
- PR 3.1: five missing `ArtistPortraitPrefetch` errors plus the missing port method.
- PR 3.2: all three tests failed on the missing prefetch lifecycle parameter.

Verification:

- Android: **71 suites, 368 tests, 0 failures, 0 errors, 0 skips**, fresh XML verdict.
- Isolated Rust: FFI **170 passed**; Core **2531 passed, 3 ignored**.
- `cargo fmt`, Clippy, full workspace tests, Core purity, file-size checks: passed.
- `cargo audit --no-fetch`: passed with only accepted `RUSTSEC-2024-0436`.
- The four protected artist tests remained green.
- Working tree is clean; no push performed.

The literal Rust command hit **27 Core failures** because the sandbox’s default XDG cache is read-only; FFI still passed 170/170. With isolated writable XDG paths, all Core tests passed. The literal Android command stopped because the shell exposes no Android SDK variable; with `ANDROID_HOME`/`ANDROID_SDK_ROOT` and writable Gradle paths, the complete suite passed as reported above.

Task 3.3 remains open and visually unverified, as requested: no device or emulator was used. No plan line references proved stale, and `phase: planned` was left unchanged.
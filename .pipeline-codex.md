# Concerts artist portrait column — second refactor pass

Implemented R1 only. In
`crates/reprise-gnome/src/ui/concerts/concerts_artist_cover.rs`:

- Removed `a_concert_cell_has_one_cache_first_portrait_trigger`. It asserted
  source formatting rather than callback behavior. There is no honest
  ordinary-suite seam for the GTK map/bind lifecycle and runtime queue count;
  that exact invariant is now explicitly unprotected at this test level.
- Removed `portrait_fetch_is_private_to_a_mapped_cache_miss`. Rust privacy
  keeps `fetch_after_cache_miss` inside this module, but it does not prove the
  exact mapped-cache-miss call path. Covering that behavior without a display
  would require a broader runtime/GTK injection refactor outside R1, so no
  replacement test was added.
- Kept `production_portrait_resolver_is_pinned_to_the_cache_only_api` and its
  `std::ptr::fn_addr_eq` comparison. The typed
  `PRODUCTION_CACHE_ONLY_RESOLVER` constant plus that identity assertion holds
  the production cache-only resolver invariant; only the redundant source
  substring scan was removed.

No production behavior changed. No test reads the real XDG cache or reaches
Deezer; GNOME and workspace suites used fresh isolated XDG data/cache roots.

## Requested verification

- `cargo test -p reprise-view`: exit 0; 117 passed, 0 failed, 0 ignored. Both
  Concert layout tests passed, including the pre-cover stored-layout proof.
- `cargo test -p reprise-gnome` with isolated XDG roots: exit 0; 1,923 passed,
  0 failed, 762 ignored; GNOME conformance 10 passed. The run emitted one
  `GdkPixbuf-CRITICAL` width assertion but no test failure.
- `cargo clippy --all-targets -- -D warnings`: exit 0; finished without
  diagnostics.
- `scripts/check-ux-traceability.sh`: exit 0; 400 active rules covered.

## Repository pre-commit gates

- `cargo fmt --check` and `git diff --check`: exit 0.
- `cargo clippy --all-targets --workspace -- -D warnings`: exit 0.
- `cargo test --workspace` with isolated XDG roots: exit 0; all ordinary
  workspace suites passed, including Core 2,528 passed with 3 ignored and
  GNOME 1,923 passed with 762 ignored.
- `cargo audit`: exit 0; scanned 482 dependencies and reported only the
  accepted `RUSTSEC-2024-0436` warning for `paste 1.0.15`.

Per instruction, no ignored display test, app launch, visual check, push, PR,
or CI action was performed.

Implemented and committed as `653c21a988` (`fix(android): start queue landing tint at settle`).

- Tint animation now lives in [QueueReorder.kt](/home/marvin/Projects/reprise-queue-visual-queue-landing-flash-follows-the-drop/android/app/src/main/java/de/reprise/spike/QueueReorder.kt:209) and starts at settle completion before `move`.
- `release()` no longer handles tint state.
- `queueFlashSlot` performs the offset-aware handover.
- Row-local animation and all four obsolete flash members are removed.
- Four predicate cases are covered in [QueueFlashSlotTest.kt](/home/marvin/Projects/reprise-queue-visual-queue-landing-flash-follows-the-drop/android/app/src/test/java/de/reprise/spike/QueueFlashSlotTest.kt:7).
- Protected constants, reload predicate, row key, and stationary-drop behavior remain unchanged.

Verification:

- Android suite: 85 suites, 485 tests, zero failures/errors/skips; debug APK assembled.
- Android theme lint passed.
- Format, strict workspace Clippy, full workspace tests, documentation, source-quality, architecture, accessibility, packaging, and audit gates passed. Audit reported only accepted `RUSTSEC-2024-0436`.
- The normal merge-readiness fetch was unavailable because sandbox SSH configuration was unreadable. Its `--no-fetch` run passed through isolated workspace and Linux tests, but the unrelated GTK rule-owned display phase stalled without output and was terminated.
- No device measurement was performed or claimed.
- Worktree is clean; nothing was pushed.

The only scope extension was the required progress-ledger entry.

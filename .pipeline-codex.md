# Showroom review follow-up report

## Existing plan work

The four planned tasks and their review fixes were already committed and were not reimplemented:

- Task 1, tile hover: `59805d8597`.
- Task 2, CH.02 pipeline: `339e5038aa`.
- Task 3, header hire link and Hero offer row: `6206b7468e`.
- Task 4, mobile ledger: `09583158b7`.
- Accepted implementation review fixes: `0f97c958ac`.
- Stable verdict-node width follow-up: `da751c5e5d`.

## Follow-up change

- Changed only the `short` labels for the Boundaries and Distribution gate groups to `Bounds` and `Install`; group names, explanatory lines, and gate names are unchanged.
- Changed `.gate-cluster` from a 74px flex basis and 68px minimum to a 60px basis and 60px minimum so all six groups fit within the measured 483px desktop gate node.
- Preserved `.pipeline__node--verdict { min-width: 169px; }`, its mobile `min-width: 0` reset, `flex: 1 1 0` on each gate mark, and the 44px touch target below 46rem.

## Tests changed

- `showroom/tests/gate-derivation.test.mjs`: added an exact production assertion for all six derived short labels. The existing generic derivation and conflict assertions remain unchanged.
- `showroom/tests/chapter-two.test.mjs`: added assertions pinning the 60px cluster flex basis and minimum width. Existing `show-6` through `show-21` assertions remain in place, including the 169px verdict-width contract and mobile behavior checks.

The label assertion was observed red with actual values `Boundaries` and `Distribution` before the source change, then green. The cluster assertion was observed red with `flex: 1 1 74px` and `min-width: 68px` before the CSS change, then green.

## Verification commands

Run from `showroom/`:

- `node --test tests/gate-derivation.test.mjs` — expected red: 1/2 passed; the exact production-label assertion reported `Boundaries` and `Distribution` instead of `Bounds` and `Install`.
- `node --test tests/gate-derivation.test.mjs` — green: 2/2 passed.
- `node --test tests/chapter-two.test.mjs` — expected red: 6/7 passed; `show-21` reported the old 74px flex basis.
- `node --test tests/chapter-two.test.mjs` — green: 7/7 passed.
- `npm run lint` — passed; Biome checked 95 files and applied no fixes.
- `npm run typecheck` — passed; `tsc --noEmit` reported no errors.
- `npm run test` — passed; Vite built the client and SSR bundles, prerendered 54,858 bytes, and all 89 tests passed.

No network access was needed by npm, and no network failure occurred. The requested rendered checks at 1280px, 1440px, and 1920px remain for the user's real-browser acceptance pass.

## Plan and scope findings

- The committed plan's original 74px/68px cluster sizing passed its 1280px acceptance target, but did not anticipate the wider-viewport wrap introduced when the later verdict-width fix permanently reserved 169px. The follow-up contract is correct.
- The follow-up note said `gate-derivation.test.mjs` already pinned the six production short labels. It only checked a synthetic short label, so the exact six-label assertion was added rather than merely updated.
- No dependency, design token, Rust file, plan file, or `phase:` field changed. `.pipeline-findings.md` remains untouched and untracked. No push was performed.

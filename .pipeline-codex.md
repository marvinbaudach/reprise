Implemented and committed R9 as `edf0c14c47` (`fix(releases): scope undo memory reconciliation`).

- Added full and ID-scoped loaders in [deleted_releases.rs](/home/marvin/Projects/reprise-releases-deleted-memory/crates/reprise-core/src/deleted_releases.rs:47), sharing the unchanged `holds()` predicate.
- Both loaders filter `removed_at IS NULL` in SQL; `missing_since` remains intentionally unrestricted.
- Extended the undo regression to assert exactly one row read. It failed at 2 before the fix and passes at 1 afterward.
- Standalone Core: 2,347 passed, 2 ignored.
- Workspace Core: 2,348 passed, 2 ignored.
- Clippy, formatting, gettext, Core purity, and audit passed. Audit reports only accepted RUSTSEC-2024-0436.
- Architecture and UX checks added no findings; they retain only the two unrelated oversized files and NR-20/NR-25 respectively.
- Full workspace retained the unchanged GNOME baseline: 1,713 passed, 1 failed, 600 ignored.
- No display-backed tests were run; the NR-32 display fixture was untouched.
- Edited code files are 480 and 186 lines.
- Existing pipeline and findings files remain untouched and uncommitted.
# Strand A report - the UI follows the service

## Completed tasks

- A-1 `e5d96e35ef` - the playback service publishes playback, settings, and
  sleep-timer state through `StateFlow`.
- A-2 `f3affce979` - the activity collects those flows while `STARTED` and uses
  one `boundService` source for state and commands.
- A-3 `098f2b4c6c` - refused and silent binds are logged, and a silent accepted
  bind is retried once after 2 seconds.
- A-4 `790653bb81` - a transient `isPlaying == false` no longer kills a ticker
  whose last published state still carries play intent.
- A-5 `f3cee26201` - slider cancellation releases scrub ownership without
  seeking; a held finger keeps ownership.
- A-6 - recreation now has a regression test that publishes a newer snapshot
  while the old activity is stopped and requires the recreated activity to
  reconcile it.

## Red runs

All Gradle runs used Java 21. Compile-red runs are called out separately from
behavioural reds; no compile-red is presented as a runtime assertion.

### A-1

Both channel tests were written against the intended API before it existed.
`a1-red-2.log`:

```text
PlaybackSnapshotChannelTest.kt:35:40 Unresolved reference 'playbackSnapshots'.
PlaybackSnapshotChannelTest.kt:44:40 Unresolved reference 'playbackSnapshots'.
> Task :app:compileDebugUnitTestKotlin FAILED
BUILD FAILED in 16s
```

This covers `aSnapshotPublishedWithNobodyListeningIsStillReadableAfterwards`
and `aLateCollectorReceivesTheStateThatWasPublishedBeforeItArrived`.

### A-2

The four-test file was compile-red against the intended activity state seam.
`a2-red.log`:

```text
MainActivityPlaybackChannelTest.kt:49:30 Unresolved reference 'currentPlaybackState'.
MainActivityPlaybackChannelTest.kt:67:38 Unresolved reference 'currentPlaybackState'.
MainActivityPlaybackChannelTest.kt:100:30 Unresolved reference 'currentPlaybackState'.
> Task :app:compileDebugUnitTestKotlin FAILED
BUILD FAILED in 11s
```

After adding only that read seam, the legacy callback implementation produced
the behavioural red that pins the disconnect contract. `a2-red-runtime.log`:

```text
MainActivityPlaybackChannelTest > losingTheServiceDoesNotClaimNothingIsPlaying FAILED
    java.lang.AssertionError at MainActivityPlaybackChannelTest.kt:101
4 tests completed, 1 failed
BUILD FAILED in 16s
```

The normal Robolectric bind callback made the three symptom tests green under
the legacy callbacks once the test-only read seam existed; this is not claimed
as behavioural-red evidence. Their initial compile-red still preceded the
implementation, and the final focused gate exercises all three against the new
flow channel.

### A-3

Both watchdog tests were compile-red against names that did not exist.
`a3-red.log`:

```text
MainActivityPlaybackChannelTest.kt:121:13 Unresolved reference 'PLAYBACK_BIND_WATCHDOG_MS'.
MainActivityPlaybackChannelTest.kt:176:60 Unresolved reference 'PLAYBACK_BIND_FAILURE_LOG'.
> Task :app:compileDebugUnitTestKotlin FAILED
BUILD FAILED in 8s
```

This covers `aBindThatNeverConnectsIsRetriedAndLogged` and
`aRefusedBindIsRecorded`.

The distinct greppable phrase is:

```text
Playback service bind did not connect
```

### A-4

The fake player's Java proxy first exposed an unrelated fixture typo; that run
was discarded. After correcting the fixture, restoring the old non-rescheduling
ticker produced the sensitivity red in `a4-red-validated.log`:

```text
Media3PlaybackPortTest > aMomentaryNotPlayingDoesNotStopPositionEventsForGood FAILED
    java.lang.AssertionError at Media3PlaybackPortTest.kt:50
1 test completed, 1 failed
BUILD FAILED in 12s
```

The assertion message was `position events must resume without another
listener callback: [1000]`. `aPausedPlayerProducesNoPositionEvents` is the
plan's guard and stayed green before and after the implementation.

### A-5

The intended interaction-source seam was compile-red first.
`a5-red-intended-api.log`:

```text
NowPlayingGesturesTest.kt:271:21 No parameter with name 'interactionSource' found.
> Task :app:compileDebugUnitTestKotlin FAILED
BUILD FAILED in 9s
```

With only that seam and no cancel collector, the behavioural red was:

```text
NowPlayingGesturesTest > aCancelledSeekGestureReturnsTheHeadToThePlaybackPosition FAILED
    java.lang.AssertionError at NowPlayingGesturesTest.kt:65
1 test completed, 1 failed
BUILD FAILED in 19s
```

`aStillFingerKeepsTheHead` is the explicitly required green guard: a timeout
implementation would make it fail.

### A-6

Because A-6 is a guard added after A-2, its correct first run was green. I
discarded that weak run, strengthened the test so track 7 is published while
the activity is stopped, and mutation-tested it by temporarily suppressing the
service flow's replay. `a6-red-validated.log` then produced:

```text
MainActivityPlaybackChannelTest > theUiStateSurvivesAnActivityRecreationWhilePlaying FAILED
    java.lang.AssertionError at MainActivityPlaybackChannelTest.kt:160
1 test completed, 1 failed
BUILD FAILED in 40s
```

The XML failure was `expected:<7> but was:<null>`. The temporary mutation was
restored before the green run and is absent from the diff.

## Verification

- Focused Android selection: 14 fresh XML suites, 74 tests, 0 failures, 0
  errors, 0 skipped.
- Full Android gate: `suites=73 tests=382 failures=0 errors=0 skips=0
  verdict=fresh`.
- Fresh A-6 XML: 1 test, 0 failures, 0 errors.
- The committed `scripts/verify-now-playing-scene.sh` aborts before Gradle with
  `missing test source: de.reprise.spike.scene.CoreShapeTest`. Its inventory is
  stale for four suites removed before this strand: `CoreShapeTest`,
  `NowPlayingBurstTest`, `NowPlayingBurstPixelsTest`, and
  `MainActivityVisualizerTest`. The script is outside strand ownership and was
  not changed. As a non-mutating fallback, its nine currently present suites
  were run explicitly: 9 fresh suites, 61 tests, 0 failures, 0 errors, 0
  skipped. The full Android gate also includes them.
- `cargo fmt --all` and `cargo fmt --all --check`: passed.
- `cargo clippy --all-targets --all-features -- -D warnings`: passed.
- Isolated `cargo test --workspace`: passed.
- Project Python, YAML, and Markdown source-quality gate: passed.
- Cached `cargo audit`: passed with only the accepted
  `RUSTSEC-2024-0436` warning for `paste`.
- `MainActivity.kt` is 790 lines; every substantially edited code file remains
  below 800 lines.
- No frame-rate or device measurement was run.

## Ownership and findings

No tracked production or test source outside the strand's ownership list was
changed. This report is the one explicitly requested non-source file. Test
setup regenerated ignored UniFFI bindings and ordinary ignored build/cache
artifacts under `target/`, `android/app/build/`, `.gradle-user-home/`, and
`android/.kotlin/`; none is in the commit diff. No plan file, scene file,
Android FFI source, or `android/app/build.gradle.kts` was edited.

Keeping the last playback state in `onServiceDisconnected` caused no unexpected
breakage: its focused behavioural test is green, the 74-test focused selection
is green, and the fresh 382-test Android gate is green. The residual risk is
the stale scene-verifier inventory described above, not a scene regression in
this strand.

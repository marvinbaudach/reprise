# Concerts artist portrait column

Implemented the settled plan on `feature/concerts-artist-portrait-column` and
rebased the seven task commits onto `origin/dev` at `6cba526b6d`. No push was
performed.

## Task commits

- T1 `14870fc419` — extended `LazyReleaseCover` with a reconstructible artist
  key, reusable started state, and paintable presentation while keeping the
  release callers and release-fetch path intact.
- T2 `5c22df50f0` — added the leading pinned `ConcertColumn::Cover`, its label
  and width mappings, and both layout regressions. The pre-cover stored-layout
  test passed, so no migration was added.
- T3 `0020417131` — added `ConcertsArtistImage` and the id-less, unsorted cover
  column factory. Cache resolution runs off the GTK main thread; network work
  goes only through `ArtistPortraitRuntime`; decoded portraits use the shared
  `CoverLoader` at `ThumbnailSize::Portrait`; all asynchronous boundaries
  recheck the artist key.
- T4 `2aa65d1945` — appended the cover column first in `append_columns`, retained
  `ConcertsView::new`, wired the shared loader and portrait runtime from the
  window, and made test views use `for_test(|_| None)` so they cannot read the
  real XDG portrait cache or contact Deezer.
- T5 `893ab1a468` — raised the shared Concerts/Releases artwork cell edge from
  40 to 56 pixels. The Updates popover remains at its independent 44 pixels.
- T6 `c1ab5d694b` — replaced CONC-17 with active CONC-17a and re-pointed the
  existing sortable-header test name. UX traceability reports all 400 active
  rules covered.
- T7 (this commit) — added the three named ignored display contracts for column
  identity, initials fallback, and recycled-cell stale-image clearing. The
  unchanged Concerts visual fixture moved to a sibling test file so every
  edited code file remains below 800 lines.

## Verification

Final post-rebase acceptance runs:

- `cargo test -p reprise-view`: exit 0; 117 passed, 0 failed, 0 ignored. Both T2
  layout tests passed, including the binding no-migration proof.
- `XDG_DATA_HOME=$(mktemp -d) XDG_CACHE_HOME=$(mktemp -d) cargo test -p reprise-gnome`:
  exit 0; unit suite 1,922 passed, 0 failed, 762 ignored; GNOME conformance 10
  passed, 0 failed.
- `cargo clippy --all-targets -- -D warnings`: exit 0; finished the dev profile
  without diagnostics.
- `cargo fmt --check`: exit 0.
- `scripts/check-ux-traceability.sh`: exit 0; 400 active rules covered.
- `cargo audit --no-fetch`: completed against 1,217 cached advisories and 482
  dependencies; only the explicitly accepted `RUSTSEC-2024-0436` warning for
  `paste 1.0.15` was reported. Live index locking was unavailable because the
  shared Cargo package-cache lock is outside this worktree.
- The complete isolated-XDG workspace suite and strict workspace-wide Clippy
  gate also passed before the final unrelated one-commit `origin/dev` advance;
  the three required package-level acceptance commands above were rerun after
  the clean rebase.
- `git diff --check`: exit 0. Final branch state: seven commits ahead of
  `origin/dev`, with no tracked worktree changes.

## Deferred review work

Per the run instructions, I did not execute the display stage or start the
application. Review still needs to run the three new CONC-17a ignored tests,
the two existing release-cover ignored tests, and the visual/NET-1 checks from
acceptance items 5 and 6. The ordinary GNOME suite compiled all three new
tests and reported them ignored. No other implementation item is unfinished.

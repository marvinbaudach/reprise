Docs hygiene is implemented on `feature/docs-hygiene`.

- Release archives omit `docs/plans/` and `docs/superpowers/plans/`, while retaining `docs/superpowers/specs/`.
- Eleven unreferenced `phase: shipped` plans were deleted with history intact.
- Five referenced shipped plans remain in place with a retention note: `motion-player.md`, `multi-frontend-core.md`, `ptr-e2e-harness-debt.md`, `queue-section-preseed.md`, and `ux-rules-motion.md`.
- Every surviving tracked document now uses `~` instead of the private home prefix.

Verification passed with archive counts `0 / 0 / 20` for plans, superpowers plans, and specs; zero private-home hits in tracked docs; exactly the five approved shipped plans retained; 367 active UX rules covered; formatting, strict all-target workspace Clippy, and the complete workspace suite green. Cached dependency audit found only accepted RUSTSEC-2024-0436; live refresh was unavailable because the sandbox cannot lock the read-only global advisory cache.

The following tracked files outside `docs/` still contain the private home prefix and were deliberately not changed:

- `.superpowers/sdd/progress.md`
- `AGENTS.md`
- `crates/reprise-core/src/diagnostics/tests.rs`
- `crates/reprise-core/src/library/library_doctor/remote/diagnostics.rs`
- `crates/reprise-mcp/tests/job_status.rs`
- `crates/reprise-mcp/tests/leak_matrix.rs`
- `crates/reprise-mcp/tests/resources.rs`
- `crates/reprise-mcp/tests/source_management.rs`
- `crates/reprise-platform-linux/src/player/tests.rs`
- `crates/reprise-runtime/src/jobs_tests.rs`
- `scripts/reprise-worktree-gc.sh`
- `scripts/tests/weekly-portfolio-sync.sh`
- `scripts/tests/worktree-gc-schedule.sh`
- `scripts/weekly-portfolio-sync.sh`

The supplied untracked `docs/plans/docs-hygiene.md` session plan remains untouched and uncommitted.

## Sidebar marks the visible view

- Task 1 control arm run: mapping `library-doctor` to `SidebarPlace::Source` produced `test result: FAILED. 0 passed; 1 failed`; the restored resolver produced `test result: ok. 1 passed`.
- Task 2 has no control arm by plan; none was run. Its structural state is exercised by Tasks 3 and 4.
- Task 3 control arm run: restoring `row.set_selectable(false)` produced `test result: FAILED. 0 passed; 1 failed` in the Doctor-row test.
- Task 3 control arm run: forcing the rebuild tail back to `SidebarPlace::Source` before marking produced `test result: FAILED. 0 passed; 1 failed` in the rebuild-preservation test.
- Task 3 fixture note: the first draft used a local preselected proposal, which correctly counted as a quiet fix and failed before reaching the marking assertion. It was replaced with a review-required MusicBrainz proposal; only the behavior-sensitive control-arm failures above count as red proofs.
- Task 4 mandatory control arm run: restoring `route_row`'s old `current_source`-only dedup made both Doctor-entry dead-click tests exit 101 with `test result: FAILED. 0 passed; 1 failed`; after restoring the `(current_place, current_source)` condition, both tests and the focus-resync test produced `test result: ok. 1 passed`.
- Task 4 focus-leave control arm was not run; its green regression test produced `test result: ok. 1 passed`. The first unisolated workspace run hit 27 expected read-only user-cache failures; the repeated run under fresh XDG data/cache/config/runtime roots passed completely.
- Task 5 mandatory coexistence control arm run: adding `device-card-current` to `DeviceCard::update`'s removal list made `nav_18_a_sync_progress_update_keeps_the_open_device_marked` exit 101 with `test result: FAILED. 0 passed; 1 failed`; the restored implementation produced `test result: ok. 1 passed` for all three device display tests.
- Task 5 control arms that remove the `Device`-branch marker and restore source-only dedup were not run. The first complete isolated suite exposed the stronger Current+Active hover selector through an existing substring-based contrast guard; ordering the combined hover rules after the base hover rules preserved CSS specificity and made that guard and the repeated complete suite green.
- Task 6 empty-row control arm run: falling back to source marking when the Doctor row is absent made `nav_18_the_vanishing_doctor_row_leaves_nothing_marked_instead_of_the_old_source` exit 101 with `test result: FAILED. 0 passed; 1 failed`; the restored empty marking produced `test result: ok. 1 passed`.
- Task 6 collapsed-overlay control arm run as the pre-fix red: with no collapsed branch, `nav_18_opening_the_doctor_closes_a_collapsed_sidebar_overlay` exited 101 with `test result: FAILED. 0 passed; 1 failed`; the ancestor-based branch then produced `test result: ok. 1 passed`. The Doctor-row routing-branch control arm was not run; its idempotence test passed exactly 1 test. An initial context-field wiring draft touched actively owned `library_doctor/mod.rs`, so it was discarded before gates in favor of resolving the existing split-view ancestor entirely inside `navigation.rs`.
- Task 7 traceability control arm run: renaming one `nav_18_` test to `nav_99_` made `scripts/check-ux-traceability.sh` fail with `ERROR: test references unknown rule NAV-99`; restoring the name returned the gate to green.
- Task 7 traceability control arm run: renaming all twelve `nav_18_` tests while leaving NAV-18 active made `scripts/check-ux-traceability.sh` fail with `ERROR: [active] rule NAV-18 has no rule-named test`; restoring every name returned the gate to `UX traceability ok: 371 active rules covered`.
- Task 7 rule-named display gate passed all 489 discovered tests with `failed: 0 of 489`. The initial serial run was stopped before producing a result and repeated with the script's supported four-worker mode; only the completed repeat is reported as verification.
- Task 8 drift-guard control arm run: omitting `device-sync` from the known placeless-page classification made the exact source-text test report `test result: FAILED. 0 passed; 1 failed`; restoring it produced `test result: ok. 1 passed`. The first scanner draft also failed because it selected a nested widget-label `Some` rather than the final page-name argument; that invalid red was diagnosed, corrected with last-literal extraction, and is not counted as the control proof.
- Task 8 aggregate non-display verification produced `test result: ok. 2 passed; 0 failed; 11 ignored`; the ignored tests are the separately exercised display cases.
- Task 8 final pre-commit gates passed formatting, strict all-target workspace Clippy, architecture, the isolated complete workspace suite (2436 Core tests plus 2 ignored; 1824 GNOME tests plus 682 display ignores), and cached audit with only accepted RUSTSEC-2024-0436.

Die drei Befunde sind chirurgisch umgesetzt und separat committed:

- `4db248ee0a` — `fix(gnome): satisfy equalizer preference lints`
- `3c596cc0e0` — `fix(gnome): keep equalizer smoke fallback`
- `994bcd9618` — `test(android): require distinct equalizer labels`

Verifikation:

```text
$ cargo clippy -p reprise-gnome --all-targets -- -D warnings
Compiling reprise-gnome v0.1.57 (...)
Finished `dev` profile [unoptimized + debuginfo] target(s) in 21.13s
Exit 0
```

```text
$ cargo test -p reprise-gnome preferences
test result: ok. 75 passed; 0 failed; 55 ignored; 2662 filtered out
Exit 0
```

Android-JVM-Lauf über `scripts/check-android-suite.sh` mit JDK 21:

```text
BUILD SUCCESSFUL in 1m 58s
BUILD SUCCESSFUL in 1s
suites=76 tests=410 failures=0 errors=0 skips=0 verdict=fresh
Android unit-suite gate passed
Exit 0
```

Die betroffene Klasse wurde nachweislich ausgeführt:

```text
SettingsContentTest: tests=9 skipped=0 failures=0 errors=0
thePickerOffersEverySharedPreset: passed
```

Der neue Test erhielt außerdem einen Red–Green-Mutationsnachweis: Mit temporär dupliziertem Label schlug exakt `thePickerOffersEverySharedPreset` fehl (`410 tests completed, 1 failed`); nach Rücknahme war der Lauf grün.

Kein Push und kein vollständiges Repository-Gate. Die einzige verbleibende Änderung im Worktree ist die bereits vorher vorhandene, ungestagte Planphase in `docs/plans/equalizer-profiles-lead-the-surface-ui.md`; sie wurde nicht angefasst.

All six accepted findings are fixed in commit `e6f63ac7f2` (`fix(gnome): restore certified list geometry`).

Key changes:

- `configured_upper` now records only actual `Adjustment::configure` writes; settled model identity is tracked separately.
- Model swaps retain remembered row height and safely preseed the new range.
- Anchor application again requires widget-measured settled geometry.
- One- and two-row lists can certify their complete measurement set.
- Display tests share the production sample threshold.
- NAV-10b’s target oracle is restored to `< 1.0` pixel.

Required isolated display results:

BROWSE-11, server `:8501`:

```text
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 2860 filtered out; finished in 1.08s
```

TAG-1, server `:8502`:

```text
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 2860 filtered out; finished in 0.31s
```

Additional verification:

- Row-height inventory: 53 passed, 0 failed.
- NAV-10b exact display test: 1 passed, 0 failed.
- `cargo fmt --check`: passed.
- Workspace Clippy with warnings denied: passed.
- `cargo test --workspace`: passed.
- `cargo audit`: only allowed `RUSTSEC-2024-0436`.
- Clean-tree merge readiness against cached `origin/dev` `8cdaa48401`: passed.
- Rule-owned display inventory: 572 passed, 0 failed.
- Private runtime D-Bus tests: 25 passed, 0 failed.
- Worktree is clean; nothing was pushed or merged.

The initial merge-readiness fetch was blocked by the host SSH configuration, so the successful wrapper used the exact cached user-specified base with `--no-fetch`. One GTK initialization environment fault was also discarded and retried as instructed. No manual/CUA rendering check was performed; visual evidence is limited to isolated headless GTK display tests.


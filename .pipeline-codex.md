Implementation is complete and scoped correctly, but I stopped before committing because the required gettext gate fails on pre-existing catalog drift.

Completed:

- Renamed actions to “Clear filters” and “Clear selection”.
- Added the specified German and Spanish translations.
- Left ar, bn, fr, hi, and zh_CN untranslated.
- Removed `select_row` only from the `SessionRestore` branch.
- Updated podcast tests using red/green TDD.

Verification:

- `cargo fmt --check` — passed
- `cargo clippy --locked --all-targets --workspace -- -D warnings` — passed
- Isolated `cargo test --workspace` — passed
- `bash scripts/tests/gettext-catalogs.sh` — failed because `po/ar.po` already lacks 81 unrelated extracted messages

No commit was made because repository rules require all gates to pass. The worktree contains only the scoped changes but is currently dirty.

Should I expand scope to synchronize the broader translation catalogs, or should that baseline failure be repaired separately before I resume and commit?
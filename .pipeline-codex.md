# Refactor pass report

- **Finding 11:** Made the Releases date-window selection a non-removable
  picker action and taught the shared chip handler to reopen that facet's full
  value page without changing the filter
  (`crates/reprise-gnome/src/ui/releases/releases_filter_model.rs:139`,
  `crates/reprise-gnome/src/ui/browse/filter_bar.rs:438`). Added the Xvfb
  regression at `crates/reprise-gnome/src/ui/releases/releases_filter_bar_tests.rs:185`.
- **Finding 12:** The shared chooser now removes selected values, omits
  already-filled single facets, and disables "+ Add filter" when no addable
  value remains (`crates/reprise-gnome/src/ui/browse/filter_bar.rs:462`). Added
  the exhausted Releases regression at
  `crates/reprise-gnome/src/ui/releases/releases_filter_bar_tests.rs:212`.
- **Finding 13:** Kept the intentional shared warn-and-continue behavior and
  added a persistence-failure characterization proving that the visible state
  and change notification survive (`crates/reprise-gnome/src/ui/browse/filter_bar_tests.rs:171`).
- **Finding 14:** No Core file was changed because `reprise-core` is outside
  strand B ownership. Recorded both post-merge resolution alternatives in
  `docs/plans/responsive-editing-and-one-table-grammar-b.md:191`.
- **Finding 15:** Recorded why B2's BrowseBar conversion was dropped and why
  B4 remains explicitly deferred in this finding-only pass
  (`docs/plans/responsive-editing-and-one-table-grammar-b.md:177`). No B4 file
  was touched.
- **Finding 16:** Routed the single-definition check through `frontend_code()`
  so it excludes comments and test-only code consistently
  (`scripts/check-frontend-thinness.sh:147`).

## Verification

- Red then green under Xvfb:
  `cargo test -p reprise-gnome -- --ignored window_chip_opens_the_inline_picker_without_changing_the_filter`
  and
  `cargo test -p reprise-gnome -- --ignored add_filter_is_disabled_when_every_addable_release_value_is_selected`;
  each finished with 1 passed after first failing on the reviewed defect.
- Characterization under Xvfb:
  `cargo test -p reprise-gnome -- --ignored persistence_failure_keeps_the_visible_filter_and_notifies_the_caller`;
  1 passed.
- `cargo test -p reprise-gnome filter_bar`; 24 passed, 22 ignored, 0 failed.
- `scripts/check-frontend-thinness.sh`; the old implementation failed with a
  temporary test-only `FACET_PAGE` (found 2), the new implementation passed
  with that same fixture, and the clean-tree run passed. The fixture was
  removed.
- `cargo fmt --check`; passed.
- `cargo clippy --all-targets --workspace -- -D warnings`; passed.
- `XDG_CACHE_HOME=/tmp/reprise-responsive-b/xdg-cache XDG_DATA_HOME=/tmp/reprise-responsive-b/xdg-data cargo test --workspace`;
  passed. The task-specific XDG roots keep cache-writing tests inside the
  writable sandbox.
- `cargo audit --no-fetch`; passed with the repository's one allowed advisory.
  `--no-fetch` used the installed advisory database because the sandbox cannot
  lock the global Cargo cache for an update.
- `scripts/check-architecture.sh`; passed.

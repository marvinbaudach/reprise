Implemented T1–T8 and tests 1–19. The worktree is clean and four commits ahead of `origin/dev`:

- `fe06ec1f67` `docs: carry the Android SAF absence plan`
- `aacf565df9` `fix(android): carry confirmed SAF absence into Core`
- `bd50a8b7bf` `fix(scan): confirm SAF deletion from walk evidence`
- `a1e10629d4` `fix(android): classify confirmed SAF absence`

Key outcomes:

- Added the typed `NotFound` channel through Kotlin, UniFFI, and Core.
- Added safe tree-URI parent derivation.
- Added walk evidence, failed-parent poisoning, the empty-walk guard, and Present precedence.
- Kept `listChildren` failures loud.
- Added causal exception classification without message matching.
- Added all planned Rust, plain-JVM, and Robolectric regressions using red-green TDD.
- Preserved the two plan documents byte-for-byte.
- Changed only the authorized files; all edited code files remain below 800 lines.

Verification:

- `cargo clippy --locked --all-targets --workspace -- -D warnings` — exit 0.
- `cargo test --locked --workspace --exclude reprise-platform-linux` — exit 0 with isolated XDG directories. Core: 2,576 passed, 3 ignored; GNOME: 1,961 passed, 766 ignored; remaining suites passed.
- Android UniFFI generation — exit 0.
- Android JVM/Robolectric suite — exit 0: 73 suites, 377 tests, zero failures/errors/skips; repository parser and test-floor verdict both passed.
- `RUSTDOCFLAGS="-D warnings" cargo doc` — exit 0.
- `cargo fmt --check` and `git diff --check` — exit 0.
- `cargo audit --db /tmp/reprise-saf-advisory-db` — exit 0; only permitted `RUSTSEC-2024-0436`.
- Core purity check found no GTK/libadwaita/GStreamer/zbus dependencies.

Deliberately omitted:

- I did not run device verification or access real library data.
- I did not build or install an APK. Consequently, I did not invoke `scripts/check-android-suite.sh` end-to-end because it unconditionally runs `:app:assembleDebug`; instead I ran its binding generation, complete unit-test phase, result parser, and verdict logic separately.
- Nothing was pushed.

Real-hardware verification remains necessary to confirm the measured provider exception and URI behavior.
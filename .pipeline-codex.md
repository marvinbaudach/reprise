# Showroom review implementation report

## Completed plan work

- Task 1, tile hover (`59805d8597`): replaced screenshot zoom and lift with edge, inset, glow, and ambient-shadow states while preserving matching hover and keyboard-focus behavior.
- Task 2, CH.02 pipeline (`339e5038aa`): replaced the empty rail with the four-node source/gates/mutations/verdict pipeline and derived six gate clusters.
- Task 3, hire routes (`6206b7468e`): linked the header and Hero offer row to the existing availability section without publishing an email address.
- Task 4, mobile ledger (`09583158b7`): changed the ledger to labelled cards below 34rem while preserving explicit table semantics.
- Accepted implementation review fixes (`0f97c958ac`): completed the ledger roles and kept the hire-link hover selector scoped to the hire link.

Those five commits were already present when this browser follow-up began and were not reimplemented.

## Browser follow-up

The green verdict node measured 107.229px and the blocked verdict 167.526px in the existing rendered figure. The rounded measurement concealed a 169px intrinsic layout width for the blocked node. This follow-up therefore reserves `min-width: 169px` for `.pipeline__node--verdict` and clears that reservation to `0` in the existing `max-width: 46rem` column layout. The four accepted label strings are unchanged.

The focused Chromium recheck measured:

| Viewport | Green | Blocked | Result |
| --- | --- | --- | --- |
| 1440px | verdict 169px, gates 510px, strip 208px, 2 cluster rows | verdict 169px, gates 510px, strip 208px, 2 cluster rows | no state jump |
| 1280px | verdict 169px, gates 519px, strip 140px, 1 cluster row | verdict 169px, gates 519px, strip 140px, 1 cluster row | no state jump or wrap regression |
| 1024px | verdict 169px, strip 208px, 2 cluster rows | verdict 169px, strip 208px, 2 cluster rows | no state jump |
| 900px | verdict 169px, gates 200px, strip 276px, 3 cluster rows | verdict 169px, gates 200px, strip 276px, 3 cluster rows | no state jump |
| 720px | computed `min-width: 0`, verdict 643px, strip 643px | computed `min-width: 0`, verdict 643px, strip 643px | mobile nodes stay full-width |

## Tests changed

- `showroom/tests/chapter-two.test.mjs`: added one assertion to `show-21` pinning the measured `min-width: 169px` declaration on `.pipeline__node--verdict`. No existing assertion was removed, replaced, or loosened; `show-6` through `show-21` remain green.

The first focused run was red at 6/7 because the verdict had no dedicated rule. A provisional 168px implementation made the focused suite green, but the browser then exposed a 169px blocked `offsetWidth`. The assertion was tightened to 169px and observed red against the provisional 168px declaration before the final CSS made it green at 7/7.

## Verification commands

Run from `showroom/` unless noted otherwise:

- `node --test tests/chapter-two.test.mjs` — expected red, 6/7 passed; `show-21` reported that `.pipeline__node--verdict` had no own rule.
- `node --test tests/chapter-two.test.mjs` — provisional 168px implementation green, 7/7 passed.
- `node --test tests/chapter-two.test.mjs` — expected refinement red, 6/7 passed; expected 169px but found 168px.
- `node --test tests/chapter-two.test.mjs` — final focused result green, 7/7 passed.
- `npm run lint` — passed; Biome checked 95 files with no fixes.
- `npm run typecheck` — passed; `tsc --noEmit` reported no errors.
- `npm run test` — passed; the production client and SSR builds plus prerender completed, then 89/89 tests passed.
- `npm run preview -- --host 127.0.0.1 --port 4173` — preview started at `http://127.0.0.1:4174/reprise/` because 4173 was already occupied.
- `chromium --headless=new --no-sandbox --disable-gpu --remote-debugging-port=9222 --user-data-dir=/tmp/reprise-verdict-chromium http://127.0.0.1:4174/reprise/` — started Chromium 151 for the rendered geometry check; an inline Node CDP query measured verdict/gates widths, strip height, cluster rows, and computed mobile `min-width` before and after clicking one gate mark.

## Plan discrepancies and scope

The plan's CH.02 acceptance check named 1280px, where both states were already quiet, so it did not cover the wider 1440px wrap threshold found in the browser. The contract itself and all accepted copy remain valid; the missing wider-state geometry regression was the only defect found.

No dependency, design token, Rust file, plan file, or `phase:` field changed. `.pipeline-findings.md` remains untouched and untracked. No push was performed.

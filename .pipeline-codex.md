Umsetzung abgeschlossen. Der Standort-Chip verwendet nun die lokalisierte Stadt, während die Einstellungen `{Stadt}, {Land}` zeigen. Der Worktree ist sauber, auf aktuellem `origin/dev` rebased und nicht gepusht oder gemergt.

### Commits

Zusätzlich zum bereits vorhandenen Plan-Commit entstanden genau fünf Aufgaben-Commits:

- `45ea554049` — Aufgabe 1: `fix(concerts): parse city from geocode response`
- `66737fdd40` — Aufgabe 1b: `feat(location): persist localized country name`
- `fa5eed7d8b` — Aufgabe 2: `test(concerts): cover localized location labels`
- `b37043a582` — Aufgabe 3: `fix(preferences): show city and country for location`
- `66abc114fb` — Aufgabe 4: `docs(ux): define localized concert location labels`

Enthalten sind unter anderem:

- Stadtwahl `city → town → village → municipality → erstes display_name-Segment`
- lokalisiertes `accept-language`, einschließlich `zh_CN → zh-CN`
- neues, separat persistiertes `location.country`
- zentrale `{Stadt}, {Land}`-Formatierung ohne hängendes Komma
- unveränderter Portal-Pfad und unverändertes `country_code`
- ergänzte CONC-2- und SET-15-Verträge
- neue reine String-Tests; bestehende F-2-, F-3- und F-6-Tests wurden nicht aufgeweicht

### Gate-Lauf

Finaler Lauf:

```fish
env DISPLAY_TEST_JOBS=4 MERGE_READINESS_BASE_REF=origin/dev \
    scripts/check-merge-readiness.sh --no-fetch
```

Bestanden:

- Branch-Diff
- ShellCheck für 100 Shell-Dateien
- Worktree-GC-Sicherheit und Zeitplan
- Architektur- und Frontend-Grenzen
- Android-Opus/MP3-Laufzeit
- Accessibility und Input-Parity
- Runtime-Service-Installation
- Frontend-Thinness
- UX-Traceability: 380 aktive Regeln
- GNOME-Conformance
- Motion-Tokens
- `cargo fmt`
- striktes Workspace-Clippy
- warnungsfreie Rustdoc
- komplette Workspace-Tests
- serialisierte Linux-/GStreamer-Tests
- Displaytests: 722/722
- Runtime-Service-Bustests: 25/25
- `cargo audit`; ausschließlich erlaubtes `RUSTSEC-2024-0436` für `paste`
- Core-Purity und Dateigrößenprüfung

Hinweise:

- Der Fetch-Versuch vor dem ersten Lauf scheiterte vor den Gates an falschen Rechten in `/etc/ssh/ssh_config.d/20-systemd-ssh-proxy.conf`. Deshalb wurde gegen den lokal aktuellen `origin/dev`-Ref mit `--no-fetch` geprüft.
- Ein früher serieller Lauf war bei einem unveränderten Stats-Displaytest 715/716 rot. Der exakte isolierte Wiederholungstest bestand; die Stats-Implementierung ist identisch mit `origin/dev`. Nach dem Rebase bestand der vollständige Lauf mit 722/722. Es bleibt somit kein bekannter roter `dev`-Gate.
- Die geplanten GNOME-Warnungen GP-2/3/4/19/20 blieben nicht blockierend. `cargo-machete` wurde vom Gate als nicht installiert übersprungen.

### Noch ausstehend

Aufgabe 5 bleibt wie angeordnet beim Eigentümer: Ist-Screenshot mit der eigenen Datenbank, Standort über echtes Nominatim neu suchen und Danach-Screenshot aufnehmen. Ich habe weder den echten Abruf noch die reale Datenbank verwendet.

Plan-Frontmatter blieb unverändert. Kein Push, kein Merge und keine Änderungen an anderen Worktrees.
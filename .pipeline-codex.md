Implemented the complete plan in four commits:

- `bb4ddcaa07` — `Tag the exception, not the state, in the Updates feed`
- `bc325bca75` — `Prove the popover pill and the table pill are one pill`
- `96f9ece9d3` — `Record R4 as closed by construction`
- `429b5390b3` — `Capture the counter-proof to 41-footer-loaded`

Key results:

- Updates tags `Off sale` and `Unknown`; `On sale` remains untagged.
- Popover ticket tones match the Concerts table declarations.
- The Releases Neutral chip is unchanged.
- NR-39 is active and traceable.
- R4 has its dated, addition-only closure.
- The tracked probe and blocked-run [manifest](/home/marvin/Projects/reprise-feed-tags-mark-the-exception/artifacts/feed-tags-mark-the-exception/manifest.txt) are committed; no PNG is tracked.
- Progress ledger is current.

Verification:

- `cargo fmt --check` — passed
- strict locked workspace Clippy — passed
- rustdoc with warnings denied — passed
- locked workspace tests excluding `reprise-platform-linux` — passed
- UX traceability — `380 active rules covered`
- ShellCheck — `101 tracked shell files`
- AI hygiene — passed with pre-existing planned GP-19/GP-20 warnings
- Architecture — passed
- Frontend thinness — passed; budgets `113/13/15/7`, view floor `2116`, allowlist `46`
- Display suite:
  - `origin/dev` baseline: `715/716`, one unrelated performance diagnostic failed
  - branch: `715/717`, two untouched list-geometry tests failed
  - both branch failures passed exact isolated reruns on both `origin/dev` and this branch
  - the new pill-render test passed

The CUA probe captured all three required screenshots under `target/feed-tags-scratch/feed-tags.bsgUML/evidence`. Visual and accessibility evidence confirms the expected states. Numeric screenshot sampling remains explicitly blocked because `cua-driver 0.19.3` exposes the nested pill text without a usable AT-SPI frame; the deterministic GTK geometry/color test is green.

No push or PR was made. The only remaining worktree entry is the preserved user-owned untracked plan:

`docs/plans/feed-tags-mark-the-exception.md`

`origin/dev` advanced afterward by one CI-only commit; no affected implementation path changed. Goal usage: 880,614 tokens over approximately 1 hour 46 minutes.
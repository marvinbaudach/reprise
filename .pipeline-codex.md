# Sound Similarity implementation report

Status: complete locally on `feature/sound-similarity`, based on the requested
pinned `origin/dev` revision `527d5cbbbc`. The remote branch moved during the
work; this branch was deliberately not rebased because the plan fixes its base.
Nothing was pushed.

## Packages and commits

1. P1 `66d0f5d69d` — `feat(sound): derive and store sound features`
2. P2 `1638db269b` — `feat(sound): rank similar tracks`
3. P3 `f17dbfb467` — `feat(sound): backfill derived profiles`
4. P4 `57e7cef66a` — `feat(sound): add now-playing similarity panel`
5. P5 `638d0c155f` — `feat(sound): add similarity panel entry points`
6. P6 `1521b1dc94` — `feat(plugins): declare integration points`
7. P7 — this commit, `docs(ux): activate sound similarity rules`

P1-P3 implement the derived, versioned profile cache, library statistics,
standardized distance/ranking, and cache generation from the existing stored
spectrogram. P4-P5 add the retained Sound panel, profile/file/result views,
ordered queue action, module gating, info entry point, shortcut, and measured
four-label switcher. P6 adds typed static provisions, deterministic plugin
badges, preferences, and the module-gated track-menu route. P7 activates the
rulebook, updates gettext catalogs, repairs same-album identity and compact
result-row navigation, and closes all gates.

## Verification

- `cargo fmt --check`: passed.
- `cargo build --workspace`: passed.
- `cargo clippy --workspace --all-targets -- -D warnings`: passed.
- `cargo test -p reprise-core --quiet`: 2,111 passed, 0 failed, 1 ignored.
- `cargo test --workspace --quiet`: passed; GNOME reports 1,450 passed,
  0 failed, 439 display tests ignored, and all other crates are green.
- `scripts/check-ux-traceability.sh`: 323 active rules covered.
- `scripts/tests/gettext-catalogs.sh`: passed with no fuzzy new entries.
- Core purity check: no GTK, libadwaita, GStreamer, or zbus dependency.
- `cargo audit --no-fetch`: only the accepted `RUSTSEC-2024-0436` warning for
  `paste`.
- `git diff --check`: passed.
- Every Rust file changed by this feature ends below 800 lines. The general
  architecture helper still reports six untouched legacy files above 800:
  `cover.rs`, `library/tag_mutation.rs`, `external_media_state.rs`, the podcast
  and radio `add_dialog.rs` files, and `radio_view.rs`.

E3's measurement gate passed. With the actual Adwaita switcher and installed
font, 224 px is the last ellipsizing width and 228 px the first fitting width.
At the fixed 300 px panel width all four labels are present and none is Pango-
ellipsized. The proof was rerun through private D-Bus, disposable XDG roots,
Xvfb/X11, and the fake audio sink with one test thread.

The full workspace gate exposed a real concurrent test-infrastructure race in
the existing yt-dlp redaction assertions: short-lived tracing subscribers could
lose a callsite to tracing's global interest cache and capture an empty log.
The test-only capture now uses one stable global subscriber and thread-local
routing. The original 40-run concurrent reproducer passes after the repair,
and both full Core and workspace suites pass without weakening any redaction
assertion or changing production logging.

## Deferred acceptance and residual risk

The following plan acceptance checks require a complete spectrogram/profile
backfill and therefore remain manual, outside the merge gate:

- a histogram over at least 50 sample tracks to validate distribution spread;
- quiet-versus-loud result-list overlap;
- same-album exclusion against the real library;
- the 1,843-track title-change timer benchmark, including the required
  disabled-path countercheck.

The open-tab disable fallback is covered by a display-free GTK rule test. The
tempo estimator still has the specified octave-error risk and needs the planned
library-level validation after backfill. The repository-wide sequential runner
for all 439 ignored display tests was not completed; the two relevant Sound
panel widget proofs were run individually in the required isolated environment.

## Assumptions and questions

- Same-album identity is album title plus album artist, preventing unrelated
  albums with a shared title from being filtered together.
- The pinned base already used schema version 56 for New Releases, so the sound
  migration follows the existing idempotent-repair convention for either v56
  shape instead of silently inventing a different version.
- No unresolved product questions remain.

No real database, music file, live desktop, account, external service, or file
outside this worktree was accessed or changed.

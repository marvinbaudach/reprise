Applied exactly findings 1–5 in three commits:

- `e2266d1a56` — continue prefetch after transient failures
- `51b1dd7409` — narrow DB-lock and Core cache boundaries
- `3d76a9870a` — correct prefetch copy and add failure logging

Verification:

- Finding 2 red: `aTransientlyFailingWholeBatchDoesNotHideLaterArtists` failed as expected — 1 test, 1 failure.
- Rust: Core 2,531 passed, 3 ignored; Android FFI 170 passed.
- Android: 71 suites, 369 tests, 0 failures/errors/skips, fresh XML.
- All four protected tests are green.
- Formatting, strict Clippy, full workspace tests and Core purity passed.
- Cached audit reported only accepted `RUSTSEC-2024-0436`.

Task 3.3, the plan status, and rejected findings 6–8 remain untouched. The two user-owned untracked pipeline files are preserved. No push performed. The aggregate merge-readiness wrapper was not run because those preserved files keep the worktree dirty; its individual gates passed.
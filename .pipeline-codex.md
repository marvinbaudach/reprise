# Strand B implementation record

## Changes

- `crates/reprise-core/src/library/settings_geometry.rs`: added the guarded,
  transactional schema-v80 cleanup for `ui.row_height` and
  `ui.section_header_height`.
- `crates/reprise-core/src/db.rs`: raised `SUPPORTED_SCHEMA_VERSION` from 79 to
  80, registered v80 after v79, and registered the focused migration-test
  module.
- `crates/reprise-core/src/library/settings_geometry_migration_tests.rs`: added
  four synthetic-database regressions for cleanup/versioning, idempotence,
  unrelated-setting preservation, and the already-v80 early return.
- `docs/plans/the-table-follows-the-music-again.md`: corrected the predecessor's
  closing manual check from "still owed" to performed-and-failed on 2026-08-25,
  with a link to the successor plan.
- `docs/plans/the-row-height-certifies-itself-b.md`: recorded every required
  mutation probe and the restored four-test green run.
- `.pipeline-codex.md`: recorded this file-by-file rationale, verification,
  probes, and the one out-of-scope gate failure.

## Verification commands and actual results

```text
git fetch origin dev
FAIL: SSH refused /etc/ssh/ssh_config.d/20-systemd-ssh-proxy.conf because of
bad ownership or permissions. No remote ref changed.

git rev-parse HEAD origin/dev
0af481fd5a2f89616c7be3a7c99782c27fe24a7a
0af481fd5a2f89616c7be3a7c99782c27fe24a7a

git log --oneline HEAD..origin/dev
PASS: no commits listed before implementation; no locally known competing v80.

rg -n '^pub const SUPPORTED_SCHEMA_VERSION' crates/reprise-core/src/db.rs
PASS before implementation: version was exactly 79.

cargo test -p reprise-core settings_geometry_migration_tests
PASS: running 4 tests; 4 passed, 0 failed, 2600 filtered out; exit 0.

cargo test -p reprise-core settings_geometry
PASS: running 4 tests; 4 passed, 0 failed, 2600 filtered out; exit 0.

cargo fmt --check
First run: FAIL, exit 1; rustfmt requested one closure-layout correction in
the new migration-test file. After that correction: PASS, exit 0.

cargo clippy --locked --all-targets --workspace -- -D warnings
PASS: finished the dev profile in 2m 09s; exit 0.

env XDG_DATA_HOME=<temporary>/data XDG_CACHE_HOME=<temporary>/cache \
  REPRISE_AUDIO_SINK=fakesink cargo test --locked --workspace
FAIL: Core ran 2605 tests; 2601 passed, 3 ignored, and the sole failure was the
out-of-scope `db_concerts::tests::supported_schema_version_is_v79`, whose
literal expected 79 but actual v80 is 80; exit 101. All summaries before that
Core target were green.

cargo tree -p reprise-core
PASS: exit 0; a separate search for gtk4, libadwaita, gstreamer, or zbus
returned no matches, so Core purity is intact.

cargo audit
UNAVAILABLE: exit 1 because the sandbox could not obtain
`/home/marvin/.cargo/advisory-db..lock`.

cargo audit --no-fetch
PASS: scanned 484 dependencies; only allowed RUSTSEC-2024-0436 (`paste`) was
reported; exit 0. The crates.io package-cache lock was also unavailable.

git diff --check
PASS: no whitespace errors.

git log --oneline HEAD..origin/dev
PASS before final bookkeeping: no locally known commits arrived on origin/dev;
the locally stored remote ref contains no competing migration.

rg -n '^pub const SUPPORTED_SCHEMA_VERSION' crates/reprise-core/src/db.rs
PASS after implementation: line 26 reports version 80.

wc -l crates/reprise-core/src/library/settings_geometry.rs \
  crates/reprise-core/src/db.rs \
  crates/reprise-core/src/library/settings_geometry_migration_tests.rs
PASS: 185, 796, and 97 lines respectively; every edited code file is below
800 lines.

MERGE_READINESS_BASE_REF=origin/dev scripts/check-merge-readiness.sh --no-fetch
UNAVAILABLE: exit 1 before any gate because the frozen, user-supplied mother
plan `docs/plans/the-row-height-certifies-itself.md` is intentionally untracked.
The strand explicitly forbids modifying that file.

UV_CACHE_DIR=/tmp/reprise-b-uv-cache \
UV_TOOL_DIR=/tmp/reprise-b-uv-tools \
MERGE_READINESS_SKIP_ANDROID_QUALITY=1 \
MERGE_READINESS_BASE_REF=origin/dev \
bash <(
  sed -e 's|^repo_root=.*|repo_root=/home/marvin/Projects/reprise-the-row-height-certifies-itself-b|' \
      -e '/if \[\[ -n "$(git status --porcelain --untracked-files=all)" \]\]; then/,/^fi$/d' \
      scripts/check-merge-readiness.sh
) --no-fetch
PARTIAL: every exact wrapper gate through workspace tests passed: branch diff,
123 shell files, project and Showroom quality, worktree-GC checks, gettext,
script self-tests, architecture, device-sync GStreamer, accessibility, input
parity, runtime install, frontend thinness, UX traceability, AppStream, Flatpak,
GNOME idioms, AI hygiene, motion tokens, formatting, strict workspace Clippy,
and warning-free workspace rustdoc. Workspace tests then stopped the script on
the same sole out-of-scope v79 assertion: 2601 passed, 1 failed, 3 ignored;
exit 101.

scripts/check-project-quality.sh (through the exact wrapper sequence)
First attempt: unavailable because uv could not write its default cache.
Second attempt with UV_CACHE_DIR: unavailable because uv could not write its
default tool directory. With both redirected: project and Showroom quality
passed. The full Android-quality route first failed on a read-only global
Gradle lock, then on missing SDK configuration with a worktree-local Gradle
home, then reached Kotlin compilation with Java 21 plus the documented SDK and
failed because generated `uniffi` bindings are absent. Generating those files
under `android/app/src/main/java/uniffi` is outside strand ownership, so the
documented Core-route Android-quality skip was used for the complete sequence.

env XDG_DATA_HOME=<temporary>/data XDG_CACHE_HOME=<temporary>/cache \
  REPRISE_AUDIO_SINK=fakesink cargo test --locked -p reprise-platform-linux \
  -- --test-threads=1
PASS: 158 passed and 2 ignored in the library target; the runtime-service
integration target reported 1 passed and 25 ignored; exit 0.

scripts/check-display-tests.sh --rule-named
PASS: 572 passed, 0 failed; exactly 3 declared measurement tools were skipped;
exit 0.

dbus-run-session -- cargo test --locked -p reprise-platform-linux \
  --test runtime_service -- --ignored --test-threads=1
PASS: running 25 tests; 25 passed, 0 failed, 1 filtered out; exit 0.

if ! cargo audit; then cargo audit --no-fetch; fi
PASS through cached fallback: live refresh could not obtain the advisory lock;
cached audit scanned 484 dependencies and reported only allowed
RUSTSEC-2024-0436; final exit 0.

UV_CACHE_DIR=/tmp/reprise-b-uv-cache \
UV_TOOL_DIR=/tmp/reprise-b-uv-tools \
scripts/check-project-quality.sh --project --showroom
PASS after the final pipeline update: Python/YAML/Markdown quality and all 91
Showroom tests passed; project source-quality exit 0.
```

## Mutation probes

### 1a — cleanup is registered

Mutation: removed the v80 call from `migrate_with_cache_dirs`.

```text
running 1 test
test db::settings_geometry_migration_tests::v80_clears_persisted_list_geometry_and_records_the_version ... FAILED
assertion `left == right` failed
  left: Some("30")
 right: None
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 2603 filtered out
mutation_probe_exit=101
```

### 1b — second invocation is a no-op

Mutation: removed the v80 version guard.

```text
running 1 test
test db::settings_geometry_migration_tests::v80_is_a_no_op_when_run_a_second_time ... FAILED
assertion `left == right` failed
  left: [("online-sources-enabled", "0")]
 right: [("online-sources-enabled", "0"), ("ui.row_height", "45")]
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 2601 filtered out
mutation_probe_exit=101
```

### 1c — deletes remain keyed

Mutation: replaced the keyed deletes with an unkeyed settings-table delete.

```text
running 1 test
test db::settings_geometry_migration_tests::v80_preserves_unrelated_settings ... FAILED
assertion `left == right` failed
  left: None
 right: Some("{\"title\":320,\"artist\":180}")
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 2602 filtered out
mutation_probe_exit=101
```

### 1d — an already-v80 database is untouched

Mutation: removed the v80 version guard.

```text
running 1 test
test db::settings_geometry_migration_tests::v80_leaves_an_already_current_database_alone ... FAILED
assertion `left == right` failed
  left: [("online-sources-enabled", "0")]
 right: [("online-sources-enabled", "0"), ("ui.row_height", "45"), ("ui.section_header_height", "49")]
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 2603 filtered out
mutation_probe_exit=101
```

Every mutation was discarded immediately after its red run. The restored
four-test module passed 4/4.

## Could not complete

- The complete workspace/merge-readiness gate cannot be green without changing
  `crates/reprise-core/src/db_concerts_migration_tests.rs:370`, where
  `supported_schema_version_is_v79` hard-codes 79. That path is outside this
  strand's hard ownership boundary, so it was not edited.
- The authoritative clean-tree wrapper cannot run while the frozen untracked
  mother plan remains present. It was not moved, ignored, staged, or modified.
- A live remote refresh was blocked by host SSH configuration permissions; the
  locally stored `origin/dev` remained equal to the strand base and contained
  no competing migration.
- Android source quality could not compile without generated UniFFI bindings.
  The generation output path is outside the strand's hard ownership boundary;
  project and Showroom quality passed under the wrapper's documented Core
  route, and Android is explicitly unverified here.

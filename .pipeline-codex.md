Implemented and committed R1 as `01bc174131`.

- Removed the two source-format tests without misleading replacements.
- Preserved the real `fn_addr_eq` cache-only resolver check.
- Updated [.pipeline-codex.md](/home/marvin/Projects/reprise-concerts-artist-portrait-column/.pipeline-codex.md).
- No production behavior changed.

Verification:

- `cargo test -p reprise-view`: 117 passed.
- `cargo test -p reprise-gnome`: 1,923 passed, 762 ignored; 10 conformance tests passed. One non-failing `GdkPixbuf-CRITICAL` warning appeared.
- `cargo clippy --all-targets -- -D warnings`: passed.
- `scripts/check-ux-traceability.sh`: 400 active rules covered.
- Full workspace tests, workspace Clippy, formatting, and audit also passed; audit reported only accepted `RUSTSEC-2024-0436`.

The worktree is clean. No display tests, application launch, push, PR, Deezer request, or real XDG access occurred.
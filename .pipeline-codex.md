# Strand A report - the UI follows the service

## Outcome

Strand A and the five accepted review fixes are committed. The final host-side
Android gate is fresh and green:

```text
suites=73 tests=383 failures=0 errors=0 skips=0 verdict=fresh
Android unit-suite gate passed
```

This replaces the previous report's false `tests=382` claim. The old A-6
`ActivityController.recreate()` test did not pass: a fresh rerun produced
`tests="7" failures="1" errors="0"` and died in UniFFI initialization before
its assertions. Finding A below records the corrected test and its real proof.

No device, emulator, `adb`, or frame-rate measurement was used.

## Original tasks

- A-1 `e5d96e35ef` - publish playback, settings, and sleep-timer state through
  `StateFlow`.
- A-2 `f3affce979` - collect those flows while `STARTED` and use one bound
  service source for state and commands.
- A-3 `098f2b4c6c` - log refused/silent binds and retry one accepted silent bind
  after two seconds.
- A-4 `790653bb81` - keep position ticks alive through a transient
  `isPlaying == false` while the published state retains play intent.
- A-5 `f3cee26201` - release scrub ownership on slider drag cancellation without
  seeking.
- A-6 `a492b27e01` - add the recreation guard. Its defective recreation harness
  is corrected by Finding A in `92e2360eca`.

The distinct A-3 log phrase is:

```text
Playback service bind did not connect
```

## Original task red runs

All Gradle invocations used `JAVA_HOME=/usr/lib/jvm/java-21-openjdk`. Compile
reds are identified as such and are not presented as runtime verdicts.

### A-1

The channel tests were written against the intended API first:

```text
PlaybackSnapshotChannelTest.kt:35:40 Unresolved reference 'playbackSnapshots'.
PlaybackSnapshotChannelTest.kt:44:40 Unresolved reference 'playbackSnapshots'.
> Task :app:compileDebugUnitTestKotlin FAILED
BUILD FAILED
```

This covers both publishing with no collector and late-collector replay.

### A-2

The activity tests first compile-failed on the intended state seam:

```text
MainActivityPlaybackChannelTest.kt:49:30 Unresolved reference 'currentPlaybackState'.
MainActivityPlaybackChannelTest.kt:67:38 Unresolved reference 'currentPlaybackState'.
MainActivityPlaybackChannelTest.kt:100:30 Unresolved reference 'currentPlaybackState'.
> Task :app:compileDebugUnitTestKotlin FAILED
BUILD FAILED
```

After adding only that seam, the disconnect contract was behavior-red:

```text
MainActivityPlaybackChannelTest > losingTheServiceDoesNotClaimNothingIsPlaying FAILED
4 tests completed, 1 failed
BUILD FAILED
```

### A-3

The bind watchdog tests first compile-failed on their intended constants:

```text
MainActivityPlaybackChannelTest.kt:121:13 Unresolved reference 'PLAYBACK_BIND_WATCHDOG_MS'.
MainActivityPlaybackChannelTest.kt:176:60 Unresolved reference 'PLAYBACK_BIND_FAILURE_LOG'.
> Task :app:compileDebugUnitTestKotlin FAILED
BUILD FAILED
```

### A-4

Restoring the old non-rescheduling ticker made the new transient-false test
behavior-red:

```text
Media3PlaybackPortTest > aMomentaryNotPlayingDoesNotStopPositionEventsForGood FAILED
position events must resume without another listener callback: [1000]
BUILD FAILED
```

### A-5

The interaction-source test first compile-failed against the intended slider
parameter, then failed behaviorally with no cancel collector:

```text
NowPlayingGesturesTest.kt:271:21 No parameter with name 'interactionSource' found.
> Task :app:compileDebugUnitTestKotlin FAILED
BUILD FAILED
```

```text
NowPlayingGesturesTest > aCancelledSeekGestureReturnsTheHeadToThePlaybackPosition FAILED
1 test completed, 1 failed
BUILD FAILED
```

`aStillFingerKeepsTheHead` remains the timeout-rejection guard.

## Accepted review findings

### Finding A - ActivityScenario recreation

Commit: `92e2360eca` (`test(android): exercise playback recreation through scenario`).

The defective baseline was reproduced from fresh XML:

```text
tests="7" skipped="0" failures="1" errors="0"
java.lang.NoClassDefFoundError: Could not initialize class uniffi.reprise_android_ffi.UniffiLib
```

The test now uses the same Compose `ActivityScenario.recreate()` route as
`MainActivityPlayViewStabilityTest`. An activity lifecycle callback publishes
track 7 at the old activity's real `onActivityStopped`, preserving the stronger
requirement that the new activity must reconcile state received while away.

Sensitivity proof: temporarily changing the collector to
`playbackSnapshots.drop(1)` suppressed the replay and produced fresh red XML:

```text
tests="1" skipped="0" failures="1" errors="0"
expected:<7> but was:<null>
```

After reverting the mutation:

```text
tests="7" skipped="0" failures="0" errors="0"
```

### Finding B - a real paused-ticker guard

Commit: `6ec0badbd1` (`test(android): exercise position ticker pause transition`).

The rewritten test starts the ticker, proves it emitted position 1000, then
sets both `isPlaying=false` and `playWhenReady=false`, sends
`onIsPlayingChanged(false)`, and advances three intervals.

Sensitivity proof: temporarily removing callback cancellation and the ticker's
playing check produced:

```text
tests="1" skipped="0" failures="1" errors="0"
expected:<[1000]> but was:<[1000, 2000]>
```

After restoring production:

```text
tests="4" skipped="0" failures="0" errors="0"
```

### Finding C - preserve the connected visual engine

Commit: `2d476e519c` (`fix(android): preserve visual engine across disconnect`).

Decision: preserve the service-provided engine factory across
`onServiceDisconnected`. This matches the last-known playback-state policy and
avoids disposing the PCM-attached engine merely to create a native engine that
is attached to no sink. A successful reconnect replaces the factory.

The test-first run compile-failed on the intended internal observation seam and
produced no XML, so it measured no runtime behavior:

```text
Unresolved reference 'currentVisualSceneEngineFactory'.
> Task :app:compileDebugUnitTestKotlin FAILED
```

With only that seam added, the pre-fix behavior produced fresh red XML:

```text
tests="1" skipped="0" failures="1" errors="0"
expected same:<ReprisePlaybackService lambda> was not:<NativeVisualSceneEngineFactory>
```

After deleting the disconnect reset:

```text
tests="7" skipped="0" failures="0" errors="0"
```

Keeping the last playback state on disconnect caused no unexpected failure in
the final affected-class run or the fresh 383-test Android gate.

### Finding D - scrub ownership guard

Commit: `73cf45c587` (`fix(android): guard seek scrub ownership`).

`releaseScrub` now returns `null` and mutates nothing when the requested track
does not own the scrub. `onValueChangeFinished` seeks only after a successful
owned release. The new regression keeps an outgoing track's held drag while a
stale cancel targets the incoming track.

Before the guard:

```text
tests="1" skipped="0" failures="1" errors="0"
expected:<40000> but was:<0>
```

After the guard:

```text
NowPlayingGesturesTest: tests="12" failures="0" errors="0"
MobileSurfaceStateTest: tests="14" failures="0" errors="0"
```

`aStillFingerKeepsTheHead` is included in the 12 green gesture tests.

### Finding E - remove unreachable press cancellation

Commit: `73de808800` (`refactor(android): remove unreachable seek press cancel`).

The slider collector now handles only `DragInteraction.Cancel`; the unreachable
`PressInteraction.Cancel` branch and import are gone.

Sensitivity proof: temporarily leaving only the press branch made the supported
drag-cancel regression fail:

```text
tests="1" skipped="0" failures="1" errors="0"
expected:<30000.0> but was:<57258.0>
```

After restoring drag cancellation and removing press cancellation:

```text
tests="12" skipped="0" failures="0" errors="0"
```

## Final verification

The exact five-class command requested by the review was rerun after clearing
the result directory. Exactly five fresh XML files remain:

```text
MainActivityPlaybackChannelTest:  tests="7"  skipped="0" failures="0" errors="0"
PlaybackSnapshotChannelTest:      tests="2"  skipped="0" failures="0" errors="0"
Media3PlaybackPortTest:           tests="4"  skipped="0" failures="0" errors="0"
NowPlayingGesturesTest:           tests="12" skipped="0" failures="0" errors="0"
MainActivityPlayViewStabilityTest tests="4"  skipped="0" failures="0" errors="0"
```

The full Android gate was also rerun with both Android SDK variables pointing
to `/home/marvin/.local/share/android-sdk`:

```text
suites=73 tests=383 failures=0 errors=0 skips=0 verdict=fresh
Android unit-suite gate passed
```

The first full-gate attempt had no SDK environment variable and exited before
tests; it measured nothing and is not counted. `android/local.properties`
already pointed at the correct SDK and was left unchanged.

`scripts/verify-now-playing-scene.sh` also exited before Gradle and produced no
scene verdict:

```text
missing test source: de.reprise.spike.scene.CoreShapeTest
```

That source is absent in both this branch and current `origin/dev`; the stale
script is outside strand ownership and was not edited. The full Android gate
did execute all 73 currently discoverable suites successfully. No Rust was
touched in this repair round, so the review's conditional Cargo format/Clippy
commands were not run.

## Ownership, assumptions, and residual checks

No tracked production or test source outside Strand A ownership was changed.
`.pipeline-codex.md` is the explicitly requested report. Ignored UniFFI
bindings, Gradle state, and test/build outputs were regenerated but are not in
the tracked diff. No plan, scene, Android FFI, or Gradle build file was edited.

All edited code files remain below 800 lines; `MainActivity.kt` is 791 lines.
The only residual verification gap is the stale scene-wrapper inventory above.
Device frame-rate measurement remains intentionally deferred to Strand B.

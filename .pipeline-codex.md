# Codex handoff — The row height certifies itself, strand A

## Changes by file

- `crates/reprise-gnome/src/ui/list_geometry.rs`: records geometry-owned range
  writes by row count, distinguishes GTK-authored ranges, prevents a settled
  GTK range from being overwritten, takes layout geometry from GTK's quotient,
  removes contradiction-based persistence, and requires at least three rows
  whose allocation reached natural height before persisting a measurement.
- `crates/reprise-gnome/src/ui/list_geometry_acceptance_tests.rs`: adds the 14
  GTK-free acceptance tests for Tasks 1-5.
- `crates/reprise-gnome/src/ui/track_list/track_list_geometry.rs`: passes the
  live range and row count to the authoritative layout selector.
- `crates/reprise-gnome/src/ui/track_list/track_list_reload.rs` and
  `track_list_reload_geometry.rs`: gate flat-list reload capture on a
  GTK-authored range and extract the helper to keep the edited code file below
  800 lines.
- `crates/reprise-gnome/src/ui/track_list/track_list_reload_display_tests.rs`:
  adds poisoned-cache capture/healing witnesses and uses production CSS in the
  geometry fixture.
- `crates/reprise-gnome/src/ui/track_list/display_test_geometry.rs`: supplies
  the independent realized-row modal helper used by repaired display oracles.
- `crates/reprise-gnome/src/ui/track_list/row_height_contract_display_tests.rs`:
  adds the independent large-flat-list widget/range/persistence/reveal witness.
- `crates/reprise-gnome/src/ui/track_list/fresh_start_allocation_display_tests.rs`
  and `reload_restore.rs`: strengthen the cold-start no-jump witness and keep
  one complete guard-row of slack against ColumnView chrome.
- `crates/reprise-gnome/src/ui/track_list/reload_anchor_scroll.rs` and
  `track_reveal.rs`: retry geometry-dependent work on allocation frames and
  accept GTK-authored flat-list geometry without treating persistence evidence
  as a layout prerequisite.
- `crates/reprise-gnome/src/ui/track_list/reveal_track_display_tests.rs`,
  `navback_anchor_display_tests.rs`, `search_viewport_display_tests.rs`,
  `source_switch_centering_display_tests.rs`,
  `current_track_selection_tests.rs`, and
  `tag_mutation_refresh_display_tests.rs`: replace scroll-target expectations
  reconstructed from `upper / count` with the shared realized-row witness.
- `crates/reprise-gnome/src/ui/track_list/delete_follow_display_tests.rs`,
  `start_restore_tests.rs`, and `queue_section_centering_display_tests.rs`: mark
  range-derived tolerance/scrollability uses as non-target oracles.
- `crates/reprise-gnome/src/ui/track_list/mod.rs`: registers the new test-only
  helper and independent display test.
- `docs/plans/the-row-height-certifies-itself-a.md`: records mutation probes,
  exact reds/greens, gate results, residual failures, and the blocked arms.

No `reprise-core` file, frozen mother plan, or other out-of-bound repository
path was modified. The supplied untracked mother-plan file remains uncommitted.

## Verification commands and actual results

- `cargo test -p reprise-gnome list_geometry::acceptance_tests`: final run
  reported `running 14 tests`; 14 passed, 0 failed.
- `/tmp/run-rowheight-exact-display.sh <worktree> <log>
  fresh_start_deep_restore_allocates_visible_rows_at_the_anchor
  library_reload_heals_a_poisoned_persisted_row_height_from_settled_geometry
  poisoned_reload_capture_waits_for_gtk_authored_geometry
  row_height_contract_agrees_across_widgets_adjustment_persistence_and_reveal`:
  four tests resolved uniquely, each ran in its own private process, 4 passed.
- Task 8 sequential exact inventory (20 named tests across the seven requested
  files): the first inventory passed 14 and exposed six reds. After the
  data-row/header distinction and affected refinements, the complete same
  inventory passed 20, failed 0. The informative intermediate red output is
  retained in the strand acceptance section.
- `cargo fmt --check`: passed.
- `cargo clippy --all-targets --workspace -- -D warnings`: first run failed on
  one `clippy::map_unwrap_or`; after the local fix the command passed.
- `cargo test --workspace`: 2,571 passed and 27 failed because Core cover/cache
  tests hit a read-only default filesystem path.
- `env XDG_DATA_HOME=<mktemp> XDG_CACHE_HOME=<mktemp> cargo test --workspace`:
  aggregate 5,597 passed, 0 failed, 847 ignored.
- `cargo audit`: could not acquire the read-only
  `/home/marvin/.cargo/advisory-db..lock`.
- `cargo audit --no-fetch`: passed against 1,226 locally cached advisories with
  only accepted `RUSTSEC-2024-0436` (`paste`).
- `git diff --check`: passed.
- `wc -l crates/reprise-gnome/src/ui/list_geometry.rs
  crates/reprise-gnome/src/ui/track_list/track_list_reload.rs`: 771 and 784.

## Mutation probes

- Task 1: missing `gtk_authored` / `record_configured_upper` compile errors.
- Task 2: missing and then wrong-arity `preseed_unclaimed_upper` compile errors;
  the cold-start display test separately failed with two visible writes at
  32096 and 32129.
- Task 3: missing `authoritative_row_height` compile error; the independent
  origin behavior reused 30 instead of GTK's 45.
- Task 4: missing `persistable_row_height` compile error; the healing arm kept
  53 instead of 34 before the fix.
- Task 5: six missing `from_widget_measurements` compile errors.
- Task 6: capture returned `Some(RowHeight(30.0))` instead of `None`.
- Task 7 control: exact independent display test failed with layout 30 versus
  settled GTK quotient 45.

The full literal outputs are preserved in the strand plan's Acceptance
evidence section.

## Items not completed

- The control/fix real-library measurement arms were not executed: their
  supplied script opens on the live desktop/session bus and writes outside the
  authorized worktree, violating the repository safety recipe and this task's
  hard filesystem boundary.
- The requested comments in `ui/delete_tracks_large_block_display_tests.rs`
  and `ui/window/metadata_navigation.rs` were not added because those files are
  outside strand A ownership.
- No version bump, progress-ledger edit, or mother-plan edit was made because
  each would cross the user's explicit write boundary.

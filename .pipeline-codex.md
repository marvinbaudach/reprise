Implemented and committed all three findings.

Commit: `2e09d61dfc fix(android): keep queue landing tint on its destination`

Changes:

- Added one-way source→destination tint latch independent of reload offsets.
- Limited per-frame animation reads to the flashing row.
- New gestures cancel and clear an existing tint.
- Updated pure regression tests and plan explanation.
- Preserved the pre-existing YAML `phase: reviewed` change uncommitted.

Verification passed:

- Android suite: 85 suites, 486 tests, 0 failures; debug APK assembled.
- Android theme lint.
- Format, strict workspace Clippy, workspace tests.
- Audit: only accepted `RUSTSEC-2024-0436`.
- File-size and diff checks.

Merge readiness was not run, as requested.

Implemented and committed Strand A.

Commits:

- `b8850bba60` — `fix: let GTK certify track row geometry`
- `694b27225d` — `docs: record strand A acceptance evidence`

Verification:

- 14/14 focused acceptance tests passed.
- 20/20 converted display tests passed, each isolated in its own Xvfb/DBus process.
- Workspace: 5,597 passed, 0 failed, 847 ignored.
- Formatting and Clippy passed.
- Audit passed offline with only accepted `RUSTSEC-2024-0436`.
- Edited code files remain below 800 lines.

Follow-up completed: the two comments omitted at the original ownership
boundary are now present under the explicit two-file extension.
The focused compile exited 0 and seven of eight exact isolated display tests
passed; the unchanged `BROWSE-11` sampler bound remained red with exit 101.

Full evidence is in [the strand plan](/home/marvin/Projects/reprise-the-row-height-certifies-itself-a/docs/plans/the-row-height-certifies-itself-a.md) and [.pipeline-codex.md](/home/marvin/Projects/reprise-the-row-height-certifies-itself-a/.pipeline-codex.md).

Not completed:

- The real-library control/fix arms conflict with the explicit worktree-only boundary and would launch against the live desktop/session bus.
- `check-merge-readiness.sh` stopped while refreshing `origin/main` because the host SSH configuration has invalid ownership/permissions.
- No version, progress-ledger, Core, or frozen mother-plan files were modified.

The branch is ahead by two commits and behind the updated `origin/dev` by one. Nothing was pushed or history-rewritten. The only remaining worktree entry is the untouched, user-supplied untracked mother plan.
